# Micro-Frontend (Wujie) — Giải Pháp Tái Cấu Trúc Kiến Trúc Style

> Phân tích chuyên sâu kiến trúc style trong micro-frontend: chẩn đoán hiện trạng, thiết kế kiến trúc đích, chiến lược cách ly CSS, và kế hoạch migration từng phase.
> Độ khó: ⭐️⭐️⭐️ | Thời gian đọc: ~20 phút

---

## Mục Lục

1. [Chẩn Đoán Hiện Trạng](#1-chẩn-đoán-hiện-trạng)
2. [Kiến Trúc Đích](#2-kiến-trúc-đích)
3. [Chiến Lược Cách Ly Style Micro-Frontend](#3-chiến-lược-cách-ly-style-micro-frontend)
4. [Thống Nhất Màu Chủ Đạo](#4-thống-nhất-màu-chủ-đạo)
5. [Bản Đồ Migration File](#5-bản-đồ-migration-file)
6. [Các Phase Thực Hiện](#6-các-phase-thực-hiện)
7. [Đánh Giá Rủi Ro & Rollback](#7-đánh-giá-rủi-ro--rollback)
8. [Verification Checklist](#8-verification-checklist)
9. [Kết Quả Kỳ Vọng](#9-kết-quả-kỳ-vọng)
10. [Câu Hỏi Phỏng Vấn](#10-câu-hỏi-phỏng-vấn)

---

## 1. Chẩn Đoán Hiện Trạng

### 1.1 Phân Bổ File Style (Main App)

```
HIỆN TRẠNG — 3 THƯ MỤC STYLE ĐỘC LẬP, KHÔNG CÓ ENTRY THỐNG NHẤT:
═══════════════════════════════════════════════════════════════════

  apps/main/src/
  ├── assets/                          ← THƯ MỤC #1
  │   ├── main.css                     ← Vue scaffold + Tailwind entry
  │   ├── base.css                     ← Vue scaffold CSS Reset + biến
  │   └── styles/
  │       └── micro-app.css            ← "Cách ly" micro-FE (!important)
  │
  ├── styles/                          ← THƯ MỤC #2
  │   ├── index.scss                   ← ⚠️ Entry KHÔNG ĐƯỢC IMPORT!
  │   ├── var.css                      ← Layout + theme CSS variables
  │   ├── dark.css                     ← Dark theme variables
  │   ├── resizeStyle.scss             ← 632 DÒNG (EP override + layout
  │   │                                   + business logic TRỘN LẪN!)
  │   ├── dialog.scss                  ← Dialog dimensions
  │   ├── theme.scss                   ← FILE RỖNG (toàn comment)
  │   ├── variables.scss               ← Chỉ 2 biến SCSS
  │   └── global.module.scss           ← Export namespace cho JS
  │
  └── (dùng @cmclink/ui/styles)       ← THƯ MỤC #3 (package)

═══════════════════════════════════════════════════════════════════
```

### 1.2 Chuỗi Import Style Trong main.ts

```
CHUỖI IMPORT HIỆN TẠI:
═══════════════════════════════════════════════════════════════════

  main.ts
  ├── assets/main.css
  │   └── @import base.css + @import tailwindcss    ← Tailwind #1
  │
  ├── assets/styles/micro-app.css                   ← !important war
  │
  ├── element-plus/es/components/                   ← EP components
  │   ├── message-box/style/css
  │   ├── message/style/css
  │   ├── notification/style/css
  │   └── loading/style/css
  │
  └── @cmclink/ui/styles                            ← Package styles
      ├── variables.scss    → Design tokens
      ├── element-override  → EP overrides
      ├── base.scss         → Global reset         ← Reset #2!
      ├── utilities.scss    → CMC utility classes
      ├── animations.scss   → Animations
      └── tailwindcss       ←                      ← Tailwind #2!

  ⚠️ PHÁT HIỆN QUAN TRỌNG:
  ┌──────────────────────────────────────────────────────────┐
  │  styles/index.scss (chứa var.css, resizeStyle.scss,     │
  │  dialog.scss) KHÔNG ĐƯỢC IMPORT bởi bất kỳ file nào!    │
  │                                                          │
  │  → 632 dòng EP override, layout variables, dark theme   │
  │    CÓ THỂ KHÔNG HOẠT ĐỘNG                               │
  │  → Hoặc đã từng hoạt động nhưng bị bỏ sót khi refactor │
  └──────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════
```

### 1.3 Danh Sách Vấn Đề Cốt Lõi

```
10 VẤN ĐỀ CẦN GIẢI QUYẾT:
═══════════════════════════════════════════════════════════════════

  🔴 MỨC CAO:
  ┌──────────────────────────────────────────────────────────┐
  │  #1  Tailwind CSS import TRÙNG LẶP                      │
  │      → assets/main.css VÀ @cmclink/ui/styles            │
  │      → Style trùng lặp trong output                     │
  │                                                          │
  │  #2  CSS Reset chạy TRÙNG LẶP                           │
  │      → assets/base.css VÀ @cmclink/ui/base.scss         │
  │      → Global reset chạy 2 lần                          │
  │                                                          │
  │  #3  Primary color XUNG ĐỘT                             │
  │      → var.css: #005aae                                  │
  │      → @cmclink/ui: #004889                              │
  │      → resizeStyle.scss: #005aae (hard-coded)            │
  │      → 3 định nghĩa, 2 giá trị khác nhau!               │
  │                                                          │
  │  #4  styles/index.scss KHÔNG ĐƯỢC THAM CHIẾU             │
  │      → 632 dòng EP overlay, layout vars, dark theme      │
  │      → Có thể không hoạt động                            │
  │                                                          │
  │  #6  Micro-FE style = cuộc chiến !important              │
  │      → micro-app.css dùng !important để hard override    │
  │      → Không tận dụng Wujie CSS sandbox                  │
  │                                                          │
  │  #10 Trách nhiệm style KHÔNG RÕ RÀNG                    │
  │      → EP override, layout, business, theme vars         │
  │      → TẤT CẢ trộn lẫn trong 1 file                    │
  └──────────────────────────────────────────────────────────┘

  🟡 MỨC TRUNG:
  ┌──────────────────────────────────────────────────────────┐
  │  #5  body style định nghĩa 2 lần                        │
  │      → base.css VÀ @cmclink/ui/base.scss                │
  │                                                          │
  │  #7  Code scaffold còn sót                               │
  │      → base.css: biến --vt-c-*                           │
  │      → main.css: class .green                            │
  │                                                          │
  │  #8  File lỗi thời chồng chất                           │
  │      → theme.scss (rỗng), variables.scss (2 dòng)       │
  │      → global.module.scss (giá trị nghi vấn)            │
  │                                                          │
  │  #9  !important tràn lan                                 │
  │      → resizeStyle.scss + micro-app.css dùng nhiều      │
  └──────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════
```

### 1.4 Phân Tích Package @cmclink/ui

```
@CMCLINK/UI — HỆ THỐNG DESIGN ĐÃ TƯƠNG ĐỐI HOÀN CHỈNH:
═══════════════════════════════════════════════════════════════════

  packages/ui/src/styles/
  ├── variables.scss         → Design tokens đầy đủ
  │                            (colors, fonts, spacing, radius, shadows)
  │                            + CSS variable mapping
  ├── mixins.scss            → Responsive breakpoints, text truncation,
  │                            layouts, button variants
  ├── base.scss              → Global reset, headings, paragraphs,
  │                            tables, scrollbars, print, dark mode, a11y
  ├── element-override.scss  → Element Plus style override
  ├── utilities.scss         → CMC-specific utility classes (cmc-*)
  └── animations.scss        → Animation library (1250 DÒNG!)

  ⚠️ VẤN ĐỀ TRONG ANIMATIONS.SCSS:
  ┌──────────────────────────────────────────────────────────┐
  │  Chứa NHIỀU class name TRÙNG với Tailwind:              │
  │  → .translate-*  → Tailwind đã có                       │
  │  → .scale-*      → Tailwind đã có                       │
  │  → .rotate-*     → Tailwind đã có                       │
  │  → .duration-*   → Tailwind đã có                       │
  │                                                          │
  │  → ~800 dòng CẦN XÓA vì trùng Tailwind                 │
  └──────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════
```

---

## 2. Kiến Trúc Đích

### 2.1 Nguyên Tắc Thiết Kế

```
5 NGUYÊN TẮC CỐT LÕI:
═══════════════════════════════════════════════════════════════════

  ① SINGLE SOURCE OF TRUTH
     → Design token định nghĩa DUY NHẤT trong @cmclink/ui
     → Không hard-code color values

  ② PHÂN TẦNG CÁCH LY
     → Global styles / Theme vars / EP overrides /
       Layout / Business styles → TÁCH BIỆT rõ ràng

  ③ MICRO-FE CÁCH LY TỰ NHIÊN
     → Dựa vào Wujie CSS sandbox (WebComponent + iframe)
     → KHÔNG dùng !important

  ④ TAILWIND ENTRY DUY NHẤT
     → Toàn bộ main app chỉ include Tailwind 1 NƠI

  ⑤ MAINTAINABILITY
     → Mỗi file 1 chức năng, naming ngữ nghĩa
     → Người mới hiểu cấu trúc trong 5 PHÚT

═══════════════════════════════════════════════════════════════════
```

### 2.2 Cấu Trúc Thư Mục Đích

```
CẤU TRÚC MỚI — RÀNH MẠCH VÀ CÓ TỔ CHỨC:
═══════════════════════════════════════════════════════════════════

  apps/main/src/
  ├── styles/                        ← THƯ MỤC DUY NHẤT cho style
  │   ├── index.scss                 ← ⭐ ENTRY DUY NHẤT
  │   ├── tailwind.css               ← Tailwind CSS ENTRY DUY NHẤT
  │   │
  │   ├── tokens/                    ← Main-app design tokens
  │   │   ├── _variables.scss        ← SCSS variables (internal)
  │   │   └── _css-variables.scss    ← CSS custom properties (layout)
  │   │
  │   ├── themes/                    ← Theme system
  │   │   ├── _light.scss            ← Light theme
  │   │   └── _dark.scss             ← Dark theme
  │   │
  │   ├── overrides/                 ← EP style overrides
  │   │   ├── _button.scss
  │   │   ├── _table.scss
  │   │   ├── _form.scss             ← filterBox, etc.
  │   │   ├── _dialog.scss
  │   │   ├── _message-box.scss
  │   │   ├── _select.scss
  │   │   ├── _tag.scss
  │   │   ├── _tabs.scss
  │   │   └── _index.scss            ← EP overrides barrel
  │   │
  │   ├── layout/                    ← Layout styles
  │   │   ├── _app-view.scss         ← AppViewContent/Scroll
  │   │   ├── _login.scss            ← Login page
  │   │   └── _index.scss            ← Layout barrel
  │   │
  │   ├── vendors/                   ← Third-party adapters
  │   │   └── _nprogress.scss        ← NProgress theme
  │   │
  │   └── legacy/                    ← Legacy (dần migrate → scoped)
  │       ├── _si-detail.scss        ← Export document detail
  │       ├── _marketing.scss        ← Marketing module
  │       └── _index.scss            ← Legacy barrel
  │
  └── assets/                        ← CHỈ CÒN static resources
      └── images/
          ├── avatar.gif
          ├── cmc-logo.png
          └── ...

═══════════════════════════════════════════════════════════════════
```

### 2.3 Entry File Design

```
STYLES/INDEX.SCSS — ENTRY DUY NHẤT:
═══════════════════════════════════════════════════════════════════

  // ============================================================
  // CMCLink Main App Style Entry
  // Thứ tự load theo ưu tiên — KHÔNG thay đổi tùy ý
  // ============================================================

  // 1️⃣ Tailwind CSS (base atomic layer — load ĐẦU TIÊN)
  @use './tailwind.css';

  // 2️⃣ Main-app design tokens (override/extend @cmclink/ui)
  @use './tokens/css-variables';

  // 3️⃣ Theme system
  @use './themes/light';
  @use './themes/dark';

  // 4️⃣ Element Plus overrides
  @use './overrides/index';

  // 5️⃣ Layout styles
  @use './layout/index';

  // 6️⃣ Third-party adapters
  @use './vendors/nprogress';

  // 7️⃣ Legacy business styles (dần xóa bỏ)
  @use './legacy/index';

═══════════════════════════════════════════════════════════════════
```

```
MAIN.TS — SAU TÁI CẤU TRÚC:
═══════════════════════════════════════════════════════════════════

  // Style: ENTRY DUY NHẤT
  import './styles/index.scss'

  // EP feedback components (global, không auto-import)
  import 'element-plus/es/components/message-box/style/css'
  import 'element-plus/es/components/message/style/css'
  import 'element-plus/es/components/notification/style/css'
  import 'element-plus/es/components/loading/style/css'

  // Component library (@cmclink/ui design tokens + base + EP override)
  import '@cmclink/ui/styles'

  // Phần còn lại giữ nguyên...

  ⚠️ LƯU Ý:
  → Tailwind CSS import trong @cmclink/ui cần XÓA
  → Thay bằng entry duy nhất styles/tailwind.css

═══════════════════════════════════════════════════════════════════
```

### 2.4 Thứ Tự Load Style

```
STYLE LOADING ORDER — TỪ THẤP ĐẾN CAO ƯU TIÊN:
═══════════════════════════════════════════════════════════════════

  ┌─────────────────────────────────────────────────────────┐
  │ 1. Tailwind CSS (atomic base layer)                     │
  ├─────────────────────────────────────────────────────────┤
  │ 2. @cmclink/ui/styles                                   │
  │    ├── variables.scss  → Design tokens + CSS vars       │
  │    ├── base.scss       → Global reset + typography      │
  │    ├── element-override.scss → Package-level EP override│
  │    ├── utilities.scss  → CMC utility classes             │
  │    └── animations.scss → Animations (cần cleanup)       │
  ├─────────────────────────────────────────────────────────┤
  │ 3. Element Plus feedback component styles               │
  ├─────────────────────────────────────────────────────────┤
  │ 4. Main app styles/index.scss                           │
  │    ├── tokens/    → Layout vars, theme extensions       │
  │    ├── themes/    → Light / Dark theme                  │
  │    ├── overrides/ → App-level EP overrides              │
  │    ├── layout/    → Page layout                         │
  │    ├── vendors/   → Third-party adapters                │
  │    └── legacy/    → Legacy business styles              │
  ├─────────────────────────────────────────────────────────┤
  │ 5. Component <style scoped> (component-level, isolated) │
  └─────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════
```

---

## 3. Chiến Lược Cách Ly Style Micro-Frontend

### 3.1 Cơ Chế Wujie CSS Sandbox

```
WUJIE DUAL SANDBOX — WebComponent + iframe:
═══════════════════════════════════════════════════════════════════

  ┌─────────────────────────────────────────────────────────┐
  │                    MAIN APPLICATION                      │
  │                                                          │
  │  ┌───────────────────────────────────────────────────┐  │
  │  │  <wujie-app>  (WebComponent)                      │  │
  │  │  ┌─────────────────────────────────────────────┐  │  │
  │  │  │  #shadow-root (shadowDOM)                    │  │  │
  │  │  │                                              │  │  │
  │  │  │  ┌──────────────────────────────────────┐   │  │  │
  │  │  │  │  SUB-APP DOM                          │   │  │  │
  │  │  │  │  → Styles CÁCH LY tự nhiên            │   │  │  │
  │  │  │  │  → Main app CSS KHÔNG xâm nhập        │   │  │  │
  │  │  │  │  → Sub-app CSS KHÔNG leak ra ngoài    │   │  │  │
  │  │  │  └──────────────────────────────────────┘   │  │  │
  │  │  └─────────────────────────────────────────────┘  │  │
  │  │                                                    │  │
  │  │  ┌─────────────────────────────────────────────┐  │  │
  │  │  │  <iframe> (hidden)                           │  │  │
  │  │  │  → JS Sandbox: sub-app JS chạy tại đây      │  │  │
  │  │  │  → Cách ly tự nhiên                          │  │  │
  │  │  └─────────────────────────────────────────────┘  │  │
  │  └───────────────────────────────────────────────────┘  │
  │                                                          │
  │  ⚠️ NGOẠI LỆ:                                           │
  │  EP popup (Dialog/Drawer/MessageBox) mặc định mount     │
  │  vào document.body → THOÁT KHỎI shadowDOM!              │
  │                                                          │
  └─────────────────────────────────────────────────────────┘

  JS Sandbox  → iframe → cách ly TỰ NHIÊN
  CSS Sandbox → shadowDOM → cách ly TỰ NHIÊN
  ⚠️ Popup    → document.body → THOÁT sandbox!

═══════════════════════════════════════════════════════════════════
```

### 3.2 Giải Pháp Đúng Cho Popup Escape

```
VẤN ĐỀ POPUP THOÁT SHADOWDOM:
═══════════════════════════════════════════════════════════════════

  ❌ CÁCH LÀM SAI (hiện tại):
  ┌──────────────────────────────────────────────────────────┐
  │  micro-app.css:                                          │
  │  .micro-app .el-dialog { ... !important }                │
  │  .micro-app .el-drawer { ... !important }                │
  │                                                          │
  │  → Dùng !important override từ main app                 │
  │  → Fragile, khó maintain, conflict liên tục              │
  └──────────────────────────────────────────────────────────┘

  ✅ CÁCH LÀM ĐÚNG — 3 OPTION:

  ┌─ Option A: Global Config (KHUYẾN NGHỊ) ─────────────────┐
  │                                                          │
  │  // Sub-app main.ts                                      │
  │  app.use(ElementPlus, {                                  │
  │    // Popup không dùng teleport                          │
  │    // → Giữ trong sub-app DOM tree                       │
  │    // → Style trong shadowDOM → cách ly tự nhiên        │
  │  })                                                      │
  │                                                          │
  │  // Hoặc từng component:                                 │
  │  <el-dialog :teleported="false">                         │
  │  <el-dialog append-to=".child-app-root">                │
  │                                                          │
  └──────────────────────────────────────────────────────────┘

  ┌─ Option B: Wujie Plugin Interception (TỰ ĐỘNG) ─────────┐
  │                                                          │
  │  // plugins/wujie.ts                                     │
  │  setupApp({                                              │
  │    name: 'child-app',                                    │
  │    plugins: [{                                           │
  │      cssLoader: (code) => code,                         │
  │      jsLoader: (code) => code,                          │
  │    }]                                                    │
  │  })                                                      │
  │  → Tự inject config vào sub-app popup components        │
  │                                                          │
  └──────────────────────────────────────────────────────────┘

  ┌─ Option C: Shared Style Package (DỰ PHÒNG) ─────────────┐
  │                                                          │
  │  packages/ui/src/styles/                                 │
  │  └── shared/                                             │
  │      ├── dialog-standard.scss                            │
  │      └── form-standard.scss                              │
  │                                                          │
  │  // Sub-app import on-demand:                            │
  │  import '@cmclink/ui/styles/shared/dialog-standard'     │
  │                                                          │
  └──────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════
```

### 3.3 Tổng Hợp Cách Ly Style

```
  ┌─────────────────────────┬──────────────────────┬────────────┐
  │ Tình huống              │ Cơ chế cách ly        │ Cần xử lý? │
  ├─────────────────────────┼──────────────────────┼────────────┤
  │ Sub-app style bình      │ Wujie shadowDOM      │ ❌ Không   │
  │ thường                  │ cách ly tự nhiên      │            │
  ├─────────────────────────┼──────────────────────┼────────────┤
  │ Sub-app popup/drawer    │ Config teleported=   │ ✅ Sub-app │
  │                         │ false                │ config     │
  ├─────────────────────────┼──────────────────────┼────────────┤
  │ Main ↔ sub-app shared   │ @cmclink/ui package  │ ✅ Package │
  │ styles                  │ distribution          │ level      │
  ├─────────────────────────┼──────────────────────┼────────────┤
  │ Main app global styles  │ shadowDOM chặn       │ ❌ Không   │
  │                         │ xâm nhập             │            │
  └─────────────────────────┴──────────────────────┴────────────┘
```

### 3.4 Xóa micro-app.css

```
SAU TÁI CẤU TRÚC — XÓA HOÀN TOÀN micro-app.css:
═══════════════════════════════════════════════════════════════════

  ┌─────────────────────────────┬────────────────────────────┐
  │ Nội dung gốc               │ Cách xử lý                 │
  ├─────────────────────────────┼────────────────────────────┤
  │ .micro-app .el-dialog      │ Sub-app config              │
  │ override                   │ :teleported="false"         │
  ├─────────────────────────────┼────────────────────────────┤
  │ .micro-app-test.marketing  │ Test code → XÓA             │
  ├─────────────────────────────┼────────────────────────────┤
  │ .test-paragraph/.test-box  │ Test code → XÓA             │
  └─────────────────────────────┴────────────────────────────┘

═══════════════════════════════════════════════════════════════════
```

---

## 4. Thống Nhất Màu Chủ Đạo

```
HIỆN TRẠNG — 3 ĐỊNH NGHĨA, 2 GIÁ TRỊ KHÁC NHAU:
═══════════════════════════════════════════════════════════════════

  ┌────────────────────────────┬────────────┬──────────────────┐
  │ Vị trí                     │ Giá trị    │ Ghi chú          │
  ├────────────────────────────┼────────────┼──────────────────┤
  │ styles/var.css             │ #005aae    │ Main app vars    │
  │ @cmclink/ui/variables.scss │ #004889    │ Design token     │
  │ resizeStyle.scss           │ #005aae    │ Hard-coded!      │
  └────────────────────────────┴────────────┴──────────────────┘

  GIẢI PHÁP THỐNG NHẤT:
  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  ① @cmclink/ui design token = NGUỒN CHÂN LÝ DUY NHẤT   │
  │                                                          │
  │  ② Xác nhận với design team:                             │
  │     → Design = #004889 → sửa var.css                    │
  │     → Design = #005aae → sửa @cmclink/ui                │
  │                                                          │
  │  ③ THAY THẾ tất cả hard-coded colors:                   │
  │                                                          │
  │     ❌ background-color: #005aae;                        │
  │     ✅ background-color: var(--el-color-primary);        │
  │                                                          │
  │  ④ Main app tokens/_css-variables.scss:                  │
  │     → CHỈ định nghĩa layout variables                   │
  │     → Color variables THỪA KẾ từ @cmclink/ui            │
  │                                                          │
  └──────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════
```

---

## 5. Bản Đồ Migration File

### 5.1 Files Cần XÓA

```
  ┌──────────────────────────────┬────────────────────────────┐
  │ File                         │ Lý do                      │
  ├──────────────────────────────┼────────────────────────────┤
  │ assets/main.css              │ Scaffold remains, Tailwind │
  │                              │ dời sang styles/tailwind   │
  ├──────────────────────────────┼────────────────────────────┤
  │ assets/base.css              │ Trùng @cmclink/ui/base     │
  ├──────────────────────────────┼────────────────────────────┤
  │ assets/styles/micro-app.css  │ !important → wujie sandbox │
  ├──────────────────────────────┼────────────────────────────┤
  │ styles/theme.scss            │ File rỗng                  │
  ├──────────────────────────────┼────────────────────────────┤
  │ styles/variables.scss        │ 2 dòng → merge vào tokens  │
  ├──────────────────────────────┼────────────────────────────┤
  │ styles/global.module.scss    │ Giá trị nghi vấn           │
  └──────────────────────────────┴────────────────────────────┘
```

### 5.2 Files Cần TÁCH

```
RESIZESTYLE.SCSS (632 DÒNG) → 8+ FILES:
═══════════════════════════════════════════════════════════════════

  ┌───────────────────────────┬──────────────────────┬───────┐
  │ Target file               │ Nội dung              │ ~Dòng │
  ├───────────────────────────┼──────────────────────┼───────┤
  │ overrides/_button.scss    │ Button overlay (L1-20)│ ~20   │
  │ overrides/_tag.scss       │ Tag styles (L29-33)  │ ~5    │
  │ overrides/_select.scss    │ Dropdown (L36-38)    │ ~3    │
  │ overrides/_table.scss     │ Table (L41-80,       │ ~80   │
  │                           │ L374-417)            │       │
  │ overrides/_message-box    │ MessageBox (L83-98,  │ ~20   │
  │                           │ L399-406)            │       │
  │ overrides/_form.scss      │ filterBox (L484-618) │ ~135  │
  │ layout/_login.scss        │ Login page (L100-117)│ ~18   │
  │ layout/_app-view.scss     │ AppView/Scroll/tabs  │ ~190  │
  │                           │ (L119-306)           │       │
  │ legacy/_si-detail.scss    │ Export docs (L50-105)│ ~55   │
  │ legacy/_marketing.scss    │ Marketing (L308-373, │ ~70   │
  │                           │ L418-438)            │       │
  │ XÓA                       │ .container-selected  │ ~8    │
  │                           │ → Move to scoped     │       │
  └───────────────────────────┴──────────────────────┴───────┘

  VAR.CSS (154 DÒNG):
  ┌───────────────────────────┬──────────────────────────────┐
  │ tokens/_css-variables     │ Layout vars (--left-menu-*,  │
  │                           │ --top-header-*, etc.)        │
  │ XÓA                       │ Color vars → inherit         │
  │                           │ @cmclink/ui                   │
  └───────────────────────────┴──────────────────────────────┘

  INDEX.SCSS (126 DÒNG):
  ┌───────────────────────────┬──────────────────────────────┐
  │ vendors/_nprogress        │ NProgress theme (L22-38)     │
  │ overrides/_form.scss      │ append-click-input (L39-49)  │
  │ legacy/_si-detail.scss    │ si-detail-dialog (L50-105)   │
  │ XÓA                       │ .reset-margin, scrollbar     │
  └───────────────────────────┴──────────────────────────────┘

  DARK.CSS (85 DÒNG) → themes/_dark.scss
  DIALOG.SCSS (17 DÒNG) → overrides/_dialog.scss

═══════════════════════════════════════════════════════════════════
```

### 5.3 @cmclink/ui Cần Điều Chỉnh

```
  ┌──────────────────────────┬──────────────────────────────────┐
  │ File                     │ Điều chỉnh                        │
  ├──────────────────────────┼──────────────────────────────────┤
  │ styles/index.scss        │ XÓA @use 'tailwindcss'            │
  │                          │ → Tailwind chỉ import ở app layer │
  ├──────────────────────────┼──────────────────────────────────┤
  │ styles/animations.scss   │ Cleanup ~800 dòng trùng Tailwind  │
  │                          │ (.translate-*, .scale-*, etc.)    │
  ├──────────────────────────┼──────────────────────────────────┤
  │ styles/utilities.scss    │ Giữ cmc-* prefix, xóa phần       │
  │                          │ trùng Tailwind                    │
  └──────────────────────────┴──────────────────────────────────┘
```

---

## 6. Các Phase Thực Hiện

```
════════════════════════════════════════════════════════════════════
5 PHASE — TỪ THẤP RỦI RO ĐẾN CAO RỦI RO
════════════════════════════════════════════════════════════════════

  PHASE 1: Dọn dẹp cơ bản (⏱ 1-2h | 🟢 thấp rủi ro)
  ┌──────────────────────────────────────────────────────────┐
  │  ✦ Tạo cấu trúc thư mục mới                            │
  │    styles/tokens/, themes/, overrides/, layout/,         │
  │    vendors/, legacy/                                     │
  │  ✦ Tạo styles/tailwind.css                              │
  │  ✦ Xóa files lỗi thời (theme.scss rỗng)                │
  │  ✦ Thống nhất primary color → CSS variables             │
  └──────────────────────────────────────────────────────────┘

  PHASE 2: Tách + migrate files (⏱ 3-4h | 🟡 trung rủi ro)
  ┌──────────────────────────────────────────────────────────┐
  │  ✦ Tách resizeStyle.scss (632→8 files)                  │
  │  ✦ Tách var.css (layout vars → tokens/)                 │
  │  ✦ Migrate dark.css → themes/_dark.scss                 │
  │  ✦ Migrate dialog.scss → overrides/_dialog.scss         │
  │  ✦ Tách index.scss (NProgress→vendors, biz→legacy)      │
  │  ✦ Tạo entry styles/index.scss mới                      │
  └──────────────────────────────────────────────────────────┘

  PHASE 3: Tái cấu trúc entry (⏱ 1-2h | 🔴 cao rủi ro)
  ┌──────────────────────────────────────────────────────────┐
  │  ✦ Sửa main.ts:                                         │
  │    - XÓA import './assets/main.css'                     │
  │    - XÓA import './assets/styles/micro-app.css'         │
  │    + THÊM import './styles/index.scss'                  │
  │    + Điều chỉnh thứ tự @cmclink/ui/styles              │
  │  ✦ Xóa files cũ (assets/main.css, base.css, micro-app) │
  │  ✦ FULL REGRESSION TEST                                 │
  └──────────────────────────────────────────────────────────┘

  PHASE 4: Micro-FE isolation (⏱ 2-3h/sub-app)
  ┌──────────────────────────────────────────────────────────┐
  │  ✦ Sub-app config popup :teleported="false"             │
  │  ✦ Cung cấp @cmclink/micro-bootstrap unified config    │
  │  ✦ XÓA micro-app.css                                    │
  │  ✦ Verify cách ly từng sub-app                          │
  └──────────────────────────────────────────────────────────┘

  PHASE 5: @cmclink/ui optimization (⏱ 2-3h | độc lập)
  ┌──────────────────────────────────────────────────────────┐
  │  ✦ XÓA Tailwind import từ @cmclink/ui/styles            │
  │  ✦ Cleanup animations.scss (~800 dòng trùng Tailwind)   │
  │  ✦ Review utilities.scss vs Tailwind overlap             │
  └──────────────────────────────────────────────────────────┘

  GIT STRATEGY — Mỗi phase 1 commit riêng:
  ┌──────────────────────────────────────────────────────────┐
  │  feat(styles): phase-1 basic cleanup + directory         │
  │  feat(styles): phase-2 file splitting + migration        │
  │  feat(styles): phase-3 entry point reconstruction        │
  │  feat(styles): phase-4 micro-frontend isolation          │
  │  feat(styles): phase-5 @cmclink/ui optimization          │
  └──────────────────────────────────────────────────────────┘

════════════════════════════════════════════════════════════════════
```

---

## 7. Đánh Giá Rủi Ro & Rollback

```
BẢNG RỦI RO:
═══════════════════════════════════════════════════════════════════

  ┌─────────────────────────┬──────┬──────┬──────────────────────┐
  │ Rủi ro                  │ P    │ Impact│ Giảm thiểu          │
  ├─────────────────────────┼──────┼──────┼──────────────────────┤
  │ styles/index.scss tuy   │ Thấp │ Cao  │ Check DevTools xem  │
  │ không imported nhưng    │      │      │ style nào đang có   │
  │ style thực tế apply    │      │      │ hiệu lực            │
  ├─────────────────────────┼──────┼──────┼──────────────────────┤
  │ Thay đổi thứ tự load   │ TB   │ TB   │ @use theo đúng      │
  │ → override bị hỏng     │      │      │ thứ tự gốc          │
  ├─────────────────────────┼──────┼──────┼──────────────────────┤
  │ Sub-app popup           │ TB   │ TB   │ Test từng sub-app,  │
  │ :teleported="false"     │      │      │ adjust z-index      │
  │ gây vấn đề z-index     │      │      │                     │
  ├─────────────────────────┼──────┼──────┼──────────────────────┤
  │ Xóa base.css → một số  │ Thấp │ TB   │ @cmclink/ui/base    │
  │ page bị lỗi style      │      │      │ đã cover tất cả     │
  └─────────────────────────┴──────┴──────┴──────────────────────┘

  ROLLBACK: Mỗi phase commit riêng → rollback chính xác
  bất kỳ phase nào nếu có vấn đề

═══════════════════════════════════════════════════════════════════
```

---

## 8. Verification Checklist

```
═══════════════════════════════════════════════════════════════════

  8.1 STYLE CORRECTNESS:
  ┌──────────────────────────────────────────────────────────┐
  │  □ Login page OK (background, form, buttons)            │
  │  □ Main layout OK (sidebar expand/collapse, topnav)     │
  │  □ Tab styles OK (active, hover, close button)          │
  │  □ Table page OK (filter box, headers, rows, pagination)│
  │  □ Dialog/Drawer OK (large/medium/small sizes)          │
  │  □ Form styles OK (input, select, date picker)          │
  │  □ Button styles OK (primary, link, danger, disabled)   │
  │  □ NProgress progress bar color correct                 │
  │  □ Dark theme toggle works                              │
  └──────────────────────────────────────────────────────────┘

  8.2 MICRO-FRONTEND ISOLATION:
  ┌──────────────────────────────────────────────────────────┐
  │  □ Sub-app style OK after load                          │
  │  □ Main app styles DON'T intrude sub-apps               │
  │  □ Sub-app styles DON'T leak to main app                │
  │  □ Sub-app popup style correct (no body escape)         │
  │  □ No style residue switching between sub-apps          │
  └──────────────────────────────────────────────────────────┘

  8.3 BUILD DELIVERABLES:
  ┌──────────────────────────────────────────────────────────┐
  │  □ Build without errors                                 │
  │  □ CSS artifact size KHÔNG tăng (expect giảm 30%+)      │
  │  □ Tailwind CSS output KHÔNG trùng lặp                  │
  │  □ CSS Reset KHÔNG trùng lặp                            │
  └──────────────────────────────────────────────────────────┘

  8.4 DEVELOPER EXPERIENCE:
  ┌──────────────────────────────────────────────────────────┐
  │  □ HMR style hot update hoạt động                       │
  │  □ Biết rõ thêm style mới vào file nào                  │
  │  □ SCSS variables + CSS variables reference bình thường │
  └──────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════
```

---

## 9. Kết Quả Kỳ Vọng

```
SO SÁNH TRƯỚC VÀ SAU:
═══════════════════════════════════════════════════════════════════

  ┌──────────────────────┬─────────────────┬─────────────────────┐
  │ Dimension            │ Hiện trạng       │ Sau tái cấu trúc    │
  ├──────────────────────┼─────────────────┼─────────────────────┤
  │ CSS artifact size    │ Tailwind ×2 +   │ Giảm ~30-40%        │
  │                      │ Reset ×2 +      │                     │
  │                      │ nhiều trùng lặp │                     │
  ├──────────────────────┼─────────────────┼─────────────────────┤
  │ Số file style        │ Phân tán 3 thư  │ 1 thư mục, phân    │
  │                      │ mục, 8+ files   │ tầng rõ ràng        │
  ├──────────────────────┼─────────────────┼─────────────────────┤
  │ Entry file           │ main.ts import  │ main.ts import      │
  │                      │ 2+ style files  │ 1 entry duy nhất    │
  │                      │ + implicit deps │                     │
  ├──────────────────────┼─────────────────┼─────────────────────┤
  │ Newbie onboarding    │ "Không biết để  │ Hiểu cấu trúc      │
  │                      │ style ở đâu"    │ trong 5 phút        │
  ├──────────────────────┼─────────────────┼─────────────────────┤
  │ Micro-FE isolation   │ !important      │ Wujie sandbox       │
  │                      │ hard override    │ cách ly tự nhiên    │
  ├──────────────────────┼─────────────────┼─────────────────────┤
  │ Primary color        │ 3 định nghĩa,   │ 1 định nghĩa,      │
  │                      │ 2 giá trị       │ nhất quán toàn bộ   │
  ├──────────────────────┼─────────────────┼─────────────────────┤
  │ !important usage     │ Tràn lan        │ Chỉ khi cần cho EP  │
  └──────────────────────┴─────────────────┴─────────────────────┘

═══════════════════════════════════════════════════════════════════
```

---

## 10. Câu Hỏi Phỏng Vấn

### Q1: Tại sao cần tái cấu trúc kiến trúc style trong micro-frontend?

```
TRẢ LỜI:
┌──────────────────────────────────────────────────────────────┐
│                                                              │
│  Kiến trúc style trong micro-FE dễ rơi vào tình trạng:     │
│                                                              │
│  ① TRÙNG LẶP: Tailwind, CSS Reset import nhiều lần          │
│     → Tăng bundle size, performance giảm                    │
│                                                              │
│  ② XUNG ĐỘT: Design tokens định nghĩa nhiều nơi            │
│     → Primary color 3 định nghĩa, 2 giá trị khác nhau      │
│     → UI không nhất quán                                    │
│                                                              │
│  ③ !IMPORTANT WAR: Main app cố override sub-app styles     │
│     → Fragile, khó maintain, cascade bị phá vỡ              │
│                                                              │
│  ④ FILES PHÌNH TO: 1 file 632 dòng chứa EP override +      │
│     layout + business logic → vi phạm Single Responsibility │
│                                                              │
│  ⑤ DEAD CODE: Entry file không import → code không hoạt     │
│     động mà không ai biết                                    │
│                                                              │
│  Tái cấu trúc giúp:                                         │
│  → Single source of truth cho design tokens                 │
│  → Phân tầng rõ ràng → dễ maintain                          │
│  → Giảm 30-40% CSS artifact size                            │
│  → Tận dụng sandbox cách ly tự nhiên                        │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### Q2: Wujie CSS sandbox hoạt động như thế nào?

```
TRẢ LỜI:
┌──────────────────────────────────────────────────────────────┐
│                                                              │
│  Wujie dùng DUAL SANDBOX: WebComponent + iframe             │
│                                                              │
│  JS SANDBOX:                                                 │
│  → Sub-app JS chạy trong iframe ẩn                          │
│  → Cách ly hoàn toàn với main app JS context                │
│                                                              │
│  CSS SANDBOX:                                                │
│  → Sub-app DOM render trong shadowDOM của WebComponent       │
│  → Shadow boundary ngăn main CSS xâm nhập                   │
│  → Cũng ngăn sub-app CSS leak ra ngoài                      │
│                                                              │
│  NGOẠI LỆ — Popup Escape:                                   │
│  → EP Dialog/Drawer mặc định mount vào document.body        │
│  → Thoát khỏi shadowDOM boundary                            │
│  → Giải pháp: :teleported="false" hoặc append-to            │
│    để popup mount trong sub-app DOM → vẫn trong shadow      │
│                                                              │
│  Ưu điểm vs !important approach:                             │
│  → Cách ly TỰ NHIÊN, không cần manual override              │
│  → Không phụ thuộc vào selector specificity                 │
│  → Không tạo "arms race" giữa main + sub-app               │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### Q3: Thứ tự load style quan trọng như thế nào? Tại sao?

```
TRẢ LỜI:
┌──────────────────────────────────────────────────────────────┐
│                                                              │
│  CSS follow CASCADING rules — style load SAU sẽ override    │
│  style load TRƯỚC (cùng specificity).                        │
│                                                              │
│  Thứ tự đúng:                                                │
│  ① Tailwind CSS (base) → ÍT ƯU TIÊN NHẤT                   │
│  ② @cmclink/ui (design system) → override Tailwind          │
│  ③ EP component styles → override cả 2                      │
│  ④ App-level overrides → override EP                        │
│  ⑤ Component scoped → CAO ƯU TIÊN NHẤT                     │
│                                                              │
│  Nếu sai thứ tự:                                             │
│  → EP override load TRƯỚC EP component → KHÔNG override     │
│  → Tailwind load SAU app styles → Tailwind win (sai!)       │
│                                                              │
│  Nguyên tắc: Base → System → Library → Application → Local │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### Q4: Single Source of Truth cho design token nghĩa là gì?

```
TRẢ LỜI:
┌──────────────────────────────────────────────────────────────┐
│                                                              │
│  SINGLE SOURCE OF TRUTH (SSOT) = Mỗi giá trị thiết kế      │
│  CHỈ ĐỊNH NGHĨA 1 NƠI DUY NHẤT.                            │
│                                                              │
│  VÍ DỤ VỀ PRIMARY COLOR:                                     │
│                                                              │
│  ❌ Sai (3 nơi):                                             │
│  → var.css: --el-color-primary: #005aae                     │
│  → @cmclink/ui: $color-primary: #004889                     │
│  → resizeStyle.scss: background: #005aae (hard-coded!)      │
│                                                              │
│  ✅ Đúng (1 nơi):                                            │
│  → @cmclink/ui/variables.scss: $color-primary: #004889      │
│  → Tất cả nơi khác: var(--el-color-primary)                 │
│  → Đổi 1 chỗ → TOÀN BỘ app cập nhật                       │
│                                                              │
│  Lợi ích:                                                    │
│  → Consistency: UI nhất quán toàn app                       │
│  → Maintainability: Đổi 1 nơi, áp dụng everywhere          │
│  → Debugging: Dễ trace giá trị đang dùng                    │
│  → Theming: Dễ implement dark/light theme                   │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### Q5: Tại sao !important là anti-pattern trong micro-frontend?

```
TRẢ LỜI:
┌──────────────────────────────────────────────────────────────┐
│                                                              │
│  !important phá vỡ CSS cascade:                              │
│                                                              │
│  ① ARMS RACE:                                                │
│     Main app: .el-dialog { color: red !important }           │
│     Sub-app update: .el-dialog { color: blue !important }    │
│     Main app response: #app .el-dialog { ... !important }    │
│     → Specificity war leo thang → KHÔNG AI THẮNG            │
│                                                              │
│  ② FRAGILE:                                                  │
│     → Sub-app update → main app override có thể BỊ HỎng    │
│     → Khó debug: phải trace qua nhiều !important layers     │
│                                                              │
│  ③ COUPLING:                                                 │
│     → Main app phải BIẾT selector của sub-app               │
│     → Vi phạm độc lập giữa micro-frontends                  │
│     → Sub-app đổi class name → main app hỏng               │
│                                                              │
│  GIẢI PHÁP ĐÚNG:                                             │
│  → Dùng CSS sandbox (shadowDOM) xong cách ly TỰ NHIÊN      │
│  → Popup: config :teleported="false"                        │
│  → Shared styles: distribute qua package                    │
│  → !important CHỈ dùng khi override 3rd-party library       │
│    mà không có cách nào khác                                │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### Q6: Chiến lược phân phase khi tái cấu trúc style là gì?

```
TRẢ LỜI:
┌──────────────────────────────────────────────────────────────┐
│                                                              │
│  Nguyên tắc: TỪ THẤP RỦI RO → CAO RỦI RO                   │
│                                                              │
│  Phase 1 (🟢 Low): Dọn dẹp cơ bản                           │
│  → Tạo thư mục mới, xóa files rỗng/lỗi thời               │
│  → KHÔNG ảnh hưởng output hiện tại                          │
│                                                              │
│  Phase 2 (🟡 Medium): Tách file + migrate                   │
│  → Split file 632 dòng → 8 files nhỏ                       │
│  → Chưa đổi entry → styles vẫn load như cũ                  │
│                                                              │
│  Phase 3 (🔴 High): Tái cấu trúc entry                      │
│  → Đổi main.ts imports → THAY ĐỔI LOADING ORDER           │
│  → Cần FULL REGRESSION TEST                                 │
│                                                              │
│  Phase 4: Micro-FE isolation                                 │
│  → Cần cooperation từ sub-app teams                         │
│  → Có thể thực hiện độc lập per sub-app                     │
│                                                              │
│  Phase 5: Package optimization                               │
│  → Thực hiện ĐỘC LẬP, không affect main app                │
│                                                              │
│  Mỗi phase = 1 Git commit → rollback chính xác              │
│  → Nếu Phase 3 fail → revert Phase 3, giữ 1+2              │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```
