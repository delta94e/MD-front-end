# Next.js AI Coding Agents — Deep Dive!

> **Chủ đề**: Cấu hình dự án Next.js cho AI Coding Agents
> **Ngôn ngữ**: Tiếng Việt — giải thích cực kỳ chi tiết!
> **Phương châm**: Tự viết lại bằng tay — KHÔNG dùng thư viện!
> **Nguồn**: https://nextjs.org/docs/app/guides/ai-agents

---

## Mục Lục

1. [§1. Tổng Quan — AI Coding Agents Là Gì?](#1)
2. [§2. Phân Tích Sơ Đồ — Bundled Docs Structure](#2)
3. [§3. How It Works — Cách Hoạt Động Chi Tiết](#3)
4. [§4. Getting Started — New Projects](#4)
5. [§5. Getting Started — Existing Projects](#5)
6. [§6. Understanding AGENTS.md — Chi Tiết File](#6)
7. [§7. CLAUDE.md — Cross-Reference](#7)
8. [§8. Sơ Đồ Tổng Hợp — Agent Workflow Pipeline](#8)
9. [§9. Bundled Docs vs Training Data — So Sánh](#9)
10. [§10. Next.js MCP Server — Mở Rộng](#10)
11. [§11. Tự Viết — AgentDocsResolver Engine](#11)
12. [§12. Câu Hỏi Luyện Tập](#12)

---

## §1. Tổng Quan — AI Coding Agents Là Gì?

```
  AI CODING AGENTS — TỔNG QUAN:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  VẤN ĐỀ:                                              │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  AI Agents (Claude, Cursor, Copilot...)          │  │
  │  │  được train trên dữ liệu CŨ!                    │  │
  │  │                                                  │  │
  │  │  → Agent biết Next.js 13/14 patterns            │  │
  │  │  → NHƯNG bạn dùng Next.js 16!                   │  │
  │  │  → Agent viết code SAI API!                      │  │
  │  │  → Tốn thời gian debug!                          │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  │  GIẢI PHÁP CỦA NEXT.JS:                               │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │                                                  │  │
  │  │  ① BUNDLED DOCS:                                 │  │
  │  │    → Docs được ĐÓNG GÓI trong node_modules!    │  │
  │  │    → Luôn KHỚP phiên bản bạn đang dùng!        │  │
  │  │    → Không cần mạng, không cần tra cứu ngoài!  │  │
  │  │                                                  │  │
  │  │  ② AGENTS.md:                                    │  │
  │  │    → File chỉ dẫn ở root dự án!                │  │
  │  │    → Bảo agent: "ĐỌC DOCS TRƯỚC KHI CODE!"    │  │
  │  │    → Agent tự động đọc khi bắt đầu session!    │  │
  │  │                                                  │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  │  AI AGENTS ĐƯỢC HỖ TRỢ:                               │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  ✅ Claude Code                                  │  │
  │  │  ✅ Cursor                                       │  │
  │  │  ✅ GitHub Copilot                               │  │
  │  │  ✅ Bất kỳ agent nào đọc AGENTS.md!            │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §2. Phân Tích Sơ Đồ — Bundled Docs Structure!

### Hình gốc từ trang docs:

![Sơ đồ cấu trúc Bundled Docs](./images/nextjs-ai-agents-bundled-docs-structure.png)

### Phân tích chi tiết:

```
  SƠ ĐỒ GỐC — BUNDLED DOCS STRUCTURE:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  node_modules/next/dist/docs/                          │
  │  ├── 01-app/                  ← APP ROUTER DOCS!      │
  │  │   ├── 01-getting-started/  ← Hướng dẫn bắt đầu   │
  │  │   ├── 02-guides/           ← Guides chuyên sâu     │
  │  │   └── 03-api-reference/    ← API reference đầy đủ  │
  │  ├── 02-pages/                ← PAGES ROUTER DOCS!    │
  │  ├── 03-architecture/         ← Kiến trúc Next.js     │
  │  └── index.mdx                ← File gốc/mục lục     │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

```
  PHÂN TÍCH TỪNG PHẦN:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  ① VỊ TRÍ:                                            │
  │  → node_modules/next/dist/docs/                       │
  │  → TRONG package "next" mà bạn npm install!           │
  │  → dist/ = đã biên dịch (distribution)                │
  │  → docs/ = documentation folder                        │
  │                                                        │
  │  ② CẤU TRÚC SỐ THỨ TỰ (01-, 02-, 03-):              │
  │  → Số ĐẦU = THỨ TỰ sắp xếp!                         │
  │  → Mirror cấu trúc website nextjs.org/docs!           │
  │                                                        │
  │  ③ 01-app/ — APP ROUTER:                              │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  01-getting-started/                             │  │
  │  │  → Installation, project structure               │  │
  │  │  → Layouts, pages, linking, images               │  │
  │  │  → Data fetching, CSS, metadata                  │  │
  │  │                                                  │  │
  │  │  02-guides/                                      │  │
  │  │  → Authentication, caching, forms                │  │
  │  │  → Internationalization, testing                 │  │
  │  │  → AI agents (trang này!), analytics             │  │
  │  │                                                  │  │
  │  │  03-api-reference/                               │  │
  │  │  → Components (<Link>, <Image>...)               │  │
  │  │  → Functions (fetch, redirect, cookies)          │  │
  │  │  → File conventions (page.js, layout.js)         │  │
  │  │  → Config (next.config.js)                       │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  │  ④ 02-pages/ — PAGES ROUTER:                          │
  │  → Docs cho Pages Router (legacy)                     │
  │  → getServerSideProps, getStaticProps...               │
  │  → Vẫn có cho backward compatibility!                 │
  │                                                        │
  │  ⑤ 03-architecture/ — KIẾN TRÚC:                      │
  │  → Turbopack, rendering strategies                    │
  │  → Build process, caching internals                   │
  │                                                        │
  │  ⑥ index.mdx — MỤC LỤC:                              │
  │  → MDX = Markdown + JSX!                              │
  │  → File gốc, entry point của docs!                    │
  │                                                        │
  │  TẠI SAO MIRROR CẤU TRÚC WEBSITE?                     │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  Website:  nextjs.org/docs/app/getting-started   │  │
  │  │  Bundled:  dist/docs/01-app/01-getting-started/  │  │
  │  │  → CÙNG NỘI DUNG!                               │  │
  │  │  → Agent tìm được DỄ DÀNG!                     │  │
  │  │  → Không cần mạng!                               │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §3. How It Works — Cách Hoạt Động Chi Tiết!

```
  WORKFLOW — TỪ INSTALL ĐẾN AGENT ĐỌC DOCS:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  BƯỚC 1: npm install next                              │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  $ npm install next                              │  │
  │  │                                                  │  │
  │  │  → Download package "next" v16.x                 │  │
  │  │  → Package CHỨA SẴN docs/!                     │  │
  │  │  → Docs = phiên bản Next.js bạn cài!           │  │
  │  │                                                  │  │
  │  │  node_modules/                                   │  │
  │  │  └── next/                                       │  │
  │  │      └── dist/                                   │  │
  │  │          ├── server/      ← Server code          │  │
  │  │          ├── client/      ← Client code          │  │
  │  │          └── docs/        ← 📚 DOCS ĐÂY!       │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  │  BƯỚC 2: AGENTS.md ở root project                     │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  my-project/                                     │  │
  │  │  ├── AGENTS.md  ← CHỈ DẪN CHO AGENT!           │  │
  │  │  ├── app/                                        │  │
  │  │  ├── node_modules/                               │  │
  │  │  │   └── next/dist/docs/  ← DOCS!              │  │
  │  │  └── package.json                                │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  │  BƯỚC 3: Agent bắt đầu session                        │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │                                                  │  │
  │  │  Agent (Claude/Cursor/Copilot)                   │  │
  │  │    │                                              │  │
  │  │    ├── ① Đọc AGENTS.md tự động!                 │  │
  │  │    │   "Đọc docs trước khi code!"               │  │
  │  │    │                                              │  │
  │  │    ├── ② Tìm docs ở node_modules/next/dist/docs │  │
  │  │    │   → Docs KHỚP phiên bản!                   │  │
  │  │    │   → Không dùng training data cũ!           │  │
  │  │    │                                              │  │
  │  │    └── ③ Viết code ĐÚNG API!                    │  │
  │  │        → Patterns mới nhất!                      │  │
  │  │        → Không deprecated APIs!                  │  │
  │  │                                                  │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

```
  SO SÁNH: BEFORE vs AFTER:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  BEFORE (Không có AGENTS.md):                          │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  User: "Fetch data trong page"                   │  │
  │  │  Agent: "Dùng getServerSideProps!"  ← CŨ! ❌    │  │
  │  │  → Agent dùng training data (Next.js 13)        │  │
  │  │  → API đã thay đổi/deprecated!                  │  │
  │  │  → Code chạy SAI hoặc không optimal!            │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  │  AFTER (Có AGENTS.md):                                 │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  User: "Fetch data trong page"                   │  │
  │  │  Agent: → Đọc dist/docs/01-app/01-getting-      │  │
  │  │            started/fetching-data.mdx             │  │
  │  │        → "Dùng async Server Component + fetch!"│  │
  │  │        → Code ĐÚNG API Next.js 16! ✅           │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §4. Getting Started — New Projects!

```
  NEW PROJECTS — TẠO DỰ ÁN MỚI:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  CÁCH 1: create-next-app (TỰ ĐỘNG!):                  │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  $ pnpm create next-app@canary                   │  │
  │  │                                                  │  │
  │  │  → Tạo dự án Next.js mới                        │  │
  │  │  → TỰ ĐỘNG tạo AGENTS.md!                      │  │
  │  │  → TỰ ĐỘNG tạo CLAUDE.md!                      │  │
  │  │  → KHÔNG cần setup gì thêm!                     │  │
  │  │                                                  │  │
  │  │  Kết quả:                                        │  │
  │  │  my-app/                                         │  │
  │  │  ├── AGENTS.md      ← Tạo tự động!             │  │
  │  │  ├── CLAUDE.md      ← Tạo tự động!             │  │
  │  │  ├── app/                                        │  │
  │  │  │   ├── layout.tsx                              │  │
  │  │  │   └── page.tsx                                │  │
  │  │  ├── next.config.ts                              │  │
  │  │  └── package.json                                │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  │  KHÔNG MUỐN AGENTS.md?                                 │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  $ npx create-next-app@canary --no-agents-md     │  │
  │  │                                                  │  │
  │  │  → Flag --no-agents-md                          │  │
  │  │  → KHÔNG tạo AGENTS.md và CLAUDE.md!            │  │
  │  │  → Dùng khi không cần AI agent support          │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §5. Getting Started — Existing Projects!

```
  EXISTING PROJECTS — DỰ ÁN ĐÃ CÓ:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  YÊU CẦU:                                             │
  │  → Next.js >= v16.2.0-canary.37                       │
  │  → Nếu phiên bản cũ hơn → npm update next!           │
  │                                                        │
  │  BƯỚC 1: Tạo AGENTS.md ở root project:                │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  my-project/                                     │  │
  │  │  ├── AGENTS.md  ← TẠO FILE NÀY!                │  │
  │  │  ├── app/                                        │  │
  │  │  └── package.json                                │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  │  NỘI DUNG AGENTS.md:                                   │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │                                                  │  │
  │  │  <!-- BEGIN:nextjs-agent-rules -->                │  │
  │  │                                                  │  │
  │  │  # Next.js: ALWAYS read docs before coding       │  │
  │  │                                                  │  │
  │  │  Before any Next.js work, find and read the      │  │
  │  │  relevant doc in                                 │  │
  │  │  `node_modules/next/dist/docs/`.                 │  │
  │  │                                                  │  │
  │  │  Your training data is outdated — the docs       │  │
  │  │  are the source of truth.                        │  │
  │  │                                                  │  │
  │  │  <!-- END:nextjs-agent-rules -->                 │  │
  │  │                                                  │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  │  BƯỚC 2: Tạo CLAUDE.md (cho Claude Code users):       │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  // Nội dung CLAUDE.md:                          │  │
  │  │  @AGENTS.md                                      │  │
  │  │                                                  │  │
  │  │  → @ = import syntax của Claude Code!           │  │
  │  │  → Include nội dung AGENTS.md vào CLAUDE.md!    │  │
  │  │  → KHÔNG duplicate nội dung!                    │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §6. Understanding AGENTS.md — Chi Tiết File!

```
  AGENTS.md — PHÂN TÍCH CHI TIẾT:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  ① COMMENT MARKERS:                                    │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  <!-- BEGIN:nextjs-agent-rules -->                │  │
  │  │  ... nội dung Next.js quản lý ...                │  │
  │  │  <!-- END:nextjs-agent-rules -->                 │  │
  │  │                                                  │  │
  │  │  → BEGIN/END = RANH GIỚI!                        │  │
  │  │  → Next.js QUẢN LÝ phần trong markers!         │  │
  │  │  → Future updates có thể thay đổi phần này!    │  │
  │  │  → Phần NGOÀI markers = CỦA BẠN! An toàn!     │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  │  ② CÁCH THÊM RULES CỦA RIÊNG BẠN:                    │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │                                                  │  │
  │  │  <!-- BEGIN:nextjs-agent-rules -->                │  │
  │  │  # Next.js: ALWAYS read docs before coding       │  │
  │  │  ...                                             │  │
  │  │  <!-- END:nextjs-agent-rules -->                 │  │
  │  │                                                  │  │
  │  │  ← PHẦN TRÊN: Next.js quản lý ← KHÔNG SỬA!    │  │
  │  │  ──────────────────────────────────              │  │
  │  │  ← PHẦN DƯỚI: CỦA BẠN ← TỰ DO THÊM!          │  │
  │  │                                                  │  │
  │  │  # My Project Rules                              │  │
  │  │                                                  │  │
  │  │  - Always use TypeScript strict mode             │  │
  │  │  - Use Tailwind CSS for styling                  │  │
  │  │  - Follow the component naming convention:       │  │
  │  │    PascalCase for components                     │  │
  │  │  - All API calls go through lib/api/             │  │
  │  │  - Use Zod for validation                        │  │
  │  │  - Write tests for all new features              │  │
  │  │                                                  │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  │  ③ TẠI SAO "INTENTIONALLY MINIMAL"?                    │
  │  → Chỉ 1 instruction duy nhất:                       │
  │    "Đọc bundled docs trước khi code!"                 │  │
  │  → Đơn giản = agent HIỂU NGAY!                       │
  │  → Không gây confusion!                                │
  │  → Redirect từ stale training data → accurate docs!  │
  │                                                        │
  │  ④ BUNDLED DOCS CHỨA GÌ?                              │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  ✅ Guides (hướng dẫn)                           │  │
  │  │  ✅ API References (tham chiếu API)              │  │
  │  │  ✅ File Conventions (quy ước file)              │  │
  │  │  ✅ App Router docs                              │  │
  │  │  ✅ Pages Router docs                            │  │
  │  │  ✅ Architecture docs                            │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §7. CLAUDE.md — Cross-Reference!

```
  CLAUDE.md — DÀNH RIÊNG CHO CLAUDE CODE:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  TẠI SAO CẦN CLAUDE.md RIÊNG?                         │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  Claude Code đọc CLAUDE.md khi bắt đầu session! │  │
  │  │  → Khác với AGENTS.md (generic cho tất cả!)     │  │
  │  │  → CLAUDE.md = Claude-specific instructions!     │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  │  CÚ PHÁP @ IMPORT:                                    │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  // CLAUDE.md:                                   │  │
  │  │  @AGENTS.md                                      │  │
  │  │                                                  │  │
  │  │  → @AGENTS.md = "Include AGENTS.md vào đây!"   │  │
  │  │  → Claude Code hiểu @ = import syntax!          │  │
  │  │  → KHÔNG cần copy-paste nội dung!               │  │
  │  │  → 1 source of truth = AGENTS.md!               │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  │  FLOW:                                                 │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │                                                  │  │
  │  │  Claude Code bắt đầu session                     │  │
  │  │    │                                              │  │
  │  │    ├── ① Đọc CLAUDE.md                           │  │
  │  │    │   └── @AGENTS.md → include AGENTS.md!      │  │
  │  │    │                                              │  │
  │  │    ├── ② Đọc AGENTS.md (via import)              │  │
  │  │    │   └── "Đọc docs ở dist/docs/"              │  │
  │  │    │                                              │  │
  │  │    └── ③ Tìm docs → viết code đúng!             │  │
  │  │                                                  │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  │  SO SÁNH CÁC FILE AGENT:                              │
  │  ┌──────────────┬───────────────────────────────────┐  │
  │  │ File         │ Mục đích                          │  │
  │  ├──────────────┼───────────────────────────────────┤  │
  │  │ AGENTS.md    │ Generic — tất cả AI agents đọc!  │  │
  │  │ CLAUDE.md    │ Riêng Claude Code (@ import)      │  │
  │  │ .cursorrules │ Riêng Cursor editor               │  │
  │  │ .github/     │ GitHub Copilot config             │  │
  │  │ copilot-     │                                   │  │
  │  │ instructions │                                   │  │
  │  └──────────────┴───────────────────────────────────┘  │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §8. Sơ Đồ Tổng Hợp — Agent Workflow Pipeline!

```
  AGENT WORKFLOW — TOÀN BỘ QUÁ TRÌNH:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  ① SETUP (1 lần duy nhất):                            │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  npm install next@latest                         │  │
  │  │  → node_modules/next/dist/docs/ ← BUNDLED!     │  │
  │  │                                                  │  │
  │  │  Tạo AGENTS.md + CLAUDE.md ở root/              │  │
  │  │  (hoặc create-next-app tạo tự động!)            │  │
  │  └──────────┬───────────────────────────────────────┘  │
  │             │                                          │
  │             ▼                                          │
  │  ② AGENT SESSION START:                                │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  Developer mở AI chat/agent                      │  │
  │  │    │                                              │  │
  │  │    ├── Agent scan project root                    │  │
  │  │    │   ├── AGENTS.md ← phát hiện!               │  │
  │  │    │   └── CLAUDE.md ← (nếu Claude Code)       │  │
  │  │    │                                              │  │
  │  │    └── Agent đọc AGENTS.md                       │  │
  │  │        "Đọc docs tại dist/docs/"                │  │
  │  └──────────┬───────────────────────────────────────┘  │
  │             │                                          │
  │             ▼                                          │
  │  ③ DEVELOPER HỎI:                                     │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  "Tạo một dynamic route với params"              │  │
  │  └──────────┬───────────────────────────────────────┘  │
  │             │                                          │
  │             ▼                                          │
  │  ④ AGENT TRA CỨU DOCS (KHÔNG DÙNG TRAINING DATA!):   │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  Agent:                                          │  │
  │  │  → Tìm: dist/docs/01-app/01-getting-started/   │  │
  │  │         routing.mdx                              │  │
  │  │  → Đọc: Dynamic Routes, params API              │  │
  │  │  → Biết: Next.js 16 dùng async params!         │  │
  │  │  → Viết code ĐÚNG phiên bản!                    │  │
  │  └──────────┬───────────────────────────────────────┘  │
  │             │                                          │
  │             ▼                                          │
  │  ⑤ AGENT TRẢ LỜI ĐÚNG:                                │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  // Next.js 16 — async params (ĐÚNG!)           │  │
  │  │  export default async function Page({            │  │
  │  │    params                                        │  │
  │  │  }: {                                            │  │
  │  │    params: Promise<{ slug: string }>             │  │
  │  │  }) {                                            │  │
  │  │    const { slug } = await params                 │  │
  │  │    return <div>{slug}</div>                      │  │
  │  │  }                                               │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §9. Bundled Docs vs Training Data — So Sánh!

```
  SO SÁNH CHI TIẾT:
  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  ┌──────────────┬─────────────────┬───────────────────┐ │
  │  │              │ Training Data   │ Bundled Docs      │ │
  │  ├──────────────┼─────────────────┼───────────────────┤ │
  │  │ Phiên bản    │ Cũ (thời điểm   │ CHÍNH XÁC phiên  │ │
  │  │              │ train model)    │ bản bạn cài!     │ │
  │  ├──────────────┼─────────────────┼───────────────────┤ │
  │  │ Cập nhật     │ ❌ Không (cố    │ ✅ Tự động khi   │ │
  │  │              │ định khi train) │ npm update next!  │ │
  │  ├──────────────┼─────────────────┼───────────────────┤ │
  │  │ Network      │ ❌ Không cần    │ ❌ Không cần     │ │
  │  │              │ (trong model)   │ (trong node_mods) │ │
  │  ├──────────────┼─────────────────┼───────────────────┤ │
  │  │ Độ tin cậy   │ ❌ Có thể sai   │ ✅ Source of     │ │
  │  │              │ cho API mới!    │ truth!            │ │
  │  ├──────────────┼─────────────────┼───────────────────┤ │
  │  │ Deprecated   │ ❌ Agent vẫn    │ ✅ Docs đã xóa   │ │
  │  │ APIs         │ suggest!        │ API deprecated!   │ │
  │  ├──────────────┼─────────────────┼───────────────────┤ │
  │  │ Breaking     │ ❌ Agent không  │ ✅ Docs phản ánh  │ │
  │  │ Changes      │ biết!           │ changes!          │ │
  │  └──────────────┴─────────────────┴───────────────────┘ │
  │                                                          │
  │  VÍ DỤ THỰC TẾ:                                         │
  │  ┌──────────────────────────────────────────────────┐    │
  │  │  Feature      │ Next.js 13  │ Next.js 16         │    │
  │  │  ─────────────┼─────────────┼────────────────    │    │
  │  │  Middleware    │ middleware  │ proxy.ts ← ĐỔI!   │    │
  │  │  file          │ .ts         │                    │    │
  │  │  ─────────────┼─────────────┼────────────────    │    │
  │  │  Params       │ sync        │ async (await!)     │    │
  │  │  ─────────────┼─────────────┼────────────────    │    │
  │  │  Data fetch   │ getServer   │ async Server       │    │
  │  │               │ SideProps   │ Components!        │    │
  │  └──────────────────────────────────────────────────┘    │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

---

## §10. Next.js MCP Server — Mở Rộng!

```
  MCP SERVER — MỞ RỘNG THÊM:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  AGENTS.md = Docs tĩnh (read-only!)                   │
  │  MCP Server = Docs ĐỘNG + Application State!          │
  │                                                        │
  │  MCP = Model Context Protocol                          │
  │  → Cho phép agent truy cập application state!         │
  │  → Đọc errors, logs, runtime info!                    │
  │  → Tích hợp sâu hơn AGENTS.md!                       │
  │                                                        │
  │  SO SÁNH:                                              │
  │  ┌──────────────┬─────────────────┬─────────────────┐  │
  │  │              │ AGENTS.md       │ MCP Server      │  │
  │  ├──────────────┼─────────────────┼─────────────────┤  │
  │  │ Dữ liệu     │ Static docs     │ Dynamic state   │  │
  │  │ Cập nhật     │ Khi install     │ Real-time       │  │
  │  │ Phạm vi      │ API/patterns    │ App state,      │  │
  │  │              │                 │ errors, logs    │  │
  │  │ Setup        │ 1 file          │ Server config   │  │
  │  └──────────────┴─────────────────┴─────────────────┘  │
  │                                                        │
  │  → Xem thêm: nextjs.org/docs/app/guides/mcp          │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §11. Tự Viết — AgentDocsResolver Engine!

```javascript
var AgentDocsResolver = (function () {
  // ═══════════════════════════════════
  // 1. SIMULATED BUNDLED DOCS
  // ═══════════════════════════════════
  var bundledDocs = {
    "01-app": {
      "01-getting-started": {
        "installation.mdx": "How to install Next.js...",
        "project-structure.mdx": "File conventions: app/, page.tsx...",
        "layouts-and-pages.mdx": "Layouts wrap pages, shared UI...",
        "fetching-data.mdx": "Use async Server Components with fetch...",
        "route-handlers.mdx": "Export GET, POST from route.ts...",
        "proxy.mdx": "Proxy (formerly Middleware) runs before routes...",
      },
      "02-guides": {
        "ai-agents.mdx": "Configure AGENTS.md for AI coding agents...",
        "authentication.mdx": "Auth patterns with Server Components...",
        "caching.mdx": "Data Cache, Full Route Cache...",
      },
      "03-api-reference": {
        components: {
          "link.mdx": "<Link> component for client navigation...",
          "image.mdx": "<Image> component for optimization...",
        },
        functions: {
          "fetch.mdx": "Extended fetch with caching options...",
          "redirect.mdx": "redirect() for server-side redirects...",
          "cookies.mdx": "cookies() for reading/writing cookies...",
        },
      },
    },
    "02-pages": {
      "getServerSideProps.mdx": "SSR data fetching (Pages Router)...",
      "getStaticProps.mdx": "SSG data fetching (Pages Router)...",
    },
    "03-architecture": {
      "turbopack.mdx": "Turbopack bundler...",
      "rendering.mdx": "Server vs Client rendering...",
    },
    "index.mdx": "Welcome to Next.js documentation...",
  };

  // ═══════════════════════════════════
  // 2. AGENTS.MD PARSER
  // ═══════════════════════════════════
  function parseAgentsMd(content) {
    var beginMarker = "<!-- BEGIN:nextjs-agent-rules -->";
    var endMarker = "<!-- END:nextjs-agent-rules -->";

    var beginIdx = content.indexOf(beginMarker);
    var endIdx = content.indexOf(endMarker);

    if (beginIdx === -1 || endIdx === -1) {
      return { nextjsRules: "", customRules: content.trim() };
    }

    var nextjsRules = content
      .substring(beginIdx + beginMarker.length, endIdx)
      .trim();
    var customRules = content.substring(endIdx + endMarker.length).trim();

    return { nextjsRules: nextjsRules, customRules: customRules };
  }

  // ═══════════════════════════════════
  // 3. DOCS FINDER
  // ═══════════════════════════════════
  function findDoc(query, docsTree, path) {
    path = path || "";
    var results = [];
    var queryLower = query.toLowerCase();

    for (var key in docsTree) {
      var fullPath = path ? path + "/" + key : key;
      var value = docsTree[key];

      if (typeof value === "string") {
        // It's a file — check if name or content matches
        if (
          key.toLowerCase().indexOf(queryLower) !== -1 ||
          value.toLowerCase().indexOf(queryLower) !== -1
        ) {
          results.push({ path: fullPath, preview: value });
        }
      } else {
        // It's a directory — recurse
        var sub = findDoc(query, value, fullPath);
        results = results.concat(sub);
      }
    }

    return results;
  }

  // ═══════════════════════════════════
  // 4. MATCHER (path pattern matching)
  // ═══════════════════════════════════
  function matchPath(pattern, path) {
    var parts = pattern.split("/");
    var pathParts = path.split("/");

    for (var i = 0; i < parts.length; i++) {
      if (parts[i] === "**") return true; // wildcard
      if (i >= pathParts.length) return false;
      if (parts[i] !== "*" && parts[i] !== pathParts[i]) return false;
    }

    return pathParts.length === parts.length;
  }

  // ═══════════════════════════════════
  // 5. AGENT SESSION SIMULATOR
  // ═══════════════════════════════════
  function simulateAgentSession(agentsMdContent, userQuery) {
    console.log("╔══════════════════════════════════════╗");
    console.log("║ AGENT SESSION SIMULATOR               ║");
    console.log("╚══════════════════════════════════════╝");

    // Step 1: Parse AGENTS.md
    console.log("\n━━━ Step 1: Parse AGENTS.md ━━━");
    var parsed = parseAgentsMd(agentsMdContent);
    console.log(
      "  Next.js rules:",
      parsed.nextjsRules.substring(0, 60) + "...",
    );
    if (parsed.customRules) {
      console.log(
        "  Custom rules:",
        parsed.customRules.substring(0, 60) + "...",
      );
    }

    // Step 2: Agent reads instruction
    console.log("\n━━━ Step 2: Agent reads instruction ━━━");
    console.log('  Instruction: "Read docs at node_modules/next/dist/docs/"');
    console.log("  → Using BUNDLED docs (NOT training data!)");

    // Step 3: Find relevant doc
    console.log('\n━━━ Step 3: Find docs for query: "' + userQuery + '" ━━━');
    var results = findDoc(userQuery, bundledDocs);

    if (results.length === 0) {
      console.log("  ❌ No docs found for: " + userQuery);
      console.log("  → Fallback to training data (less reliable!)");
    } else {
      console.log("  ✅ Found " + results.length + " matching doc(s):");
      for (var i = 0; i < results.length; i++) {
        console.log("  → " + results[i].path);
        console.log("    Preview: " + results[i].preview);
      }
    }

    // Step 4: Generate response
    console.log("\n━━━ Step 4: Generate response ━━━");
    if (results.length > 0) {
      console.log("  ✅ Response based on ACCURATE bundled docs!");
      console.log("  Source: node_modules/next/dist/docs/" + results[0].path);
    } else {
      console.log("  ⚠️ Response based on training data (may be outdated!)");
    }

    return results;
  }

  // ═══════════════════════════════════
  // 6. DEMO
  // ═══════════════════════════════════
  function demo() {
    var agentsMd = [
      "<!-- BEGIN:nextjs-agent-rules -->",
      "# Next.js: ALWAYS read docs before coding",
      "Before any Next.js work, find and read the relevant doc in",
      "`node_modules/next/dist/docs/`.",
      "Your training data is outdated — the docs are the source of truth.",
      "<!-- END:nextjs-agent-rules -->",
      "",
      "# My Project Rules",
      "- Use TypeScript strict mode",
      "- All API routes in app/api/",
    ].join("\n");

    // Test 1: Fetch data query
    console.log("\n\n========== TEST 1 ==========");
    simulateAgentSession(agentsMd, "fetch");

    // Test 2: Route handlers
    console.log("\n\n========== TEST 2 ==========");
    simulateAgentSession(agentsMd, "route");

    // Test 3: Proxy/Middleware
    console.log("\n\n========== TEST 3 ==========");
    simulateAgentSession(agentsMd, "proxy");

    // Test 4: Unknown topic
    console.log("\n\n========== TEST 4 ==========");
    simulateAgentSession(agentsMd, "graphql");
  }

  return {
    demo: demo,
    findDoc: findDoc,
    parseAgentsMd: parseAgentsMd,
  };
})();
// Chạy: AgentDocsResolver.demo();
```

---

## §12. Câu Hỏi Luyện Tập!

**Câu 1**: Tại sao Next.js đóng gói docs vào node_modules thay vì để AI agents tra cứu online?

<details><summary>Đáp án</summary>

3 lý do chính:

1. **Version-matched**: Docs KHỚP CHÍNH XÁC phiên bản bạn cài! Nếu bạn dùng Next.js 16.1.6, docs là của 16.1.6 — KHÔNG phải docs chung cho "latest" trên website.

2. **Offline / No network**: Không cần request HTTP đến nextjs.org → nhanh hơn, ổn định hơn, hoạt động offline.

3. **Deterministic**: Mỗi lần agent chạy → ĐỌC CÙNG docs → kết quả NHẤT QUÁN! Nếu tra cứu online, website có thể thay đổi giữa 2 lần chạy.

</details>

---

**Câu 2**: Giải thích vai trò của markers `<!-- BEGIN:nextjs-agent-rules -->` và `<!-- END:nextjs-agent-rules -->` trong AGENTS.md.

<details><summary>Đáp án</summary>

Markers tạo ra **ranh giới** (boundary) giữa 2 phần:

- **Trong markers** (BEGIN → END): Phần do **Next.js quản lý**. Nội dung này có thể được cập nhật bởi future Next.js releases (qua tools, scripts, hoặc create-next-app updates). Bạn **KHÔNG nên** sửa phần này!

- **Ngoài markers**: Phần **của bạn**! Bạn TỰ DO thêm project-specific rules mà không sợ bị overwrite. Ví dụ: coding conventions, project structure rules, testing requirements...

Đây là pattern quen thuộc, tương tự `<!-- managed by X -->` trong nhiều config generators.

</details>

---

**Câu 3**: CLAUDE.md dùng `@AGENTS.md` — giải thích cú pháp này.

<details><summary>Đáp án</summary>

`@AGENTS.md` là **Claude Code import syntax**:

- `@` = directive ra lệnh "include file này!"
- Khi Claude Code đọc `CLAUDE.md`, nó gặp `@AGENTS.md` → tự động đọc THÊM nội dung `AGENTS.md`
- **Tại sao không copy-paste?**:
  - **DRY** (Don't Repeat Yourself) — 1 nguồn sự thật duy nhất!
  - Cập nhật `AGENTS.md` → Claude Code tự động nhận nội dung mới
  - Các agent khác đọc `AGENTS.md` trực tiếp, Claude Code đọc qua `CLAUDE.md` → cùng nội dung!

</details>

---

**Câu 4**: So sánh AGENTS.md (bundled docs) vs Next.js MCP Server.

<details><summary>Đáp án</summary>

|                  | AGENTS.md + Bundled Docs     | MCP Server                         |
| ---------------- | ---------------------------- | ---------------------------------- |
| **Loại dữ liệu** | Static documentation         | Dynamic application state          |
| **Cập nhật**     | Khi npm install/update       | Real-time (live)                   |
| **Phạm vi**      | API docs, guides, patterns   | Errors, logs, runtime state        |
| **Setup**        | 1 file (AGENTS.md)           | Server configuration               |
| **Khi nào dùng** | Agent cần biết **cách code** | Agent cần biết **app đang làm gì** |

**Kết hợp cả 2** = agent biết cách code ĐÚNG (bundled docs) VÀ biết context ứng dụng (MCP Server) → kết quả tốt nhất!

</details>

---

**Câu 5**: Viết code mô phỏng quá trình agent tìm và đọc bundled docs khi nhận query.

<details><summary>Đáp án</summary>

```javascript
function findRelevantDoc(query) {
  // Bước 1: Xác định chủ đề từ query
  var keywords = query.toLowerCase().split(" ");

  // Bước 2: Map keyword → docs path
  var docMap = {
    fetch: "01-app/01-getting-started/fetching-data.mdx",
    route: "01-app/01-getting-started/route-handlers.mdx",
    proxy: "01-app/01-getting-started/proxy.mdx",
    middleware: "01-app/01-getting-started/proxy.mdx",
    layout: "01-app/01-getting-started/layouts-and-pages.mdx",
    image: "01-app/03-api-reference/components/image.mdx",
    link: "01-app/03-api-reference/components/link.mdx",
    cache: "01-app/02-guides/caching.mdx",
    auth: "01-app/02-guides/authentication.mdx",
  };

  // Bước 3: Tìm docs phù hợp
  var results = [];
  for (var i = 0; i < keywords.length; i++) {
    if (docMap[keywords[i]]) {
      results.push({
        keyword: keywords[i],
        docPath: "node_modules/next/dist/docs/" + docMap[keywords[i]],
      });
    }
  }

  // Bước 4: Trả kết quả
  if (results.length > 0) {
    console.log("✅ Found docs → reading BUNDLED version!");
    results.forEach(function (r) {
      console.log("  " + r.keyword + " → " + r.docPath);
    });
  } else {
    console.log("⚠️ No matching docs → fallback to training data");
  }

  return results;
}

// Test:
findRelevantDoc("how to fetch data in server component");
// ✅ Found docs → reading BUNDLED version!
//   fetch → node_modules/next/dist/docs/01-app/01-getting-started/fetching-data.mdx
```

**Flow**: Query → Extract keywords → Match docs map → Return bundled doc path → Agent reads accurate docs!

</details>
