# Next.js CSS — Deep Dive!

> **Chủ đề**: CSS trong Next.js App Router — Tailwind, Modules, Global & More!
> **Ngôn ngữ**: Tiếng Việt — giải thích cực kỳ chi tiết!
> **Phương châm**: Tự viết lại bằng tay — KHÔNG dùng thư viện!
> **Nguồn**: https://nextjs.org/docs/app/getting-started/css
> **Hình ảnh**: Trang gốc KHÔNG có hình minh hoạ (0 images) — tự vẽ ASCII!

---

## Mục Lục

1. [§1. Tổng Quan — CSS Trong Next.js](#1)
2. [§2. Tailwind CSS — Utility-First Framework](#2)
3. [§3. CSS Modules — Scoped Styling](#3)
4. [§4. Global CSS — Styles Toàn Ứng Dụng](#4)
5. [§5. External Stylesheets — Import Từ npm](#5)
6. [§6. Ordering & Merging — Thứ Tự CSS](#6)
7. [§7. Recommendations — Best Practices](#7)
8. [§8. Development vs Production — Sự Khác Biệt](#8)
9. [§9. Sơ Đồ Tổng Hợp — CSS Architecture](#9)
10. [§10. Tự Viết — CSS Resolution Engine](#10)
11. [§11. Tổng Kết & Câu Hỏi Luyện Tập](#11)

---

## §1. Tổng Quan — CSS Trong Next.js!

```
  CSS TRONG NEXT.JS — TỔNG QUAN:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  Next.js hỗ trợ NHIỀU cách viết CSS:                  │
  │                                                        │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │                                                  │  │
  │  │  ① Tailwind CSS    → Utility-first framework    │  │
  │  │  ② CSS Modules     → Scoped, .module.css        │  │
  │  │  ③ Global CSS      → Áp dụng toàn app          │  │
  │  │  ④ External CSS    → Import từ npm packages     │  │
  │  │  ⑤ Sass            → .scss, .sass (cần cài)    │  │
  │  │  ⑥ CSS-in-JS       → styled-components, etc.   │  │
  │  │                                                  │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  │  PHÂN LOẠI THEO SCOPE:                                 │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │                                                  │  │
  │  │  GLOBAL (toàn app):                              │  │
  │  │  ├── ③ Global CSS (globals.css)                 │  │
  │  │  ├── ④ External CSS (bootstrap, etc.)           │  │
  │  │  └── ① Tailwind base styles                     │  │
  │  │                                                  │  │
  │  │  SCOPED (từng component):                        │  │
  │  │  ├── ② CSS Modules (.module.css)                │  │
  │  │  ├── ① Tailwind utility classes                 │  │
  │  │  └── ⑥ CSS-in-JS (styled-components)            │  │
  │  │                                                  │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  │  NEXT.JS RECOMMENDATION (ƯU TIÊN):                    │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  1. Tailwind CSS    → phần lớn styling          │  │
  │  │  2. CSS Modules     → khi Tailwind không đủ     │  │
  │  │  3. Global CSS      → chỉ cho truly global      │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

**Tại sao quan trọng?**

Cách bạn viết CSS trong Next.js ảnh hưởng đến:

- **Performance**: CSS được chunk và minify tự động
- **Maintainability**: Scoped CSS tránh naming conflicts
- **DX (Developer Experience)**: Hot reload CSS nhanh chóng
- **Production output**: Chỉ load CSS cần thiết cho mỗi route

---

## §2. Tailwind CSS — Utility-First Framework!

```
  TAILWIND CSS — SETUP TRONG NEXT.JS:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  Tailwind CSS = framework CSS dùng utility classes     │
  │  → Viết style TRỰC TIẾP trên HTML/JSX                │
  │  → KHÔNG cần viết file CSS riêng cho mỗi component   │
  │                                                        │
  │  SETUP 4 BƯỚC:                                         │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │                                                  │  │
  │  │  STEP 1: Install dependencies                    │  │
  │  │  ┌────────────────────────────────────────────┐  │  │
  │  │  │  pnpm add -D tailwindcss @tailwindcss/postcss│ │  │
  │  │  └────────────────────────────────────────────┘  │  │
  │  │  → Cài tailwindcss + PostCSS plugin!            │  │
  │  │                                                  │  │
  │  │  STEP 2: Config PostCSS                          │  │
  │  │  ┌────────────────────────────────────────────┐  │  │
  │  │  │  // postcss.config.mjs                     │  │  │
  │  │  │  export default {                          │  │  │
  │  │  │    plugins: {                              │  │  │
  │  │  │      '@tailwindcss/postcss': {},           │  │  │
  │  │  │    },                                      │  │  │
  │  │  │  }                                         │  │  │
  │  │  └────────────────────────────────────────────┘  │  │
  │  │                                                  │  │
  │  │  STEP 3: Import trong globals.css                │  │
  │  │  ┌────────────────────────────────────────────┐  │  │
  │  │  │  @import 'tailwindcss';                    │  │  │
  │  │  └────────────────────────────────────────────┘  │  │
  │  │  → Chú ý: Tailwind v4 dùng @import!            │  │
  │  │  → v3 dùng @tailwind base/components/utilities  │  │
  │  │                                                  │  │
  │  │  STEP 4: Import CSS vào Root Layout              │  │
  │  │  ┌────────────────────────────────────────────┐  │  │
  │  │  │  // app/layout.tsx                         │  │  │
  │  │  │  import './globals.css'                    │  │  │
  │  │  │  export default function RootLayout({      │  │  │
  │  │  │    children                                │  │  │
  │  │  │  }) {                                      │  │  │
  │  │  │    return (                                 │  │  │
  │  │  │      <html lang="en">                      │  │  │
  │  │  │        <body>{children}</body>              │  │  │
  │  │  │      </html>                                │  │  │
  │  │  │    )                                        │  │  │
  │  │  │  }                                         │  │  │
  │  │  └────────────────────────────────────────────┘  │  │
  │  │                                                  │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

```typescript
// SỬ DỤNG TAILWIND:
export default function Page() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between p-24">
      <h1 className="text-4xl font-bold">Welcome to Next.js!</h1>
    </main>
  )
}
// → KHÔNG cần viết file .css riêng!
// → Style NGAY trên className!
```

```
  PHÂN TÍCH TAILWIND CLASSES:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  "flex min-h-screen flex-col items-center              │
  │   justify-between p-24"                                │
  │                                                        │
  │  Tương đương CSS:                                      │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  flex           → display: flex                  │  │
  │  │  min-h-screen   → min-height: 100vh              │  │
  │  │  flex-col       → flex-direction: column         │  │
  │  │  items-center   → align-items: center            │  │
  │  │  justify-between→ justify-content: space-between │  │
  │  │  p-24           → padding: 6rem (24 × 0.25rem)  │  │
  │  │  text-4xl       → font-size: 2.25rem            │  │
  │  │  font-bold      → font-weight: 700              │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  │  ✅ ƯU ĐIỂM:                                          │
  │  → Không cần đặt tên class → không naming conflicts  │
  │  → Purge unused CSS → bundle nhỏ                     │
  │  → Consistent design system (spacing, colors, etc.)   │
  │                                                        │
  │  ⚠️ LƯU Ý: Tailwind v3 vs v4:                        │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  v3: @tailwind base; @tailwind components;       │  │
  │  │      @tailwind utilities;                        │  │
  │  │  v4: @import 'tailwindcss';  ← MỚI, đơn giản! │  │
  │  │                                                  │  │
  │  │  Nếu cần hỗ trợ browser cũ → dùng v3!          │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §3. CSS Modules — Scoped Styling!

```
  CSS MODULES — CÁC BƯỚC:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  CSS Modules = CSS được SCOPE cục bộ (locally scope)  │
  │  → Tự động generate TÊN CLASS DUY NHẤT               │
  │  → KHÔNG SỢ naming collisions giữa các file!         │
  │                                                        │
  │  QUY TẮC:                                              │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  File phải có extension: .module.css             │  │
  │  │  → blog.module.css  ✅                           │  │
  │  │  → blog.css         ❌ (sẽ là global CSS!)      │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  │  BEHIND THE SCENES:                                    │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │                                                  │  │
  │  │  BẠN VIẾT:                                       │  │
  │  │  /* blog.module.css */                           │  │
  │  │  .blog { padding: 24px; }                        │  │
  │  │                                                  │  │
  │  │  NEXT.JS GENERATE:                               │  │
  │  │  .blog_a1b2c3 { padding: 24px; }                │  │
  │  │       ↑                                          │  │
  │  │       hash DUY NHẤT!                             │  │
  │  │                                                  │  │
  │  │  → Dù 10 file đều có class .blog                │  │
  │  │  → Mỗi file sẽ có hash KHÁC NHAU!              │  │
  │  │  → KHÔNG BAO GIỜ conflict!                      │  │
  │  │                                                  │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

```typescript
// File: blog.module.css
// .blog { padding: 24px; }

// File: page.tsx
import styles from './blog.module.css'

export default function Page() {
  return <main className={styles.blog}></main>
  //                      ↑
  //                      styles.blog = "blog_a1b2c3" (unique!)
}
```

```
  CSS MODULES vs TAILWIND vs GLOBAL:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  ┌───────────────┬──────────┬──────────┬────────────┐  │
  │  │               │ Tailwind │ Modules  │ Global     │  │
  │  ├───────────────┼──────────┼──────────┼────────────┤  │
  │  │ Scope         │ Scoped   │ Scoped   │ Global     │  │
  │  │ File riêng?   │ Không    │ Có       │ Có         │  │
  │  │ Naming clash? │ Không    │ Không    │ CÓ THỂ!   │  │
  │  │ Customizable? │ Có       │ Có       │ Có         │  │
  │  │ Design system?│ Built-in │ Manual   │ Manual     │  │
  │  │ Learning      │ Trung bình│ Dễ      │ Dễ         │  │
  │  │ Bundle size   │ Nhỏ      │ Nhỏ     │ Có thể lớn │  │
  │  └───────────────┴──────────┴──────────┴────────────┘  │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §4. Global CSS — Styles Toàn Ứng Dụng!

```
  GLOBAL CSS — DÙNG KHI NÀO?
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  Global CSS = áp dụng TOÀN BỘ ứng dụng               │
  │  → Import trong Root Layout → mọi route đều có       │
  │                                                        │
  │  SETUP:                                                │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  // app/global.css                               │  │
  │  │  body {                                          │  │
  │  │    padding: 20px 20px 60px;                      │  │
  │  │    max-width: 680px;                             │  │
  │  │    margin: 0 auto;                               │  │
  │  │  }                                               │  │
  │  │                                                  │  │
  │  │  // app/layout.tsx                               │  │
  │  │  import './global.css'  ← Import ở Root Layout! │  │
  │  │                                                  │  │
  │  │  export default function RootLayout({ children })│  │
  │  │  {                                               │  │
  │  │    return (                                       │  │
  │  │      <html lang="en">                            │  │
  │  │        <body>{children}</body>                    │  │
  │  │      </html>                                      │  │
  │  │    )                                              │  │
  │  │  }                                               │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  │  ⚠️ CẢNH BÁO QUAN TRỌNG:                              │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │                                                  │  │
  │  │  Global CSS CÓ THỂ import ở BẤT KỲ đâu:        │  │
  │  │  → layout, page, hoặc component                 │  │
  │  │                                                  │  │
  │  │  NHƯNG! Next.js dùng React's Suspense để tích   │  │
  │  │  hợp stylesheet → hiện tại KHÔNG xóa stylesheet │  │
  │  │  khi navigate giữa routes!                       │  │
  │  │                                                  │  │
  │  │  → Có thể gây CONFLICTS giữa các route!        │  │
  │  │                                                  │  │
  │  │  GIẢI PHÁP:                                      │  │
  │  │  ✅ Global CSS → chỉ cho truly global (body,    │  │
  │  │     resets, base styles)                          │  │
  │  │  ✅ Tailwind → component styling                 │  │
  │  │  ✅ CSS Modules → custom scoped CSS              │  │
  │  │                                                  │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §5. External Stylesheets — Import Từ npm!

```
  EXTERNAL STYLESHEETS:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  Import CSS từ npm packages (Bootstrap, Ant Design...) │
  │  → Import TRỰC TIẾP ở bất kỳ file nào trong app/     │
  │                                                        │
  │  VÍ DỤ:                                               │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  // app/layout.tsx                               │  │
  │  │  import 'bootstrap/dist/css/bootstrap.css'       │  │
  │  │                                                  │  │
  │  │  export default function RootLayout({ children })│  │
  │  │  {                                               │  │
  │  │    return (                                       │  │
  │  │      <html lang="en">                            │  │
  │  │        <body className="container">              │  │
  │  │          {children}                               │  │
  │  │        </body>                                    │  │
  │  │      </html>                                      │  │
  │  │    )                                              │  │
  │  │  }                                               │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  │  💡 REACT 19 — CÁCH MỚI:                               │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  Có thể dùng thẻ <link> trực tiếp trong JSX:   │  │
  │  │                                                  │  │
  │  │  <link rel="stylesheet" href="..." />            │  │
  │  │                                                  │  │
  │  │  → React 19 hỗ trợ tích hợp stylesheet!        │  │
  │  │  → Xem docs: react.dev/reference/.../link       │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §6. Ordering & Merging — Thứ Tự CSS!

```
  CSS ORDERING — CRITICAL CONCEPT:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  Next.js tự động CHUNK (merge) CSS trong PRODUCTION!  │
  │  → Thứ tự CSS phụ thuộc vào THỨ TỰ IMPORT!           │
  │                                                        │
  │  VÍ DỤ THỰC TẾ:                                       │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │                                                  │  │
  │  │  // page.tsx                                     │  │
  │  │  import { BaseButton } from './base-button' ← ① │  │
  │  │  import styles from './page.module.css'     ← ② │  │
  │  │                                                  │  │
  │  │  export default function Page() {                │  │
  │  │    return <BaseButton className={styles.primary}/>│ │
  │  │  }                                               │  │
  │  │                                                  │  │
  │  │  // base-button.tsx                              │  │
  │  │  import styles from './base-button.module.css'   │  │
  │  │  export function BaseButton() {                  │  │
  │  │    return <button className={styles.primary} />  │  │
  │  │  }                                               │  │
  │  │                                                  │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  │  THỨ TỰ CSS TRONG OUTPUT:                              │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │                                                  │  │
  │  │  ① base-button.module.css   ← TRƯỚC (import ①) │  │
  │  │  ② page.module.css          ← SAU   (import ②) │  │
  │  │                                                  │  │
  │  │  → Vì <BaseButton> được import TRƯỚC             │  │
  │  │    page.module.css!                              │  │
  │  │                                                  │  │
  │  │  → CSS sau sẽ OVERRIDE CSS trước                │  │
  │  │    (nếu cùng specificity!)                       │  │
  │  │                                                  │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  │  IMPORT ORDER = CSS ORDER:                             │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │                                                  │  │
  │  │  import A from './a.module.css'  ← CSS order: 1 │  │
  │  │  import B from './comp-b'       ← CSS order: 2 │  │
  │  │  import C from './c.module.css'  ← CSS order: 3 │  │
  │  │                                                  │  │
  │  │  Nếu cả A, B, C đều define .primary:            │  │
  │  │  → C thắng! (vì xuất hiện SAU)                  │  │
  │  │                                                  │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §7. Recommendations — Best Practices!

```
  NEXT.JS OFFICIAL RECOMMENDATIONS:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  ① Tập trung CSS imports vào 1 file JS/TS entry      │
  │     → Dễ kiểm soát thứ tự!                           │
  │                                                        │
  │  ② Import global + Tailwind ở ROOT (app/layout.tsx)   │
  │     → Đảm bảo load đúng thứ tự!                      │
  │                                                        │
  │  ③ Dùng Tailwind cho PHẦN LỚN styling                │
  │     → Utility classes cover hầu hết patterns!        │
  │                                                        │
  │  ④ Dùng CSS Modules khi Tailwind KHÔNG ĐỦ            │
  │     → Complex animations, specific selectors          │
  │                                                        │
  │  ⑤ Naming convention NHẤT QUÁN cho Modules            │
  │     → <name>.module.css  ✅                           │
  │     → KHÔNG dùng <name>.tsx.module.css  ❌            │
  │                                                        │
  │  ⑥ Shared styles → shared components                  │
  │     → Tránh duplicate CSS imports!                    │
  │                                                        │
  │  ⑦ TẮT auto-sort imports (ESLint sort-imports)        │
  │     → Auto-sort có thể PHÁ VỠ CSS ordering!          │
  │     → CSS ordering phụ thuộc import order!            │
  │                                                        │
  │  ⑧ Dùng cssChunking trong next.config.js              │
  │     → Kiểm soát cách CSS được chunk!                 │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

```
  ⚠️ ĐIỂM NGUY HIỂM — AUTO-SORT IMPORTS:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  TRƯỚC auto-sort:               SAU auto-sort:        │
  │  ┌────────────────────┐         ┌────────────────────┐│
  │  │  import A.css      │         │  import B.css      ││
  │  │  import B.css      │   →→→   │  import A.css      ││
  │  │  import C.css      │         │  import C.css      ││
  │  └────────────────────┘         └────────────────────┘│
  │                                                        │
  │  → THỨ TỰ thay đổi = CSS override thay đổi!          │
  │  → UI có thể BỊ VỠ trong production!                  │
  │  → RẤT KHÓ DEBUG! (chỉ xảy ra khi build)             │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §8. Development vs Production — Sự Khác Biệt!

```
  DEV vs PROD — CSS BEHAVIOR:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  ┌───────────────────┬─────────────┬─────────────────┐│
  │  │                   │ Development │ Production       ││
  │  │                   │ (next dev)  │ (next build)     ││
  │  ├───────────────────┼─────────────┼─────────────────┤│
  │  │ CSS update        │ Fast Refresh│ Build-time only  ││
  │  │ CSS files         │ Inline      │ Minified .css    ││
  │  │ Code splitting    │ Không       │ Có, tự động     ││
  │  │ JS disabled?      │ CSS KHÔNG   │ CSS VẪN load!   ││
  │  │                   │ hoạt động!  │                  ││
  │  │ CSS ordering      │ Có thể KHÁC│ CHÍNH XÁC       ││
  │  │                   │ production! │                  ││
  │  └───────────────────┴─────────────┴─────────────────┘│
  │                                                        │
  │  ⚠️ ĐIỂM CỰC KỲ QUAN TRỌNG:                          │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  → CSS ordering KHÁC NHAU giữa dev và prod!    │  │
  │  │  → LUÔN check bằng "next build" trước deploy!  │  │
  │  │  → Dev: CSS inline qua JS → cần JS enabled     │  │
  │  │  → Prod: CSS extracted riêng → JS disabled OK  │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  │  PRODUCTION OPTIMIZATION:                              │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │                                                  │  │
  │  │  Source Files:                                    │  │
  │  │  ├── globals.css (12KB)                          │  │
  │  │  ├── header.module.css (3KB)                     │  │
  │  │  ├── button.module.css (2KB)                     │  │
  │  │  └── card.module.css (4KB)                       │  │
  │  │       │                                          │  │
  │  │       ▼ next build                               │  │
  │  │                                                  │  │
  │  │  Output (.next/):                                │  │
  │  │  ├── chunk-abc123.css (8KB, minified!)           │  │
  │  │  └── chunk-def456.css (5KB, minified!)           │  │
  │  │                                                  │  │
  │  │  → AUTO concatenate + minify!                    │  │
  │  │  → AUTO code-split per route!                    │  │
  │  │  → Load MINIMAL CSS cho mỗi route!              │  │
  │  │                                                  │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §9. Sơ Đồ Tổng Hợp — CSS Architecture!

```
  NEXT.JS CSS — TOÀN BỘ ARCHITECTURE:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  app/                                                  │
  │  ├── globals.css ──────────┐                           │
  │  │   @import 'tailwindcss' │                           │
  │  │   body { ... }          │                           │
  │  │                         ▼                           │
  │  ├── layout.tsx ─── import './globals.css' ← ROOT     │
  │  │                                                     │
  │  ├── page.tsx                                          │
  │  │   ├── import styles from './page.module.css' ← ②  │
  │  │   ├── import { Button } from './button'      ← ① │
  │  │   └── <main className="flex p-24">  ← Tailwind    │
  │  │            <Button className={styles.hero} />      │
  │  │                                                     │
  │  ├── page.module.css ........... scoped (hash unique) │
  │  │                                                     │
  │  ├── button.tsx                                        │
  │  │   └── import styles from './button.module.css'     │
  │  │                                                     │
  │  └── button.module.css ...... scoped (hash unique)    │
  │                                                        │
  │  ══════════════════════════════════════════════════    │
  │                                                        │
  │  BUILD OUTPUT (next build):                            │
  │  ┌──────────────────────────────────────────────────┐  │
  │  │  .next/static/css/                               │  │
  │  │  ├── chunk-layout.css  (globals + tailwind base) │  │
  │  │  └── chunk-page.css    (button.module + page.mod)│  │
  │  │                         ↑ ORDER: ① trước ②!     │  │
  │  └──────────────────────────────────────────────────┘  │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

```
  DECISION FLOW — CHỌN CSS NÀO?
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  Bạn cần style GÌ?                                    │
  │  │                                                     │
  │  ├── Body reset, font, base?                           │
  │  │   └── ✅ Global CSS (globals.css)                  │
  │  │                                                     │
  │  ├── Component styling thông thường?                   │
  │  │   └── ✅ Tailwind CSS (className="...")             │
  │  │                                                     │
  │  ├── Complex animation / pseudo-elements / media?      │
  │  │   └── ✅ CSS Modules (.module.css)                 │
  │  │                                                     │
  │  ├── 3rd party library CSS? (Bootstrap, etc.)          │
  │  │   └── ✅ External Stylesheet (import từ npm)       │
  │  │                                                     │
  │  ├── Dynamic styles dựa trên props/state?              │
  │  │   └── ✅ CSS-in-JS (styled-components) hoặc       │
  │  │       ✅ Tailwind conditional classes               │
  │  │                                                     │
  │  └── Sass/SCSS features? (variables, mixins, nesting)  │
  │      └── ✅ Sass (.scss) → cài sass package           │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §10. Tự Viết — CSS Resolution Engine!

```javascript
var CSSResolutionEngine = (function () {
  // ═══════════════════════════════════
  // 1. STYLESHEET REGISTRY
  // ═══════════════════════════════════
  var stylesheets = [];
  var importOrder = 0;

  function registerCSS(name, type, rules) {
    importOrder++;
    var entry = {
      name: name,
      type: type, // 'global' | 'module' | 'tailwind' | 'external'
      rules: rules,
      order: importOrder,
      scopedRules: null,
    };
    if (type === "module") {
      var hash =
        "_" +
        name.replace(/[^a-z]/gi, "") +
        Math.random().toString(36).substr(2, 4);
      entry.scopedRules = {};
      Object.keys(rules).forEach(function (cls) {
        entry.scopedRules[cls] = cls + hash;
      });
      console.log(
        "  [CSS Module] " + name + " → scoped classes:",
        entry.scopedRules,
      );
    }
    stylesheets.push(entry);
    return entry;
  }

  // ═══════════════════════════════════
  // 2. RESOLVE ORDERING
  // ═══════════════════════════════════
  function resolveOrder() {
    console.log("\n━━━ CSS Ordering (Production) ━━━");
    var sorted = stylesheets.slice().sort(function (a, b) {
      return a.order - b.order;
    });
    sorted.forEach(function (s, i) {
      console.log(
        "  " +
          (i + 1) +
          ". [" +
          s.type.toUpperCase() +
          "] " +
          s.name +
          " (import #" +
          s.order +
          ")",
      );
    });
    console.log("\n  ⚠️ CSS sau sẽ OVERRIDE CSS trước (cùng specificity)!");
    return sorted;
  }

  // ═══════════════════════════════════
  // 3. CHECK CONFLICTS
  // ═══════════════════════════════════
  function checkConflicts() {
    console.log("\n━━━ Conflict Detection ━━━");
    var globalRules = {};
    var conflicts = [];
    stylesheets.forEach(function (s) {
      if (s.type === "module") return; // modules are scoped → no conflicts
      Object.keys(s.rules).forEach(function (selector) {
        if (globalRules[selector]) {
          conflicts.push({
            selector: selector,
            file1: globalRules[selector],
            file2: s.name,
          });
        }
        globalRules[selector] = s.name;
      });
    });
    if (conflicts.length === 0) {
      console.log("  ✅ No global CSS conflicts detected!");
    } else {
      conflicts.forEach(function (c) {
        console.log(
          '  ⚠️ CONFLICT: "' +
            c.selector +
            '" in ' +
            c.file1 +
            " AND " +
            c.file2,
        );
      });
    }
  }

  // ═══════════════════════════════════
  // 4. PRODUCTION BUILD SIM
  // ═══════════════════════════════════
  function simulateBuild() {
    console.log("\n━━━ Production Build ━━━");
    var chunks = { layout: [], page: [] };
    stylesheets.forEach(function (s) {
      if (
        s.type === "global" ||
        s.type === "tailwind" ||
        s.type === "external"
      ) {
        chunks.layout.push(s.name);
      } else {
        chunks.page.push(s.name);
      }
    });
    var totalSize = 0;
    Object.keys(chunks).forEach(function (chunk) {
      if (chunks[chunk].length === 0) return;
      var size = chunks[chunk].length * 2; // simplified
      totalSize += size;
      console.log("  chunk-" + chunk + ".css (" + size + "KB minified):");
      chunks[chunk].forEach(function (name) {
        console.log("    └── " + name);
      });
    });
    console.log("  TOTAL: " + totalSize + "KB (minified + code-split)");
  }

  function demo() {
    console.log("╔══════════════════════════════════════╗");
    console.log("║ CSS RESOLUTION ENGINE                 ║");
    console.log("╚══════════════════════════════════════╝");

    registerCSS("globals.css", "global", {
      body: "padding:20px",
      "*": "box-sizing:border-box",
    });
    registerCSS("tailwind", "tailwind", {
      ".flex": "display:flex",
      ".p-24": "padding:6rem",
    });
    registerCSS("bootstrap.css", "external", {
      ".container": "max-width:1140px",
      body: "font-family:sans",
    });
    registerCSS("button.module.css", "module", {
      primary: "bg:blue",
      secondary: "bg:gray",
    });
    registerCSS("page.module.css", "module", {
      primary: "bg:red",
      hero: "padding:48px",
    });

    resolveOrder();
    checkConflicts();
    simulateBuild();
  }

  return { demo: demo, register: registerCSS, resolve: resolveOrder };
})();
// Chạy: CSSResolutionEngine.demo();
```

---

## §11. Tổng Kết & Câu Hỏi Luyện Tập!

```
  TỔNG KẾT — 8 ĐIỂM CHÍNH:
  ┌────────────────────────────────────────────────────────┐
  │  ① Tailwind CSS v4: @import 'tailwindcss' + PostCSS  │
  │  ② CSS Modules: .module.css → auto-generate unique   │
  │  ③ Global CSS: import ở layout → apply toàn app      │
  │  ④ External: import trực tiếp từ node_modules        │
  │  ⑤ Ordering: thứ tự IMPORT = thứ tự CSS output      │
  │  ⑥ TẮT auto-sort imports → tránh phá vỡ CSS order   │
  │  ⑦ Dev ≠ Prod: luôn check bằng "next build"!        │
  │  ⑧ Prod: auto concatenate + minify + code-split      │
  └────────────────────────────────────────────────────────┘
```

### Câu Hỏi Luyện Tập

**Câu 1**: Tại sao CSS Modules không bao giờ gây naming conflict?

<details><summary>Đáp án</summary>

CSS Modules tự động generate **hash duy nhất** cho mỗi class name:

- Bạn viết: `.blog { ... }` trong `blog.module.css`
- Next.js generate: `.blog_a1b2c3 { ... }`
- Mỗi file `.module.css` có hash **khác nhau**
- Dù 10 file dùng `.blog` → output 10 class names **khác nhau hoàn toàn**
- → **KHÔNG BAO GIỜ** conflict!

</details>

---

**Câu 2**: Tại sao phải TẮT ESLint `sort-imports` khi dùng CSS Modules?

<details><summary>Đáp án</summary>

Vì **thứ tự CSS output phụ thuộc vào thứ tự import**:

- ESLint `sort-imports` tự động **sắp xếp lại** thứ tự import
- → CSS ordering **thay đổi** so với bạn mong đợi
- → CSS rules bị override **sai thứ tự** trong production
- → UI **vỡ layout** nhưng chỉ xảy ra ở production build!
- → **Rất khó debug** vì dev mode có thể hoạt động đúng!

</details>

---

**Câu 3**: Giải thích sự khác biệt giữa dev và production CSS behavior.

<details><summary>Đáp án</summary>

| Aspect         | Development (`next dev`)    | Production (`next build`) |
| -------------- | --------------------------- | ------------------------- |
| CSS update     | **Fast Refresh** (instant)  | Build-time only           |
| CSS delivery   | Inline qua JS               | Extracted `.css` riêng    |
| JS disabled    | CSS **KHÔNG** hoạt động!    | CSS **VẪN** hoạt động!    |
| Code splitting | Không                       | Có, tự động per route     |
| CSS ordering   | Có thể **KHÁC** production! | **Chính xác**             |
| Minification   | Không                       | Có, tự động               |

→ **Luôn kiểm tra** bằng `next build` trước khi deploy!

</details>

---

**Câu 4**: Global CSS có thể import ở bất kỳ đâu. Tại sao Next.js vẫn khuyên chỉ dùng cho truly global styles?

<details><summary>Đáp án</summary>

Vì **React Suspense** integration hiện tại:

1. Next.js dùng React's built-in support cho stylesheets
2. Khi navigate giữa routes → **KHÔNG xóa** stylesheets cũ!
3. Global CSS từ route A vẫn **tồn tại** khi vào route B
4. → Nếu route A và B đều define `.title { color: red }` và `.title { color: blue }`
5. → **Conflict!** — không biết style nào được apply

**Giải pháp**: Chỉ dùng global CSS cho body resets, font imports, base styles — những thứ **KHÔNG đổi** giữa các routes!

</details>

---

**Câu 5**: Cho file structure, xác định thứ tự CSS output:

```
// page.tsx
import { Card } from './card'        // ①
import { Button } from './button'    // ②
import styles from './page.module.css' // ③
```

<details><summary>Đáp án</summary>

Thứ tự CSS output:

1. **card.module.css** (import ① — Card component)
2. **button.module.css** (import ② — Button component)
3. **page.module.css** (import ③ — trực tiếp)

→ Nếu cả 3 đều define `.primary`:

- `page.module.css` thắng (vì ở sau cùng)
- Nhưng vì dùng CSS Modules → mỗi `.primary` có hash **khác nhau**
- → Thực tế **KHÔNG conflict** nếu dùng CSS Modules!

→ Conflict chỉ xảy ra khi dùng **Global CSS** hoặc **Tailwind classes giống nhau**!

</details>
