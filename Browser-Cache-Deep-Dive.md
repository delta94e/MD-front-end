# Browser Cache — Caching Mechanism Deep Dive

> 📅 2026-02-11 · ⏱ 20 phút đọc
>
> Tài liệu chuyên sâu về Browser Caching: Strong Cache vs Conditional Cache,
> Cache-Control, Expires, ETag, Last-Modified, và hành vi refresh.
> Độ khó: ⭐️⭐️⭐️⭐️ | Chủ đề: Browser Performance

---

## Mục Lục

0. [Tại sao cần Browser Cache?](#0-tại-sao-cần-browser-cache)
1. [Quy trình Browser Caching](#1-quy-trình-browser-caching)
2. [Strong Cache (Cache mạnh)](#2-strong-cache-cache-mạnh)
3. [Conditional Cache (Cache có điều kiện)](#3-conditional-cache-cache-có-điều-kiện)
4. [So sánh Strong Cache vs Conditional Cache](#4-so-sánh-strong-cache-vs-conditional-cache)
5. [Hành vi Refresh — F5 vs Ctrl+F5 vs Enter](#5-hành-vi-refresh--f5-vs-ctrlf5-vs-enter)
6. [Tóm Tắt & Câu Hỏi Phỏng Vấn](#6-tóm-tắt--câu-hỏi-phỏng-vấn)

---

## 0. Tại sao cần Browser Cache?

```
TẠI SAO CẦN BROWSER CACHE?
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  Browser cache nhắm vào STATIC RESOURCES (tài nguyên    │
  │  tĩnh) ở frontend: JS, CSS, images, fonts...            │
  │                                                          │
  │  LẦN ĐẦU:  Request → Server → Download → LƯU LOCAL    │
  │  LẦN SAU:  Server chưa update? → ĐỌC TỪ LOCAL!        │
  │            Server đã update?   → Fetch mới + LƯU lại   │
  │                                                          │
  │  → Giảm số requests gửi server                          │
  │  → Tăng hiệu suất website                               │
  │                                                          │
  └──────────────────────────────────────────────────────────┘

  ✅ LỢI ÍCH:
  ┌──────────────────────────────────────────────────────────┐
  │ ① Giảm TẢI cho server                                   │
  │ ② Tăng TỐC ĐỘ load trang ở client                     │
  │ ③ Giảm TRUYỀN TẢI data không cần thiết qua network    │
  └──────────────────────────────────────────────────────────┘

  ĐỊNH NGHĨA:
  → Browser lưu static resources trên LOCAL DISK
  → Truy cập lại → load TRỰC TIẾP từ local
  → KHÔNG cần request server lần nữa
```

---

## 1. Quy trình Browser Caching

> **🎯 Toàn bộ quy trình caching từ lần đầu đến lần sau**

```
QUY TRÌNH BROWSER CACHING — TOÀN DIỆN:
═══════════════════════════════════════════════════════════════

  ① LẦN ĐẦU TIÊN LOAD RESOURCE:
  ┌──────────────────────────────────────────────────────────┐
  │ Browser ──request──► Server                              │
  │ Server ──200 + resource + headers──► Browser             │
  │ Browser LƯU resource + response headers vào cache       │
  └──────────────────────────────────────────────────────────┘

  ② LẦN SAU LOAD RESOURCE:
  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  Browser gửi request                                      │
  │       │                                                  │
  │       ▼                                                  │
  │  ┌─────────────┐  CÓ                                    │
  │  │ Có cache?    ├──────────────┐                         │
  │  └──────┬──────┘               │                         │
  │         │ KHÔNG                 ▼                         │
  │         │                 ┌──────────┐  CHƯA             │
  │         │                 │ Hết hạn?  ├───────┐           │
  │         │                 └────┬─────┘        │           │
  │         │                      │ HẾT HẠN     │           │
  │         │                      ▼              ▼           │
  │         │                ┌──────────┐   ┌──────────┐     │
  │         │                │ Có ETag? │   │ STRONG   │     │
  │         │                └────┬─────┘   │ CACHE    │     │
  │         │               CÓ   │ KHÔNG    │ HIT! ✅  │     │
  │         │                ▼    ▼          │ Dùng     │     │
  │         │         ┌───────┐┌────────┐   │ local    │     │
  │         │         │If-None││Last-   │   └──────────┘     │
  │         │         │-Match ││Modified│                     │
  │         │         └───┬───┘└───┬────┘                     │
  │         │             │        │                          │
  │         │             └───┬────┘                          │
  │         │                 ▼                               │
  │         │         ┌──────────────┐                        │
  │         │         │ Server kiểm  │                        │
  │         │         │ tra freshness│                        │
  │         │         └──────┬───────┘                        │
  │         │           ┌────┴────┐                           │
  │         │           ▼         ▼                           │
  │         │     ┌──────────┐┌──────────┐                   │
  │         │     │ 304      ││ 200      │                   │
  │         │     │ Not      ││ Trả      │                   │
  │         │     │ Modified ││ resource │                   │
  │         │     │ → dùng   ││ MỚI +    │                   │
  │         │     │ cache    ││ headers  │                   │
  │         │     └──────────┘└──────────┘                   │
  │         │                      │                          │
  │         └──────────────────────┘                          │
  │              Gửi request mới tới server                  │
  │              → 200 + download resource                   │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### Version Number trong URL

```
VERSION NUMBER — CACHE BUSTING:
═══════════════════════════════════════════════════════════════

  Nhiều website thêm VERSION NUMBER vào resource URL:

  VD: style.css?v=1.0  →  style.css?v=2.0
      main.js?v=abc123 →  main.js?v=def456

  → Khi deploy JS/CSS mới → ĐỔI version number
  → Browser thấy URL KHÁC → coi là resource MỚI
  → KHÔNG dùng cache cũ → download BẮT BUỘC
  → User luôn nhận được bản CẬP NHẬT mới nhất
```

---

## 2. Strong Cache (Cache mạnh)

> **🎯 Strong cache hit → dùng TRỰC TIẾP, KHÔNG gửi request tới server**

```
STRONG CACHE — 2 PHƯƠNG THỨC:
═══════════════════════════════════════════════════════════════

  Strong Cache HIT:
  ┌──────┐                              ┌──────┐
  │      │  KHÔNG gửi request!          │      │
  │Client│  → Dùng local cache TRỰC TIẾP│Server│
  │      │  → Status: 200 (from cache)  │      │
  └──────┘                              └──────┘
```

### ① Expires (HTTP/1.0)

```
EXPIRES — THỜI GIAN HẾT HẠN TUYỆT ĐỐI:
═══════════════════════════════════════════════════════════════

  Response header:
  Expires: Thu, 10 Nov 2025 08:12:00 GMT

  → Server chỉ định THỜI ĐIỂM hết hạn (absolute time)
  → Trước thời điểm đó → dùng cache, KHÔNG gửi request
  → Sau thời điểm đó → resource hết hạn → request mới

  ❌ NHƯỢC ĐIỂM:
  ┌──────────────────────────────────────────────────────────┐
  │ → Thời gian TUYỆT ĐỐI = thời gian SERVER               │
  │ → Client time ≠ Server time → SAI!                      │
  │ → User CÓ THỂ sửa clock → ảnh hưởng cache             │
  └──────────────────────────────────────────────────────────┘
```

### ② Cache-Control (HTTP/1.1)

```
CACHE-CONTROL — KIỂM SOÁT CHÍNH XÁC HƠN:
═══════════════════════════════════════════════════════════════

  ┌────────────────┬──────────────────────────────────────────┐
  │ Field           │ Mô tả                                   │
  ├────────────────┼──────────────────────────────────────────┤
  │ public         │ BẤT KỲ ai cũng cache được              │
  │                │ (client, proxy, CDN)                     │
  │                │ Ít dùng, thường dùng max-age thay       │
  ├────────────────┼──────────────────────────────────────────┤
  │ private        │ Chỉ BROWSER user cache được             │
  │                │ PROXY KHÔNG được cache                   │
  │                │ → Thường dùng cho HTML chứa user info   │
  │                │ → Tránh CDN cache data cá nhân         │
  ├────────────────┼──────────────────────────────────────────┤
  │ no-cache       │ Có cache nhưng PHẢI HỎI server trước   │
  │                │ → Không có STRONG cache                  │
  │                │ → CÓ conditional cache                  │
  ├────────────────┼──────────────────────────────────────────┤
  │ no-store       │ KHÔNG cache gì cả                       │
  │                │ → Mỗi lần request = fetch MỚI từ server│
  ├────────────────┼──────────────────────────────────────────┤
  │ max-age=N      │ Cache hợp lệ trong N GIÂY              │
  │                │ (thời gian tương đối, không tuyệt đối) │
  ├────────────────┼──────────────────────────────────────────┤
  │ s-maxage=N     │ Như max-age nhưng CHỈ CHO shared cache │
  │                │ (CDN). ƯU TIÊN CAO HƠN max-age/Expires│
  ├────────────────┼──────────────────────────────────────────┤
  │ max-stale[=N]  │ Client CHẤP NHẬN resource đã hết hạn   │
  │                │ nhưng KHÔNG quá N giây                  │
  └────────────────┴──────────────────────────────────────────┘

  ƯU TIÊN: Cache-Control > Expires
  (Khi cả 2 có → Cache-Control được dùng)
```

### no-cache vs no-store

```
no-cache vs no-store — DỄ NHẦM LẪN:
═══════════════════════════════════════════════════════════════

  ┌──────────────────┬──────────────────────────────────────┐
  │  no-cache         │  no-store                            │
  ├──────────────────┼──────────────────────────────────────┤
  │ CÓ lưu cache     │ KHÔNG lưu cache gì cả              │
  │                   │                                     │
  │ Nhưng PHẢI HỎI   │ Mỗi lần = fetch MỚI                │
  │ server trước khi  │ hoàn toàn từ server                │
  │ dùng              │                                     │
  │                   │                                     │
  │ = Không có strong │ = Không có bất kỳ                   │
  │   cache           │   cache nào                         │
  │ = CÓ conditional  │                                     │
  │   cache           │                                     │
  └──────────────────┴──────────────────────────────────────┘

  💡 GHI NHỚ:
  no-cache = "hỏi trước rồi dùng cache"
  no-store = "không cache, lấy mới hoàn toàn"
```

---

## 3. Conditional Cache (Cache có điều kiện)

> **🎯 Conditional cache: gửi request hỏi server → 304 hoặc 200**

```
CONDITIONAL CACHE — KHI NÀO KÍCH HOẠT?
═══════════════════════════════════════════════════════════════

  Conditional cache kích hoạt khi:
  ┌──────────────────────────────────────────────────────────┐
  │ ① Strong cache HẾT HẠN (max-age expired)               │
  │ ② Hoặc Cache-Control: no-cache (bỏ qua strong cache)  │
  └──────────────────────────────────────────────────────────┘

  Flow:
  ┌──────┐  request + headers    ┌──────┐
  │      │─────────────────────►│      │
  │Client│                      │Server│
  │      │◄─────────────────────│      │
  └──────┘  304 (dùng cache)    └──────┘
            HOẶC
            200 (resource mới)
```

### ① Last-Modified / If-Modified-Since

```
LAST-MODIFIED — THỜI GIAN SỬA ĐỔI CUỐI:
═══════════════════════════════════════════════════════════════

  LẦN ĐẦU:
  Server → Response header:
  Last-Modified: Wed, 10 Feb 2026 08:00:00 GMT

  LẦN SAU:
  Browser → Request header:
  If-Modified-Since: Wed, 10 Feb 2026 08:00:00 GMT
  (giá trị = Last-Modified lần trước)

  ┌──────┐  If-Modified-Since: ...    ┌──────┐
  │      │──────────────────────────►│      │
  │Client│                           │Server│
  │      │  So sánh với thời gian    │      │
  │      │  sửa đổi thực tế         │      │
  │      │                           │      │
  │      │◄─ 304 (chưa đổi, cache) ─│      │
  │      │◄─ 200 (đã đổi, mới)   ──│      │
  └──────┘                           └──────┘

  ❌ NHƯỢC ĐIỂM:
  ┌──────────────────────────────────────────────────────────┐
  │ → Chỉ chính xác tới GIÂY (second)                      │
  │ → File sửa NHIỀU LẦN trong 1 giây?                     │
  │   → Last-Modified KHÔNG ĐỔI → cache SAI!               │
  │ → File nội dung KHÔNG đổi nhưng save lại?              │
  │   → Last-Modified ĐỔI → cache miss SÁI!               │
  └──────────────────────────────────────────────────────────┘
```

### ② ETag / If-None-Match

```
ETAG — UNIQUE IDENTIFIER CHO RESOURCE:
═══════════════════════════════════════════════════════════════

  LẦN ĐẦU:
  Server → Response header:
  ETag: "5d8c72a5e4b0a3c7..."
  (unique hash dựa trên NỘI DUNG resource)

  LẦN SAU:
  Browser → Request header:
  If-None-Match: "5d8c72a5e4b0a3c7..."
  (giá trị = ETag lần trước)

  ┌──────┐  If-None-Match: "abc..."   ┌──────┐
  │      │──────────────────────────►│      │
  │Client│                           │Server│
  │      │  So sánh ETag hiện tại    │      │
  │      │  với ETag trong request   │      │
  │      │                           │      │
  │      │◄─ 304 (ETag khớp, cache)─│      │
  │      │◄─ 200 (ETag khác, mới) ──│      │
  └──────┘                           └──────┘

  ✅ ƯU ĐIỂM so với Last-Modified:
  ┌──────────────────────────────────────────────────────────┐
  │ → Dựa trên NỘI DUNG (content hash), không phải time   │
  │ → Chính xác hơn! File đổi nội dung = ETag đổi         │
  │ → File không đổi nội dung = ETag giữ nguyên            │
  └──────────────────────────────────────────────────────────┘

  ⚠️ LƯU Ý LOAD BALANCING:
  ┌──────────────────────────────────────────────────────────┐
  │ → ETag KHÁC NHAU trên mỗi server                       │
  │ → Multi-server environment: ETag không nhất quán        │
  │ → Giải pháp: dùng Last-Modified (nhất quán hơn)        │
  │ → Hoặc: đảm bảo ETag generation đồng nhất              │
  └──────────────────────────────────────────────────────────┘
```

### Ưu tiên: ETag > Last-Modified

```
ƯU TIÊN — ETAG > LAST-MODIFIED:
═══════════════════════════════════════════════════════════════

  Khi CẢ HAI có trong response:
  → Server kiểm tra ETag TRƯỚC
  → ETag khớp? → 304, KHÔNG cần check Last-Modified
  → ETag không khớp? → 200, trả resource mới

  ┌──────────────┬───────────────────┬───────────────────┐
  │               │ Last-Modified     │ ETag               │
  ├──────────────┼───────────────────┼───────────────────┤
  │ Độ chính xác │ Tới GIÂY          │ Tới NỘI DUNG      │
  │ Cơ chế       │ Thời gian sửa đổi│ Content hash       │
  │ Request hdr  │ If-Modified-Since │ If-None-Match      │
  │ Ưu tiên      │ Thấp hơn          │ CAO HƠN ★         │
  │ Load balance │ Nhất quán ✅      │ Khác mỗi server ❌│
  └──────────────┴───────────────────┴───────────────────┘
```

---

## 4. So sánh Strong Cache vs Conditional Cache

```
STRONG CACHE vs CONDITIONAL CACHE:
═══════════════════════════════════════════════════════════════

  ┌──────────────────┬───────────────────┬───────────────────┐
  │  Tiêu chí         │ Strong Cache      │ Conditional Cache │
  ├──────────────────┼───────────────────┼───────────────────┤
  │ Gửi request?     │ KHÔNG ❌           │ CÓ ✅             │
  │                   │ (dùng local       │ (hỏi server       │
  │                   │  trực tiếp)       │  trước khi dùng) │
  ├──────────────────┼───────────────────┼───────────────────┤
  │ Status code       │ 200 (from cache)  │ 304 Not Modified │
  ├──────────────────┼───────────────────┼───────────────────┤
  │ Response body     │ Từ local          │ Server KHÔNG trả │
  │                   │                   │ body (chỉ header) │
  ├──────────────────┼───────────────────┼───────────────────┤
  │ Headers           │ Expires           │ Last-Modified /   │
  │                   │ Cache-Control     │ If-Modified-Since │
  │                   │                   │ ETag /            │
  │                   │                   │ If-None-Match     │
  ├──────────────────┼───────────────────┼───────────────────┤
  │ Khi cache HIT    │ Dùng local TRỰC   │ Dùng local copy  │
  │                   │ TIẾP (nhanh nhất) │ (sau khi xác     │
  │                   │                   │  nhận server)    │
  ├──────────────────┼───────────────────┼───────────────────┤
  │ Khi cache MISS   │ Gửi request       │ Server trả 200   │
  │                   │ tới server        │ + resource mới   │
  └──────────────────┴───────────────────┴───────────────────┘
```

### Kết hợp Strong + Conditional Cache

```
KẾT HỢP STRONG + CONDITIONAL CACHE:
═══════════════════════════════════════════════════════════════

  Trong thực tế, CẢ HAI được dùng CÙNG LÚC:

  Browser request
       │
       ▼
  ┌───────────────────┐
  │ Strong cache HIT? │
  └────┬──────┬───────┘
       │      │
      YES     NO
       │      │
       ▼      ▼
  ┌────────┐ ┌────────────────────┐
  │ Dùng   │ │ Conditional cache  │
  │ local  │ │ → Gửi request +   │
  │ TRỰC   │ │   If-None-Match /  │
  │ TIẾP   │ │   If-Modified-Since│
  │ ✅     │ │                    │
  └────────┘ └───────┬────────────┘
                     │
               ┌─────┴─────┐
               │           │
            304          200
               │           │
               ▼           ▼
          ┌────────┐  ┌────────┐
          │ Dùng   │  │ Nhận   │
          │ local  │  │ resource│
          │ copy   │  │ MỚI    │
          └────────┘  └────────┘

  ƯU TIÊN HEADERS:
  ┌─────────────────────────────────────────────────────────┐
  │ Strong:      Cache-Control > Expires                    │
  │ Conditional: ETag > Last-Modified                       │
  │ Overall:     Strong cache > Conditional cache           │
  └─────────────────────────────────────────────────────────┘
```

---

## 5. Hành vi Refresh — F5 vs Ctrl+F5 vs Enter

> **🎯 3 cách refresh → 3 hành vi cache khác nhau**

```
3 CÁCH REFRESH — HÀNH VI CACHE:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │  ① CLICK REFRESH / F5                                    │
  │  ──────────────────────                                  │
  │  → Local cache HẾT HẠN ngay lập tức                    │
  │  → NHƯNG VẪN GỬI If-Modified-Since + If-None-Match     │
  │  → Server kiểm tra freshness                             │
  │  → Kết quả: 304 (cache) HOẶC 200 (mới)                 │
  │                                                          │
  │  = Bỏ qua STRONG cache                                   │
  │  = VẪN CÓ conditional cache                              │
  ├──────────────────────────────────────────────────────────┤
  │  ② CTRL + F5 (FORCE REFRESH)                              │
  │  ─────────────────────────────                           │
  │  → Local cache HẾT HẠN ngay lập tức                    │
  │  → KHÔNG GỬI If-Modified-Since, If-None-Match           │
  │  → Như chưa bao giờ request trước đó                    │
  │  → Kết quả: LUÔN LUÔN 200 (download mới)               │
  │                                                          │
  │  = Bỏ qua STRONG cache                                   │
  │  = Bỏ qua CONDITIONAL cache                              │
  │  = Fetch HOÀN TOÀN MỚI                                   │
  ├──────────────────────────────────────────────────────────┤
  │  ③ ENTER TRONG ADDRESS BAR                                │
  │  ──────────────────────────                              │
  │  → Quy trình BÌNH THƯỜNG                                 │
  │  → Kiểm tra local cache hết hạn chưa (strong cache)    │
  │  → Chưa hết hạn? → dùng luôn ✅                        │
  │  → Hết hạn? → conditional cache → server check          │
  │                                                          │
  │  = CÓ strong cache                                        │
  │  = CÓ conditional cache                                   │
  │  = Quy trình đầy đủ                                      │
  └──────────────────────────────────────────────────────────┘

  BẢNG SO SÁNH:
  ┌──────────────────┬──────────┬──────────────┬─────────────┐
  │  Hành vi          │ Strong   │ Conditional  │ Kết quả     │
  │                   │ Cache    │ Cache        │ có thể      │
  ├──────────────────┼──────────┼──────────────┼─────────────┤
  │ Enter address bar│ ✅ CÓ    │ ✅ CÓ        │ 200/304     │
  │ F5 / Refresh     │ ❌ BỎ QUA│ ✅ CÓ        │ 200/304     │
  │ Ctrl+F5          │ ❌ BỎ QUA│ ❌ BỎ QUA    │ LUÔN 200    │
  └──────────────────┴──────────┴──────────────┴─────────────┘
```

---

## 6. Tóm Tắt & Câu Hỏi Phỏng Vấn

### Quick Reference

```
BROWSER CACHE — QUICK REFERENCE:
═══════════════════════════════════════════════════════════════

  STRONG CACHE (không gửi request):
    Expires     — HTTP/1.0, absolute time (nhược: clock skew)
    Cache-Control — HTTP/1.1, relative time, chính xác hơn
    Priority: Cache-Control > Expires

  CONDITIONAL CACHE (gửi request, server check):
    Last-Modified / If-Modified-Since — timestamp (tới giây)
    ETag / If-None-Match — content hash (chính xác hơn)
    Priority: ETag > Last-Modified

  FLOW: Strong cache → miss → Conditional cache → miss → 200

  CACHE-CONTROL VALUES:
    public     — ai cũng cache được
    private    — chỉ browser user
    no-cache   — có cache, phải hỏi server (= conditional)
    no-store   — KHÔNG cache gì cả
    max-age=N  — hợp lệ N giây

  REFRESH:
    Enter      — full flow (strong + conditional)
    F5         — skip strong, keep conditional
    Ctrl+F5    — skip ALL, always 200
```

### Câu Hỏi Phỏng Vấn Thường Gặp

**1. Quy trình browser caching hoạt động thế nào?**

> Lần đầu: server trả 200 + resource + caching headers → browser lưu local. Lần sau: kiểm tra **strong cache** (Cache-Control/Expires) → hết hạn? → gửi request với If-None-Match/If-Modified-Since (**conditional cache**) → server kiểm tra → 304 (dùng cache) hoặc 200 (resource mới).

**2. Strong cache và Conditional cache khác nhau thế nào?**

> **Strong cache**: KHÔNG gửi request, dùng local trực tiếp (200 from cache). **Conditional cache**: GỬI request hỏi server, server trả 304 (dùng cache) hoặc 200 (mới). Cả hai đều dùng local copy khi hit, nhưng conditional phải xác nhận với server trước.

**3. Cache-Control có những giá trị gì?**

> `public` (ai cũng cache), `private` (chỉ browser), `no-cache` (có cache nhưng phải hỏi server = conditional only), `no-store` (không cache), `max-age=N` (hợp lệ N giây), `s-maxage=N` (cho CDN, ưu tiên hơn max-age). **Cache-Control ưu tiên hơn Expires**.

**4. no-cache vs no-store?**

> `no-cache`: VẪN LƯU cache nhưng phải hỏi server trước khi dùng (bỏ strong cache, giữ conditional cache). `no-store`: KHÔNG lưu cache gì cả, mỗi lần fetch mới hoàn toàn.

**5. ETag vs Last-Modified?**

> **Last-Modified**: dựa trên thời gian sửa đổi, chính xác tới giây. Nhược: sửa nhiều lần/giây → không phát hiện; save lại không đổi nội dung → cache miss sai. **ETag**: dựa trên content hash, chính xác hơn. Nhược: mỗi server tạo ETag khác nhau → vấn đề load balancing. **ETag ưu tiên hơn Last-Modified**.

**6. F5 vs Ctrl+F5 vs Enter address bar?**

> **Enter**: quy trình đầy đủ (strong + conditional cache). **F5**: bỏ qua strong cache, giữ conditional → kết quả 304 hoặc 200. **Ctrl+F5**: bỏ qua TẤT CẢ cache, không gửi If-Modified-Since/If-None-Match → LUÔN 200 (fetch mới hoàn toàn).

**7. Tại sao thêm version number vào URL?**

> Khi deploy JS/CSS mới, đổi version number (VD: `style.css?v=1.0` → `?v=2.0`) → browser thấy URL khác → coi là resource mới → download bắt buộc → user nhận bản cập nhật mới nhất. Đây là **cache busting** technique.

**8. Load Balancing ảnh hưởng caching thế nào?**

> Multi-server: **Last-Modified** nên nhất quán (cùng file = cùng mtime). **ETag** có thể khác nhau trên mỗi server (khác algorithm/config) → request tới server khác → ETag không khớp → cache miss sai. Giải pháp: đồng nhất ETag generation hoặc ưu tiên Last-Modified.

---

## Checklist Học Tập

- [ ] Hiểu tại sao cần browser cache (3 lợi ích)
- [ ] Mô tả toàn bộ quy trình browser caching
- [ ] Phân biệt Strong Cache vs Conditional Cache
- [ ] Biết Expires vs Cache-Control (ưu tiên + nhược điểm)
- [ ] Biết 7 giá trị Cache-Control (public, private, no-cache...)
- [ ] Phân biệt no-cache vs no-store
- [ ] Biết Last-Modified vs ETag (ưu tiên + nhược điểm)
- [ ] Hiểu 304 Not Modified trong caching flow
- [ ] Phân biệt F5 vs Ctrl+F5 vs Enter address bar
- [ ] Hiểu cache busting với version numbers

---

_Cập nhật lần cuối: Tháng 2, 2026_
