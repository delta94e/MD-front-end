# Next.js `Font` Module — Deep Dive!

> **Chủ đề**: `next/font` — Automatic Font Optimization!
> **Ngôn ngữ**: Tiếng Việt — giải thích cực kỳ chi tiết!
> **Phương châm**: Tự viết lại bằng tay — KHÔNG dùng thư viện!
> **Nguồn**: https://nextjs.org/docs/app/api-reference/components/font
> **Hình ảnh**: 0 diagrams trong trang gốc — TẤT CẢ sơ đồ tự vẽ!

---

## Mục Lục

1. [§1. Tổng Quan — Font Module!](#1)
2. [§2. Google Fonts — next/font/google!](#2)
3. [§3. Local Fonts — next/font/local!](#3)
4. [§4. All Options — Complete Reference!](#4)
5. [§5. Applying Styles — 3 Methods!](#5)
6. [§6. Advanced — Multiple Fonts + Tailwind!](#6)
7. [§7. Tự Viết — NextFontEngine!](#7)
8. [§8. Câu Hỏi Luyện Tập](#8)

---

## §1. Tổng Quan — Font Module!

```
  NEXT/FONT MODULE:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  WHAT: Automatic font optimization + self-hosting!          │
  │                                                              │
  │  import { Inter } from 'next/font/google'                   │
  │  const inter = Inter({ subsets: ['latin'], display: 'swap' })│
  │  <html className={inter.className}>                         │
  │                                                              │
  │  WHAT IT DOES:                                                │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ ① Self-hosting     → downloads fonts at BUILD time! │    │
  │  │ ② No external req  → no requests to Google at runtime│   │
  │  │ ③ No layout shift  → adjustFontFallback + preload!  │    │
  │  │ ④ Subsetting        → only needed characters!       │    │
  │  │ ⑤ CSS @font-face   → generated automatically!       │    │
  │  │ ⑥ Privacy           → no data sent to Google!       │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  TWO MODULES:                                                 │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │                                                      │    │
  │  │  next/font/google        next/font/local            │    │
  │  │  ┌────────────────┐     ┌────────────────┐          │    │
  │  │  │ Import by name!│     │ Import from    │          │    │
  │  │  │ Inter, Roboto, │     │ local files!   │          │    │
  │  │  │ Lora, etc.     │     │ .woff2, .ttf,  │          │    │
  │  │  │                │     │ .otf, .woff    │          │    │
  │  │  │ Downloaded at  │     │ Already in     │          │    │
  │  │  │ build time!    │     │ your project!  │          │    │
  │  │  └────────────────┘     └────────────────┘          │    │
  │  │                                                      │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  TRADITIONAL vs NEXT/FONT:                                    │
  │  ┌──────────────────┬────────────────┬──────────────────┐    │
  │  │                  │ Traditional    │ next/font        │    │
  │  ├──────────────────┼────────────────┼──────────────────┤    │
  │  │ Font loading     │ Runtime HTTP!  │ Build time! ✅  │    │
  │  │ Layout shift     │ ❌ FOUT/FOIT! │ ✅ Prevented!   │    │
  │  │ Privacy          │ ❌ Google req! │ ✅ Self-hosted! │    │
  │  │ Performance      │ Render block!  │ ✅ Preloaded!   │    │
  │  │ CSS generation   │ Manual @font!  │ ✅ Automatic!   │    │
  │  │ Subsetting       │ Full file!     │ ✅ Auto subset! │    │
  │  │ Fallback metrics │ ❌ Manual!    │ ✅ Adjusted!    │    │
  │  └──────────────────┴────────────────┴──────────────────┘    │
  │                                                              │
  │  OPTIMIZATION FLOW:                                           │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │                                                      │    │
  │  │  BUILD TIME:                                         │    │
  │  │  ① Import { Inter } from 'next/font/google'         │    │
  │  │       │                                              │    │
  │  │       ▼                                              │    │
  │  │  ② Next.js downloads Inter from Google Fonts         │    │
  │  │       │                                              │    │
  │  │       ▼                                              │    │
  │  │  ③ Subset to 'latin' (smaller file!)                │    │
  │  │       │                                              │    │
  │  │       ▼                                              │    │
  │  │  ④ Generate @font-face CSS                          │    │
  │  │       │                                              │    │
  │  │       ▼                                              │    │
  │  │  ⑤ Generate fallback font metrics (CLS prevention!) │    │
  │  │       │                                              │    │
  │  │       ▼                                              │    │
  │  │  ⑥ Self-host font files with static assets          │    │
  │  │                                                      │    │
  │  │  RUNTIME:                                             │    │
  │  │  ⑦ <link rel="preload"> injected in <head>          │    │
  │  │       │                                              │    │
  │  │       ▼                                              │    │
  │  │  ⑧ Font served from same origin! (no Google!)       │    │
  │  │       │                                              │    │
  │  │       ▼                                              │    │
  │  │  ⑨ Fallback font → custom font (swap, no shift!)   │    │
  │  │                                                      │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

---

## §2. Google Fonts — next/font/google!

```
  GOOGLE FONTS:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  VARIABLE FONT (recommended!):                                │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ import { Inter } from 'next/font/google'            │    │
  │  │                                                      │    │
  │  │ const inter = Inter({                                │    │
  │  │   subsets: ['latin'],  ← only Latin characters!     │    │
  │  │   display: 'swap',    ← show fallback then swap!   │    │
  │  │ })                                                   │    │
  │  │ // Variable font → NO weight needed! ✅             │    │
  │  │ // Single file contains ALL weights!                 │    │
  │  │                                                      │    │
  │  │ <html className={inter.className}>                   │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  NON-VARIABLE FONT (must specify weight!):                    │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ import { Roboto } from 'next/font/google'           │    │
  │  │                                                      │    │
  │  │ const roboto = Roboto({                              │    │
  │  │   weight: '400',       ← REQUIRED for non-variable!│    │
  │  │   subsets: ['latin'],                                │    │
  │  │   display: 'swap',                                   │    │
  │  │ })                                                   │    │
  │  │                                                      │    │
  │  │ // Multiple weights + styles:                        │    │
  │  │ const roboto = Roboto({                              │    │
  │  │   weight: ['400', '700'],      ← array!            │    │
  │  │   style: ['normal', 'italic'], ← array!            │    │
  │  │   subsets: ['latin'],                                │    │
  │  │ })                                                   │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  VARIABLE FONT vs NON-VARIABLE:                               │
  │  ┌──────────────────┬────────────────┬──────────────────┐    │
  │  │                  │ Variable       │ Non-variable     │    │
  │  ├──────────────────┼────────────────┼──────────────────┤    │
  │  │ Files            │ 1 file only!   │ 1 per weight!    │    │
  │  │ weight prop      │ Optional!      │ REQUIRED!        │    │
  │  │ Weight range     │ "100 900"!     │ ['400','700']    │    │
  │  │ Flexibility      │ Any weight!    │ Fixed weights!   │    │
  │  │ File size        │ Larger single! │ Smaller each!    │    │
  │  │ Performance      │ ✅ Better!    │ OK (multiple!)    │    │
  │  │ Examples         │ Inter, Roboto  │ Roboto (fixed)   │    │
  │  │                  │ Flex           │                    │    │
  │  └──────────────────┴────────────────┴──────────────────┘    │
  │                                                              │
  │  NAMING RULE:                                                 │
  │  Multi-word font names → use underscore!                     │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ ❌ import { Roboto Mono } → syntax error!           │    │
  │  │ ✅ import { Roboto_Mono } from 'next/font/google'   │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  SUBSET SPECIFICATION:                                        │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ Google Fonts are AUTO subset!                        │    │
  │  │ → Reduces file size dramatically!                    │    │
  │  │ → You MUST specify which subsets to preload!         │    │
  │  │ → If preload:true (default) without subsets→warning! │    │
  │  │                                                      │    │
  │  │ const inter = Inter({ subsets: ['latin'] })          │    │
  │  │ // Only Latin characters preloaded!                  │    │
  │  │ // Other subsets: 'latin-ext', 'cyrillic', etc.     │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

---

## §3. Local Fonts — next/font/local!

```
  LOCAL FONTS:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  SINGLE FILE:                                                 │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ import localFont from 'next/font/local'             │    │
  │  │                                                      │    │
  │  │ const myFont = localFont({                           │    │
  │  │   src: './my-font.woff2',    ← relative to file!   │    │
  │  │   display: 'swap',                                   │    │
  │  │ })                                                   │    │
  │  │                                                      │    │
  │  │ <html className={myFont.className}>                  │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  MULTIPLE FILES (font family):                                │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ const roboto = localFont({                           │    │
  │  │   src: [                                             │    │
  │  │     {                                                │    │
  │  │       path: './Roboto-Regular.woff2',               │    │
  │  │       weight: '400',                                 │    │
  │  │       style: 'normal',                               │    │
  │  │     },                                               │    │
  │  │     {                                                │    │
  │  │       path: './Roboto-Italic.woff2',                │    │
  │  │       weight: '400',                                 │    │
  │  │       style: 'italic',                               │    │
  │  │     },                                               │    │
  │  │     {                                                │    │
  │  │       path: './Roboto-Bold.woff2',                  │    │
  │  │       weight: '700',                                 │    │
  │  │       style: 'normal',                               │    │
  │  │     },                                               │    │
  │  │     {                                                │    │
  │  │       path: './Roboto-BoldItalic.woff2',            │    │
  │  │       weight: '700',                                 │    │
  │  │       style: 'italic',                               │    │
  │  │     },                                               │    │
  │  │   ],                                                 │    │
  │  │ })                                                   │    │
  │  │                                                      │    │
  │  │ KEY: src = array of { path, weight, style }!        │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  PATH RESOLUTION:                                             │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ Path is RELATIVE to the file calling localFont!      │    │
  │  │                                                      │    │
  │  │ If called in app/page.tsx:                           │    │
  │  │   src: './fonts/my-font.woff2'                      │    │
  │  │   → resolves to app/fonts/my-font.woff2             │    │
  │  │                                                      │    │
  │  │   src: '../styles/fonts/my-font.ttf'                │    │
  │  │   → resolves to styles/fonts/my-font.ttf            │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  google vs local:                                             │
  │  ┌──────────────────┬────────────────┬──────────────────┐    │
  │  │                  │ next/font/     │ next/font/       │    │
  │  │                  │ google         │ local            │    │
  │  ├──────────────────┼────────────────┼──────────────────┤    │
  │  │ src              │ ❌ Not used!  │ ✅ Required!     │    │
  │  │ weight           │ Auto/Required! │ Optional!         │    │
  │  │ subsets           │ ✅ Use it!   │ ❌ Not used!     │    │
  │  │ axes             │ ✅ Available! │ ❌ Not used!     │    │
  │  │ display          │ ✅            │ ✅               │    │
  │  │ preload          │ ✅            │ ✅               │    │
  │  │ fallback         │ ✅            │ ✅               │    │
  │  │ adjustFont       │ Boolean!       │ String|false!     │    │
  │  │ Fallback         │ (true/false)   │ 'Arial'|'Times'  │    │
  │  │ variable         │ ✅            │ ✅               │    │
  │  │ declarations     │ ❌ Not used!  │ ✅ Available!    │    │
  │  └──────────────────┴────────────────┴──────────────────┘    │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

---

## §4. All Options — Complete Reference!

```
  ALL OPTIONS:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  src (local only, REQUIRED):                                  │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ String: src: './fonts/my-font.woff2'                │    │
  │  │ Array:  src: [                                       │    │
  │  │           { path:'./Inter-Thin.ttf', weight:'100' }, │    │
  │  │           { path:'./Inter-Bold.ttf', weight:'700',   │    │
  │  │             style:'italic' },                        │    │
  │  │         ]                                            │    │
  │  │ → Path relative to calling file!                     │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  weight:                                                      │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ String:   weight: '400'          ← single weight!   │    │
  │  │ Range:    weight: '100 900'      ← variable font!   │    │
  │  │ Array:    weight: ['400','700']   ← multiple!       │    │
  │  │ → Variable font: optional (all weights included!)   │    │
  │  │ → Non-variable: REQUIRED!                            │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  style:                                                       │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ String:   style: 'italic'        ← single!         │    │
  │  │ String:   style: 'oblique'       ← local only!     │    │
  │  │ Array:    style: ['normal','italic'] ← google only! │    │
  │  │ Default:  'normal'                                   │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  subsets (google only):                                       │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ subsets: ['latin']                                   │    │
  │  │ → Preloads only specified subsets!                    │    │
  │  │ → Injects <link rel="preload"> in <head>!           │    │
  │  │ → If preload:true + no subsets → WARNING!           │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  axes (google only):                                          │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ axes: ['slnt']                                       │    │
  │  │ → Extra variable font axes! (beyond weight!)        │    │
  │  │ → Inter has 'slnt' (slant) axis!                    │    │
  │  │ → Reduces file size (only needed axes!)              │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  display:                                                     │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ display: 'swap'     ← DEFAULT! Show fallback,      │    │
  │  │                        then swap to custom font!     │    │
  │  │ display: 'block'    ← Hide text until loaded!       │    │
  │  │ display: 'fallback' ← Short block, then fallback!  │    │
  │  │ display: 'optional' ← Use only if cached!          │    │
  │  │ display: 'auto'     ← Browser decides!             │    │
  │  │                                                      │    │
  │  │ VISUAL:                                              │    │
  │  │ 'swap':     [...fallback text...]  →  [custom font!]│    │
  │  │ 'block':    [...invisible.........]  →  [custom font!]│   │
  │  │ 'fallback': [..block..]→[fallback]  →  [custom?]   │    │
  │  │ 'optional': [fallback or custom if cached!]         │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  preload:                                                     │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ preload: true  ← DEFAULT! Inject <link preload>!   │    │
  │  │ preload: false ← No preloading!                     │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  fallback:                                                    │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ fallback: ['system-ui', 'arial']                    │    │
  │  │ → Fonts to use while custom font loads!              │    │
  │  │ → No default value!                                  │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  adjustFontFallback:                                          │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ PURPOSE: Reduce Cumulative Layout Shift (CLS)!      │    │
  │  │                                                      │    │
  │  │ Google: boolean (default: true)                      │    │
  │  │   → Auto-generates fallback with matching metrics!   │    │
  │  │                                                      │    │
  │  │ Local: string | false (default: 'Arial')            │    │
  │  │   → 'Arial' or 'Times New Roman' or false           │    │
  │  │   → Adjusts fallback font size-adjust to match!     │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  variable:                                                    │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ variable: '--font-inter'                             │    │
  │  │ → Creates CSS custom property!                       │    │
  │  │ → Use with font-family: var(--font-inter)!          │    │
  │  │ → Best for Tailwind CSS integration!                │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  declarations (local only):                                   │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ declarations: [                                      │    │
  │  │   { prop: 'ascent-override', value: '90%' }         │    │
  │  │ ]                                                    │    │
  │  │ → Extra @font-face descriptors!                     │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

---

## §5. Applying Styles — 3 Methods!

```
  3 WAYS TO APPLY FONT:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  METHOD 1: className                                          │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ // Returns a CSS class with font-family set!        │    │
  │  │ <p className={inter.className}>Hello!</p>            │    │
  │  │                                                      │    │
  │  │ // Apply globally in root layout:                    │    │
  │  │ <html className={inter.className}>                   │    │
  │  │   <body>{children}</body>                            │    │
  │  │ </html>                                              │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  METHOD 2: style                                              │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ // Returns { fontFamily: '...', ... } object!       │    │
  │  │ <p style={inter.style}>Hello!</p>                    │    │
  │  │                                                      │    │
  │  │ // Access fontFamily directly:                       │    │
  │  │ inter.style.fontFamily                               │    │
  │  │ // → "'Inter', 'Inter Fallback'"                    │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  METHOD 3: CSS Variable                                       │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ const inter = Inter({ variable: '--font-inter' })   │    │
  │  │                                                      │    │
  │  │ // Step 1: Add variable to container                 │    │
  │  │ <main className={inter.variable}>                    │    │
  │  │   <p className={styles.text}>Hello!</p>              │    │
  │  │ </main>                                              │    │
  │  │                                                      │    │
  │  │ // Step 2: Use in CSS                                │    │
  │  │ .text {                                              │    │
  │  │   font-family: var(--font-inter);                    │    │
  │  │   font-weight: 200;                                  │    │
  │  │   font-style: italic;                                │    │
  │  │ }                                                    │    │
  │  │                                                      │    │
  │  │ KEY: Most flexible! External CSS control!            │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  PRELOADING SCOPE:                                            │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ Font preloaded based on WHERE it's called!          │    │
  │  │                                                      │    │
  │  │ ┌─ root layout ─────────────────────────────────┐   │    │
  │  │ │ Inter → preloaded on ALL routes! 🌍           │   │    │
  │  │ │                                                │   │    │
  │  │ │ ┌─ /blog layout ─────────────────────────┐    │   │    │
  │  │ │ │ Lora → preloaded on /blog/* routes! 📝 │    │   │    │
  │  │ │ │                                         │    │   │    │
  │  │ │ │ ┌─ /blog/post page ────────────────┐   │    │   │    │
  │  │ │ │ │ Roboto → preloaded on THIS only! │   │    │   │    │
  │  │ │ │ └──────────────────────────────────┘   │    │   │    │
  │  │ │ └─────────────────────────────────────────┘    │   │    │
  │  │ └────────────────────────────────────────────────┘   │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

---

## §6. Advanced — Multiple Fonts + Tailwind!

```
  MULTIPLE FONTS:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  APPROACH 1: Utility Function (className)                     │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ // fonts.ts                                          │    │
  │  │ import { Inter, Roboto_Mono } from 'next/font/google'│    │
  │  │ export const inter = Inter({                         │    │
  │  │   subsets: ['latin'], display: 'swap' })             │    │
  │  │ export const roboto_mono = Roboto_Mono({             │    │
  │  │   subsets: ['latin'], display: 'swap' })             │    │
  │  │                                                      │    │
  │  │ // layout.tsx → global font                          │    │
  │  │ <html className={inter.className}>                   │    │
  │  │                                                      │    │
  │  │ // page.tsx → specific element                       │    │
  │  │ <h1 className={roboto_mono.className}>Title</h1>    │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  APPROACH 2: CSS Variables                                    │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ const inter = Inter({                                │    │
  │  │   variable: '--font-inter', ...                      │    │
  │  │ })                                                   │    │
  │  │ const roboto_mono = Roboto_Mono({                    │    │
  │  │   variable: '--font-roboto-mono', ...                │    │
  │  │ })                                                   │    │
  │  │                                                      │    │
  │  │ // Apply BOTH variables:                             │    │
  │  │ <html className={`${inter.variable}                  │    │
  │  │                   ${roboto_mono.variable}`}>         │    │
  │  │                                                      │    │
  │  │ // Use in CSS:                                       │    │
  │  │ html { font-family: var(--font-inter); }             │    │
  │  │ h1   { font-family: var(--font-roboto-mono); }       │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  TAILWIND CSS v4:                                             │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ @import 'tailwindcss';                               │    │
  │  │ @theme inline {                                      │    │
  │  │   --font-sans: var(--font-inter);                    │    │
  │  │   --font-mono: var(--font-roboto-mono);              │    │
  │  │ }                                                    │    │
  │  │ // → font-sans class uses Inter!                     │    │
  │  │ // → font-mono class uses Roboto Mono!               │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  TAILWIND CSS v3:                                             │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ // tailwind.config.js                                │    │
  │  │ module.exports = {                                   │    │
  │  │   theme: {                                           │    │
  │  │     extend: {                                        │    │
  │  │       fontFamily: {                                  │    │
  │  │         sans: ['var(--font-inter)'],                 │    │
  │  │         mono: ['var(--font-roboto-mono)'],           │    │
  │  │       },                                             │    │
  │  │     },                                               │    │
  │  │   },                                                 │    │
  │  │ }                                                    │    │
  │  │ // → <p class="font-sans">Inter text</p>            │    │
  │  │ // → <p class="font-mono">Roboto Mono text</p>      │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  FONT DEFINITIONS FILE:                                       │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ PURPOSE: Centralize all font definitions!            │    │
  │  │                                                      │    │
  │  │ // styles/fonts.ts                                   │    │
  │  │ import { Inter, Lora, Source_Sans_3 }               │    │
  │  │   from 'next/font/google'                            │    │
  │  │ import localFont from 'next/font/local'              │    │
  │  │                                                      │    │
  │  │ export const inter = Inter()                         │    │
  │  │ export const lora = Lora()                           │    │
  │  │ export const source400 = Source_Sans_3({weight:'400'})│    │
  │  │ export const greatVibes = localFont({                │    │
  │  │   src: './GreatVibes-Regular.ttf'                    │    │
  │  │ })                                                   │    │
  │  │                                                      │    │
  │  │ // tsconfig.json path alias:                         │    │
  │  │ { "paths": { "@/fonts": ["./styles/fonts"] } }       │    │
  │  │                                                      │    │
  │  │ // Usage:                                            │    │
  │  │ import { inter, lora } from '@/fonts'                │    │
  │  │                                                      │    │
  │  │ KEY: Each font loaded ONCE! Shared across app!       │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  ⚠️ Use multiple fonts CONSERVATIVELY!                        │
  │  Each font = additional download for client!                  │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

---

## §7. Tự Viết — NextFontEngine!

```javascript
var NextFontEngine = (function () {
  // ═══════════════════════════════════
  // 1. FONT REGISTRY
  // ═══════════════════════════════════
  var registry = {};
  var cssOutput = [];
  var preloadLinks = [];
  var stats = { googleFonts: 0, localFonts: 0, totalSize: 0 };

  // ═══════════════════════════════════
  // 2. GOOGLE FONT LOADER
  // ═══════════════════════════════════
  function loadGoogleFont(name, options) {
    options = options || {};
    var font = {
      name: name,
      type: "google",
      weight: options.weight || "variable",
      style: options.style || "normal",
      subsets: options.subsets || [],
      display: options.display || "swap",
      preload: options.preload !== false,
      variable: options.variable || null,
      fallback: options.fallback || [],
      adjustFontFallback: options.adjustFontFallback !== false,
      axes: options.axes || [],
    };

    // Validate
    var isVariable =
      font.weight === "variable" ||
      (typeof font.weight === "string" && font.weight.indexOf(" ") !== -1);

    if (!isVariable && !font.weight) {
      throw new Error(name + ": weight is REQUIRED for non-variable font!");
    }

    if (font.preload && font.subsets.length === 0) {
      console.warn("⚠️ " + name + ": preload:true but no subsets specified!");
    }

    // Generate className
    var className =
      "__" +
      name.toLowerCase().replace(/\s+/g, "_") +
      "_" +
      Math.random().toString(36).substr(2, 6);

    // Generate @font-face
    var fontFace =
      "@font-face {\n" +
      '  font-family: "' +
      name +
      '";\n' +
      '  src: url("/_next/static/fonts/' +
      name.toLowerCase() +
      '.woff2")' +
      ' format("woff2");\n' +
      "  font-display: " +
      font.display +
      ";\n";

    if (isVariable) {
      fontFace += "  font-weight: 100 900;\n";
    } else if (Array.isArray(font.weight)) {
      // Multiple @font-face for each weight
    } else {
      fontFace += "  font-weight: " + font.weight + ";\n";
    }
    fontFace += "}";
    cssOutput.push(fontFace);

    // Generate fallback font
    var fallbackName = name + " Fallback";
    if (font.adjustFontFallback) {
      var fallbackFace =
        "@font-face {\n" +
        '  font-family: "' +
        fallbackName +
        '";\n' +
        '  src: local("Arial");\n' +
        "  size-adjust: 107.64%;\n" +
        "  ascent-override: 90%;\n" +
        "  descent-override: 22%;\n" +
        "  line-gap-override: 0%;\n" +
        "}";
      cssOutput.push(fallbackFace);
    }

    // Generate className CSS
    var familyValue = '"' + name + '"';
    if (font.adjustFontFallback) {
      familyValue += ', "' + fallbackName + '"';
    }
    if (font.fallback.length > 0) {
      familyValue += ", " + font.fallback.join(", ");
    }

    var classCss =
      "." + className + " {\n" + "  font-family: " + familyValue + ";\n" + "}";
    cssOutput.push(classCss);

    // CSS variable
    var variableCss = null;
    if (font.variable) {
      variableCss =
        "." +
        className +
        "-var {\n" +
        "  " +
        font.variable +
        ": " +
        familyValue +
        ";\n" +
        "}";
      cssOutput.push(variableCss);
    }

    // Preload link
    if (font.preload) {
      for (var i = 0; i < font.subsets.length; i++) {
        preloadLinks.push(
          '<link rel="preload" as="font" type="font/woff2"' +
            ' href="/_next/static/fonts/' +
            name.toLowerCase() +
            "-" +
            font.subsets[i] +
            '.woff2" crossorigin>',
        );
      }
    }

    // Estimate size
    var estimatedSize = isVariable ? 120 : 45;
    if (font.subsets.length > 0) {
      estimatedSize = Math.round(estimatedSize * 0.6);
    }

    stats.googleFonts++;
    stats.totalSize += estimatedSize;

    var result = {
      className: className,
      style: { fontFamily: familyValue },
      variable: font.variable ? className + "-var" : null,
    };

    registry[name] = { font: font, result: result, size: estimatedSize };
    return result;
  }

  // ═══════════════════════════════════
  // 3. LOCAL FONT LOADER
  // ═══════════════════════════════════
  function loadLocalFont(options) {
    if (!options.src) throw new Error("src is REQUIRED for local fonts!");

    var srcs = Array.isArray(options.src)
      ? options.src
      : [{ path: options.src }];

    var name =
      options.src && typeof options.src === "string"
        ? options.src
            .split("/")
            .pop()
            .replace(/\.\w+$/, "")
        : "LocalFont_" + Object.keys(registry).length;

    var font = {
      name: name,
      type: "local",
      srcs: srcs,
      display: options.display || "swap",
      preload: options.preload !== false,
      variable: options.variable || null,
      adjustFontFallback: options.adjustFontFallback || "Arial",
      declarations: options.declarations || [],
    };

    var className =
      "__" +
      name.toLowerCase().replace(/[^a-z0-9]/g, "_") +
      "_" +
      Math.random().toString(36).substr(2, 6);

    // Generate @font-face for each src
    for (var i = 0; i < srcs.length; i++) {
      var s = srcs[i];
      var ff =
        "@font-face {\n" +
        '  font-family: "' +
        name +
        '";\n' +
        '  src: url("' +
        s.path +
        '");\n' +
        "  font-display: " +
        font.display +
        ";\n";
      if (s.weight) ff += "  font-weight: " + s.weight + ";\n";
      if (s.style) ff += "  font-style: " + s.style + ";\n";

      for (var d = 0; d < font.declarations.length; d++) {
        ff +=
          "  " +
          font.declarations[d].prop +
          ": " +
          font.declarations[d].value +
          ";\n";
      }
      ff += "}";
      cssOutput.push(ff);
    }

    var familyValue = '"' + name + '"';
    var classCss = "." + className + " { font-family: " + familyValue + "; }";
    cssOutput.push(classCss);

    stats.localFonts++;
    stats.totalSize += srcs.length * 40;

    var result = {
      className: className,
      style: { fontFamily: familyValue },
      variable: font.variable ? className + "-var" : null,
    };

    registry[name] = { font: font, result: result, size: srcs.length * 40 };
    return result;
  }

  // ═══════════════════════════════════
  // 4. PRELOAD SCOPE RESOLVER
  // ═══════════════════════════════════
  function resolvePreloadScope(fileType) {
    var scopes = {
      "root-layout": "ALL routes! 🌍",
      layout: "All routes wrapped by this layout!",
      page: "This specific page route only!",
    };
    return scopes[fileType] || "Unknown scope!";
  }

  // ═══════════════════════════════════
  // 5. DISPLAY STRATEGY SIMULATOR
  // ═══════════════════════════════════
  function simulateDisplay(strategy, loadTimeMs) {
    var timeline = [];
    switch (strategy) {
      case "swap":
        timeline.push({ time: 0, state: "Fallback font visible!" });
        timeline.push({ time: loadTimeMs, state: "SWAP to custom font!" });
        break;
      case "block":
        timeline.push({ time: 0, state: "Text INVISIBLE!" });
        timeline.push({
          time: Math.min(loadTimeMs, 3000),
          state: loadTimeMs <= 3000 ? "Custom font!" : "Fallback font!",
        });
        break;
      case "fallback":
        timeline.push({ time: 0, state: "Text INVISIBLE! (100ms block)" });
        timeline.push({ time: 100, state: "Fallback font visible!" });
        timeline.push({
          time: loadTimeMs,
          state: loadTimeMs <= 3000 ? "Custom font!" : "Stays fallback!",
        });
        break;
      case "optional":
        timeline.push({ time: 0, state: "Fallback font!" });
        timeline.push({
          time: loadTimeMs,
          state: loadTimeMs <= 100 ? "Custom font!" : "Stays fallback!",
        });
        break;
    }
    return timeline;
  }

  // ═══════════════════════════════════
  // 6. DEMO
  // ═══════════════════════════════════
  function demo() {
    console.log("╔══════════════════════════════════════╗");
    console.log("║   NEXT FONT ENGINE DEMO               ║");
    console.log("╚══════════════════════════════════════╝");

    // Google Font — Variable
    console.log("\n── Google Font: Variable ──");
    var inter = loadGoogleFont("Inter", {
      subsets: ["latin"],
      display: "swap",
      variable: "--font-inter",
    });
    console.log("  className:", inter.className);
    console.log("  style:", JSON.stringify(inter.style));
    console.log("  variable:", inter.variable);

    // Google Font — Non-variable
    console.log("\n── Google Font: Non-variable ──");
    var roboto = loadGoogleFont("Roboto", {
      weight: ["400", "700"],
      style: ["normal", "italic"],
      subsets: ["latin"],
      display: "swap",
    });
    console.log("  className:", roboto.className);

    // Local Font
    console.log("\n── Local Font ──");
    var myFont = loadLocalFont({
      src: [
        {
          path: "./fonts/MyFont-Regular.woff2",
          weight: "400",
          style: "normal",
        },
        { path: "./fonts/MyFont-Bold.woff2", weight: "700", style: "normal" },
      ],
      display: "swap",
      variable: "--font-custom",
      declarations: [{ prop: "ascent-override", value: "90%" }],
    });
    console.log("  className:", myFont.className);

    // Preload scope
    console.log("\n── Preload Scope ──");
    console.log("  root-layout:", resolvePreloadScope("root-layout"));
    console.log("  layout:", resolvePreloadScope("layout"));
    console.log("  page:", resolvePreloadScope("page"));

    // Display strategy
    console.log("\n── Display Strategy (300ms load) ──");
    var strategies = ["swap", "block", "fallback", "optional"];
    for (var i = 0; i < strategies.length; i++) {
      var timeline = simulateDisplay(strategies[i], 300);
      console.log("  " + strategies[i] + ":");
      for (var j = 0; j < timeline.length; j++) {
        console.log("    " + timeline[j].time + "ms → " + timeline[j].state);
      }
    }

    // Generated CSS
    console.log("\n── Generated CSS ──");
    console.log("  @font-face rules:", cssOutput.length);
    console.log("  Preload links:", preloadLinks.length);

    // Stats
    console.log("\n── Stats ──");
    console.log(
      "  Google:",
      stats.googleFonts,
      "Local:",
      stats.localFonts,
      "Total ~" + stats.totalSize + "KB",
    );
  }

  return { demo: demo };
})();
// Chạy: NextFontEngine.demo();
```

---

## §8. Câu Hỏi Luyện Tập!

**Câu 1**: Variable font vs non-variable font — khác biệt?

<details><summary>Đáp án</summary>

```
Variable font:
  → 1 file chứa TẤT CẢ weights (100-900)!
  → weight prop: OPTIONAL (hoặc range "100 900")
  → Linh hoạt: dùng bất kỳ weight nào!
  → File đơn lớn hơn nhưng hiệu quả hơn!
  → Ví dụ: Inter, Roboto Flex

Non-variable font:
  → 1 file PER weight! (400.woff2, 700.woff2, ...)
  → weight prop: REQUIRED!
  → weight: ['400', '700'] → array of fixed values!
  → Mỗi file nhỏ hơn nhưng nhiều files!
  → Ví dụ: Roboto (fixed weights)

KẾT LUẬN: Ưu tiên variable font cho performance!
```

</details>

---

**Câu 2**: `display: 'swap'` vs `display: 'optional'` — khi nào dùng?

<details><summary>Đáp án</summary>

```
display: 'swap' (DEFAULT):
  ① Show fallback font NGAY (text visible!)
  ② Khi custom font load xong → SWAP!
  ③ → Có thể thấy text "nhảy" (FOUT - Flash of Unstyled Text)
  ④ → Dùng khi: text PHẢI hiển thị ngay!
  ⑤ → adjustFontFallback giảm layout shift!

display: 'optional':
  ① Show fallback font NGAY
  ② Nếu custom font ĐANG trong cache → dùng!
  ③ Nếu KHÔNG trong cache → BỎ QUA, giữ fallback!
  ④ → KHÔNG BAO GIỜ swap giữa chừng!
  ⑤ → Dùng khi: performance > branding!
  ⑥ → Best for: slow connections, non-critical fonts!

RULE:
  Brand font (logo, headings) → 'swap'!
  Body text font → 'optional' (nếu performance ưu tiên!)
```

</details>

---

**Câu 3**: Tailwind CSS integration — cách nào?

<details><summary>Đáp án</summary>

```
Step 1: Create with CSS variable:
  const inter = Inter({
    variable: '--font-inter',
    subsets: ['latin'],
  })

Step 2: Apply variable to HTML:
  <html className={`${inter.variable} antialiased`}>

Step 3: Configure Tailwind:
  Tailwind v4:
    @import 'tailwindcss';
    @theme inline {
      --font-sans: var(--font-inter);
    }

  Tailwind v3:
    // tailwind.config.js
    theme: {
      extend: {
        fontFamily: {
          sans: ['var(--font-inter)'],
        },
      },
    }

Step 4: Use in JSX:
  <p class="font-sans">Uses Inter!</p>

KEY INSIGHT:
  → CSS variable = bridge between next/font and Tailwind!
  → variable option creates the CSS custom property!
  → Tailwind reads the variable for utility classes!
```

</details>

---

**Câu 4**: Font preloading scope — hoạt động thế nào?

<details><summary>Đáp án</summary>

```
Font preloaded BASED ON where it's called:

Root Layout (app/layout.tsx):
  → Font preloaded on ALL routes! 🌍
  → Use for: primary body font!

Nested Layout (app/blog/layout.tsx):
  → Font preloaded on /blog/* routes only!
  → Use for: section-specific fonts!

Page (app/blog/[slug]/page.tsx):
  → Font preloaded on THIS page only!
  → Use for: page-specific decorative fonts!

RULE:
  → Load font where it's FIRST needed!
  → ❌ Don't load ALL fonts in root layout!
  → ✅ Load specific fonts in specific layouts!
  → Each new font = additional download!
  → Use conservatively!

KEY: localFont/Google font called once,
     imported everywhere else!
     → Centralize in fonts.ts!
```

</details>
