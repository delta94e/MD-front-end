# HTTP Protocol — Comprehensive Deep Dive

> 📅 2026-02-11 · ⏱ 40 phút đọc
>
> Tài liệu toàn diện về giao thức HTTP: methods, headers, versions,
> Keep-Alive, HTTP/2, HTTP/3, cấu trúc URL, message format, và hiệu năng.
> Độ khó: ⭐️⭐️⭐️⭐️ | Chủ đề: Network — Application Layer

---

## Mục Lục

**Phần 1: HTTP Methods & Headers** 0. [GET vs POST](#0-get-vs-post)

1. [POST vs PUT](#1-post-vs-put)
2. [Các HTTP Request Methods](#2-các-http-request-methods)
3. [OPTIONS Method & Use Cases](#3-options-method--use-cases)
4. [Common Request & Response Headers](#4-common-request--response-headers)

**Phần 2: HTTP Message Format** 5. [HTTP Request Message](#5-http-request-message) 6. [HTTP Response Message](#6-http-response-message) 7. [Cấu trúc URL](#7-cấu-trúc-url)

**Phần 3: HTTP Versions & Performance** 8. [HTTP/1.0 vs HTTP/1.1](#8-http10-vs-http11) 9. [HTTP/1.1 vs HTTP/2.0](#9-http11-vs-http20) 10. [HTTP/3.0 & QUIC](#10-http30--quic) 11. [HTTP vs HTTPS](#11-http-vs-https)

**Phần 4: Cơ chế & Hiệu năng** 12. [Keep-Alive](#12-keep-alive) 13. [HTTP Caching Headers & 304](#13-http-caching-headers--304) 14. [GET URL Length Limit](#14-get-url-length-limit) 15. [Multiple Images Loading](#15-multiple-images-loading) 16. [HTTP/2 Header Compression (HPACK)](#16-http2-header-compression-hpack) 17. [HTTP Performance](#17-http-performance) 18. [Ưu nhược điểm HTTP](#18-ưu-nhược-điểm-http)

**Phần 5: Tổng hợp** 19. [Nhập Google.com — Chuyện gì xảy ra?](#19-nhập-googlecom--chuyện-gì-xảy-ra) 20. [Tóm Tắt & Câu Hỏi Phỏng Vấn](#20-tóm-tắt--câu-hỏi-phỏng-vấn)

---

# Phần 1: HTTP Methods & Headers

---

## 0. GET vs POST

> **🎯 Sự khác biệt giữa GET và POST**

```
GET vs POST — 6 ĐIỂM KHÁC BIỆT:
═══════════════════════════════════════════════════════════════

  ┌──────────────────┬─────────────────────┬─────────────────────┐
  │  Tiêu chí         │  GET                │  POST               │
  ├──────────────────┼─────────────────────┼─────────────────────┤
  │ Use case          │ Idempotent          │ Non-idempotent      │
  │                   │ KHÔNG ảnh hưởng     │ CÓ ảnh hưởng       │
  │                   │ server resource     │ server resource     │
  │                   │ (lấy data)          │ (tạo/sửa data)     │
  ├──────────────────┼─────────────────────┼─────────────────────┤
  │ Cache             │ Browser THƯỜNG      │ Browser HIẾM KHI   │
  │                   │ cache GET           │ cache POST          │
  ├──────────────────┼─────────────────────┼─────────────────────┤
  │ Message body      │ Entity body TRỐNG   │ Entity body CHỨA   │
  │                   │                     │ data gửi server    │
  ├──────────────────┼─────────────────────┼─────────────────────┤
  │ Security          │ Params trong URL    │ Params trong body   │
  │                   │ → lưu history       │ → AN TOÀN hơn      │
  │                   │ → KÉM AN TOÀN      │                     │
  ├──────────────────┼─────────────────────┼─────────────────────┤
  │ Request length    │ BỊ GIỚI HẠN bởi    │ KHÔNG bị giới hạn  │
  │                   │ browser (URL limit) │ (hoặc rất lớn)     │
  ├──────────────────┼─────────────────────┼─────────────────────┤
  │ Parameter types   │ Chỉ text/ASCII      │ Hỗ trợ NHIỀU       │
  │                   │                     │ kiểu dữ liệu      │
  └──────────────────┴─────────────────────┴─────────────────────┘

  💡 GHI NHỚ:
  GET = "Cho tôi xem" (đọc, không đổi)
  POST = "Tôi gửi cho bạn" (tạo/sửa, có thay đổi)
```

---

## 1. POST vs PUT

```
POST vs PUT — TẠOLM MỚI vs CẬP NHẬT:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │  PUT — CẬP NHẬT dữ liệu                                 │
  │  → Gửi data lên server, SỬA NỘI DUNG                   │
  │  → KHÔNG thay đổi LOẠI dữ liệu                         │
  │  → IDEMPOTENT: PUT bao nhiêu lần → kết quả NHƯ NHAU   │
  │  → VD: PUT /users/123 → cập nhật user 123              │
  ├──────────────────────────────────────────────────────────┤
  │  POST — TẠO MỚI dữ liệu                                 │
  │  → Gửi data lên server, TẠO resource mới               │
  │  → CÓ THỂ thay đổi loại dữ liệu, tạo nội dung mới   │
  │  → NON-IDEMPOTENT: POST nhiều lần → tạo NHIỀU bản ghi │
  │  → VD: POST /users → tạo user mới mỗi lần             │
  └──────────────────────────────────────────────────────────┘

  💡 PUT = Update (cập nhật) | POST = Create (tạo mới)
```

---

## 2. Các HTTP Request Methods

```
8 HTTP REQUEST METHODS:
═══════════════════════════════════════════════════════════════

  ┌──────────┬───────────────────────────────────────────────┐
  │  Method  │  Mô tả                                       │
  ├──────────┼───────────────────────────────────────────────┤
  │  GET     │  Lấy data từ server                          │
  ├──────────┼───────────────────────────────────────────────┤
  │  POST    │  Gửi data tới resource → tạo/sửa resource   │
  ├──────────┼───────────────────────────────────────────────┤
  │  PUT     │  Upload file, cập nhật data                  │
  ├──────────┼───────────────────────────────────────────────┤
  │  DELETE  │  Xóa object trên server                      │
  ├──────────┼───────────────────────────────────────────────┤
  │  HEAD    │  Lấy message HEADER (không có body)          │
  │          │  Giống GET nhưng không trả body              │
  ├──────────┼───────────────────────────────────────────────┤
  │  OPTIONS │  Hỏi các methods được hỗ trợ                │
  │          │  (dùng cho CORS preflight)                   │
  ├──────────┼───────────────────────────────────────────────┤
  │  CONNECT │  Yêu cầu proxy thiết lập TUNNEL             │
  │          │  Dùng túnel TCP communication                │
  ├──────────┼───────────────────────────────────────────────┤
  │  TRACE   │  Echo lại request → dùng test/debug         │
  └──────────┴───────────────────────────────────────────────┘
```

---

## 3. OPTIONS Method & Use Cases

```
OPTIONS — PREFLIGHT REQUEST:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  OPTIONS dùng để TRUY VẤN các tùy chọn khả dụng        │
  │  cho resource tại Request-URI.                           │
  │                                                          │
  │  → Cho phép client ĐÁNH GIÁ server capabilities        │
  │  → TRƯỚC KHI gửi request thực sự                       │
  │  → Response KHÔNG ĐƯỢC CACHE                             │
  │                                                          │
  │  2 USE CASES CHÍNH:                                       │
  │                                                          │
  │  ① Lấy tất cả HTTP methods server hỗ trợ               │
  │                                                          │
  │  ② CORS Preflight Request (cross-domain)                │
  │     → Với complex request, browser tự gửi OPTIONS      │
  │     → Kiểm tra xem có quyền truy cập resource không   │
  │                                                          │
  │  VD Response:                                             │
  │  Access-Control-Allow-Methods:                           │
  │    GET, HEAD, PUT, PATCH, POST, DELETE                   │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

---

## 4. Common Request & Response Headers

### Request Headers

```
HTTP REQUEST HEADERS PHỔ BIẾN:
═══════════════════════════════════════════════════════════════

  ┌────────────────────┬────────────────────────────────────┐
  │ Header             │ Mô tả                              │
  ├────────────────────┼────────────────────────────────────┤
  │ Accept             │ Content types browser xử lý được  │
  │ Accept-Charset     │ Character set browser hiển thị    │
  │ Accept-Encoding    │ Compression encoding hỗ trợ       │
  │ Accept-Language    │ Ngôn ngữ hiện tại browser         │
  │ Connection         │ Loại kết nối (keep-alive/close)   │
  │ Cookie             │ Cookies của page hiện tại         │
  │ Host               │ Domain của page gửi request       │
  │ Referer            │ URL page đã gửi request           │
  │ User-Agent         │ Chuỗi user agent browser          │
  └────────────────────┴────────────────────────────────────┘
```

### Response Headers

```
HTTP RESPONSE HEADERS PHỔ BIẾN:
═══════════════════════════════════════════════════════════════

  ┌────────────────────┬────────────────────────────────────┐
  │ Header             │ Mô tả                              │
  ├────────────────────┼────────────────────────────────────┤
  │ Date               │ Thời gian gửi message             │
  │ Server             │ Tên server                         │
  │ Connection         │ Loại kết nối                       │
  │ Cache-Control      │ Điều khiển HTTP caching            │
  │ Content-Type       │ MIME type của document             │
  └────────────────────┴────────────────────────────────────┘
```

### Content-Type — 4 giá trị phổ biến

```
CONTENT-TYPE — 4 GIÁ TRỊ THƯỜNG GẶP:
═══════════════════════════════════════════════════════════════

  ┌─────────────────────────────────────┬────────────────────┐
  │ Content-Type                        │ Use case           │
  ├─────────────────────────────────────┼────────────────────┤
  │ application/x-www-form-urlencoded   │ Form mặc định      │
  │ → key1=val1&key2=val2              │ (URL encoded)      │
  ├─────────────────────────────────────┼────────────────────┤
  │ multipart/form-data                 │ Upload file        │
  │ → boundary-separated parts         │ qua form           │
  ├─────────────────────────────────────┼────────────────────┤
  │ application/json                    │ JSON string        │
  │ → serialized JSON body             │ (API phổ biến)     │
  ├─────────────────────────────────────┼────────────────────┤
  │ text/xml                            │ XML data           │
  │ → XML format body                  │                    │
  └─────────────────────────────────────┴────────────────────┘
```

---

# Phần 2: HTTP Message Format

---

## 5. HTTP Request Message

```
HTTP REQUEST MESSAGE — 4 PHẦN:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │ ① REQUEST LINE                                           │
  │ ┌─────────┬───┬──────────────┬───┬──────────┬────┬────┐ │
  │ │ Method  │ ░ │  URL         │ ░ │ Version  │ CR │ LF │ │
  │ └─────────┴───┴──────────────┴───┴──────────┴────┴────┘ │
  │ VD: GET /index.html HTTP/1.1                             │
  ├──────────────────────────────────────────────────────────┤
  │ ② REQUEST HEADERS                                        │
  │ ┌────────────────┬───┬─────────────────┬────┬────┐       │
  │ │ Header Name    │ : │ Value           │ CR │ LF │       │
  │ └────────────────┴───┴─────────────────┴────┴────┘       │
  │ VD: User-Agent: Mozilla/5.0                              │
  │     Accept: text/html                                    │
  │     Host: www.example.com                                │
  ├──────────────────────────────────────────────────────────┤
  │ ③ BLANK LINE (CR LF)                                     │
  ├──────────────────────────────────────────────────────────┤
  │ ④ REQUEST BODY                                           │
  │ Data gửi kèm (POST, PUT...)                             │
  │ VD: flag=abc&submit=Submit                               │
  └──────────────────────────────────────────────────────────┘
```

---

## 6. HTTP Response Message

```
HTTP RESPONSE MESSAGE — 4 PHẦN:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │ ① RESPONSE LINE (Status Line)                            │
  │ ┌──────────┬───┬─────────────┬───┬───────────────────┐   │
  │ │ Version  │ ░ │ Status Code │ ░ │ Reason Phrase     │   │
  │ └──────────┴───┴─────────────┴───┴───────────────────┘   │
  │ VD: HTTP/1.1 200 OK                                      │
  ├──────────────────────────────────────────────────────────┤
  │ ② RESPONSE HEADERS                                       │
  │ Date: Tue, 10 Jul 2012 06:50:15 GMT                      │
  │ Content-Length: 362                                       │
  │ Content-Type: text/html                                  │
  ├──────────────────────────────────────────────────────────┤
  │ ③ BLANK LINE                                              │
  ├──────────────────────────────────────────────────────────┤
  │ ④ RESPONSE BODY                                          │
  │ <html>...</html>                                         │
  │ (Server response data)                                   │
  └──────────────────────────────────────────────────────────┘
```

---

## 7. Cấu trúc URL

```
URL STRUCTURE — CÁC THÀNH PHẦN:
═══════════════════════════════════════════════════════════════

  http://www.aspxfans.com:8080/news/index.asp?boardID=5&ID=24618&page=1#name
  └─┬──┘ └───────┬───────┘└┬─┘└────┬───┘└───┬───┘└────────────┬────────┘└┬─┘
    │            │          │       │        │                  │          │
    │            │          │       │        │                  │          │
  ┌────────┬─────────────┬──────┬──────────┬──────────┬──────────────┬────────┐
  │Protocol│  Domain     │ Port │ Virtual  │ File     │ Parameters   │ Anchor │
  │        │             │      │ Dir      │ name     │              │        │
  ├────────┼─────────────┼──────┼──────────┼──────────┼──────────────┼────────┤
  │ http:  │aspxfans.com │ 8080 │ /news/   │index.asp │boardID=5    │ name   │
  │        │             │      │          │          │&ID=24618    │        │
  │        │             │      │          │          │&page=1      │        │
  ├────────┼─────────────┼──────┼──────────┼──────────┼──────────────┼────────┤
  │Required│  Required   │Option│ Optional │ Optional │ Optional     │Optional│
  │        │(hoặc IP)    │(80/  │          │          │ ? → #       │ # →end │
  │        │             │ 443) │          │          │ &-separated │        │
  └────────┴─────────────┴──────┴──────────┴──────────┴──────────────┴────────┘

  MẶC ĐỊNH PORT:
  HTTP  = 80
  HTTPS = 443
```

---

# Phần 3: HTTP Versions & Performance

---

## 8. HTTP/1.0 vs HTTP/1.1

```
HTTP/1.0 vs HTTP/1.1 — 5 ĐIỂM KHÁC BIỆT:
═══════════════════════════════════════════════════════════════

  ┌──────────────┬─────────────────────┬─────────────────────┐
  │  Tiêu chí     │  HTTP/1.0           │  HTTP/1.1           │
  ├──────────────┼─────────────────────┼─────────────────────┤
  │ Connection    │ Non-persistent      │ Persistent          │
  │              │ (mỗi request 1 TCP) │ (reuse TCP conn.)   │
  │              │                     │ → giảm latency      │
  ├──────────────┼─────────────────────┼─────────────────────┤
  │ Resource      │ Server gửi TOÀN BỘ│ Hỗ trợ Range header │
  │ request      │ object              │ → request 1 PHẦN   │
  │              │ Không resume        │ → 206 Partial       │
  │              │ download            │ → Resume download   │
  ├──────────────┼─────────────────────┼─────────────────────┤
  │ Caching       │ If-Modified-Since   │ Thêm: ETag,        │
  │              │ Expires             │ If-None-Match,      │
  │              │ (đơn giản)          │ If-Unmodified-Since │
  │              │                     │ (linh hoạt hơn)     │
  ├──────────────┼─────────────────────┼─────────────────────┤
  │ Host header  │ KHÔNG có            │ Thêm Host field     │
  │              │ (1 server = 1 IP)   │ → virtual hosting   │
  │              │                     │ → nhiều website/IP  │
  ├──────────────┼─────────────────────┼─────────────────────┤
  │ New methods  │ GET, POST, HEAD     │ Thêm: PUT, HEAD,    │
  │              │                     │ OPTIONS, DELETE...  │
  └──────────────┴─────────────────────┴─────────────────────┘
```

---

## 9. HTTP/1.1 vs HTTP/2.0

```
HTTP/1.1 vs HTTP/2.0 — 4 CẢI TIẾN LỚN:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │  ① BINARY PROTOCOL (Giao thức nhị phân)                  │
  │  ───────────────────────────────────                     │
  │  HTTP/1.1: header = TEXT (ASCII), body = text/binary    │
  │  HTTP/2:   header + body = BINARY (gọi là "frames")    │
  │                                                          │
  │  → Header frames + Data frames                           │
  │  → Binary → nền tảng cho multiplexing                   │
  ├──────────────────────────────────────────────────────────┤
  │  ② MULTIPLEXING (Ghép kênh)                              │
  │  ──────────────────────────                              │
  │  HTTP/1.1: Requests gửi TUẦN TỰ → head-of-line block  │
  │  HTTP/2:   Nhiều req/res ĐỒNG THỜI trên 1 TCP conn.   │
  │                                                          │
  │  ┌──────────────────────────────────────────┐            │
  │  │  TCP Connection                          │            │
  │  │  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐       │            │
  │  │  │Req 1│ │Req 2│ │Req 3│ │Res 1│  ...  │            │
  │  │  └─────┘ └─────┘ └─────┘ └─────┘       │            │
  │  │  → Không cần gửi theo thứ tự!          │            │
  │  └──────────────────────────────────────────┘            │
  ├──────────────────────────────────────────────────────────┤
  │  ③ DATA STREAMS (Luồng dữ liệu)                         │
  │  ──────────────────────────────                          │
  │  → Packets gửi KHÔNG theo thứ tự                        │
  │  → Mỗi packet được ĐÁNH SỐ (stream ID)                 │
  │  → Phân biệt packet thuộc request/response nào         │
  │  → Mỗi request/response = 1 data stream với unique ID  │
  ├──────────────────────────────────────────────────────────┤
  │  ④ HEADER COMPRESSION (Nén header)                       │
  │  ─────────────────────────────────                       │
  │  HTTP/1.1: Stateless → MỌI request gửi ĐẦY ĐỦ headers │
  │            Cookie, User-Agent... lặp lại → lãng phí     │
  │  HTTP/2:                                                  │
  │    → Nén header bằng gzip/compress trước khi gửi       │
  │    → Client + Server duy trì HEADER TABLE               │
  │    → Field đã gửi → chỉ gửi INDEX number              │
  │    → Giảm 50-90% kích thước header!                     │
  ├──────────────────────────────────────────────────────────┤
  │  ⑤ SERVER PUSH                                           │
  │  ─────────────                                           │
  │  → Server CHỦ ĐỘNG gửi resource mà client chưa request │
  │  → Giảm latency (gửi trước resource cần thiết)         │
  │                                                          │
  │  ⚠️ Server push chỉ cho STATIC RESOURCES                 │
  │  → Khác với WebSocket/SSE (real-time data)              │
  └──────────────────────────────────────────────────────────┘

  HEAD-OF-LINE BLOCKING (Tắc nghẽn đầu hàng đợi):
  ┌──────────────────────────────────────────────────────────┐
  │ HTTP yêu cầu gửi/nhận message TUẦN TỰ (FIFO serial)   │
  │ → Request đầu xử lý CHẬM → TẤT CẢ phía sau PHẢI CHỜ │
  │ → Các request sau chịu chi phí thời gian KHÔNG CẦN     │
  │ → HTTP/2 Multiplexing GIẢI QUYẾT vấn đề này           │
  └──────────────────────────────────────────────────────────┘
```

---

## 10. HTTP/3.0 & QUIC

```
HTTP/3.0 — DỰA TRÊN UDP + QUIC:
═══════════════════════════════════════════════════════════════

  HTTP/2 Stack:           HTTP/3 Stack:
  ┌──────────────┐        ┌──────────────┐
  │   HTTP/2     │        │   HTTP/3     │
  ├──────────────┤        ├──────────────┤
  │    TLS       │        │    QUIC      │
  ├──────────────┤        │ ┌──────────┐ │
  │              │        │ │Multiplex │ │
  │              │        │ ├────┬─────┤ │
  │    TCP       │        │ │TLS │Order│ │
  │              │        │ ├────┼─────┤ │
  │              │        │ │Fast│Reli-│ │
  │              │        │ │Hand│able │ │
  ├──────────────┤        ├─┴────┴─────┴─┤
  │     IP       │        │     UDP      │
  └──────────────┘        ├──────────────┤
                          │      IP      │
                          └──────────────┘

  QUIC PROTOCOL (trên nền UDP):
  ┌──────────────────────────────────────────────────────────┐
  │ ① Flow Control + Reliability                             │
  │   → Thêm layer trên UDP đảm bảo truyền tin cậy        │
  │   → Retransmission, congestion control (như TCP)        │
  │                                                          │
  │ ② Tích hợp TLS 1.3                                       │
  │   → Giảm số RTT cho handshake                           │
  │                                                          │
  │ ③ Multiplexing                                            │
  │   → Nhiều data stream ĐỘC LẬP trên 1 connection       │
  │   → Giải quyết head-of-line blocking của TCP            │
  │                                                          │
  │ ④ Fast Handshake                                          │
  │   → Dựa trên UDP → 0-1 RTT để thiết lập connection    │
  │   → Nhanh hơn nhiều so với TCP (1-3 RTT)               │
  └──────────────────────────────────────────────────────────┘
```

---

## 11. HTTP vs HTTPS

```
HTTP vs HTTPS — 4 ĐIỂM KHÁC BIỆT:
═══════════════════════════════════════════════════════════════

  ┌──────────────────┬──────────────────┬──────────────────┐
  │  Tiêu chí         │  HTTP             │  HTTPS            │
  ├──────────────────┼──────────────────┼──────────────────┤
  │ CA Certificate   │ KHÔNG cần        │ CẦN (tốn phí)    │
  ├──────────────────┼──────────────────┼──────────────────┤
  │ Truyền tải        │ PLAINTEXT        │ SSL/TLS ENCRYPT  │
  │                   │ (bản rõ)         │ (mã hóa)         │
  ├──────────────────┼──────────────────┼──────────────────┤
  │ Port              │ 80               │ 443              │
  ├──────────────────┼──────────────────┼──────────────────┤
  │ Security          │ Đơn giản,        │ SSL encryption   │
  │                   │ stateless        │ + authentication │
  │                   │                  │ → AN TOÀN hơn    │
  └──────────────────┴──────────────────┴──────────────────┘
```

---

# Phần 4: Cơ chế & Hiệu năng

---

## 12. Keep-Alive

> **🎯 Short connection vs Long connection**

```
KEEP-ALIVE — DUY TRÌ KẾT NỐI:
═══════════════════════════════════════════════════════════════

  SHORT CONNECTION (không Keep-Alive):
  ┌──────┐  TCP open → Request → Response → TCP close  ┌──────┐
  │Client│  TCP open → Request → Response → TCP close  │Server│
  │      │  TCP open → Request → Response → TCP close  │      │
  └──────┘  (mỗi request = 1 TCP connection mới)       └──────┘

  LONG CONNECTION (có Keep-Alive):
  ┌──────┐  TCP open → Req1 → Res1                     ┌──────┐
  │Client│            → Req2 → Res2                     │Server│
  │      │            → Req3 → Res3                     │      │
  │      │            → ...                              │      │
  │      │  TCP close (khi cần)                          │      │
  └──────┘  (nhiều req/res trên 1 TCP connection)       └──────┘

  PHIÊN BẢN:
  ┌──────────────┬──────────────────────────────────────┐
  │ HTTP/1.0     │ Mặc định KHÔNG có Keep-Alive         │
  │              │ Phải gửi: Connection: keep-alive     │
  │              │ Ngắt: Connection: close              │
  ├──────────────┼──────────────────────────────────────┤
  │ HTTP/1.1     │ Mặc định CÓ Keep-Alive              │
  │              │ Ngắt: Connection: close              │
  └──────────────┴──────────────────────────────────────┘
```

### Keep-Alive Setup Process

```
THIẾT LẬP KEEP-ALIVE:
═══════════════════════════════════════════════════════════════

  ① Client gửi Connection: keep-alive trong header
  ② Server nhận và xử lý Connection field
  ③ Server gửi lại Connection: Keep-Alive
  ④ Client nhận → Keep-Alive THÀNH CÔNG ✅

  TỰ ĐỘNG NGẮT (không Keep-Alive):
  ① Client gửi request KHÔNG có Connection field
  ② Server xử lý request
  ③ Server trả resource → ĐÓNG connection
  ④ Client nhận → không thấy Connection → NGẮT

  CLIENT YÊU CẦU NGẮT:
  ① Client gửi Connection: close
  ② Server nhận → xử lý
  ③ Server trả resource → ĐÓNG connection
  ④ Client nhận → NGẮT
```

### Ưu nhược điểm Keep-Alive

```
KEEP-ALIVE — ƯU NHƯỢC ĐIỂM:
═══════════════════════════════════════════════════════════════

  ✅ ƯU ĐIỂM:
  → Ít CPU & memory hơn (ít open connection đồng thời)
  → Hỗ trợ HTTP pipelining
  → Giảm congestion control (ít TCP connections)
  → Giảm latency các request tiếp theo (không handshake lại)
  → Báo lỗi không cần đóng TCP connection

  ❌ NHƯỢC ĐIỂM:
  → TCP connection kéo dài → lãng phí tài nguyên hệ thống
    nếu connection không được sử dụng hiệu quả
```

---

## 13. HTTP Caching Headers & 304

### 304 Status Code

```
304 NOT MODIFIED — BROWSER CACHING:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │  304 KHÔNG phải lỗi!                                     │
  │  → Server: "Resource chưa thay đổi, dùng cache đi!"    │
  │  → Chỉ trả HEADER, không trả CONTENT → tăng hiệu suất │
  │                                                          │
  │  QUÁ NHIỀU 304? (content không update):                  │
  │  ❌ Website snapshot ngừng cập nhật                      │
  │  ❌ Số entries (pages indexed) giảm                      │
  │  ❌ SEO weight giảm                                      │
  │  ❌ Search engine giảm tần suất crawl                    │
  │                                                          │
  │  NGUYÊN NHÂN NHIỀU 304:                                   │
  │  → Chu kỳ cập nhật page QUÁ DÀI                        │
  │  → Page hoàn toàn static, không có dynamic content      │
  └──────────────────────────────────────────────────────────┘
```

### Caching Headers

```
HTTP CACHING HEADERS:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │  STRONG CACHING (Caching mạnh):                           │
  │  → Expires         (HTTP/1.0, absolute time)             │
  │  → Cache-Control   (HTTP/1.1, relative time, linh hoạt) │
  │                                                          │
  │  → Nếu cache HỢP LỆ → dùng TRỰC TIẾP, không hỏi      │
  │    server (Status: 200 from cache)                       │
  ├──────────────────────────────────────────────────────────┤
  │  CONDITIONAL CACHING (Caching có điều kiện):              │
  │  → ETag / If-None-Match       (content hash)             │
  │  → Last-Modified / If-Modified-Since (timestamp)         │
  │                                                          │
  │  → Hỏi server: "Resource có thay đổi không?"            │
  │  → Không đổi → 304 (dùng cache)                         │
  │  → Có đổi → 200 (trả resource mới)                      │
  └──────────────────────────────────────────────────────────┘
```

---

## 14. GET URL Length Limit

```
GET URL LENGTH LIMIT — DO BROWSER, KHÔNG PHẢI RFC:
═══════════════════════════════════════════════════════════════

  ⚠️ HTTP RFC KHÔNG giới hạn URL length!
  → Giới hạn do BROWSER và SERVER quy định

  BROWSER LIMITS:
  ┌──────────────────────┬───────────────────────────────────┐
  │ Internet Explorer    │ 2,083 characters (MIN!)           │
  │ Firefox              │ 65,536 characters                 │
  │ Safari               │ 80,000 characters                 │
  │ Opera                │ 190,000 characters                │
  │ Chrome               │ 8,182 characters                  │
  └──────────────────────┴───────────────────────────────────┘

  SERVER LIMITS:
  ┌──────────────────────┬───────────────────────────────────┐
  │ Apache               │ 8,192 characters                  │
  │ IIS                  │ 16,384 characters                 │
  └──────────────────────┴───────────────────────────────────┘

  💡 AN TOÀN: giữ URL ≤ 2,083 characters (IE limit)
  → Đảm bảo hoạt động trên TẤT CẢ browsers & servers

  Công thức: GET data length = 2083 - domain - path - 2 (?=)
```

---

## 15. Multiple Images Loading

```
MULTIPLE IMAGES — HTTP/1 vs HTTP/2:
═══════════════════════════════════════════════════════════════

  HTTP/1.1:
  → Browser giới hạn TỐI ĐA 6 TCP connections / domain
  → Nhiều ảnh → nhiều requests → CHẬM
  → Giải pháp: DOMAIN SHARDING (chia nhiều subdomain)
    img1.domain.com, img2.domain.com, img3.domain.com
    → Tăng số concurrent connections → load NHANH hơn

  HTTP/2:
  → MULTIPLEXING: load RẤT NHIỀU resources trên 1 conn.
  → KHÔNG cần domain sharding
  → Tất cả images load ĐỒNG THỜI trên 1 TCP connection
```

---

## 16. HTTP/2 Header Compression (HPACK)

```
HPACK — THUẬT TOÁN NÉN HEADER HTTP/2:
═══════════════════════════════════════════════════════════════

  NGUYÊN LÝ:
  → Dùng DICTIONARY (bảng từ điển) ở cả client + server
  → Index numbers đại diện cho chuỗi lặp lại
  → Huffman coding nén integers + strings
  → Tỉ lệ nén: 50% - 90%!

  HEADER TABLE:
  → Tồn tại SUỐT vòng đời HTTP/2 connection
  → Client + Server CẬP NHẬT liên tục
  → Key-value mới: append hoặc thay thế giá trị cũ

  VÍ DỤ:
  ┌──────────────────────┐  ┌──────────────────────┐
  │ Request #1 (đầy đủ)  │  │ Request #2 (chỉ diff)│
  ├──────────────────────┤  ├──────────────────────┤
  │ :method  GET      ✓ │  │ :method  (đã có)     │
  │ :scheme  https    ✓ │  │ :scheme  (đã có)     │
  │ :host    ex.com   ✓ │  │ :host    (đã có)     │
  │ :path    /resource✓ │  │ :path    /new_res ★  │
  │ accept   image/j  ✓ │  │ accept   (đã có)     │
  │ user-agt Mozilla  ✓ │  │ user-agt (đã có)     │
  └──────────────────────┘  └──────────────────────┘
  → Gửi 6 fields              → Chỉ gửi 1 field (:path)!
```

---

## 17. HTTP Performance

```
HTTP PERFORMANCE — 3 VẤN ĐỀ CHÍNH:
═══════════════════════════════════════════════════════════════

  ① LONG CONNECTION vs SHORT CONNECTION:
  ┌──────────────────────────────────────────────────────────┐
  │ Non-persistent: mỗi request = 1 TCP (3-way handshake)  │
  │ Persistent: TCP giữ mở → nhiều requests reuse          │
  │                                                          │
  │ HTTP/1.0: short conn (thêm Connection: keep-alive)     │
  │ HTTP/1.1: long conn mặc định (tối đa 6/domain)        │
  └──────────────────────────────────────────────────────────┘

  ② PIPELINE (HTTP/1.1):
  ┌──────────────────────────────────────────────────────────┐
  │ Trong 1 TCP, client gửi request TIẾP THEO               │
  │ mà KHÔNG CHỜ response trước đó                          │
  │ → Giảm overall response time                             │
  │                                                          │
  │ ⚠️ Server vẫn trả response TUẦN TỰ                      │
  │ → Head-of-line blocking vẫn tồn tại                     │
  └──────────────────────────────────────────────────────────┘

  ③ HEAD-OF-LINE BLOCKING:
  ┌──────────────────────────────────────────────────────────┐
  │ Message gửi/nhận TUẦN TỰ → FIFO queue                  │
  │ Request đầu CHẬM → TẤT CẢ phía sau BỊ CHẶN            │
  │                                                          │
  │ GIẢI PHÁP:                                                │
  │ (1) Concurrent connections: cho phép NHIỀU long conn.   │
  │     tới 1 domain → tăng task queue                      │
  │ (2) Domain sharding: chia thành NHIỀU subdomain         │
  │     → tất cả trỏ về cùng server                        │
  │     → tăng concurrent long connections                  │
  └──────────────────────────────────────────────────────────┘
```

---

## 18. Ưu nhược điểm HTTP

```
HTTP PROTOCOL — ƯU NHƯỢC ĐIỂM:
═══════════════════════════════════════════════════════════════

  ✅ ƯU ĐIỂM:
  ┌──────────────────────────────────────────────────────────┐
  │ ① Client/Server mode                                     │
  │ ② Simple & Fast: chỉ gửi method + path → nhanh         │
  │ ③ Connectionless: 1 conn = 1 request → tiết kiệm       │
  │ ④ Stateless: không cần info trước → response nhanh     │
  │ ⑤ Flexible: truyền bất kỳ data type (Content-Type)     │
  └──────────────────────────────────────────────────────────┘

  ❌ NHƯỢC ĐIỂM:
  ┌──────────────────────────────────────────────────────────┐
  │ ① Stateless: cần info trước → phải gửi lại → nhiều data│
  │ ② Plaintext: message dạng text → LỘ ra ngoài           │
  │ ③ Unsafe:                                                │
  │   → Truyền plaintext → bị NGHE LÉN                    │
  │   → Không xác thực danh tính → bị GIẢ MẠO             │
  │   → Không chứng minh toàn vẹn → bị SỬA ĐỔI           │
  └──────────────────────────────────────────────────────────┘
```

---

# Phần 5: Tổng hợp

---

## 19. Nhập Google.com — Chuyện gì xảy ra?

```
GÕ GOOGLE.COM + ENTER — 9 BƯỚC:
═══════════════════════════════════════════════════════════════

  ① URL PARSING
  ┌──────────────────────────────────────────────────────────┐
  │ → Phân tích protocol + resource path                    │
  │ → Protocol/hostname sai? → gửi search engine           │
  │ → Có ký tự illegal? → escape trước khi tiếp           │
  └──────────────────────────────────────────────────────────┘
                              ▼
  ② CACHE JUDGMENT
  ┌──────────────────────────────────────────────────────────┐
  │ → Resource trong cache + chưa hết hạn? → dùng luôn ✅ │
  │ → Không? → gửi request mới tới server                  │
  └──────────────────────────────────────────────────────────┘
                              ▼
  ③ DNS RESOLUTION
  ┌──────────────────────────────────────────────────────────┐
  │ → Local cache → Local DNS → Root → TLD → Auth. DNS    │
  │ → Client→Local DNS: recursive query                    │
  │ → Local DNS→servers: iterative query                   │
  └──────────────────────────────────────────────────────────┘
                              ▼
  ④ MAC ADDRESS
  ┌──────────────────────────────────────────────────────────┐
  │ → Cần MAC address để truyền ở Data Link Layer          │
  │ → Cùng subnet? → ARP lấy MAC đích trực tiếp           │
  │ → Khác subnet? → ARP lấy MAC gateway → forward        │
  └──────────────────────────────────────────────────────────┘
                              ▼
  ⑤ TCP THREE-WAY HANDSHAKE
  ┌──────────────────────────────────────────────────────────┐
  │ Client → SYN + seq → Server                             │
  │ Server → SYN ACK + seq → Client                         │
  │ Client → ACK → Server  (Connection established!)       │
  └──────────────────────────────────────────────────────────┘
                              ▼
  ⑥ TLS HANDSHAKE (nếu HTTPS)
  ┌──────────────────────────────────────────────────────────┐
  │ → Client: version + RN1 + cipher suites                 │
  │ → Server: cipher + certificate + RN2                    │
  │ → Client: verify cert + encrypt RN3 + hash             │
  │ → Server: decrypt RN3 + hash                            │
  │ → Session key = f(RN1, RN2, RN3) → symmetric encrypt  │
  └──────────────────────────────────────────────────────────┘
                              ▼
  ⑦ SERVER RETURNS DATA
  ┌──────────────────────────────────────────────────────────┐
  │ → Server trả HTML file                                   │
  │ → Browser bắt đầu parse HTML                            │
  └──────────────────────────────────────────────────────────┘
                              ▼
  ⑧ PAGE RENDERING
  ┌──────────────────────────────────────────────────────────┐
  │ → Parse HTML → DOM Tree                                  │
  │ → Parse CSS → CSSOM Tree                                 │
  │ → <script>? Check defer/async (nếu không → BLOCK)     │
  │ → DOM + CSSOM → Render Tree                              │
  │ → Layout → Paint → Display! 🎉                         │
  └──────────────────────────────────────────────────────────┘
                              ▼
  ⑨ TCP FOUR-WAY HANDSHAKE (đóng kết nối)
  ┌──────────────────────────────────────────────────────────┐
  │ Client → FIN → Server (tôi muốn đóng)                  │
  │ Server → ACK → Client (OK, đợi tôi gửi xong)         │
  │ Server → FIN → Client (tôi cũng xong rồi)             │
  │ Client → ACK → Server (OK!) → TIME-WAIT (2MSL)        │
  │ → CLOSED!                                                │
  └──────────────────────────────────────────────────────────┘
```

---

## 20. Tóm Tắt & Câu Hỏi Phỏng Vấn

### Quick Reference

```
HTTP PROTOCOL — QUICK REFERENCE:
═══════════════════════════════════════════════════════════════

  METHODS: GET(đọc), POST(tạo), PUT(sửa), DELETE(xóa),
           HEAD(header), OPTIONS(preflight), CONNECT, TRACE

  GET vs POST:
    GET = idempotent, cached, params in URL, limited length
    POST = non-idempotent, not cached, params in body

  VERSIONS:
    1.0: short conn, full object, simple cache
    1.1: persistent conn, range, host, more methods
    2.0: binary, multiplexing, header compress, server push
    3.0: QUIC/UDP, 0-1 RTT handshake

  KEEP-ALIVE:
    1.0: off by default (add Connection: keep-alive)
    1.1: on by default (send Connection: close to stop)

  CACHING: Strong (Expires, Cache-Control)
           Conditional (ETag, Last-Modified)

  PORT: HTTP=80, HTTPS=443
```

### Câu Hỏi Phỏng Vấn Thường Gặp

**1. GET và POST khác nhau thế nào?**

> GET: idempotent, params trong URL (kém bảo mật, bị cache, giới hạn length). POST: non-idempotent, params trong body (an toàn hơn, không cache, không giới hạn, hỗ trợ nhiều data types).

**2. HTTP/1.0, 1.1 và 2.0 khác nhau thế nào?**

> 1.0: non-persistent connection, không có Host, caching đơn giản. 1.1: persistent connection, Range request, Host header, thêm methods. 2.0: binary protocol, multiplexing (giải quyết head-of-line blocking), header compression (HPACK, giảm 50-90%), server push.

**3. HTTP/3.0 giải quyết vấn đề gì?**

> HTTP/3 dùng QUIC trên UDP thay TCP. Giữ multiplexing nhưng giải quyết TCP head-of-line blocking. Handshake 0-1 RTT (nhanh hơn TCP 1-3 RTT). Tích hợp TLS 1.3.

**4. Keep-Alive là gì? Tại sao cần?**

> Keep-Alive giữ TCP connection mở cho nhiều requests, tránh 3-way handshake mỗi lần. HTTP/1.0: off by default. HTTP/1.1: on by default. Ưu: ít CPU/memory, giảm latency. Nhược: lãng phí resource nếu connection idle.

**5. Head-of-line blocking là gì? Giải pháp?**

> HTTP message FIFO → request đầu chậm → tất cả phía sau bị chặn. Giải pháp HTTP/1.1: concurrent connections (nhiều TCP/domain), domain sharding. Giải pháp HTTP/2: multiplexing (nhiều stream trên 1 TCP).

**6. Mô tả quá trình khi gõ URL vào browser?**

> 9 bước: ① URL parsing → ② Cache check → ③ DNS resolution → ④ MAC address (ARP) → ⑤ TCP 3-way handshake → ⑥ TLS handshake (HTTPS) → ⑦ Server trả HTML → ⑧ Page rendering (DOM → CSSOM → Render Tree → Layout → Paint) → ⑨ TCP 4-way handshake đóng connection.

**7. 302 vs 303 vs 307?**

> 302 (HTTP/1.0): redirect tạm thời, chuẩn cấm đổi method nhưng browser đổi POST→GET. 303 (HTTP/1.1): rõ ràng yêu cầu GET. 307 (HTTP/1.1): giữ nguyên method, không đổi POST→GET.

**8. HTTP có những ưu nhược điểm gì?**

> Ưu: client/server mode, simple/fast, connectionless, stateless, flexible (any content type). Nhược: stateless (cần retransmit info), plaintext (exposed), unsafe (nghe lén, giả mạo, sửa đổi).

---

## Checklist Học Tập

- [ ] Phân biệt GET vs POST (6 tiêu chí)
- [ ] Biết 8 HTTP methods và use case của OPTIONS
- [ ] Hiểu cấu trúc HTTP request/response message
- [ ] Biết 4 giá trị Content-Type phổ biến
- [ ] Phân biệt HTTP/1.0, 1.1, 2.0, 3.0
- [ ] Hiểu Keep-Alive (setup, ưu/nhược)
- [ ] Biết HTTP caching headers (strong vs conditional)
- [ ] Hiểu head-of-line blocking và giải pháp
- [ ] Biết HPACK header compression hoạt động thế nào
- [ ] Mô tả 9 bước khi gõ URL vào browser

---

_Cập nhật lần cuối: Tháng 2, 2026_
