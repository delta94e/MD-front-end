# DNS Protocol — Domain Name System Deep Dive

> 📅 2026-02-11 · ⏱ 15 phút đọc
>
> Tài liệu chuyên sâu về giao thức DNS: khái niệm, cơ chế hoạt động,
> quá trình phân giải tên miền, iterative vs recursive queries, DNS records.
> Độ khó: ⭐️⭐️⭐️ | Chủ đề: Network — Application Layer

---

## Mục Lục

0. [DNS là gì?](#0-dns-là-gì)
1. [DNS dùng TCP hay UDP?](#1-dns-dùng-tcp-hay-udp)
2. [Quá trình phân giải DNS hoàn chỉnh](#2-quá-trình-phân-giải-dns-hoàn-chỉnh)
3. [Iterative Query vs Recursive Query](#3-iterative-query-vs-recursive-query)
4. [DNS Records & Messages](#4-dns-records--messages)
5. [Tóm Tắt & Câu Hỏi Phỏng Vấn](#5-tóm-tắt--câu-hỏi-phỏng-vấn)

---

## 0. DNS là gì?

> **🎯 Học xong phần này, bạn sẽ biết:**
>
> - DNS là gì và tại sao cần DNS
> - Chức năng chính của DNS
> - Kiến trúc phân cấp của DNS

### Khái niệm

```
DNS — DOMAIN NAME SYSTEM:
═══════════════════════════════════════════════════════════════

  DNS = Domain Name System (Hệ thống tên miền)

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  ĐỊNH NGHĨA:                                             │
  │  → Cung cấp dịch vụ chuyển đổi HOSTNAME → IP ADDRESS   │
  │  → Là CƠ SỞ DỮ LIỆU PHÂN TÁN (distributed database)  │
  │    bao gồm các DNS server xếp theo TẦNG BẬC            │
  │  → Là giao thức TẦNG ỨNG DỤNG (Application Layer)      │
  │                                                          │
  │  TẠI SAO CẦN DNS?                                        │
  │  → Con người nhớ TÊN MIỀN dễ hơn nhớ IP                │
  │  → Máy tính cần IP ADDRESS để giao tiếp                 │
  │  → DNS = "danh bạ" của Internet                          │
  │                                                          │
  │  VÍ DỤ:                                                  │
  │  www.google.com  ──DNS──►  142.250.190.4                │
  │  www.baidu.com   ──DNS──►  220.181.38.148               │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### Chức năng

```
CHỨC NĂNG CỦA DNS:
═══════════════════════════════════════════════════════════════

  Client muốn truy cập www.example.com:

  ┌──────────┐                           ┌──────────────┐
  │  Client  │ ── "www.example.com" ──► │  DNS Server  │
  │ (Browser)│                           │ (IP riêng)   │
  │          │ ◄── "93.184.216.34" ──── │              │
  └──────────┘                           └──────────────┘
       │
       │  Giờ đã biết IP, kết nối trực tiếp
       ▼
  ┌──────────────┐
  │  Web Server  │
  │ 93.184.216.34│
  └──────────────┘

  QUY TRÌNH:
  ① Client gửi yêu cầu TRUY VẤN tên miền → DNS Server
  ② DNS Server tra cứu và trả về IP ADDRESS
  ③ Client dùng IP đó để kết nối Web Server
```

### Kiến trúc phân cấp DNS

```
DNS HIERARCHY — CẤU TRÚC PHÂN CẤP:
═══════════════════════════════════════════════════════════════

                    ┌───────────────┐
                    │  Root DNS     │  ← 13 root servers
                    │  Servers (.)  │     trên toàn thế giới
                    └───────┬───────┘
                            │
            ┌───────────────┼───────────────┐
            ▼               ▼               ▼
     ┌──────────┐    ┌──────────┐    ┌──────────┐
     │ .com TLD │    │ .org TLD │    │ .vn TLD  │
     │  Server  │    │  Server  │    │  Server  │
     └────┬─────┘    └──────────┘    └──────────┘
          │
     ┌────┴────────────────┐
     ▼                     ▼
  ┌────────────┐    ┌────────────┐
  │ google.com │    │ baidu.com  │
  │ Auth. DNS  │    │ Auth. DNS  │
  └────────────┘    └────────────┘

  4 LOẠI DNS SERVER:
  ┌────────────────────┬────────────────────────────────────┐
  │ Root Name Server   │ Biết địa chỉ TLD servers          │
  │                    │ (13 cụm server toàn cầu)          │
  ├────────────────────┼────────────────────────────────────┤
  │ TLD Name Server    │ Quản lý domain top-level           │
  │ (Top-Level Domain) │ (.com, .org, .vn, .net...)        │
  ├────────────────────┼────────────────────────────────────┤
  │ Authoritative DNS  │ Quản lý zone cụ thể               │
  │                    │ (google.com, baidu.com)            │
  ├────────────────────┼────────────────────────────────────┤
  │ Local DNS Server   │ ISP cung cấp, gần người dùng nhất│
  │ (DNS Resolver)     │ Cache kết quả để tăng tốc         │
  └────────────────────┴────────────────────────────────────┘
```

---

## 1. DNS dùng TCP hay UDP?

> **🎯 Học xong phần này, bạn sẽ biết:**
>
> - DNS dùng port nào
> - Khi nào dùng TCP, khi nào dùng UDP
> - Tại sao zone transfer dùng TCP

### DNS sử dụng CẢ TCP và UDP — Port 53

```
DNS PROTOCOL — PORT 53 (TCP + UDP):
═══════════════════════════════════════════════════════════════

  DNS sử dụng ĐỒNG THỜI cả TCP và UDP trên PORT 53!

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  (1) TCP — ZONE TRANSFER (Chuyển vùng)                   │
  │  ─────────────────────────────────────                   │
  │                                                          │
  │  Secondary    ──── "Có thay đổi?" ────►   Primary       │
  │  DNS Server   ◄── "Có, đây là data" ────  DNS Server    │
  │               (mỗi ~3 giờ kiểm tra)                     │
  │                                                          │
  │  TẠI SAO DÙNG TCP?                                       │
  │  → Data đồng bộ giữa Primary ↔ Secondary RẤT LỚN      │
  │    (lớn hơn nhiều so với 1 query/response thông thường) │
  │  → TCP là kết nối TIN CẬY → đảm bảo data CHÍNH XÁC    │
  │  → Không thể mất data khi đồng bộ DNS records          │
  │                                                          │
  ├──────────────────────────────────────────────────────────┤
  │                                                          │
  │  (2) UDP — DOMAIN NAME RESOLUTION (Phân giải tên miền)   │
  │  ─────────────────────────────────────────────           │
  │                                                          │
  │  Client ── "IP của google.com?" ──► DNS Server           │
  │         ◄── "142.250.190.4" ──────                       │
  │                                                          │
  │  TẠI SAO DÙNG UDP?                                       │
  │  → Response thường ≤ 512 bytes → UDP đủ dùng           │
  │  → KHÔNG cần 3-way handshake → NHANH hơn               │
  │  → DNS Server tải THẤP hơn (không duy trì connection)  │
  │  → Tốc độ phản hồi NHANH hơn đáng kể                   │
  │                                                          │
  │  ⚠️ LƯU Ý:                                               │
  │  → Về lý thuyết, client CÓ THỂ chỉ định dùng TCP      │
  │  → Nhưng thực tế, nhiều DNS server CHỈ hỗ trợ UDP      │
  │                                                          │
  └──────────────────────────────────────────────────────────┘

  TÓM TẮT:
  ┌──────────────┬──────────────────┬────────────────────────┐
  │  Tình huống   │  Protocol        │  Lý do                 │
  ├──────────────┼──────────────────┼────────────────────────┤
  │ Zone Transfer│  TCP             │ Data lớn, cần tin cậy  │
  │ (đồng bộ)   │                  │                        │
  ├──────────────┼──────────────────┼────────────────────────┤
  │ Name Resolve │  UDP             │ Data nhỏ (≤512B),     │
  │ (phân giải)  │                  │ cần nhanh              │
  └──────────────┴──────────────────┴────────────────────────┘
```

---

## 2. Quá trình phân giải DNS hoàn chỉnh

> **🎯 Học xong phần này, bạn sẽ biết:**
>
> - 7 bước phân giải DNS hoàn chỉnh
> - Vai trò của cache ở mỗi giai đoạn
> - Ví dụ thực tế phân giải www.baidu.com

### 7 bước phân giải DNS

```
DNS RESOLUTION — 7 BƯỚC HOÀN CHỈNH:
═══════════════════════════════════════════════════════════════

  ① Browser Cache
  ┌──────────┐
  │ Browser  │── Tìm IP trong CACHE BROWSER
  │          │── Tìm thấy? → Trả về ngay ✅
  │          │── Không tìm thấy? → Bước ②
  └────┬─────┘
       │
  ② Local DNS Server (DNS Resolver)
       ▼
  ┌──────────────┐
  │  Local DNS   │── Tìm IP trong CACHE LOCAL DNS
  │  Server      │── Tìm thấy? → Trả về ngay ✅
  │  (ISP cung   │── Không tìm thấy? → Bước ③
  │   cấp)       │
  └────┬─────────┘
       │
  ③ Root Name Server
       ▼
  ┌──────────────┐
  │  Root DNS    │── KHÔNG trả về IP trực tiếp
  │  Server (.)  │── Trả về địa chỉ TLD Server
  │              │   phụ trách domain đó
  └────┬─────────┘   (VD: .com TLD server)
       │
  ④ TLD Name Server
       ▼
  ┌──────────────┐
  │  TLD Server  │── Kiểm tra cache
  │  (.com)      │── Tìm thấy? → Trả về ✅
  │              │── Không? → Trả về địa chỉ
  └────┬─────────┘   Authoritative DNS Server
       │
  ⑤ Authoritative Name Server
       ▼
  ┌──────────────┐
  │ Authoritative│── Trả về IP ADDRESS tương ứng
  │ DNS Server   │   với hostname được truy vấn ✅
  │ (baidu.com)  │
  └──────────────┘

  ⑥ Local DNS Server LƯU CACHE
  → Lưu kết quả vào cache để lần sau dùng lại

  ⑦ Trả kết quả về BROWSER
  → Browser nhận IP, kết nối Web Server
```

### Ví dụ: Phân giải www.baidu.com

```
VÍ DỤ THỰC TẾ — PHÂN GIẢI www.baidu.com:
═══════════════════════════════════════════════════════════════

  Browser: "Tôi cần IP của www.baidu.com"

  ① Browser cache     → Không có
  ② Local DNS cache   → Không có
  ③ Root DNS Server   → "Đây là danh sách TLD server
                          phụ trách .com"

     Local DNS ──► TLD Server (.com)
  ④ TLD Server (.com) → "Đây là danh sách Auth. DNS
                          phụ trách .baidu"

     Local DNS ──► Authoritative DNS (baidu.com)
  ⑤ Auth. DNS         → "www.baidu.com = 220.181.38.148" ✅

  ⑥ Local DNS lưu cache: www.baidu.com → 220.181.38.148
  ⑦ Trả 220.181.38.148 về browser

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  Browser ──► Local DNS ──► Root ──► .com TLD             │
  │                  ▲                      │                │
  │                  │          ◄────────────┘                │
  │                  │                                       │
  │                  ├──► Authoritative (baidu.com)           │
  │                  │          │                             │
  │                  ◄──────────┘                             │
  │                  │                                       │
  │  Browser ◄───────┘  (220.181.38.148)                     │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

---

## 3. Iterative Query vs Recursive Query

> **🎯 Học xong phần này, bạn sẽ biết:**
>
> - Sự khác biệt giữa Recursive và Iterative Query
> - Trong quá trình DNS, đoạn nào dùng loại nào

### Hai loại truy vấn DNS

```
RECURSIVE vs ITERATIVE QUERY:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │  RECURSIVE QUERY (Truy vấn đệ quy)                      │
  │  ─────────────────────────────────                       │
  │                                                          │
  │  Client ────► Local DNS                                  │
  │         ◄──── (KẾT QUẢ CUỐI CÙNG)                       │
  │                                                          │
  │  → Client CHỈ GỬI 1 request duy nhất                    │
  │  → Local DNS sẽ "chạy giùm" tất cả các bước            │
  │  → Client chỉ việc NGỒI CHỜ kết quả cuối cùng          │
  │                                                          │
  │  💡 Giống như nhờ ai đó đi tìm thông tin cho mình       │
  │     → mình chỉ cần ngồi đợi kết quả                     │
  │                                                          │
  ├──────────────────────────────────────────────────────────┤
  │  ITERATIVE QUERY (Truy vấn lặp)                          │
  │  ────────────────────────────                            │
  │                                                          │
  │  Local DNS ────► Root DNS                                │
  │            ◄──── "Đây là TLD server"                     │
  │                                                          │
  │  Local DNS ────► TLD Server                              │
  │            ◄──── "Đây là Auth. server"                   │
  │                                                          │
  │  Local DNS ────► Auth. Server                            │
  │            ◄──── "Đây là IP!" ✅                         │
  │                                                          │
  │  → Mỗi server chỉ trả về KẾT QUẢ ĐƠN LẺ              │
  │  → Local DNS TỰ MÌNH gửi tiếp request kế tiếp          │
  │  → Phải gửi NHIỀU requests                               │
  │                                                          │
  │  💡 Giống như tự đi hỏi từng người một                   │
  │     → mỗi người chỉ cho mình hỏi ai tiếp theo          │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### Kết hợp trong thực tế

```
DNS RESOLUTION = RECURSIVE + ITERATIVE:
═══════════════════════════════════════════════════════════════

  Client                Local DNS              DNS Servers
  ┌──────┐             ┌──────────┐          ┌──────────────┐
  │      │── query ──►│          │── q1 ──► │  Root DNS    │
  │      │             │          │◄─ a1 ──  │              │
  │      │  RECURSIVE  │          │          └──────────────┘
  │      │  (1 request │          │── q2 ──► ┌──────────────┐
  │      │   duy nhất) │          │◄─ a2 ──  │  TLD Server  │
  │      │             │          │          └──────────────┘
  │      │             │          │── q3 ──► ┌──────────────┐
  │      │             │          │◄─ a3 ──  │  Auth. DNS   │
  │      │◄─ result ──│          │ ITERATIVE└──────────────┘
  └──────┘             └──────────┘ (nhiều
                                     requests)

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  Client → Local DNS:  RECURSIVE QUERY                    │
  │  → Client chỉ gửi 1 request, nhận 1 kết quả cuối cùng │
  │                                                          │
  │  Local DNS → Root/TLD/Auth:  ITERATIVE QUERY             │
  │  → Local DNS tự gửi nhiều requests lần lượt             │
  │  → Mỗi DNS server chỉ trả kết quả đơn lẻ              │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

---

## 4. DNS Records & Messages

> **🎯 Học xong phần này, bạn sẽ biết:**
>
> - Resource Record format
> - 4 loại Type phổ biến: A, NS, CNAME, MX
> - Ý nghĩa TTL

### Resource Record Format

```
DNS RESOURCE RECORD — ĐỊNH DẠNG:
═══════════════════════════════════════════════════════════════

  Mỗi DNS server lưu thông tin dưới dạng RESOURCE RECORDS.
  Mỗi DNS response thường chứa NHIỀU resource records.

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  FORMAT:  ( Name, Value, Type, TTL )                     │
  │                                                          │
  │  ┌───────┬───────────────────────────────────────────┐   │
  │  │ Name  │ Tên miền hoặc hostname                    │   │
  │  ├───────┼───────────────────────────────────────────┤   │
  │  │ Value │ Giá trị tương ứng (IP, hostname, alias)  │   │
  │  ├───────┼───────────────────────────────────────────┤   │
  │  │ Type  │ Loại record (A, NS, CNAME, MX)           │   │
  │  ├───────┼───────────────────────────────────────────┤   │
  │  │ TTL   │ Time To Live — thời gian record có thể   │   │
  │  │       │ được CACHE ở các DNS server khác          │   │
  │  └───────┴───────────────────────────────────────────┘   │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### 4 loại Type phổ biến

```
DNS RECORD TYPES — 4 LOẠI PHỔ BIẾN:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │  TYPE A — Address Record (Hostname → IP)                 │
  │  ──────────────────────────────────────                  │
  │  Name  = hostname                                        │
  │  Value = IP address                                      │
  │  → Ánh xạ CHUẨN từ hostname → IP                       │
  │                                                          │
  │  VD: (www.google.com, 142.250.190.4, A, 300)            │
  │  → www.google.com có IP là 142.250.190.4                │
  │  → Cache được 300 giây (5 phút)                         │
  ├──────────────────────────────────────────────────────────┤
  │  TYPE NS — Name Server Record                            │
  │  ────────────────────────────                            │
  │  Name  = domain name                                     │
  │  Value = hostname của DNS server phụ trách domain đó    │
  │  → Dùng trong DNS CHAINING (truy vấn liên tiếp)        │
  │  → Trả về DNS server CẤP TIẾP THEO cần truy vấn       │
  │                                                          │
  │  VD: (google.com, ns1.google.com, NS, 86400)            │
  │  → Domain google.com được quản lý bởi ns1.google.com   │
  ├──────────────────────────────────────────────────────────┤
  │  TYPE CNAME — Canonical Name Record                      │
  │  ─────────────────────────────────                       │
  │  Name  = alias (tên bí danh)                             │
  │  Value = canonical hostname (tên chính thức)             │
  │  → Tạo TÊN GỌI ĐƠN GIẢN cho hostname phức tạp        │
  │  → Client sẽ tiếp tục tra cứu IP của canonical name    │
  │                                                          │
  │  VD: (blog.google.com,                                   │
  │       ghs.googlehosted.com, CNAME, 3600)                │
  │  → blog.google.com thực chất là ghs.googlehosted.com   │
  ├──────────────────────────────────────────────────────────┤
  │  TYPE MX — Mail Exchange Record                          │
  │  ─────────────────────────────                           │
  │  Name  = alias của mail server                           │
  │  Value = canonical hostname của mail server              │
  │  → Giống CNAME nhưng DÀNH RIÊNG cho email               │
  │  → Giải quyết vấn đề canonical hostname khó nhớ        │
  │                                                          │
  │  VD: (google.com, smtp.google.com, MX, 3600)            │
  │  → Email @google.com được xử lý bởi smtp.google.com    │
  └──────────────────────────────────────────────────────────┘
```

### Tóm tắt DNS Record Types

```
BẢNG TÓM TẮT:
═══════════════════════════════════════════════════════════════

  ┌───────┬───────────┬──────────────────┬──────────────────┐
  │ Type  │  Name     │  Value           │  Mục đích        │
  ├───────┼───────────┼──────────────────┼──────────────────┤
  │   A   │ Hostname  │ IP address       │ Host → IP        │
  ├───────┼───────────┼──────────────────┼──────────────────┤
  │  NS   │ Domain    │ DNS server name  │ DNS chaining     │
  ├───────┼───────────┼──────────────────┼──────────────────┤
  │ CNAME │ Alias     │ Canonical name   │ Alias → tên thật │
  ├───────┼───────────┼──────────────────┼──────────────────┤
  │  MX   │ Mail alias│ Mail server name │ Email routing    │
  └───────┴───────────┴──────────────────┴──────────────────┘
```

---

## 5. Tóm Tắt & Câu Hỏi Phỏng Vấn

### Quick Reference

```
DNS QUICK REFERENCE:
═══════════════════════════════════════════════════════════════

  DNS = Domain Name System, port 53

  PROTOCOL:
    UDP → phân giải tên miền (response ≤ 512B, nhanh)
    TCP → zone transfer (data lớn, cần tin cậy)

  DNS LOOKUP (7 bước):
    ① Browser cache
    ② Local DNS cache
    ③ Root DNS Server → trả TLD server address
    ④ TLD Server → trả Auth. DNS address
    ⑤ Authoritative DNS → trả IP address ✅
    ⑥ Local DNS lưu cache
    ⑦ Trả kết quả về browser

  QUERY TYPES:
    Recursive  → Client → Local DNS (1 request, 1 answer)
    Iterative  → Local DNS → Root/TLD/Auth (nhiều requests)

  RECORD TYPES:
    A     → hostname → IP address
    NS    → domain → DNS server name (chaining)
    CNAME → alias → canonical hostname
    MX    → mail alias → mail server name
```

### Câu Hỏi Phỏng Vấn Thường Gặp

**1. DNS là gì và chức năng chính?**

> DNS (Domain Name System) là hệ thống phân giải tên miền, chuyển đổi hostname thành IP address. Là cơ sở dữ liệu phân tán gồm các DNS server xếp theo tầng bậc, hoạt động ở tầng Application Layer.

**2. DNS dùng TCP hay UDP?**

> Dùng CẢ HAI trên port 53. UDP cho phân giải tên miền (response ≤ 512B, không cần 3-way handshake → nhanh). TCP cho zone transfer giữa Primary và Secondary DNS server (data lớn, cần tin cậy).

**3. Mô tả quá trình phân giải DNS?**

> 7 bước: ① Browser cache → ② Local DNS cache → ③ Root DNS (trả TLD address) → ④ TLD Server (trả Auth. DNS address) → ⑤ Authoritative DNS (trả IP) → ⑥ Local DNS lưu cache → ⑦ Trả về browser.

**4. Recursive query và Iterative query khác nhau thế nào?**

> Recursive: client gửi 1 request, DNS server "chạy giùm" toàn bộ và trả kết quả cuối cùng. Iterative: mỗi DNS server chỉ trả kết quả đơn lẻ (địa chỉ server tiếp theo), client/local DNS phải tự gửi request tiếp. Trong thực tế: Client → Local DNS dùng recursive; Local DNS → Root/TLD/Auth dùng iterative.

**5. Giải thích 4 loại DNS record phổ biến?**

> A: hostname → IP (ánh xạ chuẩn). NS: domain → DNS server name (dùng trong DNS chaining). CNAME: alias → canonical hostname (tạo tên đơn giản cho hostname phức tạp). MX: mail alias → mail server name (dành riêng cho email routing).

**6. TTL trong DNS record là gì?**

> TTL (Time To Live) xác định thời gian tối đa mà resource record có thể được cache ở các DNS server khác. Hết TTL → DNS server phải query lại để lấy record mới nhất.

---

## Checklist Học Tập

- [ ] Giải thích được DNS là gì và tại sao cần DNS
- [ ] Biết DNS dùng port nào, khi nào dùng TCP, khi nào UDP
- [ ] Mô tả được 7 bước phân giải DNS hoàn chỉnh
- [ ] Phân biệt Recursive Query và Iterative Query
- [ ] Biết 4 loại DNS record: A, NS, CNAME, MX
- [ ] Hiểu kiến trúc phân cấp DNS (Root → TLD → Authoritative)
- [ ] Giải thích được TTL và vai trò của DNS cache

---

_Cập nhật lần cuối: Tháng 2, 2026_
