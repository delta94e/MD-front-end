# HTTP Status Codes — Deep Dive

> 📅 2026-02-11 · ⏱ 20 phút đọc
>
> Tài liệu chuyên sâu về HTTP Status Codes: phân loại, ý nghĩa chi tiết,
> use cases thực tế, so sánh các redirect codes, và câu hỏi phỏng vấn.
> Độ khó: ⭐️⭐️⭐️ | Chủ đề: Network — HTTP Protocol

---

## Mục Lục

0. [Tổng Quan Phân Loại](#0-tổng-quan-phân-loại)
1. [2XX — Success](#1-2xx--success)
2. [3XX — Redirection](#2-3xx--redirection)
3. [4XX — Client Error](#3-4xx--client-error)
4. [5XX — Server Error](#4-5xx--server-error)
5. [So Sánh 302 vs 303 vs 307](#5-so-sánh-302-vs-303-vs-307)
6. [Tóm Tắt & Câu Hỏi Phỏng Vấn](#6-tóm-tắt--câu-hỏi-phỏng-vấn)

---

## 0. Tổng Quan Phân Loại

> **🎯 Học xong phần này, bạn sẽ biết:**
>
> - 5 nhóm status code chính
> - Ý nghĩa tổng quát của từng nhóm

```
HTTP STATUS CODES — 5 NHÓM CHÍNH:
═══════════════════════════════════════════════════════════════

  ┌──────────┬────────────────────┬────────────────────────────┐
  │ Category │ Tên gọi            │ Mô tả                     │
  ├──────────┼────────────────────┼────────────────────────────┤
  │   1XX    │ Informational      │ Request đang được xử lý   │
  │          │ (Thông tin)        │                            │
  ├──────────┼────────────────────┼────────────────────────────┤
  │   2XX    │ Success            │ Request được xử lý THÀNH  │
  │          │ (Thành công)       │ CÔNG                       │
  ├──────────┼────────────────────┼────────────────────────────┤
  │   3XX    │ Redirection        │ Cần thêm thao tác để hoàn │
  │          │ (Chuyển hướng)     │ thành request              │
  ├──────────┼────────────────────┼────────────────────────────┤
  │   4XX    │ Client Error       │ Server KHÔNG THỂ xử lý    │
  │          │ (Lỗi phía Client)  │ request (lỗi client)      │
  ├──────────┼────────────────────┼────────────────────────────┤
  │   5XX    │ Server Error       │ Server GẶP LỖI khi xử lý │
  │          │ (Lỗi phía Server)  │ request                    │
  └──────────┴────────────────────┴────────────────────────────┘

  💡 GHI NHỚ NHANH:
  1XX = "Đang xử lý..."
  2XX = "OK, thành công!"
  3XX = "Đi chỗ khác đi!"
  4XX = "Lỗi của BẠN!"
  5XX = "Lỗi của TÔI (server)!"
```

---

## 1. 2XX — Success

> **🎯 Các status code 2XX cho biết request được xử lý BÌNH THƯỜNG.**

### 200 OK

```
200 OK — REQUEST THÀNH CÔNG:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  Client ── GET /api/users ──► Server                     │
  │         ◄── 200 OK + body ──                             │
  │                                                          │
  │  → Request từ client được server xử lý BÌNH THƯỜNG     │
  │  → Response có chứa ENTITY BODY (nội dung phản hồi)    │
  │  → Đây là status code THƯỜNG GẶP NHẤT                   │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### 204 No Content

```
204 NO CONTENT — THÀNH CÔNG NHƯNG KHÔNG CÓ NỘI DUNG:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  Client ── PUT /api/settings ──► Server                  │
  │         ◄── 204 No Content ────                          │
  │              (không có body)                              │
  │                                                          │
  │  → Server xử lý request BÌNH THƯỜNG                     │
  │  → KHÔNG trả về nội dung nào                             │
  │  → Response KHÔNG có entity body                         │
  │                                                          │
  │  USE CASE:                                                │
  │  → Client chỉ cần GỬI thông tin lên server              │
  │  → Server KHÔNG cần gửi gì ngược lại                    │
  │  → VD: cập nhật settings, toggle trạng thái             │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### 206 Partial Content

```
206 PARTIAL CONTENT — RANGE REQUEST:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  Client ── GET /video.mp4 ──► Server                     │
  │            Range: bytes=0-1023                            │
  │         ◄── 206 Partial Content ──                       │
  │             Content-Range: bytes 0-1023/146515           │
  │             (chỉ trả phần 0-1023)                        │
  │                                                          │
  │  → Client gửi RANGE REQUEST (yêu cầu 1 phần)           │
  │  → Server trả về ĐÚNG PHẦN đó                           │
  │  → Response chứa Content-Range header                    │
  │                                                          │
  │  USE CASE:                                                │
  │  → Download file lớn (resume download)                   │
  │  → Streaming video/audio                                 │
  │  → Phân trang dữ liệu lớn                               │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

---

## 2. 3XX — Redirection

> **🎯 Các status code 3XX yêu cầu browser thực hiện thao tác ĐẶC BIỆT.**

### 301 Moved Permanently

```
301 MOVED PERMANENTLY — CHUYỂN HƯỚNG VĨNH VIỄN:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  Client ── GET /old-page ──► Server                      │
  │         ◄── 301 Moved Permanently ──                     │
  │             Location: https://new-domain.com/new-page    │
  │                                                          │
  │  Client ── GET /new-page ──► new-domain.com (tự động)   │
  │         ◄── 200 OK ──                                    │
  │                                                          │
  │  ĐẶC ĐIỂM:                                               │
  │  → Resource đã được gán URI MỚI VĨNH VIỄN              │
  │  → Client nên dùng URI MỚI từ bây giờ                  │
  │  → Bookmark sẽ được CẬP NHẬT sang URI mới              │
  │  → Search engine sẽ THAY THẾ URL cũ bằng URL mới       │
  │                                                          │
  │  USE CASES:                                               │
  │  ① Đổi domain name:                                      │
  │    old-domain.com → 301 → new-domain.com                │
  │    → Báo search engine index domain mới                 │
  │                                                          │
  │  ② Domain không www → có www (hoặc ngược lại):           │
  │    example.com → 301 → www.example.com                  │
  │    → Báo search engine domain nào là chính              │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### 302 Found

```
302 FOUND — CHUYỂN HƯỚNG TẠM THỜI:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  Client ── GET /home ──► Server                          │
  │         ◄── 302 Found ──                                 │
  │             Location: /event-page                        │
  │                                                          │
  │  ĐẶC ĐIỂM:                                               │
  │  → Resource được gán URI MỚI TẠM THỜI                   │
  │  → URI có thể THAY ĐỔI lại trong tương lai             │
  │  → Bookmark KHÔNG cập nhật (giữ URI cũ)                │
  │  → Search engine crawl nội dung mới NHƯNG GIỮ URL cũ   │
  │                                                          │
  │  SO SÁNH VỚI 301:                                        │
  │  ┌──────────┬──────────────────┬──────────────────┐      │
  │  │          │     301          │     302          │      │
  │  ├──────────┼──────────────────┼──────────────────┤      │
  │  │ Loại     │ Vĩnh viễn       │ Tạm thời        │      │
  │  │ Bookmark │ Cập nhật URL mới│ Giữ URL cũ      │      │
  │  │ SEO      │ Chuyển index    │ Giữ index cũ    │      │
  │  └──────────┴──────────────────┴──────────────────┘      │
  │                                                          │
  │  USE CASES:                                               │
  │  ① Homepage → Event page (đang chạy sự kiện)            │
  │  ② User chưa login → Redirect login page                │
  │  ③ Trang 404 → Redirect về homepage                     │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### 303 See Other

```
303 SEE OTHER — REDIRECT VỚI GET METHOD:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  Client ── POST /upload ──► Server                       │
  │         ◄── 303 See Other ──                             │
  │             Location: /upload/success                    │
  │                                                          │
  │  Client ── GET /upload/success ──► Server (dùng GET!)   │
  │         ◄── 200 OK ──                                    │
  │                                                          │
  │  ĐẶC ĐIỂM:                                               │
  │  → Resource tồn tại ở URI KHÁC                           │
  │  → Client PHẢI dùng GET để lấy resource                 │
  │  → Chức năng tương tự 302, nhưng RÕ RÀNG hơn           │
  │                                                          │
  │  USE CASE:                                                │
  │  → Sau PUT/POST → redirect đến trang xác nhận           │
  │  → Redirect link KHÔNG trỏ đến resource vừa upload     │
  │    mà trỏ đến confirmation page hoặc progress page     │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### 304 Not Modified

```
304 NOT MODIFIED — LIÊN QUAN ĐẾN BROWSER CACHING:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  Client ── GET /styles.css ──► Server                    │
  │            If-None-Match: "abc123"                        │
  │            If-Modified-Since: Wed, 01 Jan 2026           │
  │         ◄── 304 Not Modified ──                          │
  │             (KHÔNG có body!)                              │
  │                                                          │
  │  → Browser: "Dùng bản cache đi, chưa thay đổi gì!"    │
  │                                                          │
  │  ĐẶC ĐIỂM:                                               │
  │  → Server CHO PHÉP truy cập resource                    │
  │  → NHƯNG điều kiện KHÔNG thỏa mãn                      │
  │  → Response KHÔNG CÓ body                                │
  │  → Tuy thuộc nhóm 3XX nhưng KHÔNG liên quan redirect   │
  │  → KHÔNG phải lỗi, chỉ báo "dùng cache!"               │
  │                                                          │
  │  CONDITIONAL REQUEST HEADERS:                             │
  │  ┌──────────────────────┬───────────────────────────┐    │
  │  │ If-Match             │ ETag phải khớp            │    │
  │  │ If-None-Match        │ ETag không được khớp      │    │
  │  │ If-Modified-Since    │ Modified sau thời điểm    │    │
  │  │ If-Unmodified-Since  │ Chưa modified sau TĐ     │    │
  │  │ If-Range             │ Dùng với range request    │    │
  │  └──────────────────────┴───────────────────────────┘    │
  │                                                          │
  │  → Giúp TĂNG HIỆU SUẤT: chỉ trả header, bỏ content   │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### 307 Temporary Redirect

```
307 TEMPORARY REDIRECT — GIỐNG 302 NHƯNG GIỮ METHOD:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  Client ── POST /api/data ──► Server                     │
  │         ◄── 307 Temporary Redirect ──                    │
  │             Location: /api/v2/data                       │
  │                                                          │
  │  Client ── POST /api/v2/data ──► Server (GIỮ POST!)    │
  │         ◄── 200 OK ──                                    │
  │                                                          │
  │  ĐẶC ĐIỂM:                                               │
  │  → Ý nghĩa GIỐNG 302 (temporary redirect)               │
  │  → NHƯNG tuân thủ chuẩn: KHÔNG đổi POST → GET          │
  │  → Browser PHẢI tiếp tục POST đến Location mới         │
  │                                                          │
  │  ⚠️ LƯU Ý VỀ 301/302 VÀ METHOD CHANGE:                  │
  │  → Tiêu chuẩn 301/302 CẤM đổi POST → GET              │
  │  → Nhưng thực tế hầu hết browser ĐỀU ĐỔI POST → GET   │
  │  → 307 ra đời để GIẢI QUYẾT vấn đề này                 │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

---

## 3. 4XX — Client Error

> **🎯 Các status code 4XX cho biết lỗi thuộc về phía CLIENT.**

### 400 Bad Request

```
400 BAD REQUEST — CÚ PHÁP SAI:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  → Request message có LỖI CÚ PHÁP                       │
  │  → Phải SỬA NỘI DUNG request rồi gửi lại               │
  │  → Browser xử lý tương tự 200 OK                        │
  │                                                          │
  │  VD: JSON body format sai, thiếu required field,        │
  │      invalid parameter, malformed URL                    │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### 401 Unauthorized

```
401 UNAUTHORIZED — CẦN XÁC THỰC:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  → Request cần thông tin XÁC THỰC HTTP                   │
  │    (BASIC authentication, DIGEST authentication)        │
  │  → Nếu đã gửi request trước đó: xác thực THẤT BẠI     │
  │  → Response PHẢI chứa header WWW-Authenticate            │
  │  → Browser sẽ hiển thị DIALOG XÁC THỰC khi nhận 401    │
  │                                                          │
  │  CÁC TRƯỜNG HỢP 401:                                     │
  │  ┌─────────┬──────────────────────────────────────┐      │
  │  │ 401.1   │ Đăng nhập thất bại                   │      │
  │  │ 401.2   │ Cấu hình server gây lỗi đăng nhập   │      │
  │  │ 401.3   │ Không được phép do ACL resource      │      │
  │  │ 401.4   │ Xác thực filter thất bại             │      │
  │  │ 401.5   │ Xác thực ISAPI/CGI thất bại          │      │
  │  │ 401.7   │ Bị từ chối bởi URL auth policy       │      │
  │  └─────────┴──────────────────────────────────────┘      │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### 403 Forbidden

```
403 FORBIDDEN — BỊ CẤM TRUY CẬP:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  → Quyền truy cập resource bị server TỪ CHỐI           │
  │  → Server KHÔNG bắt buộc phải nêu lý do chi tiết      │
  │  → Truy cập bị CẤM VĨNH VIỄN, không thể xác thực lại │
  │  → Liên quan chặt chẽ đến APPLICATION LOGIC             │
  │                                                          │
  │  SO SÁNH 401 vs 403:                                      │
  │  ┌──────────┬────────────────────┬──────────────────┐    │
  │  │          │      401           │      403         │    │
  │  ├──────────┼────────────────────┼──────────────────┤    │
  │  │ Nguyên   │ Chưa xác thực /   │ Đã xác thực     │    │
  │  │ nhân     │ xác thực sai      │ nhưng KHÔNG có   │    │
  │  │          │                    │ quyền truy cập   │    │
  │  ├──────────┼────────────────────┼──────────────────┤    │
  │  │ Giải     │ Đăng nhập /       │ Không có cách    │    │
  │  │ pháp     │ cung cấp token    │ (cần nâng quyền) │    │
  │  └──────────┴────────────────────┴──────────────────┘    │
  │                                                          │
  │  CÁC TRƯỜNG HỢP 403 (IIS):                               │
  │  403.1  - Quyền truy cập bị từ chối                     │
  │  403.2  - Quyền đọc bị cấm                              │
  │  403.4  - Yêu cầu SSL                                   │
  │  403.6  - IP bị từ chối                                  │
  │  403.7  - Yêu cầu client certificate                    │
  │  403.9  - Quá nhiều user                                 │
  │  403.14 - Directory listing bị cấm                      │
  │  403.16 - Client certificate không tin cậy              │
  │  403.17 - Client certificate hết hạn                    │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### 404 Not Found

```
404 NOT FOUND — KHÔNG TÌM THẤY RESOURCE:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  → Resource KHÔNG TÌM THẤY trên server                  │
  │  → Cũng có thể server TỪ CHỐI nhưng không nêu lý do   │
  │                                                          │
  │  CÁC TRƯỜNG HỢP 404:                                     │
  │  ┌─────────┬──────────────────────────────────────┐      │
  │  │ 404.0   │ Không tìm thấy file hoặc directory   │      │
  │  │ 404.1   │ Không truy cập được website trên port│      │
  │  │ 404.2   │ Web service locking policy chặn      │      │
  │  │ 404.3   │ MIME mapping policy chặn             │      │
  │  └─────────┴──────────────────────────────────────┘      │
  │                                                          │
  │  💡 Đây là status code "nổi tiếng" nhất!                │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### 405 Method Not Allowed

```
405 METHOD NOT ALLOWED — PHƯƠNG THỨC KHÔNG ĐƯỢC PHÉP:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  → Server NHẬN DIỆN method được yêu cầu                │
  │  → Nhưng CẤM sử dụng method đó cho resource này        │
  │  → GET và HEAD LUÔN LUÔN được cho phép                  │
  │                                                          │
  │  KIỂM TRA CÁC METHOD ĐƯỢC PHÉP:                          │
  │  Client gửi OPTIONS request (preflight check):           │
  │                                                          │
  │  Response header:                                         │
  │  Access-Control-Allow-Methods: GET,HEAD,PUT,PATCH,       │
  │                                POST,DELETE               │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

---

## 4. 5XX — Server Error

> **🎯 Các status code 5XX cho biết lỗi xảy ra ở phía SERVER.**

### 500 Internal Server Error

```
500 INTERNAL SERVER ERROR — LỖI NỘI BỘ SERVER:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  → Lỗi xảy ra ở SERVER khi xử lý request               │
  │  → Có thể do BUG trong web application                  │
  │  → Hoặc do SỰ CỐ TẠM THỜI                               │
  │                                                          │
  │  VD: exception chưa được catch, logic error,            │
  │      database connection failure                         │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### 502 Bad Gateway

```
502 BAD GATEWAY — GATEWAY/PROXY NHẬN RESPONSE KHÔNG HỢP LỆ:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  Client ──► Nginx (Gateway) ──► Backend Server           │
  │                    ▲                    │                │
  │                    │   Invalid Response │                │
  │                    └────────────────────┘                │
  │                         502 ◄──                          │
  │                                                          │
  │  → Server đóng vai trò GATEWAY hoặc PROXY               │
  │  → Nhận response KHÔNG HỢP LỆ từ upstream server       │
  │  → Lỗi NÀY KHÔNG phải client có thể sửa               │
  │  → Cần sửa ở WEB SERVER hoặc PROXY SERVER               │
  │                                                          │
  │  CÁC TRƯỜNG HỢP 502:                                     │
  │  ┌─────────┬──────────────────────────────────────┐      │
  │  │ 502.1   │ CGI application timeout              │      │
  │  │ 502.2   │ CGI application error                │      │
  │  └─────────┴──────────────────────────────────────┘      │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### 503 Service Unavailable

```
503 SERVICE UNAVAILABLE — SERVER TẠM THỜI KHÔNG KHẢ DỤNG:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  → Server TẠM THỜI bị quá tải hoặc đang bảo trì       │
  │  → KHÔNG THỂ xử lý request tại thời điểm này           │
  │  → Nên kèm header Retry-After để báo thời gian chờ    │
  │                                                          │
  │  USE CASES:                                               │
  │  ① Server ĐÓNG để bảo trì → chủ động trả 503          │
  │  ② Nginx đặt rate limit → vượt giới hạn → 503         │
  │  ③ Server quá tải do traffic đột biến                   │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### 504 Gateway Timeout

```
504 GATEWAY TIMEOUT — GATEWAY/PROXY TIMEOUT:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  Client ──► Nginx ──► Backend Server                     │
  │                          │                               │
  │                     (xử lý quá lâu...)                   │
  │                          │                               │
  │             Nginx: "Hết thời gian chờ!" → 504           │
  │                                                          │
  │  → Gateway/Proxy KHÔNG nhận được response               │
  │    trong THỜI GIAN QUY ĐỊNH                              │
  │  → Được thêm vào từ HTTP/1.1                             │
  │                                                          │
  │  USE CASE:                                                │
  │  → Code thực thi TIMEOUT hoặc xảy ra INFINITE LOOP     │
  │  → Backend server quá chậm hoặc không phản hồi         │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

---

## 5. So Sánh 302 vs 303 vs 307

> **🎯 Tại sao cùng là redirect nhưng có đến 3 status code?**

```
302 vs 303 vs 307 — SỰ KHÁC BIỆT:
═══════════════════════════════════════════════════════════════

  LỊCH SỬ:
  → 302 là status code từ HTTP/1.0
  → Trong HTTP/1.1, để LÀM RÕ hành vi 302, thêm 303 và 307

  ┌──────────┬────────────────────────────────────────────────┐
  │  Code    │  Hành vi                                       │
  ├──────────┼────────────────────────────────────────────────┤
  │   302    │  Temporary redirect (HTTP/1.0)                 │
  │          │  Tiêu chuẩn: KHÔNG đổi method                 │
  │          │  Thực tế: browser ĐỔI POST → GET ❌           │
  ├──────────┼────────────────────────────────────────────────┤
  │   303    │  RÕ RÀNG yêu cầu dùng GET                     │
  │          │  → POST request → redirect → GET request      │
  │          │  → Dùng khi MUỐN đổi method thành GET        │
  ├──────────┼────────────────────────────────────────────────┤
  │   307    │  Temporary redirect + GIỮ NGUYÊN method       │
  │          │  → POST request → redirect → POST request     │
  │          │  → Browser PHẢI giữ POST, không đổi GET      │
  └──────────┴────────────────────────────────────────────────┘

  VẤN ĐỀ CỦA 302:
  ┌──────────────────────────────────────────────────────────┐
  │  Tiêu chuẩn 301/302 CẤM đổi POST → GET                 │
  │  NHƯNG thực tế hầu hết browser vẫn ĐỔI!                │
  │                                                          │
  │  → 303 ra đời: "Đổi sang GET là CHÍNH THỨC"             │
  │  → 307 ra đời: "GIỮ NGUYÊN method, KHÔNG được đổi"     │
  └──────────────────────────────────────────────────────────┘

  TÓM TẮT:
  → Muốn redirect + ĐỔI sang GET     → dùng 303
  → Muốn redirect + GIỮ method cũ    → dùng 307
  → 302 = hành vi KHÔNG NHẤT QUÁN giữa các browser
```

---

## 6. Tóm Tắt & Câu Hỏi Phỏng Vấn

### Quick Reference

```
HTTP STATUS CODES — QUICK REFERENCE:
═══════════════════════════════════════════════════════════════

  2XX SUCCESS:
    200  OK                → Thành công, có body
    204  No Content        → Thành công, không body
    206  Partial Content   → Range request thành công

  3XX REDIRECTION:
    301  Moved Permanently → Redirect vĩnh viễn (SEO chuyển)
    302  Found             → Redirect tạm thời (SEO giữ)
    303  See Other         → Redirect + BẮT BUỘC dùng GET
    304  Not Modified      → Dùng cache, không có body
    307  Temporary Redirect→ Redirect tạm thời + GIỮ method

  4XX CLIENT ERROR:
    400  Bad Request       → Request sai cú pháp
    401  Unauthorized      → Cần xác thực
    403  Forbidden         → Có xác thực nhưng bị cấm
    404  Not Found         → Resource không tồn tại
    405  Method Not Allowed→ Method bị cấm cho resource này

  5XX SERVER ERROR:
    500  Internal Error    → Lỗi nội bộ server
    502  Bad Gateway       → Gateway nhận response lỗi
    503  Service Unavail.  → Server tạm thời không khả dụng
    504  Gateway Timeout   → Gateway timeout chờ upstream
```

### Câu Hỏi Phỏng Vấn Thường Gặp

**1. 301 và 302 khác nhau thế nào?**

> 301 = redirect VĨNH VIỄN: bookmark cập nhật, SEO chuyển index sang URL mới. 302 = redirect TẠM THỜI: bookmark giữ URL cũ, SEO giữ index URL cũ. 301 dùng khi đổi domain; 302 dùng khi redirect tạm (event page, login redirect).

**2. 302 vs 303 vs 307 khác nhau thế nào?**

> 302 (HTTP/1.0): tiêu chuẩn cấm đổi method nhưng browser vẫn đổi POST → GET. HTTP/1.1 thêm 303 (rõ ràng yêu cầu dùng GET) và 307 (bắt buộc giữ nguyên method). Muốn đổi GET → 303; muốn giữ method → 307.

**3. 401 và 403 khác nhau thế nào?**

> 401 = chưa xác thực hoặc xác thực sai (cần login/token). 403 = đã xác thực nhưng không có QUYỀN truy cập resource (cần nâng quyền). 401 có thể sửa bằng đăng nhập; 403 thì không.

**4. 304 Not Modified là gì? Có phải lỗi không?**

> KHÔNG phải lỗi. 304 báo client "dùng bản cache, resource chưa thay đổi." Xảy ra khi client gửi conditional request (If-None-Match, If-Modified-Since) và server xác nhận data chưa đổi. Response không có body → tăng hiệu suất.

**5. 502 và 504 khác nhau thế nào?**

> 502 Bad Gateway: gateway/proxy nhận response KHÔNG HỢP LỆ từ upstream. 504 Gateway Timeout: gateway/proxy KHÔNG nhận được response trong thời gian quy định. 502 = upstream trả lỗi; 504 = upstream không trả gì (timeout).

**6. Khi nào server trả 503?**

> Khi server TẠM THỜI không khả dụng: đang bảo trì (chủ động trả 503), quá tải, hoặc vượt rate limit (nginx). Nên kèm header Retry-After để client biết khi nào thử lại.

---

## Checklist Học Tập

- [ ] Biết 5 nhóm status code chính (1XX-5XX)
- [ ] Phân biệt 200 vs 204 vs 206
- [ ] Phân biệt 301 vs 302 (vĩnh viễn vs tạm thời)
- [ ] Hiểu tại sao có 303 và 307 bên cạnh 302
- [ ] Giải thích 304 Not Modified và conditional request headers
- [ ] Phân biệt 401 Unauthorized vs 403 Forbidden
- [ ] Phân biệt 502 Bad Gateway vs 504 Gateway Timeout
- [ ] Biết khi nào server trả 503 Service Unavailable

---

_Cập nhật lần cuối: Tháng 2, 2026_
