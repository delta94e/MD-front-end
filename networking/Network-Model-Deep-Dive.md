# Network Model — OSI & TCP/IP Deep Dive

> 📅 2026-02-11 · ⏱ 20 phút đọc
>
> Tài liệu chuyên sâu về mô hình mạng: OSI 7 tầng và TCP/IP 5 tầng.
> Bao gồm: chức năng từng tầng, so sánh, giao thức, thiết bị, peer-to-peer communication.
> Độ khó: ⭐️⭐️⭐️ | Chủ đề: Network Fundamentals

---

## Mục Lục

0. [Mô Hình OSI 7 Tầng](#0-mô-hình-osi-7-tầng)
1. [Mô Hình TCP/IP 5 Tầng](#1-mô-hình-tcpip-5-tầng)
2. [So Sánh OSI vs TCP/IP](#2-so-sánh-osi-vs-tcpip)
3. [Tóm Tắt & Câu Hỏi Phỏng Vấn](#3-tóm-tắt--câu-hỏi-phỏng-vấn)

---

## 0. Mô Hình OSI 7 Tầng

> **🎯 Học xong phần này, bạn sẽ biết:**
>
> - OSI 7 tầng là gì, do ai đề xuất
> - Chức năng chi tiết của từng tầng
> - Peer-to-peer communication hoạt động thế nào

### Tổng quan

```
OSI 7-LAYER MODEL — MÔ HÌNH THAM CHIẾU:
═══════════════════════════════════════════════════════════════

  ISO (International Organization for Standardization)
  đề xuất mô hình OSI (Open Systems Interconnection)
  để thúc đẩy ứng dụng mạng phổ biến rộng rãi.

  ┌──────────────────────────────────────────────────────────┐
  │  Tầng 7  │  Application Layer   │  Tầng Ứng Dụng       │
  ├──────────┼──────────────────────┼───────────────────────┤
  │  Tầng 6  │  Presentation Layer  │  Tầng Trình Bày      │
  ├──────────┼──────────────────────┼───────────────────────┤
  │  Tầng 5  │  Session Layer       │  Tầng Phiên           │
  ├──────────┼──────────────────────┼───────────────────────┤
  │  Tầng 4  │  Transport Layer     │  Tầng Giao Vận       │
  ├──────────┼──────────────────────┼───────────────────────┤
  │  Tầng 3  │  Network Layer       │  Tầng Mạng           │
  ├──────────┼──────────────────────┼───────────────────────┤
  │  Tầng 2  │  Data Link Layer     │  Tầng Liên Kết DL    │
  ├──────────┼──────────────────────┼───────────────────────┤
  │  Tầng 1  │  Physical Layer      │  Tầng Vật Lý         │
  └──────────┴──────────────────────┴───────────────────────┘

  GHI NHỚ (từ trên xuống):
  "All People Seem To Need Data Processing"
   A    P      S    T    N     D    P
```

### (1) Application Layer — Tầng Ứng Dụng (Tầng 7)

```
APPLICATION LAYER — GẦN NGƯỜI DÙNG NHẤT:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  CHỨC NĂNG:                                              │
  │  → Tầng GẦN NHẤT với người dùng trong mô hình OSI      │
  │  → Cung cấp GIAO DIỆN ỨNG DỤNG cho người dùng          │
  │  → Trực tiếp cung cấp các DỊCH VỤ MẠNG khác nhau      │
  │                                                          │
  │  CÁC GIAO THỨC PHỔ BIẾN:                                │
  │  ┌────────┬──────────────────────────────────────────┐   │
  │  │  HTTP  │ HyperText Transfer Protocol              │   │
  │  │        │ → Giao thức truyền siêu văn bản          │   │
  │  │        │ → Dùng khi thiết kế data API ở backend   │   │
  │  ├────────┼──────────────────────────────────────────┤   │
  │  │ HTTPS  │ HTTP + SSL/TLS encryption                │   │
  │  │        │ → Phiên bản bảo mật của HTTP             │   │
  │  ├────────┼──────────────────────────────────────────┤   │
  │  │  FTP   │ File Transfer Protocol                   │   │
  │  │        │ → Giao thức truyền tệp tin               │   │
  │  │        │ → Các trang tài nguyên (Bách Độ,         │   │
  │  │        │   Xunlei) dựa trên protocol này          │   │
  │  ├────────┼──────────────────────────────────────────┤   │
  │  │  SMTP  │ Simple Mail Transfer Protocol            │   │
  │  │        │ → Giao thức truyền email đơn giản        │   │
  │  │        │ → Dùng trong tính năng gửi mã OTP qua   │   │
  │  │        │   email để đăng nhập                     │   │
  │  ├────────┼──────────────────────────────────────────┤   │
  │  │  POP3  │ Post Office Protocol v3                  │   │
  │  │        │ → Giao thức nhận email                   │   │
  │  ├────────┼──────────────────────────────────────────┤   │
  │  │  DNS   │ Domain Name System                       │   │
  │  │        │ → Phân giải tên miền → IP                │   │
  │  ├────────┼──────────────────────────────────────────┤   │
  │  │ Telnet │ Terminal emulation protocol              │   │
  │  │        │ → Truy cập từ xa vào máy chủ            │   │
  │  └────────┴──────────────────────────────────────────┘   │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### (2) Presentation Layer — Tầng Trình Bày (Tầng 6)

```
PRESENTATION LAYER — MÃ HÓA & CHUYỂN ĐỔI:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  CHỨC NĂNG:                                              │
  │  → Cung cấp MÃ HÓA & CHUYỂN ĐỔI cho dữ liệu         │
  │    của tầng ứng dụng                                     │
  │  → Đảm bảo data từ hệ thống A có thể được NHẬN DIỆN   │
  │    bởi tầng ứng dụng của hệ thống B                     │
  │  → Chuyển đổi FORMAT nội bộ → FORMAT chuẩn truyền thông │
  │                                                          │
  │  3 CHỨC NĂNG CHÍNH:                                      │
  │  ┌──────────────────────────────────────────────────────┐│
  │  │ ① Data Format Conversion (Chuyển đổi định dạng)     ││
  │  │   → Chuyển internal format → standard format        ││
  │  │                                                      ││
  │  │ ② Data Compression (Nén dữ liệu)                    ││
  │  │   → Giảm kích thước data trước khi truyền           ││
  │  │                                                      ││
  │  │ ③ Data Encryption (Mã hóa dữ liệu)                  ││
  │  │   → Bảo mật data trong quá trình truyền             ││
  │  └──────────────────────────────────────────────────────┘│
  │                                                          │
  │  VÍ DỤ THỰC TẾ:                                          │
  │  → Base64 encoding/decoding thuộc tầng này              │
  │  → Khi truyền data, encode base64 → truyền →           │
  │    decode ở phía nhận                                    │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### (3) Session Layer — Tầng Phiên (Tầng 5)

```
SESSION LAYER — QUẢN LÝ PHIÊN GIAO TIẾP:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  CHỨC NĂNG:                                              │
  │  → THIẾT LẬP, QUẢN LÝ và KẾT THÚC các phiên           │
  │    giao tiếp giữa các thực thể tầng trình bày          │
  │                                                          │
  │  GIAO TIẾP GIỮA CÁC THIẾT BỊ:                          │
  │  ┌──────────┐                      ┌──────────┐         │
  │  │ Device A │  ◄── Session ──►    │ Device B │         │
  │  │  App 1   │   (Service Request   │  App 2   │         │
  │  └──────────┘    & Response)       └──────────┘         │
  │                                                          │
  │  → Giao tiếp = yêu cầu dịch vụ (request)               │
  │    + phản hồi (response) giữa ứng dụng trên            │
  │    các thiết bị KHÁC NHAU                                │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### (4) Transport Layer — Tầng Giao Vận (Tầng 4)

```
TRANSPORT LAYER — KẾT NỐI END-TO-END:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  CHỨC NĂNG:                                              │
  │  → Thiết lập liên kết END-TO-END giữa các host         │
  │  → Cung cấp dịch vụ truyền dữ liệu TIN CẬY            │
  │    và TRONG SUỐT cho giao thức tầng trên                │
  │  → Xử lý ERROR CONTROL và FLOW CONTROL                 │
  │  → Che giấu chi tiết truyền dữ liệu tầng dưới         │
  │    khỏi tầng trên                                       │
  │                                                          │
  │  2 GIAO THỨC CHÍNH:                                      │
  │  ┌──────────────────────────────────────────────────────┐│
  │  │  TCP (Transmission Control Protocol)                 ││
  │  │  → Connection-oriented, reliable                     ││
  │  │  → Đơn vị dữ liệu: SEGMENT (phân đoạn)             ││
  │  │                                                      ││
  │  │  UDP (User Datagram Protocol)                        ││
  │  │  → Connectionless, best-effort                       ││
  │  │  → Đơn vị dữ liệu: USER DATAGRAM                   ││
  │  └──────────────────────────────────────────────────────┘│
  │                                                          │
  │  💡 "END" ở đây chính là PORT NUMBER!                    │
  │  → End-to-end = Port-to-Port                            │
  │  → Mỗi ứng dụng được xác định bởi IP + Port           │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### (5) Network Layer — Tầng Mạng (Tầng 3)

```
NETWORK LAYER — ĐỊNH TUYẾN & ĐỊA CHỈ IP:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  CHỨC NĂNG:                                              │
  │  → Thiết lập KẾT NỐI giữa 2 node thông qua ADDRESSING │
  │  → Chọn ROUTE (đường đi) và SWITCHING NODES phù hợp    │
  │  → Đảm bảo gói tin được giao đến đúng đích             │
  │                                                          │
  │  GIAO THỨC: IP (Internet Protocol)                       │
  │  → IP là NỀN TẢNG của Internet                          │
  │                                                          │
  │  Sender ── [IP packet] ──► Router A ──► Router B ──►    │
  │            ──► Router C ──► Receiver                     │
  │                                                          │
  │  CÁC GIAO THỨC TẦNG MẠNG:                               │
  │  ┌──────┬─────────────────────────────────┐              │
  │  │  IP  │ Internet Protocol               │              │
  │  │ ICMP │ Internet Control Message Proto.  │              │
  │  │ IGMP │ Internet Group Management Proto. │              │
  │  │  RIP │ Routing Information Protocol     │              │
  │  └──────┴─────────────────────────────────┘              │
  │                                                          │
  │  SO SÁNH VỚI TRANSPORT LAYER:                            │
  │  → Network Layer: quy định ĐƯỜNG ĐI của gói tin        │
  │  → Transport Layer: quy định CÁCH THỨC truyền gói tin  │
  │                                                          │
  │  THIẾT BỊ: Router (bộ định tuyến)                       │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### (6) Data Link Layer — Tầng Liên Kết Dữ Liệu (Tầng 2)

```
DATA LINK LAYER — ĐÓNG KHUNG & KIỂM LỖI:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  CHỨC NĂNG:                                              │
  │  → Gộp BITS thành BYTES, BYTES thành FRAMES             │
  │  → Sử dụng địa chỉ tầng liên kết (MAC address)        │
  │    để truy cập phương tiện truyền                       │
  │  → Thực hiện KIỂM TRA LỖI (error detection)            │
  │                                                          │
  │  QUÁ TRÌNH ĐÓNG GÓI:                                    │
  │  Bit → Byte → Frame                                     │
  │  ┌──────────────────────────────────────────────┐        │
  │  │ Frame Header │  Data (IP Datagram)  │  FCS  │        │
  │  │  (MAC addr)  │                      │(check)│        │
  │  └──────────────────────────────────────────────┘        │
  │                                                          │
  │  THÔNG TIN KIỂM SOÁT TRONG FRAME:                       │
  │  ① Thông tin đồng bộ (synchronization)                  │
  │  ② Thông tin địa chỉ (MAC address)                     │
  │  ③ Thông tin kiểm soát lỗi (error control)             │
  │                                                          │
  │  SO SÁNH VỚI NETWORK LAYER:                              │
  │  → Network Layer: QUY HOẠCH đường truyền gói tin       │
  │  → Data Link Layer: chính là ĐƯỜNG TRUYỀN đó            │
  │    + thêm chức năng kiểm soát lỗi                       │
  │                                                          │
  │  GIAO THỨC: ARP, RARP, IEEE802.3, PPP, CSMA/CD         │
  │  THIẾT BỊ: Switch (bộ chuyển mạch), Bridge, NIC        │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### (7) Physical Layer — Tầng Vật Lý (Tầng 1)

```
PHYSICAL LAYER — TRUYỀN TÍN HIỆU THỰC TẾ:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  CHỨC NĂNG:                                              │
  │  → Truyền TÍN HIỆU thực tế qua phương tiện vật lý     │
  │  → Truyền BIT STREAM qua phương tiện vật lý            │
  │  → Định nghĩa MỨC ĐIỆN ÁP, TỐC ĐỘ, SƠ ĐỒ CÁP       │
  │                                                          │
  │  THIẾT BỊ TẦNG VẬT LÝ:                                  │
  │  ┌──────────────────────────────────────────────────────┐│
  │  │ Hub (bộ chia mạng)          │ Repeater (bộ lặp)     ││
  │  │ Modem (bộ điều giải)        │ Network Cable (cáp)   ││
  │  │ Twisted-pair (cáp xoắn đôi) │ Coaxial Cable (đồng) ││
  │  │ Fiber Optic (cáp quang)     │ Wireless (không dây)  ││
  │  └──────────────────────────────────────────────────────┘│
  │                                                          │
  │  Bit stream: 01101001 01001000 01100101...               │
  │  → Truyền qua: điện (twisted pair), ánh sáng (fiber),  │
  │    sóng vô tuyến (wireless)                              │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### Peer-to-Peer Communication — Truyền Thông Ngang Hàng

```
OSI PEER-TO-PEER COMMUNICATION:
═══════════════════════════════════════════════════════════════

  ĐẶC ĐIỂM TRUYỀN THÔNG CỦA OSI 7 TẦNG:

  Để gói tin truyền từ NGUỒN đến ĐÍCH, mỗi tầng trong
  mô hình OSI tại nguồn phải GIAO TIẾP với tầng TƯƠNG ỨNG
  tại đích. Đây gọi là PEER-TO-PEER COMMUNICATION.

  Source Device                          Destination Device
  ┌──────────────┐                      ┌──────────────┐
  │ Application  │◄═══ Protocol ═══════►│ Application  │
  ├──────────────┤                      ├──────────────┤
  │ Presentation │◄═══ Protocol ═══════►│ Presentation │
  ├──────────────┤                      ├──────────────┤
  │   Session    │◄═══ Protocol ═══════►│   Session    │
  ├──────────────┤                      ├──────────────┤
  │  Transport   │◄═══ Protocol ═══════►│  Transport   │
  ├──────────────┤                      ├──────────────┤
  │   Network    │◄═══ Protocol ═══════►│   Network    │
  ├──────────────┤                      ├──────────────┤
  │  Data Link   │◄═══ Protocol ═══════►│  Data Link   │
  ├──────────────┤                      ├──────────────┤
  │   Physical   │◄═══ Phương tiện ════►│   Physical   │
  └──────────────┘                      └──────────────┘

  → Mỗi tầng dùng PROTOCOL RIÊNG để giao tiếp
  → Tầng trên KHÔNG biết chi tiết của tầng dưới
  → Mỗi tầng CHỈ giao tiếp với tầng tương ứng ở bên kia
```

---

## 1. Mô Hình TCP/IP 5 Tầng

> **🎯 Học xong phần này, bạn sẽ biết:**
>
> - TCP/IP 5 tầng tương ứng thế nào với OSI 7 tầng
> - Giao thức và thiết bị ở mỗi tầng
> - Quá trình đóng gói/giải đóng gói dữ liệu

### Tổng quan — OSI → TCP/IP

```
OSI 7 TẦNG → TCP/IP 5 TẦNG:
═══════════════════════════════════════════════════════════════

  OSI 7 Tầng            TCP/IP 5 Tầng        Giao thức
  ┌──────────────┐      ┌──────────────┐     ┌─────────────────┐
  │ Application  │      │              │     │ HTTP, FTP, SMTP │
  ├──────────────┤  ──► │ Application  │     │ DNS, Telnet,    │
  │ Presentation │      │              │     │ TFTP, POP3      │
  ├──────────────┤      └──────────────┘     └─────────────────┘
  │   Session    │              │
  ├──────────────┤      ┌──────────────┐     ┌─────────────────┐
  │  Transport   │  ──► │  Transport   │     │ TCP, UDP        │
  ├──────────────┤      └──────────────┘     └─────────────────┘
  │   Network    │  ──► ┌──────────────┐     ┌─────────────────┐
  ├──────────────┤      │   Network    │     │ IP, ICMP, RIP,  │
  │  Data Link   │  ──► ├──────────────┤     │ IGMP            │
  ├──────────────┤      │  Data Link   │     ├─────────────────┤
  │   Physical   │  ──► ├──────────────┤     │ ARP, RARP,      │
  └──────────────┘      │   Physical   │     │ IEEE802.3, PPP  │
                        └──────────────┘     └─────────────────┘

  TCP/IP GỘP 3 tầng trên (Application + Presentation + Session)
  thành 1 tầng Application → MÔ HÌNH ĐƠN GIẢN HƠN!
```

### Các tầng chi tiết

```
TCP/IP 5 TẦNG — CHI TIẾT:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │  TẦNG 5: APPLICATION LAYER (Tầng Ứng Dụng)              │
  │  ──────────────────────────────────────────              │
  │  → Trực tiếp cung cấp dịch vụ cho ứng dụng             │
  │  → Định nghĩa quy tắc giao tiếp giữa các tiến trình   │
  │  → Mỗi ứng dụng khác nhau → protocol khác nhau         │
  │     HTTP (web), FTP (file), SMTP (email), DNS (lookup)  │
  ├──────────────────────────────────────────────────────────┤
  │  TẦNG 4: TRANSPORT LAYER (Tầng Giao Vận)                │
  │  ──────────────────────────────────────────              │
  │  → Cung cấp dịch vụ giao tiếp giữa 2 TIẾN TRÌNH       │
  │    trên 2 host khác nhau                                │
  │  → TCP: connection-oriented, reliable (segment)         │
  │  → UDP: connectionless, best-effort (datagram)          │
  ├──────────────────────────────────────────────────────────┤
  │  TẦNG 3: NETWORK LAYER (Tầng Mạng / Internet Layer)     │
  │  ──────────────────────────────────────────              │
  │  → Cung cấp dịch vụ giao tiếp giữa 2 HOST              │
  │  → Chọn route phù hợp để truyền data đến đích          │
  │  → Giao thức: IP, ICMP, IGMP, RIP                      │
  ├──────────────────────────────────────────────────────────┤
  │  TẦNG 2: DATA LINK LAYER (Tầng Liên Kết Dữ Liệu)       │
  │  ──────────────────────────────────────────              │
  │  → Đóng gói IP datagram thành FRAME                     │
  │  → Truyền frame giữa 2 node LIỀN KỀ trong link         │
  │  → Mỗi frame chứa: data + control info                 │
  │    (sync, address, error control)                       │
  ├──────────────────────────────────────────────────────────┤
  │  TẦNG 1: PHYSICAL LAYER (Tầng Vật Lý)                   │
  │  ──────────────────────────────────────────              │
  │  → Đảm bảo data được truyền trên phương tiện vật lý    │
  │  → Cung cấp môi trường truyền dẫn tin cậy              │
  │  → Phương tiện: cáp xoắn đôi, cáp quang, sóng radio   │
  └──────────────────────────────────────────────────────────┘
```

### Thiết bị ở mỗi tầng

```
THIẾT BỊ HOẠT ĐỘNG Ở MỖI TẦNG:
═══════════════════════════════════════════════════════════════

  ┌──────────────────┬──────────────────────────────────────┐
  │      Tầng        │         Thiết bị                     │
  ├──────────────────┼──────────────────────────────────────┤
  │ Application      │ Application Gateway                  │
  │                  │ (Cổng ứng dụng)                      │
  ├──────────────────┼──────────────────────────────────────┤
  │ Transport        │ Layer-4 Switch                       │
  │                  │ Layer-4 Router                        │
  ├──────────────────┼──────────────────────────────────────┤
  │ Network          │ Router (Bộ định tuyến)               │
  │                  │ Layer-3 Switch                        │
  ├──────────────────┼──────────────────────────────────────┤
  │ Data Link        │ Bridge (Cầu nối)                     │
  │                  │ Ethernet Switch (Bộ chuyển mạch)     │
  │                  │ NIC (Card mạng)                       │
  ├──────────────────┼──────────────────────────────────────┤
  │ Physical         │ Repeater (Bộ lặp)                    │
  │                  │ Hub (Bộ chia mạng)                    │
  │                  │ Twisted-pair Cable (Cáp xoắn đôi)   │
  └──────────────────┴──────────────────────────────────────┘

  💡 Switch thông thường = Data Link Layer
  💡 Router thông thường = Network Layer
```

### Quá trình đóng gói dữ liệu (Data Encapsulation)

```
TCP/IP DATA ENCAPSULATION — ĐÓNG GÓI TỪ TRÊN XUỐNG:
═══════════════════════════════════════════════════════════════

  GỬI DỮ LIỆU (Sender → mỗi tầng THÊM header):

  Application Layer:
  ┌──────────────────────────────┐
  │         HTTP Data            │
  └──────────────────────────────┘
              │ thêm TCP header
              ▼
  Transport Layer:
  ┌──────────┬───────────────────┐
  │TCP Header│     HTTP Data     │
  └──────────┴───────────────────┘
              │ thêm IP header
              ▼
  Network Layer:
  ┌─────────┬──────────┬─────────────────┐
  │IP Header│TCP Header│    HTTP Data    │
  └─────────┴──────────┴─────────────────┘
              │ thêm Ethernet header
              ▼
  Data Link Layer:
  ┌──────────┬─────────┬──────────┬──────────────┐
  │Eth Header│IP Header│TCP Header│  HTTP Data   │
  └──────────┴─────────┴──────────┴──────────────┘
              │ truyền qua phương tiện vật lý
              ▼
  Physical Layer:
  → Tín hiệu điện/quang/sóng radio

  ═══════════════════════════════════════════════════════════

  NHẬN DỮ LIỆU (Receiver → mỗi tầng BỎ header):

  Physical → Data Link → Network → Transport → Application
  (Quy trình NGƯỢC LẠI: bóc từng lớp header từ dưới lên)
```

### TCP/IP Peer-to-Peer Communication

```
TCP/IP PEER-TO-PEER COMMUNICATION:
═══════════════════════════════════════════════════════════════

  TƯƠNG TỰ OSI, TCP/IP cũng dùng truyền thông ngang hàng:

  Host A                                     Host B
  ┌──────────────┐                          ┌──────────────┐
  │ Application  │◄══ HTTP/FTP/SMTP ═══════►│ Application  │
  ├──────────────┤                          ├──────────────┤
  │  Transport   │◄══ TCP/UDP ═════════════►│  Transport   │
  ├──────────────┤                          ├──────────────┤
  │   Network    │◄══ IP ══════════════════►│   Network    │
  ├──────────────┤                          ├──────────────┤
  │  Data Link   │◄══ Ethernet ════════════►│  Data Link   │
  ├──────────────┤                          ├──────────────┤
  │   Physical   │◄══ Phương tiện ═════════►│   Physical   │
  └──────────────┘                          └──────────────┘

  → Mỗi tầng THÊM header khi gửi, BỎ header khi nhận
  → Tầng N ở Host A chỉ "nói chuyện" với tầng N ở Host B
```

---

## 2. So Sánh OSI vs TCP/IP

> **🎯 Tóm tắt nhanh sự khác biệt giữa 2 mô hình**

```
SO SÁNH OSI 7 TẦNG vs TCP/IP 5 TẦNG:
═══════════════════════════════════════════════════════════════

  ┌────────────────┬──────────────────┬──────────────────────┐
  │    Tiêu chí    │   OSI 7 Tầng     │   TCP/IP 5 Tầng      │
  ├────────────────┼──────────────────┼──────────────────────┤
  │ Số tầng        │ 7 tầng           │ 5 tầng               │
  ├────────────────┼──────────────────┼──────────────────────┤
  │ Tầng trên      │ App + Present.   │ Gộp thành 1 tầng    │
  │                │ + Session        │ Application          │
  ├────────────────┼──────────────────┼──────────────────────┤
  │ Tính chất      │ Mô hình LÝ      │ Mô hình THỰC TẾ     │
  │                │ THUYẾT tham chiếu│ được sử dụng         │
  ├────────────────┼──────────────────┼──────────────────────┤
  │ Ưu điểm        │ Rõ ràng, chi tiết│ Đơn giản, thực tế   │
  ├────────────────┼──────────────────┼──────────────────────┤
  │ Nhược điểm     │ Phức tạp, ít    │ Ranh giới tầng trên  │
  │                │ dùng thực tế     │ không rõ ràng        │
  ├────────────────┼──────────────────┼──────────────────────┤
  │ Giao thức      │ Mỗi tầng riêng  │ Giao thức tầng trên  │
  │                │ biệt             │ gộp chung            │
  ├────────────────┼──────────────────┼──────────────────────┤
  │ Sử dụng        │ Giáo dục,       │ Internet thực tế     │
  │                │ học thuật        │ (production)         │
  └────────────────┴──────────────────┴──────────────────────┘

  💡 OSI = mô hình LÝ TƯỞNG để HỌC
  💡 TCP/IP = mô hình THỰC TẾ đang CHẠY trên Internet
```

---

## 3. Tóm Tắt & Câu Hỏi Phỏng Vấn

### Quick Reference

```
OSI 7 TẦNG (top → bottom):
  7. Application    → HTTP, FTP, SMTP, DNS
  6. Presentation   → Base64, SSL/TLS, JPEG, MPEG
  5. Session        → Quản lý phiên giao tiếp
  4. Transport      → TCP, UDP (port number)
  3. Network        → IP, ICMP, Router
  2. Data Link      → MAC, Switch, Frame
  1. Physical       → Hub, Cable, Bit stream

TCP/IP 5 TẦNG:
  5. Application    → HTTP, FTP, SMTP, DNS, Telnet
  4. Transport      → TCP, UDP
  3. Network        → IP, ICMP, RIP, IGMP
  2. Data Link      → ARP, Ethernet, PPP
  1. Physical       → Twisted-pair, Fiber, Wireless
```

### Câu Hỏi Phỏng Vấn Thường Gặp

**1. OSI 7 tầng là gì? Kể tên từng tầng?**

> OSI (Open Systems Interconnection) là mô hình tham chiếu 7 tầng do ISO đề xuất. Từ trên xuống: Application, Presentation, Session, Transport, Network, Data Link, Physical. Mỗi tầng có chức năng riêng biệt và giao tiếp peer-to-peer với tầng tương ứng ở phía bên kia.

**2. TCP/IP khác OSI thế nào?**

> TCP/IP gộp 3 tầng trên (Application + Presentation + Session) thành 1 tầng Application → chỉ còn 5 tầng. OSI là mô hình lý thuyết; TCP/IP là mô hình thực tế đang chạy trên Internet.

**3. Tầng Network và tầng Transport khác nhau thế nào?**

> Network Layer (IP) quy định ĐƯỜNG ĐI của gói tin (routing), cung cấp giao tiếp host-to-host. Transport Layer (TCP/UDP) quy định CÁCH THỨC truyền gói tin, cung cấp giao tiếp process-to-process (qua port number).

**4. Tầng Data Link và tầng Network khác nhau thế nào?**

> Network Layer quy hoạch đường truyền (routing), Data Link Layer chính là đường truyền đó + thêm error control. Network Layer dùng IP address, Data Link Layer dùng MAC address.

**5. Dữ liệu được đóng gói thế nào khi truyền qua mạng?**

> Mỗi tầng THÊM header khi gửi (encapsulation): Application data → thêm TCP header = Segment → thêm IP header = Packet → thêm Ethernet header = Frame → truyền dưới dạng bit stream. Phía nhận bóc ngược lại (decapsulation).

**6. Switch và Router hoạt động ở tầng nào?**

> Switch thông thường hoạt động ở tầng Data Link (Layer 2), dùng MAC address. Router hoạt động ở tầng Network (Layer 3), dùng IP address để routing.

---

## Checklist Học Tập

- [ ] Kể được 7 tầng OSI từ trên xuống dưới
- [ ] Biết chức năng chính của từng tầng OSI
- [ ] Nêu được các giao thức phổ biến ở mỗi tầng
- [ ] Hiểu TCP/IP 5 tầng tương ứng thế nào với OSI
- [ ] Giải thích được peer-to-peer communication
- [ ] Mô tả được quá trình đóng gói/giải đóng gói dữ liệu
- [ ] Biết thiết bị nào hoạt động ở tầng nào
- [ ] Phân biệt Network Layer vs Transport Layer vs Data Link Layer

---

_Cập nhật lần cuối: Tháng 2, 2026_
