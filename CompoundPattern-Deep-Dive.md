# Compound Pattern — Deep Dive: Tự Viết Tay Từ Số 0

> **Tài liệu học tập chuyên sâu — Hoàn toàn bằng Tiếng Việt**
> Tự viết tay mọi thứ, không phụ thuộc thư viện — Giải thích cực kỳ chi tiết kèm sơ đồ

---

## Mục Lục

```
§1.  Compound Pattern là gì? — Tổng quan & Ví dụ thực tế
§2.  Tại sao cần Compound Pattern? — So sánh với cách thông thường
§3.  Cách 1: Context API — Triển khai chi tiết từng bước
§4.  Static Properties — Gắn sub-component vào Parent
§5.  Cách 2: React.Children.map & cloneElement
§6.  So sánh Context API vs Children.map — Ưu nhược điểm
§7.  Ví dụ thực chiến: Tự viết Accordion, Tabs, Select
§8.  Best Practices, Tối ưu & Phỏng vấn Q&A
```

---

## §1. Compound Pattern Là Gì? — Tổng Quan & Ví Dụ Thực Tế

```
═══════════════════════════════════════════════════════════════
  COMPOUND PATTERN = NHÓM COMPONENTS LÀM VIỆC CÙNG NHAU!
═══════════════════════════════════════════════════════════════

  ĐỊNH NGHĨA:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  Compound = "Hợp chất" / "Kết hợp"                   │
  │                                                        │
  │  Compound Pattern = Pattern mà NHIỀU components       │
  │  KẾT HỢP với nhau để hoàn thành MỘT nhiệm vụ!     │
  │                                                        │
  │  → Các component CHIA SẺ state chung!                  │
  │  → Các component CHIA SẺ logic chung!                  │
  │  → KHÔNG THỂ hoạt động độc lập!                       │
  │  → Giống như bộ phận cơ thể: tay + chân + đầu       │
  │    = 1 con người hoàn chỉnh!                          │
  │                                                        │
  └────────────────────────────────────────────────────────┘


  VÍ DỤ THỰC TẾ TRONG ĐỜI SỐNG:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  ① HTML <select> + <option> = Compound!                │
  │  ┌─────────────────────────────────┐                   │
  │  │  <select>           ← Parent   │                   │
  │  │    <option>Hà Nội</option>     │ ← Children        │
  │  │    <option>TP.HCM</option>     │                   │
  │  │    <option>Đà Nẵng</option>   │                   │
  │  │  </select>                      │                   │
  │  └─────────────────────────────────┘                   │
  │  → <select> quản lý STATE (giá trị được chọn!)      │
  │  → <option> thể hiện TỪNG LỰA CHỌN!                 │
  │  → <option> KHÔNG dùng được nếu thiếu <select>!     │
  │                                                        │
  │  ② HTML <table> = Compound!                            │
  │  <table>  →  <thead>  →  <tr>  →  <th>               │
  │           →  <tbody>  →  <tr>  →  <td>               │
  │  → Tất cả phối hợp tạo 1 bảng hoàn chỉnh!         │
  │                                                        │
  │  ③ Trong React:                                         │
  │  <Menu>                           ← Parent             │
  │    <Menu.Toggle />                ← Nút bấm           │
  │    <Menu.List>                    ← Danh sách          │
  │      <Menu.Item>Edit</Menu.Item>  ← Từng mục         │
  │      <Menu.Item>Delete</Menu.Item>                     │
  │    </Menu.List>                                         │
  │  </Menu>                                                │
  │                                                        │
  └────────────────────────────────────────────────────────┘


  SƠ ĐỒ KIẾN TRÚC COMPOUND PATTERN:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │           ┌──────────────────────┐                     │
  │           │   PARENT COMPONENT   │                     │
  │           │   (FlyOut)           │                     │
  │           │                      │                     │
  │           │   state: { open }    │ ← Quản lý STATE   │
  │           │   toggle()           │ ← Cung cấp LOGIC  │
  │           │                      │                     │
  │           └──────┬───────────────┘                     │
  │                  │                                      │
  │         CHIA SẺ state qua Context                      │
  │                  │                                      │
  │     ┌────────────┼────────────────┐                    │
  │     │            │                │                    │
  │     ▼            ▼                ▼                    │
  │  ┌──────┐   ┌─────────┐   ┌──────────┐               │
  │  │Toggle│   │  List    │   │  Item    │               │
  │  │      │   │          │   │          │               │
  │  │Đọc:  │   │Đọc:     │   │(Tự do   │               │
  │  │open  │   │open      │   │ render)  │               │
  │  │toggle│   │          │   │          │               │
  │  └──────┘   └─────────┘   └──────────┘               │
  │                                                        │
  │  💡 Parent GIỮ state → Children ĐỌC state!           │
  │  → Không component nào quản lý state RIÊNG!            │
  │  → State CHIA SẺ = hoạt động ĐỒNG BỘ!               │
  │                                                        │
  └────────────────────────────────────────────────────────┘


  CÁC THƯ VIỆN NỔI TIẾNG DÙNG COMPOUND PATTERN:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  ┌──────────────┬──────────────────────────────────┐   │
  │  │ Thư viện     │ Ví dụ Compound Pattern           │   │
  │  ├──────────────┼──────────────────────────────────┤   │
  │  │ Semantic UI  │ <Dropdown>                       │   │
  │  │              │   <Dropdown.Menu>                 │   │
  │  │              │     <Dropdown.Item />             │   │
  │  │              │   </Dropdown.Menu>                │   │
  │  │              │ </Dropdown>                       │   │
  │  ├──────────────┼──────────────────────────────────┤   │
  │  │ Radix UI     │ <Select.Root>                    │   │
  │  │              │   <Select.Trigger />              │   │
  │  │              │   <Select.Content>                │   │
  │  │              │     <Select.Item />               │   │
  │  │              │   </Select.Content>               │   │
  │  │              │ </Select.Root>                    │   │
  │  ├──────────────┼──────────────────────────────────┤   │
  │  │ Headless UI  │ <Menu>                           │   │
  │  │              │   <Menu.Button />                 │   │
  │  │              │   <Menu.Items>                    │   │
  │  │              │     <Menu.Item />                 │   │
  │  │              │   </Menu.Items>                   │   │
  │  │              │ </Menu>                           │   │
  │  ├──────────────┼──────────────────────────────────┤   │
  │  │ Reach UI     │ <Tabs>                           │   │
  │  │              │   <TabList>                       │   │
  │  │              │     <Tab />                       │   │
  │  │              │   </TabList>                      │   │
  │  │              │   <TabPanels>                     │   │
  │  │              │     <TabPanel />                  │   │
  │  │              │   </TabPanels>                    │   │
  │  │              │ </Tabs>                           │   │
  │  └──────────────┴──────────────────────────────────┘   │
  │                                                        │
  │  💡 BẠN THẤY ĐÚNG KHÔNG?                             │
  │  Mọi thư viện UI đều dùng pattern này!                │
  │  → Parent.Child cú pháp = DẤU HIỆU Compound!        │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §2. Tại Sao Cần Compound Pattern? — So Sánh Với Cách Thông Thường

```
═══════════════════════════════════════════════════════════════
  KHÔNG CÓ COMPOUND PATTERN → CODE RẤT XẤU!
═══════════════════════════════════════════════════════════════


  ❌ CÁCH 1: PROPS DRILLING — Truyền mọi thứ qua props!
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  // Cách TỆ: Component "đồ sộ" với HÀNG TẤN props!  │
  │                                                        │
  │  <FlyOut                                                │
  │    isOpen={isOpen}                                      │
  │    onToggle={handleToggle}                              │
  │    toggleIcon={<Icon />}                                │
  │    toggleLabel="Options"                                │
  │    listItems={[                                         │
  │      { label: "Edit", onClick: handleEdit },            │
  │      { label: "Delete", onClick: handleDelete },        │
  │    ]}                                                    │
  │    listClassName="menu-list"                            │
  │    itemClassName="menu-item"                            │
  │    showDivider={true}                                    │
  │    position="bottom-right"                              │
  │    closeOnClickOutside={true}                           │
  │    animation="fade"                                     │
  │  />                                                      │
  │                                                        │
  │  VẤN ĐỀ:                                               │
  │  → 12+ props! KINH KHỦNG!                              │
  │  → Muốn thêm feature mới? → Thêm PROPS!              │
  │  → Props list ngày càng DÀI RA!                        │
  │  → Không LINH HOẠT về layout!                          │
  │  → Khó đọc, khó bảo trì!                             │
  │                                                        │
  └────────────────────────────────────────────────────────┘


  ✅ CÁCH 2: COMPOUND PATTERN — Sạch & Linh hoạt!
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  // Cách SẠCH: Compound Pattern!                       │
  │                                                        │
  │  <FlyOut>                                               │
  │    <FlyOut.Toggle />                                    │
  │    <FlyOut.List>                                         │
  │      <FlyOut.Item>Edit</FlyOut.Item>                    │
  │      <FlyOut.Item>Delete</FlyOut.Item>                  │
  │    </FlyOut.List>                                        │
  │  </FlyOut>                                               │
  │                                                        │
  │  ƯU ĐIỂM:                                               │
  │  → KHÔNG cần props phức tạp!                            │
  │  → State tự quản lý BÊN TRONG!                         │
  │  → Parent & Children tự giao tiếp!                     │
  │  → Muốn thay đổi layout? → Di chuyển children!       │
  │  → Muốn thêm feature? → Thêm sub-component!          │
  │  → DỄ ĐỌC, DỄ HIỂU!                                 │
  │                                                        │
  └────────────────────────────────────────────────────────┘


  SO SÁNH TRỰC QUAN:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  ┌────────────────┬─────────────┬─────────────────┐    │
  │  │                │ Props Hell  │ Compound Pattern │    │
  │  ├────────────────┼─────────────┼─────────────────┤    │
  │  │ Số props      │ 10-20+      │ 0-2             │    │
  │  │ Đọc hiểu     │ Khó        │ Dễ!             │    │
  │  │ Linh hoạt    │ Thấp       │ Rất cao!        │    │
  │  │ Thêm feature  │ Thêm props │ Thêm component  │    │
  │  │ Tái sử dụng │ Khó        │ Dễ!             │    │
  │  │ Layout control│ Cứng nhắc  │ Tự do!          │    │
  │  │ State quản lý│ Bên ngoài  │ Tự quản lý!     │    │
  │  │ Import       │ 1 component │ 1 import đủ!    │    │
  │  └────────────────┴─────────────┴─────────────────┘    │
  │                                                        │
  │  💡 Compound = GIẢM props + TĂNG linh hoạt!          │
  │                                                        │
  └────────────────────────────────────────────────────────┘


  KHI NÀO DÙNG COMPOUND PATTERN?
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  ✅ NÊN DÙNG khi:                                     │
  │  → Nhóm components CHIA SẺ STATE (open/close/active) │
  │  → UI có cấu trúc Parent → Children rõ ràng          │
  │  → Cần LINH HOẠT về cách bố trí children!            │
  │  → Xây dựng component library!                         │
  │  → Component có nhiều sub-parts (toggle, list, item)  │
  │                                                        │
  │  ❌ KHÔNG CẦN khi:                                     │
  │  → Component đơn giản, ít props!                       │
  │  → Không có shared state giữa children!                │
  │  → Component không có sub-parts!                       │
  │                                                        │
  │  VÍ DỤ COMPONENTS phù hợp:                              │
  │  → Dropdown / Select / Menu                             │
  │  → Accordion / Collapse                                 │
  │  → Tabs / Tab Panel                                     │
  │  → Modal / Dialog                                       │
  │  → Tooltip / Popover                                    │
  │  → Breadcrumb                                           │
  │  → Form Group (Label + Input + Error)                   │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §3. Cách 1: Context API — Triển Khai Chi Tiết Từng Bước

```
═══════════════════════════════════════════════════════════════
  CONTEXT API = CÁCH PHỔ BIẾN NHẤT ĐỂ LÀM COMPOUND!
═══════════════════════════════════════════════════════════════


  SƠ ĐỒ HOẠT ĐỘNG:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │        ┌──────────────────────────┐                    │
  │        │   FlyOut (Parent)        │                    │
  │        │                          │                    │
  │        │   state = { open: false }│                    │
  │        │   toggle = setState      │                    │
  │        │                          │                    │
  │        │   ┌──────────────────┐   │                    │
  │        │   │ Context.Provider │   │                    │
  │        │   │ value={{ open,   │   │                    │
  │        │   │   toggle }}      │   │                    │
  │        │   └────────┬─────────┘   │                    │
  │        └────────────┼─────────────┘                    │
  │                     │                                   │
  │          Context TRUYỀN xuống!                          │
  │                     │                                   │
  │        ┌────────────┼──────────────┐                   │
  │        │            │              │                   │
  │        ▼            ▼              ▼                   │
  │   ┌────────┐  ┌──────────┐  ┌──────────┐             │
  │   │ Toggle │  │   List   │  │   Item   │             │
  │   │        │  │          │  │          │             │
  │   │useCtx()│  │useCtx()  │  │(nhận qua │             │
  │   │→ open  │  │→ open    │  │ children)│             │
  │   │→ toggle│  │(show/hide│  │          │             │
  │   │        │  │ list)    │  │          │             │
  │   └────────┘  └──────────┘  └──────────┘             │
  │                                                        │
  │  ❓ useContext() = CHÌA KHÓA!                          │
  │  → Bất kỳ child nào gọi useContext(FlyOutContext)     │
  │    đều NHẬN ĐƯỢC { open, toggle }!                    │
  │  → Không cần truyền qua props!                         │
  │  → Không giới hạn độ SÂU (nesting)!                  │
  │                                                        │
  └────────────────────────────────────────────────────────┘


  ═══ BƯỚC 1: TẠO CONTEXT + PARENT COMPONENT ═══

  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  // FlyOut.jsx — Bước 1: Tạo Context + Parent         │
  │                                                        │
  │  import { createContext, useState, useContext } from    │
  │    'react';                                             │
  │                                                        │
  │  // ① Tạo Context — "kênh liên lạc" giữa components! │
  │  const FlyOutContext = createContext();                  │
  │                                                        │
  │  💡 createContext() tạo 1 "kênh radio"!               │
  │  → Provider = "đài phát sóng" (gửi data!)            │
  │  → useContext = "radio receiver" (nhận data!)         │
  │                                                        │
  │                                                        │
  │  // ② Tạo Parent component — quản lý STATE!            │
  │  function FlyOut({ children }) {                        │
  │    // State dùng CHUNG cho tất cả children!            │
  │    const [open, setOpen] = useState(false);             │
  │                                                        │
  │    // Hàm toggle — children sẽ gọi hàm này!          │
  │    const toggle = () => setOpen(prev => !prev);        │
  │                                                        │
  │    return (                                              │
  │      // Provider = "phát sóng" { open, toggle }!       │
  │      <FlyOutContext.Provider value={{ open, toggle }}>  │
  │        {children}                                       │
  │      </FlyOutContext.Provider>                           │
  │    );                                                    │
  │  }                                                       │
  │                                                        │
  │  GIẢI THÍCH TỪNG DÒNG:                                  │
  │  ┌──────────────────────────────────────────────┐       │
  │  │ createContext()                               │       │
  │  │ → Tạo "kênh" để Parent gửi data cho Children│       │
  │  │                                               │       │
  │  │ useState(false)                               │       │
  │  │ → open = menu đang mở hay đóng?             │       │
  │  │ → false = ĐÓNG ban đầu!                     │       │
  │  │                                               │       │
  │  │ toggle = () => setOpen(prev => !prev)         │       │
  │  │ → Hàm ĐẢO TRẠNG THÁI: true↔false!          │       │
  │  │ → Dùng callback form để TRÁNH stale closure! │       │
  │  │                                               │       │
  │  │ <Context.Provider value={{ open, toggle }}>   │       │
  │  │ → "Phát sóng" object { open, toggle }        │       │
  │  │ → MỌI children bên trong đều nhận được!     │       │
  │  │                                               │       │
  │  │ {children}                                    │       │
  │  │ → Render BẤT KỲ children nào được truyền!   │       │
  │  │ → Đây là điểm LINH HOẠT của Compound!      │       │
  │  └──────────────────────────────────────────────┘       │
  │                                                        │
  └────────────────────────────────────────────────────────┘


  ═══ BƯỚC 2: TẠO TOGGLE COMPONENT ═══

  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  // Toggle — nút bấm mở/đóng menu!                    │
  │                                                        │
  │  function Toggle() {                                     │
  │    // ① "Bắt sóng" từ Context Provider!               │
  │    const { open, toggle } = useContext(FlyOutContext);   │
  │                                                        │
  │    return (                                              │
  │      <div                                                │
  │        className="flyout-toggle"                         │
  │        onClick={toggle}    // ② Click → đảo trạng thái│
  │        aria-expanded={open} // ③ Accessibility!        │
  │      >                                                   │
  │        <span>{open ? '▲' : '▼'}</span>                 │
  │      </div>                                              │
  │    );                                                    │
  │  }                                                       │
  │                                                        │
  │  LUỒNG HOẠT ĐỘNG khi user CLICK:                       │
  │  ┌──────────────────────────────────────────────┐       │
  │  │                                               │       │
  │  │  User click Toggle                            │       │
  │  │       │                                       │       │
  │  │       ▼                                       │       │
  │  │  toggle() được gọi                           │       │
  │  │       │                                       │       │
  │  │       ▼                                       │       │
  │  │  setOpen(prev => !prev) trong Parent         │       │
  │  │       │ (false → true)                       │       │
  │  │       ▼                                       │       │
  │  │  Context Provider value thay đổi!            │       │
  │  │       │ { open: TRUE, toggle }               │       │
  │  │       ▼                                       │       │
  │  │  TẤT CẢ consumers RE-RENDER!                 │       │
  │  │       │                                       │       │
  │  │       ├──→ Toggle: hiển thị ▲ (thay vì ▼)   │       │
  │  │       └──→ List: HIỆN ra! (vì open = true)  │       │
  │  │                                               │       │
  │  └──────────────────────────────────────────────┘       │
  │                                                        │
  └────────────────────────────────────────────────────────┘


  ═══ BƯỚC 3: TẠO LIST + ITEM COMPONENT ═══

  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  // List — hiện/ẩn danh sách dựa trên "open"!        │
  │                                                        │
  │  function List({ children }) {                          │
  │    const { open } = useContext(FlyOutContext);           │
  │                                                        │
  │    // open = true → render children!                    │
  │    // open = false → render NOTHING (null)!             │
  │    return open ? (                                       │
  │      <ul className="flyout-list">                       │
  │        {children}                                        │
  │      </ul>                                               │
  │    ) : null;                                             │
  │  }                                                       │
  │                                                        │
  │  💡 "open && <ul>..." = short-circuit rendering!       │
  │  → Khi open = false → JavaScript NGỪNG ngay!           │
  │  → Khi open = true → render <ul>!                      │
  │                                                        │
  │                                                        │
  │  // Item — từng mục trong danh sách!                   │
  │                                                        │
  │  function Item({ children, onClick }) {                  │
  │    const { toggle } = useContext(FlyOutContext);         │
  │                                                        │
  │    const handleClick = () => {                           │
  │      onClick?.();  // Gọi handler nếu có!              │
  │      toggle();     // Đóng menu sau khi click!         │
  │    };                                                    │
  │                                                        │
  │    return (                                              │
  │      <li                                                 │
  │        className="flyout-item"                          │
  │        onClick={handleClick}                             │
  │      >                                                   │
  │        {children}                                        │
  │      </li>                                               │
  │    );                                                    │
  │  }                                                       │
  │                                                        │
  │  💡 onClick?.() = Optional chaining!                   │
  │  → Nếu onClick KHÔNG được truyền → bỏ qua!           │
  │  → Nếu onClick CÓ → gọi nó!                           │
  │  → Sau đó toggle() đóng menu!                         │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §4. Static Properties — Gắn Sub-Component Vào Parent

```
═══════════════════════════════════════════════════════════════
  STATIC PROPERTIES = BÍ QUYẾT CỦA CÚ PHÁP Parent.Child!
═══════════════════════════════════════════════════════════════

  TẠI SAO VIẾT <FlyOut.Toggle /> MÀ KHÔNG PHẢI <Toggle />?
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  JavaScript cho phép GẮN property vào function!       │
  │                                                        │
  │  function FlyOut({ children }) { ... }                  │
  │                                                        │
  │  // FlyOut là function → cũng là Object!              │
  │  // → Có thể gắn property lên nó!                    │
  │                                                        │
  │  FlyOut.Toggle = Toggle;  // ← Gắn Toggle!            │
  │  FlyOut.List = List;      // ← Gắn List!              │
  │  FlyOut.Item = Item;      // ← Gắn Item!              │
  │                                                        │
  │  // Bây giờ:                                             │
  │  FlyOut.Toggle === Toggle  // true!                      │
  │  FlyOut.List === List      // true!                      │
  │  FlyOut.Item === Item      // true!                      │
  │                                                        │
  └────────────────────────────────────────────────────────┘

  LỢI ÍCH:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  ① CHỈ CẦN 1 IMPORT!                                  │
  │  // Không static: import { FlyOut, Toggle, List, Item }│
  │  // → 4 imports!                                       │
  │  // Có static: import { FlyOut } from './FlyOut';      │
  │  // → 1 import! Dùng FlyOut.Toggle, FlyOut.List...    │
  │                                                        │
  │  ② THỂ HIỆN QUAN HỆ "THUỘC VỀ"!                     │
  │  <FlyOut>                                               │
  │    <FlyOut.Toggle />    ← Toggle THUỘC VỀ FlyOut!     │
  │    <FlyOut.List>        ← List THUỘC VỀ FlyOut!       │
  │      <FlyOut.Item />    ← Item THUỘC VỀ FlyOut!       │
  │    </FlyOut.List>                                       │
  │  </FlyOut>                                               │
  │  → Nhìn code BIẾT NGAY các component liên quan!       │
  │  → IDE auto-complete: gõ FlyOut. sẽ gợi ý!           │
  │                                                        │
  │  ③ CODE HOÀN CHỈNH — 1 FILE DUY NHẤT!                │
  │  ┌──────────────────────────────────────────────┐       │
  │  │  // FlyOut.jsx                                │       │
  │  │  const FlyOutContext = createContext();        │       │
  │  │                                               │       │
  │  │  function FlyOut({ children }) {              │       │
  │  │    const [open, setOpen] = useState(false);   │       │
  │  │    const toggle = () => setOpen(p => !p);     │       │
  │  │    return (                                   │       │
  │  │      <FlyOutContext.Provider                  │       │
  │  │        value={{ open, toggle }}>              │       │
  │  │        {children}                             │       │
  │  │      </FlyOutContext.Provider>                 │       │
  │  │    );                                         │       │
  │  │  }                                            │       │
  │  │  function Toggle() { ... }                    │       │
  │  │  function List({ children }) { ... }          │       │
  │  │  function Item({ children }) { ... }          │       │
  │  │                                               │       │
  │  │  FlyOut.Toggle = Toggle;                      │       │
  │  │  FlyOut.List = List;                          │       │
  │  │  FlyOut.Item = Item;                          │       │
  │  │  export { FlyOut };                           │       │
  │  └──────────────────────────────────────────────┘       │
  │                                                        │
  │  SỬ DỤNG:                                               │
  │  import { FlyOut } from './FlyOut';                      │
  │  export default function FlyOutMenu() {                  │
  │    return (                                              │
  │      <FlyOut>                                            │
  │        <FlyOut.Toggle />                                 │
  │        <FlyOut.List>                                     │
  │          <FlyOut.Item>✏️ Edit</FlyOut.Item>             │
  │          <FlyOut.Item>🗑️ Delete</FlyOut.Item>           │
  │        </FlyOut.List>                                    │
  │      </FlyOut>                                           │
  │    );                                                    │
  │  }                                                       │
  │  💡 KHÔNG useState trong FlyOutMenu!                   │
  │  → State tự quản lý BÊN TRONG FlyOut!                 │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §5. Cách 2: React.Children.map & cloneElement

```
═══════════════════════════════════════════════════════════════
  CÁCH 2: TRUYỀN PROPS TỰ ĐỘNG QUA cloneElement!
═══════════════════════════════════════════════════════════════

  Ý TƯỞNG:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  Thay vì Context (kênh radio),                         │
  │  ta "NHÂN BẢN" mỗi child và                           │
  │  GẮN THÊM props { open, toggle } vào bản sao!        │
  │                                                        │
  │  💡 Giống: Bạn có 3 PHONG BÌ (children)              │
  │  → MỞ từng phong bì                                   │
  │  → BỎ THÊM 1 tờ giấy { open, toggle }               │
  │  → DÁN LẠI và gửi đi!                                │
  │                                                        │
  │  React.Children.map = DUYỆT từng child!                │
  │  React.cloneElement = NHÂN BẢN + thêm props!          │
  │                                                        │
  └────────────────────────────────────────────────────────┘

  SƠ ĐỒ SO SÁNH 2 CÁCH:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  ═══ CONTEXT API ═══                                    │
  │  FlyOut (Provider phát sóng!)                           │
  │    ├── Toggle    ← useContext() bắt sóng!  ✅         │
  │    ├── div                                              │
  │    │    └── List ← useContext() bắt sóng!  ✅         │
  │    └── Footer                                           │
  │  → BẤT KỲ con cháu nào cũng nhận được!              │
  │                                                        │
  │  ═══ cloneElement ═══                                    │
  │  FlyOut (clone + inject props!)                          │
  │    ├── Toggle  ← nhận props ✅ (con trực tiếp)       │
  │    ├── List    ← nhận props ✅ (con trực tiếp)       │
  │    └── div → Toggle ← KHÔNG nhận! ❌ (cháu, HỎNG!)  │
  │  → CHỈ CHILDREN TRỰC TIẾP mới nhận props!            │
  │                                                        │
  └────────────────────────────────────────────────────────┘

  CODE:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  function FlyOut({ children }) {                         │
  │    const [open, setOpen] = useState(false);              │
  │    const toggle = () => setOpen(prev => !prev);         │
  │                                                        │
  │    return (                                              │
  │      <div className="flyout-wrapper">                   │
  │        {React.Children.map(children, (child) =>         │
  │          React.cloneElement(child, { open, toggle })    │
  │        )}                                                │
  │      </div>                                              │
  │    );                                                    │
  │  }                                                       │
  │                                                        │
  │  GIẢI THÍCH:                                             │
  │  React.Children.map(children, callback)                  │
  │  = Duyệt TỪNG child, giống Array.map()                 │
  │  → Xử lý fragments, null, undefined AN TOÀN!          │
  │                                                        │
  │  React.cloneElement(element, newProps)                    │
  │  = NHÂN BẢN element + MERGE thêm props!               │
  │  <Toggle />  → <Toggle open={false} toggle={fn} />     │
  │                                                        │
  │  // Toggle nhận qua PROPS (không dùng useContext!)      │
  │  function Toggle({ open, toggle }) {                     │
  │    return (                                              │
  │      <div onClick={toggle}>                              │
  │        {open ? '▲' : '▼'}                               │
  │      </div>                                              │
  │    );                                                    │
  │  }                                                       │
  │                                                        │
  │  function List({ children, open }) {                     │
  │    return open ? <ul>{children}</ul> : null;             │
  │  }                                                       │
  │                                                        │
  └────────────────────────────────────────────────────────┘

  ⚠️ HẠN CHẾ QUAN TRỌNG:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  ❌ VẤN ĐỀ 1: CHỈ con TRỰC TIẾP nhận props!        │
  │  <FlyOut>                                                │
  │    <div>              ← div nhận props (vô ích!)      │
  │      <FlyOut.Toggle /> ← KHÔNG nhận! (là cháu!)      │
  │    </div>                                                │
  │  </FlyOut>                                               │
  │                                                        │
  │  ❌ VẤN ĐỀ 2: Naming collision!                       │
  │  <FlyOut>                                                │
  │    <MyComp toggle="dark-mode" />                         │
  │  </FlyOut>                                               │
  │  → toggle bị GHI ĐÈ bởi FlyOut! XUNG ĐỘT!          │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §6. So Sánh Context API vs Children.map

```
═══════════════════════════════════════════════════════════════
  HAI CÁCH — CHỌN CÁI NÀO?
═══════════════════════════════════════════════════════════════

  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  ┌──────────────┬────────────────┬─────────────────┐   │
  │  │              │ Context API    │ Children.map    │   │
  │  ├──────────────┼────────────────┼─────────────────┤   │
  │  │ Nesting      │ ✅ Sâu tùy ý │ ❌ Chỉ con     │   │
  │  │              │                │    trực tiếp   │   │
  │  ├──────────────┼────────────────┼─────────────────┤   │
  │  │ Naming       │ ✅ Không      │ ❌ Có thể     │   │
  │  │ collision    │    xung đột   │    xung đột    │   │
  │  ├──────────────┼────────────────┼─────────────────┤   │
  │  │ Setup code   │ Nhiều hơn     │ Ít code hơn    │   │
  │  ├──────────────┼────────────────┼─────────────────┤   │
  │  │ Debugging    │ DevTools      │ Props visible   │   │
  │  │              │ hiện Context  │ trực tiếp     │   │
  │  ├──────────────┼────────────────┼─────────────────┤   │
  │  │ React 18+    │ ✅ Khuyên    │ ⚠️ Legacy      │   │
  │  │ khuyên dùng │    dùng!      │    pattern      │   │
  │  └──────────────┴────────────────┴─────────────────┘   │
  │                                                        │
  │  💡 KHUYẾN NGHỊ: 95% → CONTEXT API!                   │
  │  Children.map chỉ khi component ĐƠN GIẢN, ko nesting!│
  │                                                        │
  │                                                        │
  │  TỐI ƯU CONTEXT — TRÁNH RE-RENDER THỪA:              │
  │  ┌──────────────────────────────────────────────┐       │
  │  │                                               │       │
  │  │  // ❌ TỆ: tạo object MỚI mỗi render!      │       │
  │  │  <Context.Provider value={{ open, toggle }}>  │       │
  │  │  // → Object MỚI → consumers RE-RENDER!     │       │
  │  │                                               │       │
  │  │  // ✅ TỐT: useMemo!                        │       │
  │  │  const value = useMemo(                       │       │
  │  │    () => ({ open, toggle: setOpen }),          │       │
  │  │    [open]                                     │       │
  │  │  );                                           │       │
  │  │  <Context.Provider value={value}>             │       │
  │  │  // → Chỉ tạo mới KHI open thay đổi!       │       │
  │  │                                               │       │
  │  │  // ✅✅ TỐT NHẤT: TÁCH Context!            │       │
  │  │  const StateCtx = createContext();            │       │
  │  │  const ToggleCtx = createContext();           │       │
  │  │  // Toggle dùng ToggleCtx → ko re-render khi │       │
  │  │  // open thay đổi!                          │       │
  │  │  // List dùng StateCtx → ko re-render khi    │       │
  │  │  // toggle thay đổi!                        │       │
  │  └──────────────────────────────────────────────┘       │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §7. Ví Dụ Thực Chiến: Tự Viết Accordion, Tabs, Select

```
═══════════════════════════════════════════════════════════════
  VÍ DỤ 1: TỰ VIẾT ACCORDION — Từ số 0!
═══════════════════════════════════════════════════════════════

  ACCORDION = Nhiều panel, chỉ MỘT panel mở tại 1 thời điểm!

  ┌────────────────────────────────────────────────────────┐
  │  Trạng thái UI:                                        │
  │                                                        │
  │  ┌─────────────────────────┐                            │
  │  │ ▼ Panel 1 (ĐANG MỞ)   │                            │
  │  ├─────────────────────────┤                            │
  │  │ Nội dung Panel 1...    │                            │
  │  │ Hiển thị đầy đủ!      │                            │
  │  ├─────────────────────────┤                            │
  │  │ ▶ Panel 2 (đang đóng) │                            │
  │  ├─────────────────────────┤                            │
  │  │ ▶ Panel 3 (đang đóng) │                            │
  │  └─────────────────────────┘                            │
  │                                                        │
  │  Click Panel 2 → Panel 1 ĐÓNG, Panel 2 MỞ!          │
  │                                                        │
  └────────────────────────────────────────────────────────┘


  CODE HOÀN CHỈNH:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  // Accordion.jsx                                       │
  │  import { createContext, useState, useContext } from    │
  │    'react';                                             │
  │                                                        │
  │  // ① TẠO CONTEXT                                      │
  │  const AccordionContext = createContext();               │
  │                                                        │
  │  // ② CUSTOM HOOK — an toàn!                            │
  │  function useAccordion() {                               │
  │    const context = useContext(AccordionContext);          │
  │    if (!context) {                                       │
  │      throw new Error(                                    │
  │        'Accordion sub-components phải nằm trong'       │
  │        + ' <Accordion>!'                                │
  │      );                                                  │
  │    }                                                     │
  │    return context;                                       │
  │  }                                                       │
  │                                                        │
  │  💡 TẠI SAO CẦN CUSTOM HOOK?                          │
  │  → Nếu ai đó dùng <AccordionItem /> NGOÀI             │
  │    <Accordion>, sẽ nhận ERROR RÕ RÀNG!                │
  │  → Thay vì crash khó debug!                             │
  │                                                        │
  │                                                        │
  │  // ③ PARENT — quản lý activeIndex!                     │
  │  function Accordion({ children, defaultIndex = null }) { │
  │    const [activeIndex, setActiveIndex] = useState(       │
  │      defaultIndex                                        │
  │    );                                                    │
  │                                                        │
  │    const onToggle = (index) => {                         │
  │      // Click panel đang mở → ĐÓNG nó!                │
  │      // Click panel đang đóng → MỞ nó!               │
  │      setActiveIndex(prev =>                              │
  │        prev === index ? null : index                     │
  │      );                                                  │
  │    };                                                    │
  │                                                        │
  │    return (                                              │
  │      <AccordionContext.Provider                          │
  │        value={{ activeIndex, onToggle }}                 │
  │      >                                                   │
  │        <div className="accordion">                       │
  │          {children}                                      │
  │        </div>                                            │
  │      </AccordionContext.Provider>                         │
  │    );                                                    │
  │  }                                                       │
  │                                                        │
  │  💡 activeIndex = null → không panel nào mở!          │
  │  💡 activeIndex = 2 → panel thứ 3 đang mở!           │
  │  💡 prev === index → click lại panel đã mở → đóng!  │
  │                                                        │
  │                                                        │
  │  // ④ ITEM — từng panel!                                │
  │  function AccordionItem({ children, index }) {           │
  │    const { activeIndex, onToggle } = useAccordion();     │
  │    const isOpen = activeIndex === index;                  │
  │                                                        │
  │    return (                                              │
  │      <div className={                                    │
  │        `accordion-item ${isOpen ? 'active' : ''}`       │
  │      }>                                                  │
  │        {children}                                        │
  │      </div>                                              │
  │    );                                                    │
  │  }                                                       │
  │                                                        │
  │                                                        │
  │  // ⑤ HEADER — nút bấm để mở/đóng                     │
  │  function AccordionHeader({ children, index }) {         │
  │    const { activeIndex, onToggle } = useAccordion();     │
  │    const isOpen = activeIndex === index;                  │
  │                                                        │
  │    return (                                              │
  │      <button                                             │
  │        className="accordion-header"                      │
  │        onClick={() => onToggle(index)}                   │
  │        aria-expanded={isOpen}                            │
  │      >                                                   │
  │        <span>{children}</span>                           │
  │        <span>{isOpen ? '▼' : '▶'}</span>               │
  │      </button>                                           │
  │    );                                                    │
  │  }                                                       │
  │                                                        │
  │                                                        │
  │  // ⑥ PANEL — nội dung (hiện khi mở!)                 │
  │  function AccordionPanel({ children, index }) {          │
  │    const { activeIndex } = useAccordion();               │
  │    const isOpen = activeIndex === index;                  │
  │                                                        │
  │    return isOpen ? (                                      │
  │      <div className="accordion-panel"                    │
  │           role="region">                                 │
  │        {children}                                        │
  │      </div>                                              │
  │    ) : null;                                             │
  │  }                                                       │
  │                                                        │
  │  // ⑦ GẮN STATIC PROPERTIES!                           │
  │  Accordion.Item = AccordionItem;                         │
  │  Accordion.Header = AccordionHeader;                     │
  │  Accordion.Panel = AccordionPanel;                       │
  │                                                        │
  │  export { Accordion };                                   │
  │                                                        │
  │                                                        │
  │  // ⑧ SỬ DỤNG:                                          │
  │  <Accordion defaultIndex={0}>                            │
  │    <Accordion.Item index={0}>                            │
  │      <Accordion.Header index={0}>                        │
  │        JavaScript cơ bản                                │
  │      </Accordion.Header>                                 │
  │      <Accordion.Panel index={0}>                         │
  │        JS là ngôn ngữ lập trình...                     │
  │      </Accordion.Panel>                                  │
  │    </Accordion.Item>                                     │
  │                                                        │
  │    <Accordion.Item index={1}>                            │
  │      <Accordion.Header index={1}>                        │
  │        React Hooks                                       │
  │      </Accordion.Header>                                 │
  │      <Accordion.Panel index={1}>                         │
  │        Hooks cho phép dùng state...                     │
  │      </Accordion.Panel>                                  │
  │    </Accordion.Item>                                     │
  │  </Accordion>                                            │
  │                                                        │
  └────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════
  VÍ DỤ 2: TỰ VIẾT TABS — Từ số 0!
═══════════════════════════════════════════════════════════════

  TABS = Giao diện tab quen thuộc!

  ┌────────────────────────────────────────────────────────┐
  │  Trạng thái UI:                                        │
  │                                                        │
  │  ┌──────┐ ┌──────┐ ┌──────┐                            │
  │  │ Tab1 │ │ Tab2 │ │ Tab3 │  ← TabList                │
  │  │ACTIVE│ │      │ │      │                            │
  │  └──────┘ └──────┘ └──────┘                            │
  │  ┌──────────────────────────┐                            │
  │  │  Nội dung Tab 1          │  ← TabPanel               │
  │  │  (đang hiển thị)        │                            │
  │  └──────────────────────────┘                            │
  │                                                        │
  └────────────────────────────────────────────────────────┘


  CODE:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  // Tabs.jsx                                             │
  │  const TabsContext = createContext();                     │
  │                                                        │
  │  function useTabs() {                                     │
  │    const ctx = useContext(TabsContext);                   │
  │    if (!ctx) throw new Error(                             │
  │      'Tab components phải nằm trong <Tabs>!'           │
  │    );                                                    │
  │    return ctx;                                           │
  │  }                                                       │
  │                                                        │
  │  function Tabs({ children, defaultIndex = 0 }) {         │
  │    const [activeIndex, setActiveIndex] = useState(       │
  │      defaultIndex                                        │
  │    );                                                    │
  │    return (                                              │
  │      <TabsContext.Provider                               │
  │        value={{ activeIndex, setActiveIndex }}           │
  │      >                                                   │
  │        <div className="tabs">{children}</div>            │
  │      </TabsContext.Provider>                              │
  │    );                                                    │
  │  }                                                       │
  │                                                        │
  │  function TabList({ children }) {                        │
  │    return (                                              │
  │      <div className="tab-list" role="tablist">           │
  │        {children}                                        │
  │      </div>                                              │
  │    );                                                    │
  │  }                                                       │
  │                                                        │
  │  function Tab({ children, index }) {                     │
  │    const { activeIndex, setActiveIndex } = useTabs();    │
  │    const isActive = activeIndex === index;                │
  │    return (                                              │
  │      <button                                             │
  │        className={`tab ${isActive ? 'active' : ''}`}    │
  │        onClick={() => setActiveIndex(index)}             │
  │        role="tab"                                        │
  │        aria-selected={isActive}                          │
  │      >                                                   │
  │        {children}                                        │
  │      </button>                                           │
  │    );                                                    │
  │  }                                                       │
  │                                                        │
  │  function TabPanel({ children, index }) {                │
  │    const { activeIndex } = useTabs();                    │
  │    return activeIndex === index ? (                       │
  │      <div className="tab-panel" role="tabpanel">         │
  │        {children}                                        │
  │      </div>                                              │
  │    ) : null;                                             │
  │  }                                                       │
  │                                                        │
  │  Tabs.TabList = TabList;                                 │
  │  Tabs.Tab = Tab;                                         │
  │  Tabs.Panel = TabPanel;                                  │
  │  export { Tabs };                                        │
  │                                                        │
  │                                                        │
  │  // SỬ DỤNG:                                             │
  │  <Tabs defaultIndex={0}>                                 │
  │    <Tabs.TabList>                                        │
  │      <Tabs.Tab index={0}>Profile</Tabs.Tab>             │
  │      <Tabs.Tab index={1}>Settings</Tabs.Tab>            │
  │      <Tabs.Tab index={2}>About</Tabs.Tab>               │
  │    </Tabs.TabList>                                       │
  │                                                        │
  │    <Tabs.Panel index={0}>User profile...</Tabs.Panel>   │
  │    <Tabs.Panel index={1}>App settings...</Tabs.Panel>   │
  │    <Tabs.Panel index={2}>About page...</Tabs.Panel>     │
  │  </Tabs>                                                 │
  │                                                        │
  └────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════
  VÍ DỤ 3: TỰ VIẾT SELECT DROPDOWN — Từ số 0!
═══════════════════════════════════════════════════════════════

  SELECT = Dropdown chọn 1 giá trị!

  ┌────────────────────────────────────────────────────────┐
  │  Trạng thái UI:                                        │
  │                                                        │
  │  ┌──────────────────────┐                                │
  │  │ Chọn thành phố... ▼ │  ← Trigger                   │
  │  └──────────────────────┘                                │
  │  ┌──────────────────────┐                                │
  │  │ ✓ Hà Nội            │  ← Option (selected!)        │
  │  │   TP.HCM             │                                │
  │  │   Đà Nẵng           │                                │
  │  └──────────────────────┘  ← OptionList                 │
  │                                                        │
  └────────────────────────────────────────────────────────┘


  CODE:
  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  // Select.jsx                                           │
  │  const SelectContext = createContext();                   │
  │                                                        │
  │  function useSelect() {                                   │
  │    const ctx = useContext(SelectContext);                 │
  │    if (!ctx) throw new Error(                             │
  │      'Select sub-components must be inside <Select>!'   │
  │    );                                                    │
  │    return ctx;                                           │
  │  }                                                       │
  │                                                        │
  │  function Select({ children, onChange }) {                │
  │    const [isOpen, setIsOpen] = useState(false);          │
  │    const [selected, setSelected] = useState(null);       │
  │                                                        │
  │    const toggle = () => setIsOpen(prev => !prev);       │
  │                                                        │
  │    const selectOption = (value, label) => {              │
  │      setSelected({ value, label });                      │
  │      setIsOpen(false);         // Đóng sau khi chọn!  │
  │      onChange?.(value);        // Callback ra ngoài!    │
  │    };                                                    │
  │                                                        │
  │    return (                                              │
  │      <SelectContext.Provider value={{                     │
  │        isOpen, toggle, selected, selectOption            │
  │      }}>                                                 │
  │        <div className="select-wrapper">                  │
  │          {children}                                      │
  │        </div>                                            │
  │      </SelectContext.Provider>                            │
  │    );                                                    │
  │  }                                                       │
  │                                                        │
  │  function Trigger({ placeholder = "Select..." }) {       │
  │    const { toggle, selected, isOpen } = useSelect();     │
  │    return (                                              │
  │      <button                                             │
  │        className="select-trigger"                        │
  │        onClick={toggle}                                  │
  │        aria-expanded={isOpen}                            │
  │      >                                                   │
  │        <span>{selected?.label || placeholder}</span>     │
  │        <span>{isOpen ? '▲' : '▼'}</span>               │
  │      </button>                                           │
  │    );                                                    │
  │  }                                                       │
  │                                                        │
  │  function OptionList({ children }) {                      │
  │    const { isOpen } = useSelect();                       │
  │    return isOpen ? (                                      │
  │      <ul className="option-list" role="listbox">         │
  │        {children}                                        │
  │      </ul>                                               │
  │    ) : null;                                             │
  │  }                                                       │
  │                                                        │
  │  function Option({ children, value }) {                  │
  │    const { selectOption, selected } = useSelect();       │
  │    const isSelected = selected?.value === value;          │
  │    return (                                              │
  │      <li                                                 │
  │        className={`option ${isSelected ? 'selected'     │
  │          : ''}`}                                         │
  │        onClick={() => selectOption(value, children)}     │
  │        role="option"                                     │
  │        aria-selected={isSelected}                        │
  │      >                                                   │
  │        {isSelected && '✓ '}{children}                   │
  │      </li>                                               │
  │    );                                                    │
  │  }                                                       │
  │                                                        │
  │  Select.Trigger = Trigger;                               │
  │  Select.OptionList = OptionList;                          │
  │  Select.Option = Option;                                 │
  │  export { Select };                                      │
  │                                                        │
  │                                                        │
  │  // SỬ DỤNG:                                             │
  │  <Select onChange={(val) => console.log(val)}>           │
  │    <Select.Trigger placeholder="Chọn thành phố..." />  │
  │    <Select.OptionList>                                   │
  │      <Select.Option value="hn">Hà Nội</Select.Option>  │
  │      <Select.Option value="hcm">TP.HCM</Select.Option> │
  │      <Select.Option value="dn">Đà Nẵng</Select.Option>│
  │    </Select.OptionList>                                  │
  │  </Select>                                               │
  │                                                        │
  │                                                        │
  │  SƠ ĐỒ KIẾN TRÚC CHUNG 3 VÍ DỤ:                      │
  │  ┌──────────────────────────────────────────────┐       │
  │  │                                               │       │
  │  │  Accordion     Tabs          Select           │       │
  │  │  ┌─────────┐   ┌─────────┐  ┌─────────┐      │       │
  │  │  │ Context │   │ Context │  │ Context │      │       │
  │  │  │ active  │   │ active  │  │ isOpen  │      │       │
  │  │  │ Index   │   │ Index   │  │selected │      │       │
  │  │  │ onToggle│   │setActive│  │select() │      │       │
  │  │  └────┬────┘   └────┬────┘  └────┬────┘      │       │
  │  │       │             │            │            │       │
  │  │       ▼             ▼            ▼            │       │
  │  │  Item/Header   TabList/Tab  Trigger/Option    │       │
  │  │  /Panel        /Panel       /OptionList       │       │
  │  │                                               │       │
  │  │  💡 PATTERN LÀ GIỐNG NHAU:                   │       │
  │  │  1. createContext() — tạo kênh               │       │
  │  │  2. Parent + Provider — phát state            │       │
  │  │  3. Children + useContext() — nhận state      │       │
  │  │  4. Static properties — gắn sub-components   │       │
  │  │                                               │       │
  │  └──────────────────────────────────────────────┘       │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

## §8. Best Practices, Tối Ưu & Phỏng Vấn Q&A

```
═══════════════════════════════════════════════════════════════
  BEST PRACTICES — LUẬT VÀ NGUYÊN TẮC!
═══════════════════════════════════════════════════════════════

  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  NGUYÊN TẮC 1: LUÔN TẠO CUSTOM HOOK!                 │
  │  ┌──────────────────────────────────────────────┐       │
  │  │                                               │       │
  │  │  // ❌ KHÔNG NÊN: dùng useContext trực tiếp │       │
  │  │  function Toggle() {                          │       │
  │  │    const ctx = useContext(FlyOutContext);      │       │
  │  │    // Nếu dùng ngoài Provider → undefined!   │       │
  │  │    // → CRASH không rõ lý do!                │       │
  │  │  }                                            │       │
  │  │                                               │       │
  │  │  // ✅ NÊN: custom hook với error handling!  │       │
  │  │  function useFlyOut() {                       │       │
  │  │    const ctx = useContext(FlyOutContext);      │       │
  │  │    if (!ctx) {                                │       │
  │  │      throw new Error(                         │       │
  │  │        'useFlyOut phải dùng trong <FlyOut>!' │       │
  │  │      );                                       │       │
  │  │    }                                          │       │
  │  │    return ctx;                                │       │
  │  │  }                                            │       │
  │  │                                               │       │
  │  │  💡 Custom hook = AN TOÀN + DỄ DEBUG!       │       │
  │  └──────────────────────────────────────────────┘       │
  │                                                        │
  │                                                        │
  │  NGUYÊN TẮC 2: displayName CHO DevTools!              │
  │  ┌──────────────────────────────────────────────┐       │
  │  │                                               │       │
  │  │  FlyOutContext.displayName = 'FlyOutContext';  │       │
  │  │  // → React DevTools hiện tên rõ ràng!      │       │
  │  │  // Thay vì "Context.Provider" chung chung!  │       │
  │  │                                               │       │
  │  └──────────────────────────────────────────────┘       │
  │                                                        │
  │                                                        │
  │  NGUYÊN TẮC 3: TYPESCRIPT cho type safety!            │
  │  ┌──────────────────────────────────────────────┐       │
  │  │                                               │       │
  │  │  // Định nghĩa TYPE cho Context!             │       │
  │  │  interface FlyOutContextType {                │       │
  │  │    open: boolean;                             │       │
  │  │    toggle: () => void;                        │       │
  │  │  }                                            │       │
  │  │                                               │       │
  │  │  const FlyOutContext =                        │       │
  │  │    createContext<FlyOutContextType | null>(    │       │
  │  │      null                                     │       │
  │  │    );                                         │       │
  │  │                                               │       │
  │  │  // Custom hook VỪA type-safe VỪA null-safe! │       │
  │  │  function useFlyOut(): FlyOutContextType {    │       │
  │  │    const ctx = useContext(FlyOutContext);      │       │
  │  │    if (!ctx) throw new Error('...');           │       │
  │  │    return ctx; // TypeScript biết KHÔNG null! │       │
  │  │  }                                            │       │
  │  │                                               │       │
  │  └──────────────────────────────────────────────┘       │
  │                                                        │
  │                                                        │
  │  NGUYÊN TẮC 4: TÁCH CÁC CONCERN!                      │
  │  ┌──────────────────────────────────────────────┐       │
  │  │                                               │       │
  │  │  // Nếu component PHỨC TẠP → tách file!     │       │
  │  │                                               │       │
  │  │  src/components/Accordion/                    │       │
  │  │  ├── index.ts        ← re-export             │       │
  │  │  ├── Accordion.tsx   ← Parent + Context       │       │
  │  │  ├── AccordionItem.tsx                        │       │
  │  │  ├── AccordionHeader.tsx                      │       │
  │  │  ├── AccordionPanel.tsx                       │       │
  │  │  ├── useAccordion.ts ← Custom hook            │       │
  │  │  └── types.ts        ← Type definitions       │       │
  │  │                                               │       │
  │  │  // index.ts                                  │       │
  │  │  export { Accordion } from './Accordion';     │       │
  │  │  // Re-export với static properties đã gắn! │       │
  │  │                                               │       │
  │  └──────────────────────────────────────────────┘       │
  │                                                        │
  │                                                        │
  │  NGUYÊN TẮC 5: ACCESSIBILITY (A11y)!                  │
  │  ┌──────────────────────────────────────────────┐       │
  │  │                                               │       │
  │  │  // Luôn thêm ARIA attributes!               │       │
  │  │  aria-expanded={isOpen}   ← toggle/trigger    │       │
  │  │  aria-selected={isActive} ← tab/option        │       │
  │  │  role="tablist"           ← tab container     │       │
  │  │  role="tab"               ← individual tab    │       │
  │  │  role="tabpanel"          ← tab content       │       │
  │  │  role="listbox"           ← select list       │       │
  │  │  role="option"            ← select item       │       │
  │  │  role="region"            ← accordion panel   │       │
  │  │                                               │       │
  │  │  💡 Screen readers CẦN các attributes này!  │       │
  │  └──────────────────────────────────────────────┘       │
  │                                                        │
  └────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════
  PHỎNG VẤN Q&A — CÂU HỎI THƯỜNG GẶP!
═══════════════════════════════════════════════════════════════

  ┌────────────────────────────────────────────────────────┐
  │                                                        │
  │  ❓ Q1: Compound Pattern là gì?                        │
  │  ┌──────────────────────────────────────────────┐       │
  │  │  A: Pattern mà nhiều components KẾT HỢP     │       │
  │  │  với nhau để hoàn thành 1 nhiệm vụ.         │       │
  │  │  Chúng CHIA SẺ state chung qua Context API. │       │
  │  │  VD: <Select>, <Tabs>, <Accordion>...        │       │
  │  │  Parent quản lý state, Children đọc state.   │       │
  │  └──────────────────────────────────────────────┘       │
  │                                                        │
  │                                                        │
  │  ❓ Q2: Có mấy cách triển khai?                       │
  │  ┌──────────────────────────────────────────────┐       │
  │  │  A: 2 cách:                                   │       │
  │  │  1. Context API (KHUYÊN DÙNG!)               │       │
  │  │     → Nesting sâu tùy ý                     │       │
  │  │     → Không naming collision                  │       │
  │  │  2. React.Children.map + cloneElement         │       │
  │  │     → Chỉ con trực tiếp                     │       │
  │  │     → Đơn giản nhưng hạn chế               │       │
  │  └──────────────────────────────────────────────┘       │
  │                                                        │
  │                                                        │
  │  ❓ Q3: Ưu điểm so với props drilling?                │
  │  ┌──────────────────────────────────────────────┐       │
  │  │  A: 4 ưu điểm chính:                        │       │
  │  │  1. GIẢM số props (0-2 thay vì 10-20+)      │       │
  │  │  2. LINH HOẠT về layout (di chuyển children)│       │
  │  │  3. DỄ mở rộng (thêm sub-component)        │       │
  │  │  4. API RÕ RÀNG (Parent.Child syntax)        │       │
  │  └──────────────────────────────────────────────┘       │
  │                                                        │
  │                                                        │
  │  ❓ Q4: Nhược điểm?                                   │
  │  ┌──────────────────────────────────────────────┐       │
  │  │  A: 3 nhược điểm:                            │       │
  │  │  1. Setup CODE nhiều hơn (Context, Provider) │       │
  │  │  2. Children chỉ dùng ĐƯỢC khi nằm TRONG   │       │
  │  │     Parent! (context boundary)               │       │
  │  │  3. Context re-render tất cả consumers khi  │       │
  │  │     value thay đổi (cần useMemo tối ưu)    │       │
  │  └──────────────────────────────────────────────┘       │
  │                                                        │
  │                                                        │
  │  ❓ Q5: Khi nào KHÔNG nên dùng?                       │
  │  ┌──────────────────────────────────────────────┐       │
  │  │  A: Không nên khi:                            │       │
  │  │  1. Component ĐƠN GIẢN, ít props (< 5)     │       │
  │  │  2. Không có SHARED STATE giữa children      │       │
  │  │  3. Component không có sub-parts rõ ràng     │       │
  │  │  4. Over-engineering cho use case nhỏ        │       │
  │  └──────────────────────────────────────────────┘       │
  │                                                        │
  │                                                        │
  │  ❓ Q6: So sánh với Render Props pattern?              │
  │  ┌──────────────────────────────────────────────┐       │
  │  │  A: Khác biệt:                               │       │
  │  │  Compound: children KHAI BÁO cấu trúc UI   │       │
  │  │  → <Tabs><Tabs.Tab /><Tabs.Panel /></Tabs>   │       │
  │  │                                               │       │
  │  │  Render Props: truyền FUNCTION để render!    │       │
  │  │  → <DataFetcher render={(data) => <div />} />│       │
  │  │                                               │       │
  │  │  Compound = UI composition (cấu trúc)       │       │
  │  │  Render Props = logic sharing (chia sẻ logic)│       │
  │  └──────────────────────────────────────────────┘       │
  │                                                        │
  │                                                        │
  │  ❓ Q7: Static properties hoạt động thế nào?          │
  │  ┌──────────────────────────────────────────────┐       │
  │  │  A: JavaScript functions là OBJECTS!          │       │
  │  │  → Có thể gắn property lên function!        │       │
  │  │  FlyOut.Toggle = ToggleComponent;             │       │
  │  │  → FlyOut.Toggle === ToggleComponent // true  │       │
  │  │  → Cho phép import 1 lần, dùng qua dot      │       │
  │  │    notation: <FlyOut.Toggle />                │       │
  │  └──────────────────────────────────────────────┘       │
  │                                                        │
  │                                                        │
  │  ❓ Q8: Làm sao tối ưu re-render trong Compound?     │
  │  ┌──────────────────────────────────────────────┐       │
  │  │  A: 3 cách:                                   │       │
  │  │  1. useMemo cho Provider value                │       │
  │  │  2. TÁCH Context: StateCtx + DispatchCtx     │       │
  │  │  3. React.memo cho leaf components            │       │
  │  │  4. useReducer thay useState (state phức tạp)│       │
  │  └──────────────────────────────────────────────┘       │
  │                                                        │
  │                                                        │
  │  ❓ Q9: Cho ví dụ code nhanh 1 compound component?    │
  │  ┌──────────────────────────────────────────────┐       │
  │  │  A: Toggle component đơn giản:               │       │
  │  │                                               │       │
  │  │  const Ctx = createContext();                  │       │
  │  │  const useCtx = () => {                       │       │
  │  │    const c = useContext(Ctx);                  │       │
  │  │    if (!c) throw Error('Outside Provider!');   │       │
  │  │    return c;                                  │       │
  │  │  };                                           │       │
  │  │                                               │       │
  │  │  function Toggle({ children }) {              │       │
  │  │    const [on, setOn] = useState(false);       │       │
  │  │    return (                                   │       │
  │  │      <Ctx.Provider value={{ on, setOn }}>     │       │
  │  │        {children}                             │       │
  │  │      </Ctx.Provider>                          │       │
  │  │    );                                         │       │
  │  │  }                                            │       │
  │  │  Toggle.On = ({ children }) => {              │       │
  │  │    const { on } = useCtx();                   │       │
  │  │    return on ? children : null;               │       │
  │  │  };                                           │       │
  │  │  Toggle.Off = ({ children }) => {             │       │
  │  │    const { on } = useCtx();                   │       │
  │  │    return on ? null : children;               │       │
  │  │  };                                           │       │
  │  │  Toggle.Button = () => {                      │       │
  │  │    const { on, setOn } = useCtx();            │       │
  │  │    return (                                   │       │
  │  │      <button onClick={() => setOn(p => !p)}> │       │
  │  │        {on ? 'ON' : 'OFF'}                    │       │
  │  │      </button>                                │       │
  │  │    );                                         │       │
  │  │  };                                           │       │
  │  │                                               │       │
  │  │  // Sử dụng:                                  │       │
  │  │  <Toggle>                                     │       │
  │  │    <Toggle.On>Đang BẬT!</Toggle.On>          │       │
  │  │    <Toggle.Off>Đang TẮT!</Toggle.Off>        │       │
  │  │    <Toggle.Button />                          │       │
  │  │  </Toggle>                                    │       │
  │  └──────────────────────────────────────────────┘       │
  │                                                        │
  │                                                        │
  │  TỔNG KẾT — NHỚ 4 BƯỚC:                               │
  │  ┌──────────────────────────────────────────────┐       │
  │  │                                               │       │
  │  │  Bước 1: createContext()                      │       │
  │  │          → Tạo kênh giao tiếp!               │       │
  │  │                                               │       │
  │  │  Bước 2: Parent + Provider                    │       │
  │  │          → Quản lý và phát state!             │       │
  │  │                                               │       │
  │  │  Bước 3: Children + useContext()              │       │
  │  │          → Nhận và dùng state!                │       │
  │  │                                               │       │
  │  │  Bước 4: Parent.Child = Component             │       │
  │  │          → Gắn static properties!             │       │
  │  │                                               │       │
  │  │  💡 NHỚ: Context + Provider + useContext     │       │
  │  │     + Static Properties = COMPOUND PATTERN!  │       │
  │  │                                               │       │
  │  └──────────────────────────────────────────────┘       │
  │                                                        │
  └────────────────────────────────────────────────────────┘
```

---

> 🎯 **Tài liệu này tự viết tay 100% — không dùng thư viện!**
> Nắm vững Compound Pattern = Bạn đã hiểu cách các UI library lớn hoạt động!
