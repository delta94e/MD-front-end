# React Debug Source Code — Deep Dive!

> **Chủ đề**: Cách debug React source code — 2 phương pháp chi tiết
> **Ngôn ngữ**: Tiếng Việt — giải thích cực kỳ chi tiết!
> **Phương châm**: Tự viết lại bằng tay — KHÔNG dùng thư viện!
> **Nguồn**: "How to debug React source code"

---

## Mục Lục

1. [§1. Tổng Quan — Tại Sao Debug Source Code?](#1)
2. [§2. Phương Pháp 1: Official — yarn link](#2)
3. [§3. Phương Pháp 2: Webpack Alias — Debug Trực Tiếp](#3)
4. [§4. 6 Lỗi Và Cách Sửa](#4)
5. [§5. Sơ Đồ Tự Vẽ](#5)
6. [§6. Tự Viết — DebugSetupEngine](#6)
7. [§7. Câu Hỏi Luyện Tập](#7)

---

## §1. Tổng Quan — Tại Sao Debug Source Code?

```
  TẠI SAO DEBUG REACT SOURCE CODE?
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  LÝ DO:                                                       │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ 1. Hiểu SÂU cách React hoạt động bên trong!       │    │
  │  │ 2. Đặt breakpoint, console.log vào SOURCE!         │    │
  │  │ 3. Trace execution flow (Scheduler, Reconciler...)  │    │
  │  │ 4. Phục vụ phỏng vấn Senior/Staff Engineer!        │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  SCHEDULER LÀ PACKAGE RIÊNG BIỆT:                            │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ → Scheduler KHÔNG phụ thuộc React!                  │    │
  │  │ → Có thể đọc source Scheduler ĐỘC LẬP!           │    │
  │  │ → Nằm trong: react/packages/scheduler/             │    │
  │  │ → v18.2.0: chỉ ~629 dòng!                         │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  2 PHƯƠNG PHÁP:                                               │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ ① Official: yarn build + yarn link                  │    │
  │  │   → Debug file ĐÃ BUILD (react-dom.development.js) │    │
  │  │   → Sửa source → phải build lại! (PHiỀN!)        │    │
  │  │                                                      │    │
  │  │ ② Webpack Alias: trỏ thẳng vào source!            │    │
  │  │   → Debug file SOURCE GỐC!                         │    │
  │  │   → Sửa source → HOT RELOAD ngay! ✅              │    │
  │  │   → Cần fix ~6 lỗi compile!                       │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

---

## §2. Phương Pháp 1: Official — yarn link!

```
  PHƯƠNG PHÁP CHÍNH THỨC:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  BƯỚC 1: Tạo project                                         │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ npx create-react-app react-app                       │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  BƯỚC 2: Download React source code                           │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ git clone git@github.com:facebook/react.git          │    │
  │  │ cd react                                             │    │
  │  │ yarn                                                 │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  BƯỚC 3: Build + Link                                         │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ # Build các packages cần thiết                      │    │
  │  │ yarn build react/index,react/jsx,react-dom/index,    │    │
  │  │   scheduler --type=NODE                              │    │
  │  │                                                      │    │
  │  │ # Link react                                         │    │
  │  │ cd build/node_modules/react                          │    │
  │  │ yarn link                                            │    │
  │  │                                                      │    │
  │  │ # Link react-dom                                     │    │
  │  │ cd ../react-dom                                      │    │
  │  │ yarn link                                            │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  ⚠️ CẦN CÀI JDK! Nếu lỗi → install JDK → build lại!    │
  │                                                              │
  │  BƯỚC 4: Link vào project                                     │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ cd react-app                                         │    │
  │  │ yarn link react react-dom                            │    │
  │  │ yarn start                                           │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  BƯỚC 5: Debug                                                │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ → Mở react-dom.development.js (file ĐÃ BUILD!)    │    │
  │  │ → Thêm console.log, breakpoint!                    │    │
  │  │ → Page auto refresh!                                │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  ⚠️ VẤN ĐỀ:                                                 │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ → Debug file ĐÃ BUILD, KHÔNG PHẢI source gốc!     │    │
  │  │ → Sửa source → phải BUILD LẠI → mới có hiệu lực!│    │
  │  │ → PHIỀN! Mất thời gian!                            │    │
  │  │ → Muốn debug SOURCE TRỰC TIẾP? → Phương pháp 2!  │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

---

## §3. Phương Pháp 2: Webpack Alias — Debug Trực Tiếp!

```
  WEBPACK ALIAS:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  NGUYÊN LÝ:                                                   │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ Webpack alias = trỏ import path → thư mục khác!   │    │
  │  │ import 'react' → react/packages/react/ (SOURCE!)   │    │
  │  │ import 'react-dom' → react/packages/react-dom/     │    │
  │  │                                                      │    │
  │  │ → Sửa source → HOT RELOAD ngay!                   │    │
  │  │ → Debug TRỰC TIẾP trên source gốc!                │    │
  │  │ → NHƯNG: phải fix ~6 lỗi compile!                 │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  BƯỚC 1: Tạo project                                         │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ npx create-react-app react-app                       │    │
  │  │ cd react-app                                         │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  BƯỚC 2: Download source (STABLE version!)                    │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ cd src                                               │    │
  │  │ # Download tag v18.2.0 — STABLE, không bị breaking!│    │
  │  │ git clone --branch v18.2.0                           │    │
  │  │   git@github.com:facebook/react.git                  │    │
  │  │                                                      │    │
  │  │ ★ Đặt trong src/ để webpack có thể import!        │    │
  │  │ ★ Dùng tag STABLE → tránh code đang phát triển!   │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  BƯỚC 3: Eject CRA (bất khả đảo!)                            │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ npm run eject                                        │    │
  │  │                                                      │    │
  │  │ TRƯỚC:                                               │    │
  │  │ "scripts": {                                         │    │
  │  │   "start": "react-scripts start"                     │    │
  │  │ }                                                    │    │
  │  │                                                      │    │
  │  │ SAU:                                                  │    │
  │  │ "scripts": {                                         │    │
  │  │   "start": "node scripts/start.js" ← webpack!      │    │
  │  │ }                                                    │    │
  │  │                                                      │    │
  │  │ → Webpack config: config/webpack.config.js           │    │
  │  │ → Env config: config/env.js                         │    │
  │  │                                                      │    │
  │  │ ⚠️ BẤT KHẢ ĐẢO! Commit code trước khi eject!    │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  BƯỚC 4: Sửa webpack alias                                    │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ // config/webpack.config.js                          │    │
  │  │                                                      │    │
  │  │ // TRƯỚC:                                            │    │
  │  │ alias: {                                             │    │
  │  │   'react-native': 'react-native-web',               │    │
  │  │   ...(modules.webpackAliases || {})                  │    │
  │  │ }                                                    │    │
  │  │                                                      │    │
  │  │ // SAU:                                               │    │
  │  │ alias: {                                             │    │
  │  │   react: path.join(paths.appSrc,                     │    │
  │  │     'react/packages/react'),                         │    │
  │  │   'react-dom': path.join(paths.appSrc,               │    │
  │  │     'react/packages/react-dom'),                     │    │
  │  │   shared: path.join(paths.appSrc,                    │    │
  │  │     'react/packages/shared'),                        │    │
  │  │   'react-reconciler': path.join(paths.appSrc,        │    │
  │  │     'react/packages/react-reconciler')               │    │
  │  │ }                                                    │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  → Chạy npm start → 142 lỗi → giảm còn 5 lỗi!           │
  │  → Fix tiếp 6 lỗi ở §4!                                    │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

---

## §4. 6 Lỗi Và Cách Sửa!

### Lỗi 1: Import React/ReactDOM

```
  LỖI 1:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  VẤN ĐỀ: import React from 'react' → LỖI!                 │
  │  → Source code KHÔNG export default!                        │
  │                                                              │
  │  SỬA: src/index.js                                           │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ // TRƯỚC:                                            │    │
  │  │ import React from 'react';                           │    │
  │  │ import ReactDOM from 'react-dom/client';             │    │
  │  │                                                      │    │
  │  │ // SAU:                                               │    │
  │  │ import * as React from 'react';                      │    │
  │  │ import * as ReactDOM from 'react-dom/client';        │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  TẠI SAO?                                                     │
  │  → Source code dùng named exports!                           │
  │  → import * as = lấy TẤT CẢ named exports!                 │
  │  → import X from = chỉ lấy default (không có!)             │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

### Lỗi 2: Scheduler Mock Functions

```
  LỖI 2:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  VẤN ĐỀ: unstable_yieldValue, unstable_setDisableYieldValue │
  │  → KHÔNG TỒN TẠI trên Scheduler thật!                     │
  │  → Chỉ có trên scheduler/unstable_mock!                    │
  │                                                              │
  │  FILE: react/packages/react-reconciler/src/Scheduler.js      │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ // Thêm import ở đầu file:                         │    │
  │  │ import * as SchedulerMock from                       │    │
  │  │   'scheduler/src/forks/SchedulerMock';               │    │
  │  │                                                      │    │
  │  │ // TRƯỚC:                                            │    │
  │  │ export const unstable_yieldValue =                   │    │
  │  │   Scheduler.unstable_yieldValue;                     │    │
  │  │ export const unstable_setDisableYieldValue =         │    │
  │  │   Scheduler.unstable_setDisableYieldValue;           │    │
  │  │                                                      │    │
  │  │ // SAU:                                               │    │
  │  │ export const unstable_yieldValue =                   │    │
  │  │   SchedulerMock.unstable_yieldValue;                 │    │
  │  │ export const unstable_setDisableYieldValue =         │    │
  │  │   SchedulerMock.unstable_setDisableYieldValue;       │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

### Lỗi 3: ESLint

```
  LỖI 3:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  VẤN ĐỀ: ESLint báo lỗi trên source React!                │
  │                                                              │
  │  SỬA: config/webpack.config.js                               │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ // TRƯỚC:                                            │    │
  │  │ const disableESLintPlugin =                          │    │
  │  │   process.env.DISABLE_ESLINT_PLUGIN === 'true';      │    │
  │  │                                                      │    │
  │  │ // SAU:                                               │    │
  │  │ const disableESLintPlugin = true;                    │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

### Lỗi 4: Environment Variables (__DEV__)

```
  LỖI 4:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  VẤN ĐỀ: Page trắng! Console: __DEV__ is not defined!      │
  │  → React source dùng __DEV__ trực tiếp!                    │
  │  → Webpack KHÔNG biết __DEV__ là gì!                       │
  │                                                              │
  │  SỬA: config/env.js                                          │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ // TRƯỚC:                                            │    │
  │  │ const stringified = {                                │    │
  │  │   'process.env': Object.keys(raw).reduce(...)        │    │
  │  │ };                                                   │    │
  │  │                                                      │    │
  │  │ // SAU:                                               │    │
  │  │ const stringified = {                                │    │
  │  │   'process.env': Object.keys(raw).reduce(...),       │    │
  │  │   __DEV__: true,          // Development mode!      │    │
  │  │   __EXPERIMENTAL__: true, // Experimental features! │    │
  │  │   __PROFILE__: true       // Profiling enabled!     │    │
  │  │ };                                                   │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  TẠI SAO 3 BIẾN?                                              │
  │  → __DEV__: bật dev warnings + dev checks!                  │
  │  → __EXPERIMENTAL__: bật features thử nghiệm!              │
  │  → __PROFILE__: bật profiling tools!                        │
  │  → Trong build thật: Rollup thay thế bằng true/false!      │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

### Lỗi 5: ReactSharedInternals

```
  LỖI 5:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  VẤN ĐỀ:                                                     │
  │  __SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED          │
  │  → Chỉ tồn tại trên BUILD artifacts!                       │
  │  → Source code KHÔNG có property này!                       │
  │                                                              │
  │  FILE: react/packages/shared/ReactSharedInternals.js         │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ // TRƯỚC:                                            │    │
  │  │ import * as React from 'react';                      │    │
  │  │ const ReactSharedInternals =                         │    │
  │  │   React.__SECRET_INTERNALS_DO_NOT_USE_...;           │    │
  │  │                                                      │    │
  │  │ // SAU: import TRỰC TIẾP từ source!                │    │
  │  │ import ReactSharedInternals from                     │    │
  │  │   'react/src/ReactSharedInternals';                  │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  GIẢI THÍCH:                                                  │
  │  → Trong BUILD: React export object có property dài đó!    │
  │  → Trong SOURCE: internals nằm riêng file!                  │
  │  → Tên dài = cảnh báo: KHÔNG dùng trong app code!          │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

### Lỗi 6: ReactFiberHostConfig

```
  LỖI 6:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  VẤN ĐỀ: "This module must be shimmed by a specific         │
  │  renderer"                                                    │
  │  → File này được THAY THẾ khi Rollup build!                │
  │  → Chạy trực tiếp = throw Error!                           │
  │                                                              │
  │  FILE: react-reconciler/src/ReactFiberHostConfig.js          │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ // TRƯỚC:                                            │    │
  │  │ throw new Error(                                     │    │
  │  │   'This module must be shimmed by a specific renderer'│   │
  │  │ );                                                   │    │
  │  │                                                      │    │
  │  │ // SAU: export từ DOM config!                       │    │
  │  │ export * from './forks/ReactFiberHostConfig.dom';    │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  GIẢI THÍCH:                                                  │
  │  → React hỗ trợ nhiều renderer (DOM, Native, Test...)      │
  │  → Rollup THAY THẾ file này bằng config phù hợp!          │
  │  → Debug trên browser → dùng DOM config!                    │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

---

## §5. Sơ Đồ Tự Vẽ!

### Sơ Đồ 1: So Sánh 2 Phương Pháp

```
  2 PHƯƠNG PHÁP:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  ① OFFICIAL (yarn link):                                     │
  │                                                              │
  │  React Source → yarn build → build/react-dom.development.js │
  │       │                           │                          │
  │       │                     yarn link                        │
  │       │                           │                          │
  │       │                     react-app/node_modules/react     │
  │       │                           │                          │
  │       │                     npm start → DEBUG!              │
  │       │                                                      │
  │       │  Sửa source? → yarn build LẠI → link lại! 😫     │
  │                                                              │
  │  ② WEBPACK ALIAS:                                            │
  │                                                              │
  │  React Source (src/react/packages/*)                          │
  │       │                                                      │
  │       │  webpack alias: 'react' → src/react/packages/react  │
  │       │                                                      │
  │       │  npm start → WEBPACK import TRỰC TIẾP từ source!   │
  │       │                                                      │
  │       │  Sửa source → HOT RELOAD ngay! ✅                  │
  │       │                                                      │
  │       │  ⚠️ Cần fix 6 lỗi (build shims, env vars...)     │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

### Sơ Đồ 2: Webpack Alias Setup Flow

```
  SETUP FLOW:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  npx create-react-app react-app                              │
  │       │                                                      │
  │       ▼                                                      │
  │  cd src && git clone --branch v18.2.0 react                  │
  │       │                                                      │
  │       ▼                                                      │
  │  npm run eject (BẤT KHẢ ĐẢO!)                              │
  │       │                                                      │
  │       ▼                                                      │
  │  Sửa config/webpack.config.js (alias)                        │
  │       │                                                      │
  │       ▼                                                      │
  │  npm start → 142 lỗi → alias thêm shared, reconciler      │
  │       │                                                      │
  │       ▼                                                      │
  │  → 5 lỗi còn lại:                                          │
  │       │                                                      │
  │  ┌── Fix 1: import * as React (named exports!)             │
  │  ├── Fix 2: SchedulerMock (mock functions!)                 │
  │  ├── Fix 3: disableESLintPlugin = true                     │
  │  ├── Fix 4: __DEV__, __EXPERIMENTAL__, __PROFILE__         │
  │  ├── Fix 5: ReactSharedInternals (import trực tiếp!)       │
  │  └── Fix 6: ReactFiberHostConfig.dom (export *)            │
  │       │                                                      │
  │       ▼                                                      │
  │  ✅ npm start → RUNS! Debug React source trực tiếp!       │
  │       │                                                      │
  │       ▼                                                      │
  │  Sửa react/packages/scheduler/src/SchedulerMinHeap.js       │
  │  → Thêm console.log → Browser PRINTS! Hot reload! ✅      │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

### Sơ Đồ 3: React Build vs Source Structure

```
  BUILD vs SOURCE:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  BUILD (react-dom.development.js):                           │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ → 1 file DUY NHẤT chứa TẤT CẢ!                   │    │
  │  │ → Rollup bundle: react-dom + reconciler + shared   │    │
  │  │ → __DEV__ đã được thay bằng true/false!           │    │
  │  │ → ReactFiberHostConfig đã shimmed!                 │    │
  │  │ → __SECRET_INTERNALS_* đã gắn vào object!        │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  SOURCE (react/packages/*):                                  │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ react/packages/                                      │    │
  │  │ ├── react/              (React core!)               │    │
  │  │ ├── react-dom/          (DOM renderer!)             │    │
  │  │ ├── react-reconciler/   (Fiber + Scheduler bridge!) │    │
  │  │ ├── scheduler/          (Task scheduling!)          │    │
  │  │ └── shared/             (Shared utilities!)         │    │
  │  │                                                      │    │
  │  │ → Nhiều files, modules riêng biệt!                │    │
  │  │ → __DEV__ = raw variable (cần DefinePlugin!)       │    │
  │  │ → HostConfig = placeholder (cần export *)          │    │
  │  │ → SharedInternals = separate file!                 │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

---

## §6. Tự Viết — DebugSetupEngine!

```javascript
/**
 * DebugSetupEngine — Mô phỏng quy trình setup debug!
 * Tự viết bằng tay, trace từng bước!
 */
var DebugSetupEngine = (function () {

  // ═══════════════════════════════════
  // 1. WEBPACK ALIAS SIMULATOR
  // ═══════════════════════════════════
  function createAliasConfig(appSrc) {
    return {
      react: appSrc + '/react/packages/react',
      'react-dom': appSrc + '/react/packages/react-dom',
      shared: appSrc + '/react/packages/shared',
      'react-reconciler': appSrc + '/react/packages/react-reconciler'
    };
  }

  function resolveImport(importPath, aliases) {
    for (var alias in aliases) {
      if (importPath === alias || importPath.indexOf(alias + '/') === 0) {
        var resolved = importPath.replace(alias, aliases[alias]);
        return { original: importPath, resolved: resolved, aliased: true };
      }
    }
    return { original: importPath, resolved: importPath, aliased: false };
  }

  // ═══════════════════════════════════
  // 2. ENV VARIABLES SIMULATOR
  // ═══════════════════════════════════
  function createEnvConfig(mode) {
    var base = {
      'process.env': { NODE_ENV: mode }
    };

    // React source needs these!
    if (mode === 'development') {
      base.__DEV__ = true;
      base.__EXPERIMENTAL__ = true;
      base.__PROFILE__ = true;
    } else {
      base.__DEV__ = false;
      base.__EXPERIMENTAL__ = false;
      base.__PROFILE__ = false;
    }
    return base;
  }

  // ═══════════════════════════════════
  // 3. ERROR FIXES CHECKLIST
  // ═══════════════════════════════════
  function getFixChecklist() {
    return [
      {
        error: 'import React from "react" fails',
        file: 'src/index.js',
        fix: 'import * as React from "react"',
        reason: 'Source code uses named exports, no default export!'
      },
      {
        error: 'unstable_yieldValue not found on Scheduler',
        file: 'react-reconciler/src/Scheduler.js',
        fix: 'Import from scheduler/src/forks/SchedulerMock',
        reason: 'These functions only exist on mock scheduler!'
      },
      {
        error: 'ESLint errors on React source',
        file: 'config/webpack.config.js',
        fix: 'const disableESLintPlugin = true',
        reason: 'React source has different lint rules!'
      },
      {
        error: '__DEV__ is not defined',
        file: 'config/env.js',
        fix: 'Add __DEV__: true, __EXPERIMENTAL__: true, __PROFILE__: true',
        reason: 'Rollup replaces these at build time, webpack needs DefinePlugin!'
      },
      {
        error: '__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED',
        file: 'react/packages/shared/ReactSharedInternals.js',
        fix: "import ReactSharedInternals from 'react/src/ReactSharedInternals'",
        reason: 'This property only exists on built artifacts!'
      },
      {
        error: 'This module must be shimmed by a specific renderer',
        file: 'react-reconciler/src/ReactFiberHostConfig.js',
        fix: 'export * from "./forks/ReactFiberHostConfig.dom"',
        reason: 'Rollup swaps this file at build time, we must do it manually!'
      }
    ];
  }

  // ═══════════════════════════════════
  // 4. BUILD vs SOURCE COMPARISON
  // ═══════════════════════════════════
  function compareApproaches() {
    return {
      official: {
        name: 'yarn link (Official)',
        steps: ['git clone', 'yarn build', 'yarn link', 'yarn link react'],
        debugTarget: 'react-dom.development.js (BUILT file!)',
        hotReload: false,
        sourceEdit: 'Need rebuild after every change!',
        setupDifficulty: 'Easy (no errors to fix!)',
        recommended: 'Quick look, no deep editing needed'
      },
      webpackAlias: {
        name: 'Webpack Alias',
        steps: ['git clone v18.2.0', 'eject', 'alias', 'fix 6 errors'],
        debugTarget: 'react/packages/* (SOURCE files!)',
        hotReload: true,
        sourceEdit: 'Edit → instant hot reload!',
        setupDifficulty: 'Medium (6 errors to fix!)',
        recommended: 'Deep source code study!'
      }
    };
  }

  // ═══════════════════════════════════
  // DEMO
  // ═══════════════════════════════════
  function demo() {
    console.log('╔═══════════════════════════════════════════╗');
    console.log('║  DEBUG SETUP ENGINE — DEMO                  ║');
    console.log('╚═══════════════════════════════════════════╝');

    // 1. Alias resolution
    console.log('\n--- 1. WEBPACK ALIAS ---');
    var aliases = createAliasConfig('/app/src');
    var imports = ['react', 'react-dom/client', 'shared/ReactSharedInternals',
      'react-reconciler/src/Scheduler', 'lodash'];
    imports.forEach(function(imp) {
      var result = resolveImport(imp, aliases);
      console.log(imp + ' → ' + result.resolved +
        (result.aliased ? ' ✅' : ' (not aliased)'));
    });

    // 2. Env config
    console.log('\n--- 2. ENV VARIABLES ---');
    var env = createEnvConfig('development');
    console.log('__DEV__:', env.__DEV__);
    console.log('__EXPERIMENTAL__:', env.__EXPERIMENTAL__);
    console.log('__PROFILE__:', env.__PROFILE__);

    // 3. Fix checklist
    console.log('\n--- 3. FIX CHECKLIST (6 errors) ---');
    var fixes = getFixChecklist();
    fixes.forEach(function(f, i) {
      console.log((i+1) + '. ' + f.error);
      console.log('   File: ' + f.file);
      console.log('   Fix: ' + f.fix);
    });

    // 4. Compare
    console.log('\n--- 4. COMPARISON ---');
    var cmp = compareApproaches();
    console.log('Official hot reload:', cmp.official.hotReload);
    console.log('Alias hot reload:', cmp.webpackAlias.hotReload);

    console.log('\n╔═══════════════════════════════════════════╗');
    console.log('║  ✅ Demo Complete!                           ║');
    console.log('╚═══════════════════════════════════════════╝');
  }

  return {
    createAliasConfig: createAliasConfig,
    resolveImport: resolveImport,
    createEnvConfig: createEnvConfig,
    getFixChecklist: getFixChecklist,
    compareApproaches: compareApproaches,
    demo: demo
  };
})();

// Chạy: DebugSetupEngine.demo();
```

---

## §7. Câu Hỏi Luyện Tập!

### ❓ Câu 1: 2 phương pháp debug khác nhau thế nào?

**Trả lời:**

| | Official (yarn link) | Webpack Alias |
|---|---|---|
| **Debug file** | react-dom.development.js (BUILT!) | react/packages/* (SOURCE!) |
| **Hot reload** | ❌ Phải build lại! | ✅ Sửa → refresh ngay! |
| **Setup** | Dễ (build + link) | Trung bình (fix 6 lỗi!) |
| **Phù hợp** | Xem nhanh | Nghiên cứu sâu! |

### ❓ Câu 2: Tại sao import * as React thay vì import React?

**Trả lời:**

Source code React dùng **named exports** (export function, export const)!
- `import React from 'react'` = lấy **default export** → KHÔNG có!
- `import * as React from 'react'` = lấy **TẤT CẢ named exports** vào 1 object!
- Trong build artifacts: Rollup tạo default export → `import React` hoạt động!

### ❓ Câu 3: __DEV__ là gì? Tại sao cần define?

**Trả lời:**

`__DEV__` = **build-time variable** dùng khắp React source!
- Bật dev warnings, dev checks!
- Trong production build: Rollup thay `__DEV__` → `false` → dead code elimination!
- Debug source trực tiếp: webpack KHÔNG biết `__DEV__` → phải define bằng **DefinePlugin**!

### ❓ Câu 4: ReactFiberHostConfig tại sao throw Error?

**Trả lời:**

React hỗ trợ **nhiều renderer**: DOM, Native, Test, Art...
- Mỗi renderer cần **config khác nhau**!
- Rollup **thay thế** file này bằng config phù hợp khi build!
- Chạy trực tiếp = **chưa được thay** → throw Error!
- Fix: `export * from './forks/ReactFiberHostConfig.dom'` → dùng DOM config!

### ❓ Câu 5: __SECRET_INTERNALS là gì?

**Trả lời:**

```javascript
// BUILD artifacts:
React.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED = {
  ReactCurrentDispatcher, ReactCurrentBatchConfig, ...
};

// SOURCE:
// Internals = separate file: react/src/ReactSharedInternals.js
// Tên dài = CẢNH BÁO: đây là internal API, KHÔNG dùng!
```

→ Build: gắn vào React object! Source: import trực tiếp file!

### ❓ Câu 6: Tại sao clone tag v18.2.0 thay vì main?

**Trả lời:**

- **main branch**: code đang phát triển, CÓ THỂ BỊ BREAKING!
- **v18.2.0 tag**: STABLE release, đã test, phù hợp npm version!
- Đảm bảo source code **KHỚP** với version đang dùng!
- Tránh lỗi "source không giống behavior"!

---

> 🎯 **Tổng kết Debug React Source:**
> - **2 phương pháp**: yarn link (quick, built files) vs webpack alias (deep, source files!)
> - **Webpack alias**: trỏ react/react-dom/shared/reconciler → source packages!
> - **6 lỗi cần fix**: import style, SchedulerMock, ESLint, __DEV__, SharedInternals, HostConfig!
> - **npm run eject**: bất khả đảo! Expose webpack config để sửa alias!
> - **Build vs Source**: build = 1 file bundled, source = nhiều modules riêng!
> - **__DEV__**: build-time variable, cần DefinePlugin trong webpack!
> - **ReactFiberHostConfig**: Rollup shims at build, manual export * for debug!
> - **DebugSetupEngine** tự viết: alias resolution, env config, fix checklist!
> - **6 câu hỏi** luyện tập với đáp án chi tiết!
