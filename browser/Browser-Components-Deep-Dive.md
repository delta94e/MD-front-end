# Browser Components — Kiến Trúc & Engine Deep Dive

> 📅 2026-02-11 · ⏱ 15 phút đọc
>
> Tài liệu chuyên sâu về cấu trúc trình duyệt: Shell vs Kernel,
> Rendering Engine vs JS Engine, so sánh Trident/Gecko/Presto/WebKit/Blink,
> và 7 thành phần chính của browser.
> Độ khó: ⭐️⭐️⭐️ | Chủ đề: Browser Architecture

---

## Mục Lục

0. [Tổng quan về Browser](#0-tổng-quan-về-browser)
1. [Browser Engine — Rendering & JS](#1-browser-engine--rendering--js)
2. [So sánh Browser Engines](#2-so-sánh-browser-engines)
3. [Browser nào dùng Engine nào?](#3-browser-nào-dùng-engine-nào)
4. [7 Thành phần chính của Browser](#4-7-thành-phần-chính-của-browser)
5. [Tóm Tắt & Câu Hỏi Phỏng Vấn](#5-tóm-tắt--câu-hỏi-phỏng-vấn)

---

## 0. Tổng quan về Browser

> **🎯 Browser = Shell + Kernel**

```
CHỨC NĂNG CHÍNH CỦA BROWSER:
═══════════════════════════════════════════════════════════════

  → Hiển thị WEB RESOURCES do user chọn
  → Request resources từ server → hiển thị trong browser window
  → Format: HTML, PDF, images, và các format khác
  → User dùng URI (Uniform Resource Identifier) để chỉ định
    vị trí resource

  Quy chuẩn: HTML & CSS specs do W3C duy trì
  Thực tế:   Browser vendors phát triển extensions riêng
             → Không tuân thủ hoàn toàn specs
             → GÂY RA VẤN ĐỀ TƯƠNG THÍCH nghiêm trọng
```

### Shell vs Kernel

```
BROWSER = SHELL + KERNEL:
═══════════════════════════════════════════════════════════════

  ┌───────────────────────────────────────────────────────────┐
  │                     BROWSER                                │
  │                                                           │
  │  ┌─────────────────────────────────────────────────────┐  │
  │  │                  SHELL (Vỏ ngoài)                   │  │
  │  │                                                     │  │
  │  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐    │  │
  │  │  │ Menu │ │ToolBar│ │Address│ │ Back │ │Book- │    │  │
  │  │  │      │ │      │ │  Bar │ │ Fwd  │ │marks │    │  │
  │  │  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘    │  │
  │  │                                                     │  │
  │  │  → User Interface, parameter settings               │  │
  │  │  → GỌI KERNEL để thực hiện các chức năng           │  │
  │  │  → NHIỀU loại shells khác nhau                      │  │
  │  └─────────────────────────────────────────────────────┘  │
  │                         │ gọi                             │
  │                         ▼                                 │
  │  ┌─────────────────────────────────────────────────────┐  │
  │  │                  KERNEL (Nhân)                       │  │
  │  │                                                     │  │
  │  │  → Chương trình/module HIỂN THỊ NỘI DUNG           │  │
  │  │    dựa trên markup language                         │  │
  │  │  → ÍT loại kernels hơn shells                      │  │
  │  │  → Bao gồm: Rendering Engine + JS Engine           │  │
  │  └─────────────────────────────────────────────────────┘  │
  └───────────────────────────────────────────────────────────┘

  💡 Một số browser không phân biệt rõ shell vs kernel
  💡 Phân biệt rõ ràng bắt đầu từ khi Mozilla tách Gecko
```

---

## 1. Browser Engine — Rendering & JS

> **🎯 Kernel = Rendering Engine + JS Engine**

```
BROWSER ENGINE — 2 PHẦN CHÍNH:
═══════════════════════════════════════════════════════════════

  ┌───────────────────────────────────────────────────────────┐
  │                   BROWSER KERNEL                           │
  │                                                           │
  │  ┌─────────────────────────────────────────────────────┐  │
  │  │            RENDERING ENGINE                          │  │
  │  │                                                     │  │
  │  │  Vai trò: RENDERING — hiển thị nội dung            │  │
  │  │                                                     │  │
  │  │  Mặc định hỗ trợ:                                   │  │
  │  │  → HTML documents                                   │  │
  │  │  → XML documents                                    │  │
  │  │  → Images                                           │  │
  │  │                                                     │  │
  │  │  Với plugins:                                        │  │
  │  │  → PDF (qua PDF reader plugin)                      │  │
  │  │  → Các format khác                                  │  │
  │  └─────────────────────────────────────────────────────┘  │
  │                                                           │
  │  ┌─────────────────────────────────────────────────────┐  │
  │  │              JS ENGINE                               │  │
  │  │                                                     │  │
  │  │  Vai trò: PARSE + EXECUTE JavaScript                │  │
  │  │  → Thực hiện DYNAMIC EFFECTS trên web pages        │  │
  │  │                                                     │  │
  │  │  VD: Chrome V8, Firefox SpiderMonkey,               │  │
  │  │      Safari JavaScriptCore                          │  │
  │  └─────────────────────────────────────────────────────┘  │
  └───────────────────────────────────────────────────────────┘

  📌 Ban đầu: Rendering Engine và JS Engine KHÔNG phân biệt rõ
  📌 Sau này: JS Engine ngày càng ĐỘC LẬP
  📌 Hiện tại: "kernel" thường chỉ RENDERING ENGINE
```

---

## 2. So sánh Browser Engines

> **🎯 5 rendering engines chính: Trident, Gecko, Presto, WebKit, Blink**

```
5 BROWSER ENGINES — SO SÁNH CHI TIẾT:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │  ① TRIDENT (IE Kernel)                                   │
  │  ──────────────────────                                  │
  │  Browser: Internet Explorer                              │
  │                                                          │
  │  Lịch sử:                                                │
  │  → IE thị phần LỚN → nhiều web viết theo Trident       │
  │  → NHƯNG không hỗ trợ tốt TRUE web standards           │
  │  → Microsoft KHÔNG cập nhật Trident trong thời gian dài│
  │  → Đi lệch W3C standards                                │
  │  → Nhiều BUGS + SECURITY ISSUES không được sửa         │
  │  → Chuyên gia nói IE không an toàn                      │
  │  → Users chuyển sang browser khác                       │
  │                                                          │
  │  ✅ Ưu: thị phần lớn (lịch sử), tương thích pages cũ  │
  │  ❌ Nhược: chậm cập nhật, nhiều bugs, bảo mật kém     │
  ├──────────────────────────────────────────────────────────┤
  │  ② GECKO (Firefox Kernel)                                │
  │  ─────────────────────────                               │
  │  Browser: Firefox, Flock                                 │
  │                                                          │
  │  ✅ Ưu: MẠNH MẼ, nhiều tính năng                       │
  │     → Hỗ trợ web page effects PHỨC TẠP                 │
  │     → Browser extension interfaces PHONG PHÚ            │
  │  ❌ Nhược: TIÊU TỐN TÀI NGUYÊN nhiều (memory)         │
  ├──────────────────────────────────────────────────────────┤
  │  ③ PRESTO (Opera cũ)                                     │
  │  ────────────────────                                    │
  │  Browser: Opera (trước khi chuyển sang Blink)            │
  │                                                          │
  │  ✅ Ưu: NHANH NHẤT được công nhận!                      │
  │     → Xử lý JS NHANH GẤP 3 LẦN các kernel khác       │
  │  ❌ Nhược: hy sinh TƯƠNG THÍCH để đạt tốc độ           │
  │     → Một số web pages hiển thị sai                     │
  │                                                          │
  │  📌 Đã bị bỏ — Opera chuyển sang Blink                 │
  ├──────────────────────────────────────────────────────────┤
  │  ④ WEBKIT (Safari Kernel)                                │
  │  ────────────────────────                                │
  │  Browser: Safari, Chrome (trước khi fork Blink)          │
  │                                                          │
  │  Nguồn gốc: Fork từ KHTML (KDE group)                  │
  │  → WebKit là open-source fork của KHTML                 │
  │                                                          │
  │  ✅ Ưu: Tốc độ NHANH                                    │
  │     → Không bằng Presto nhưng HƠN Gecko và Trident     │
  │  ❌ Nhược: KHOAN DUNG THẤP với code sai                │
  │     → Web pages viết không chuẩn → hiển thị LỖI        │
  │     → Tương thích kém với non-standard code             │
  ├──────────────────────────────────────────────────────────┤
  │  ⑤ BLINK (Chrome/Opera hiện tại)                          │
  │  ───────────────────────────────                         │
  │  Browser: Chrome, Opera, Edge (Chromium-based)           │
  │                                                          │
  │  Nguồn gốc: FORK từ WebKit                              │
  │  (Như WebKit fork từ KHTML)                              │
  │                                                          │
  │  → Google tách khỏi Apple WebKit                        │
  │  → Phát triển trong Chromium project                    │
  │  → Google + Opera Software cùng phát triển             │
  │  → Opera bỏ Presto → gia nhập Google → dùng Blink     │
  │                                                          │
  │  ✅ Hiện đại nhất, được cập nhật thường xuyên          │
  └──────────────────────────────────────────────────────────┘
```

### Chuỗi kế thừa Engine

```
CHUỖI KẾ THỪA ENGINE (FORK HISTORY):
═══════════════════════════════════════════════════════════════

  KHTML (KDE)
    │
    └──► WebKit (Apple, Safari)
           │
           └──► Blink (Google, Chrome/Opera)

  Trident (Microsoft, IE) ──► EdgeHTML ──► Blink (Edge mới)

  Gecko (Mozilla, Firefox) — vẫn độc lập

  Presto (Opera) — ĐÃ BỎ → chuyển sang Blink
```

### Bảng so sánh nhanh

```
BẢNG SO SÁNH ENGINES:
═══════════════════════════════════════════════════════════════

  ┌──────────┬────────────┬──────────┬──────────┬───────────┐
  │ Engine   │ Tốc độ      │ Tương    │ Tài      │ Trạng     │
  │          │             │ thích    │ nguyên   │ thái      │
  ├──────────┼────────────┼──────────┼──────────┼───────────┤
  │ Trident  │ Chậm       │ Nhiều    │ Trung    │ ĐÃ BỎ    │
  │          │             │ pages cũ │ bình     │ (IE chết) │
  ├──────────┼────────────┼──────────┼──────────┼───────────┤
  │ Gecko    │ Trung bình │ Tốt      │ CAO      │ Active    │
  │          │             │          │ (memory) │ (Firefox) │
  ├──────────┼────────────┼──────────┼──────────┼───────────┤
  │ Presto   │ NHANH NHẤT │ Kém      │ Thấp    │ ĐÃ BỎ    │
  │          │ (JS 3x)    │          │          │           │
  ├──────────┼────────────┼──────────┼──────────┼───────────┤
  │ WebKit   │ Nhanh      │ Trung    │ Trung   │ Active    │
  │          │             │ bình     │ bình     │ (Safari)  │
  ├──────────┼────────────┼──────────┼──────────┼───────────┤
  │ Blink    │ Nhanh      │ Tốt      │ Trung   │ Active    │
  │          │             │          │ bình     │ (Chrome+) │
  └──────────┴────────────┴──────────┴──────────┴───────────┘
```

---

## 3. Browser nào dùng Engine nào?

```
BROWSER → ENGINE MAPPING:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────┬─────────────────────────────────┐
  │ Browser              │ Engine                           │
  ├──────────────────────┼─────────────────────────────────┤
  │ Internet Explorer    │ Trident (IE kernel)              │
  ├──────────────────────┼─────────────────────────────────┤
  │ Chrome               │ WebKit (cũ) → Blink (hiện tại)  │
  ├──────────────────────┼─────────────────────────────────┤
  │ Firefox              │ Gecko                            │
  ├──────────────────────┼─────────────────────────────────┤
  │ Safari               │ WebKit                           │
  ├──────────────────────┼─────────────────────────────────┤
  │ Opera                │ Presto → WebKit → Blink         │
  ├──────────────────────┼─────────────────────────────────┤
  │ Microsoft Edge (mới) │ Blink (Chromium-based)           │
  ├──────────────────────┼─────────────────────────────────┤
  │ 360 Browser          │ IE + Chrome (DUAL kernel)        │
  │ Cheetah Browser      │                                 │
  ├──────────────────────┼─────────────────────────────────┤
  │ Sogou / Maxthon / QQ │ Trident (compatibility mode)     │
  │                      │ + WebKit (high-speed mode)       │
  ├──────────────────────┼─────────────────────────────────┤
  │ Baidu / World Browser│ IE kernel (Trident)              │
  ├──────────────────────┼─────────────────────────────────┤
  │ 2345 Browser         │ IE + Chrome (dual kernel)        │
  ├──────────────────────┼─────────────────────────────────┤
  │ UC Browser           │ U3 kernel (based on              │
  │                      │ WebKit + Trident)                │
  └──────────────────────┴─────────────────────────────────┘

  💡 XU HƯỚNG: Hầu hết browser hiện đại chuyển sang BLINK
     (Chromium-based: Chrome, Edge, Opera, Brave, Vivaldi...)

  💡 DUAL KERNEL: Nhiều browser Trung Quốc dùng 2 kernels
     → Compatibility mode (Trident): mở pages cũ
     → High-speed mode (WebKit/Blink): mở pages hiện đại
```

---

## 4. 7 Thành phần chính của Browser

> **🎯 Browser bao gồm 7 thành phần cốt lõi**

```
7 THÀNH PHẦN CHÍNH CỦA BROWSER:
═══════════════════════════════════════════════════════════════

  ┌───────────────────────────────────────────────────────────┐
  │                                                           │
  │  ┌─────────────────────────────────────────────────────┐  │
  │  │  ① USER INTERFACE (Giao diện người dùng)            │  │
  │  │  → Address bar, Back/Forward buttons                │  │
  │  │  → Bookmarks, menu, tabs...                         │  │
  │  │  → TẤT CẢ phần hiển thị NGOẠI TRỪ page chính     │  │
  │  └──────────────────────┬──────────────────────────────┘  │
  │                         │                                 │
  │  ┌──────────────────────▼──────────────────────────────┐  │
  │  │  ② BROWSER ENGINE (Động cơ trình duyệt)            │  │
  │  │  → TRUYỀN INSTRUCTIONS giữa UI ↔ Rendering Engine │  │
  │  │  → Cầu nối giữa user interface và rendering        │  │
  │  └──────────────────────┬──────────────────────────────┘  │
  │                         │                                 │
  │  ┌──────────────────────▼──────────────────────────────┐  │
  │  │  ③ RENDERING ENGINE (Động cơ dựng hình)             │  │
  │  │  → Hiển thị requested content                       │  │
  │  │  → Parse HTML + CSS → hiển thị trên screen         │  │
  │  │  → VD: Blink (Chrome), WebKit (Safari),            │  │
  │  │        Gecko (Firefox)                              │  │
  │  └─────────────────────────────────────────────────────┘  │
  │                                                           │
  │  ┌─────────────────────────────────────────────────────┐  │
  │  │  ④ NETWORKING (Mạng)                                │  │
  │  │  → Network calls (HTTP requests, etc.)              │  │
  │  │  → Interface PLATFORM-INDEPENDENT                   │  │
  │  │  → Cung cấp underlying implementation cho all       │  │
  │  │    platforms                                        │  │
  │  └─────────────────────────────────────────────────────┘  │
  │                                                           │
  │  ┌─────────────────────────────────────────────────────┐  │
  │  │  ⑤ UI BACKEND (Backend giao diện)                   │  │
  │  │  → Vẽ BASIC WINDOW WIDGETS (combo boxes, windows)  │  │
  │  │  → Expose GENERIC INTERFACES (platform-independent)│  │
  │  │  → Underlying: dùng OS user interface methods      │  │
  │  └─────────────────────────────────────────────────────┘  │
  │                                                           │
  │  ┌─────────────────────────────────────────────────────┐  │
  │  │  ⑥ JS INTERPRETER (Trình thông dịch JS)            │  │
  │  │  → Parse + execute JavaScript code                  │  │
  │  │  → VD: V8 (Chrome), SpiderMonkey (Firefox),        │  │
  │  │        JavaScriptCore (Safari)                      │  │
  │  └─────────────────────────────────────────────────────┘  │
  │                                                           │
  │  ┌─────────────────────────────────────────────────────┐  │
  │  │  ⑦ DATA STORAGE (Lưu trữ dữ liệu)                 │  │
  │  │  → Persistence layer                                │  │
  │  │  → Lưu trên hard drive: cookies, localStorage,    │  │
  │  │    sessionStorage, IndexedDB                        │  │
  │  │  → HTML5 = "web database" (lightweight in-browser  │  │
  │  │    database)                                        │  │
  │  └─────────────────────────────────────────────────────┘  │
  └───────────────────────────────────────────────────────────┘

  📌 CHROME ĐẶC BIỆT:
  Mỗi TAB = 1 rendering engine INSTANCE riêng biệt
  Mỗi TAB = 1 PROCESS ĐỘC LẬP
  (Khác biệt so với hầu hết browsers khác)
```

### Kiến trúc tổng thể

```
KIẾN TRÚC TỔNG THỂ — FLOW:
═══════════════════════════════════════════════════════════════

  User
   │
   ▼
  ┌──────────────────────────────────────────────────────┐
  │  ① USER INTERFACE                                    │
  │  [Address Bar] [← →] [⭐] [≡ Menu] [Tabs]          │
  └────────────────────────┬─────────────────────────────┘
                           │
  ┌────────────────────────▼─────────────────────────────┐
  │  ② BROWSER ENGINE                                    │
  │  (Bridge: UI ↔ Rendering)                           │
  └──────────┬─────────────┬──────────────┬──────────────┘
             │             │              │
  ┌──────────▼──────┐ ┌────▼─────┐ ┌─────▼──────────┐
  │ ③ RENDERING     │ │ ④ NET-   │ │ ⑥ JS           │
  │    ENGINE       │ │   WORKING│ │   INTERPRETER   │
  │                 │ │          │ │                  │
  │ HTML → DOM      │ │ HTTP     │ │ Parse + Execute │
  │ CSS → CSSOM     │ │ requests │ │ JavaScript      │
  │ → Render Tree   │ │          │ │                  │
  │ → Layout/Paint  │ │          │ │ V8 / Spider-    │
  │                 │ │          │ │ Monkey / JSCore  │
  └────────┬────────┘ └──────────┘ └─────────────────┘
           │
  ┌────────▼────────┐              ┌─────────────────┐
  │ ⑤ UI BACKEND   │              │ ⑦ DATA STORAGE  │
  │                 │              │                  │
  │ Draw widgets    │              │ Cookies          │
  │ OS-level render │              │ localStorage     │
  │                 │              │ IndexedDB        │
  └─────────────────┘              └─────────────────┘
```

---

## 5. Tóm Tắt & Câu Hỏi Phỏng Vấn

### Quick Reference

```
BROWSER COMPONENTS — QUICK REFERENCE:
═══════════════════════════════════════════════════════════════

  BROWSER = SHELL (vỏ ngoài) + KERNEL (nhân)
  KERNEL  = RENDERING ENGINE + JS ENGINE

  5 ENGINES:
    Trident — IE (đã chết)
    Gecko   — Firefox (tốn memory)
    Presto  — Opera cũ (nhanh nhất, đã bỏ)
    WebKit  — Safari (fork từ KHTML)
    Blink   — Chrome/Opera/Edge (fork từ WebKit)

  FORK CHAIN: KHTML → WebKit → Blink

  7 THÀNH PHẦN:
    ① User Interface     — UI ngoài page
    ② Browser Engine     — cầu nối UI ↔ Rendering
    ③ Rendering Engine   — parse HTML/CSS, hiển thị
    ④ Networking         — HTTP requests
    ⑤ UI Backend         — vẽ widgets (OS-level)
    ⑥ JS Interpreter     — parse + execute JS
    ⑦ Data Storage       — cookies, localStorage, IndexedDB

  CHROME: mỗi tab = 1 rendering instance = 1 process
```

### Câu Hỏi Phỏng Vấn Thường Gặp

**1. Browser được chia thành những phần nào?**

> Browser chia thành **Shell** (vỏ ngoài: menu, toolbar, address bar — cung cấp UI cho user) và **Kernel** (nhân: rendering engine + JS engine — xử lý nội dung). Kernel ban đầu gồm cả rendering và JS engine, nhưng JS engine ngày càng độc lập nên "kernel" hiện tại thường chỉ rendering engine.

**2. Rendering Engine và JS Engine khác nhau thế nào?**

> **Rendering Engine**: parse HTML/CSS → DOM/CSSOM → Render Tree → hiển thị trên screen. Mặc định hỗ trợ HTML, XML, images; với plugins hỗ trợ thêm PDF, etc. **JS Engine**: parse + execute JavaScript → tạo dynamic effects. VD: V8 (Chrome), SpiderMonkey (Firefox), JavaScriptCore (Safari).

**3. Kể tên các browser engine phổ biến?**

> **Trident** (IE — đã chết, không tuân thủ W3C), **Gecko** (Firefox — mạnh nhưng tốn memory), **Presto** (Opera cũ — nhanh nhất nhưng tương thích kém, đã bỏ), **WebKit** (Safari — fork từ KHTML, nhanh), **Blink** (Chrome/Opera/Edge — fork từ WebKit, hiện đại nhất). Chuỗi kế thừa: KHTML → WebKit → Blink.

**4. Chrome dùng engine gì?**

> Chrome dùng **Blink** (rendering engine, fork từ WebKit) và **V8** (JS engine). Trước đây Chrome dùng WebKit, năm 2013 Google tách ra phát triển Blink trong Chromium project. Opera bỏ Presto và cùng Google phát triển Blink.

**5. Tại sao nhiều browser Trung Quốc dùng dual kernel?**

> Dual kernel = **Trident** (compatibility mode: mở pages cũ viết cho IE) + **WebKit/Blink** (high-speed mode: mở pages hiện đại). VD: 360, QQ, Sogou, 2345. Lý do: nhiều hệ thống nội bộ/chính phủ Trung Quốc chỉ hỗ trợ IE → cần Trident; nhưng user muốn tốc độ cho web hiện đại → cần WebKit/Blink.

**6. Browser có bao nhiêu thành phần chính? Kể tên.**

> 7 thành phần: ① **User Interface** (address bar, buttons, bookmarks), ② **Browser Engine** (cầu nối UI ↔ rendering), ③ **Rendering Engine** (parse HTML/CSS, hiển thị), ④ **Networking** (HTTP requests, platform-independent), ⑤ **UI Backend** (vẽ widgets, dùng OS methods), ⑥ **JS Interpreter** (parse + execute JS), ⑦ **Data Storage** (cookies, localStorage, IndexedDB — persistence layer).

**7. Chrome có gì đặc biệt so với browser khác?**

> Chrome mỗi **tab = 1 rendering engine instance riêng = 1 process độc lập**. Hầu hết browsers khác chia sẻ rendering engine giữa các tabs. Ưu: tab crash không ảnh hưởng tabs khác (stability + security). Nhược: tiêu tốn nhiều memory hơn.

---

## Checklist Học Tập

- [ ] Phân biệt Shell vs Kernel của browser
- [ ] Hiểu Rendering Engine vs JS Engine
- [ ] Biết 5 browser engines (Trident, Gecko, Presto, WebKit, Blink)
- [ ] Biết chuỗi kế thừa: KHTML → WebKit → Blink
- [ ] Biết browser nào dùng engine nào (10+ browsers)
- [ ] Hiểu dual kernel và lý do sử dụng
- [ ] Kể tên 7 thành phần chính của browser
- [ ] Hiểu vai trò từng thành phần
- [ ] Biết Chrome mỗi tab = 1 rendering instance = 1 process

---

_Cập nhật lần cuối: Tháng 2, 2026_
