# JavaScript Function Currying — Deep Dive!

> **Chủ đề**: Currying (Kỹ thuật Curry hóa hàm)
> **Ngôn ngữ**: Tiếng Việt — giải thích cực kỳ chi tiết!
> **Phương châm**: Tự viết lại bằng tay — KHÔNG dùng thư viện!
> **Nguồn**: Bài phỏng vấn — "Have you ever used function currying in actual development?"

---

## Mục Lục

1. [§1. Currying là gì? — Định Nghĩa Cốt Lõi](#1)
2. [§2. So Sánh: Hàm Thường vs Hàm Curry](#2)
3. [§3. Tự Viết — Hàm curry() Tổng Quát](#3)
4. [§4. Giá Trị Cốt Lõi — Tại Sao Dùng Currying?](#4)
5. [§5. Hiểu Lầm Phổ Biến — Misconceptions!](#5)
6. [§6. Sơ Đồ Tự Vẽ — Tổng Hợp](#6)
7. [§7. Tự Viết — CurryEngine Nâng Cao](#7)
8. [§8. Câu Hỏi Luyện Tập](#8)

---

## §1. Currying là gì? — Định Nghĩa Cốt Lõi!

```
  CURRYING — ĐỊNH NGHĨA:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  ĐỊNH NGHĨA CHÍNH XÁC:                                       │
  │  Currying = biến đổi 1 hàm nhận NHIỀU tham số               │
  │  → thành chuỗi các hàm, MỖI HÀM nhận 1 tham số,           │
  │  → liên tục RETURN hàm mới cho đến khi đủ tham số          │
  │  → mới THỰC THI logic cuối cùng và trả kết quả!            │
  │                                                              │
  │  NÓI ĐƠN GIẢN:                                               │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ TRƯỚC: truyền TẤT CẢ params 1 lần → nhận kết quả  │    │
  │  │ SAU:   truyền TỪNG PHẦN params → return hàm mới    │    │
  │  │        → tiếp tục truyền → return hàm mới          │    │
  │  │        → đến khi ĐỦ params → THỰC THI!             │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  CÔNG THỨC:                                                   │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ f(a, b, c) → f(a)(b)(c)                            │    │
  │  │                                                      │    │
  │  │ Hàm 3 params → 3 hàm lồng nhau, mỗi hàm 1 param! │    │
  │  │                                                      │    │
  │  │ NHƯNG! Không bắt buộc truyền từng cái một!          │    │
  │  │ → f(a, b)(c) cũng OK!                              │    │
  │  │ → f(a)(b, c) cũng OK!                              │    │
  │  │ → f(a, b, c) cũng OK! (1 lần xong luôn!)          │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  TỪ KHÓA GHI NHỚ:                                            │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ 1. Batch parameter passing (truyền params theo đợt)│    │
  │  │ 2. Delayed execution (trì hoãn thực thi)           │    │
  │  │ 3. Closure (đóng gói —  nhớ params đã truyền)      │    │
  │  │ 4. Recursive check (kiểm tra đệ quy đủ params?)   │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

---

## §2. So Sánh: Hàm Thường vs Hàm Curry!

```
  SO SÁNH:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  HÀM THƯỜNG:                        HÀM CURRY:              │
  │  ┌────────────────────────┐   ┌──────────────────────────┐  │
  │  │ function add(a, b, c) {│   │ function curriedAdd(a) { │  │
  │  │   return a + b + c;    │   │   return function(b) {   │  │
  │  │ }                      │   │     return function(c) { │  │
  │  │                        │   │       return a + b + c;  │  │
  │  │ add(1, 2, 3) // → 6   │   │     };                   │  │
  │  │                        │   │   };                     │  │
  │  │ → 1 lần gọi!          │   │ }                        │  │
  │  │ → Truyền HẾT params!  │   │                          │  │
  │  │ → Nhận kết quả NGAY!  │   │ curriedAdd(1)(2)(3) // 6 │  │
  │  │                        │   │                          │  │
  │  └────────────────────────┘   │ → 3 lần gọi!            │  │
  │                               │ → Mỗi lần 1 param!      │  │
  │                               │ → Return hàm mới!       │  │
  │                               │ → Lần cuối = kết quả!   │  │
  │                               └──────────────────────────┘  │
  │                                                              │
  │  TRACE EXECUTION:                                             │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ curriedAdd(1)                                        │    │
  │  │   │                                                  │    │
  │  │   ▼ a = 1, return function(b) { ... }               │    │
  │  │   │                                                  │    │
  │  │   (2) ← gọi hàm returned                           │    │
  │  │   │                                                  │    │
  │  │   ▼ b = 2, return function(c) { ... }               │    │
  │  │   │                                                  │    │
  │  │   (3) ← gọi hàm returned                           │    │
  │  │   │                                                  │    │
  │  │   ▼ c = 3, return 1 + 2 + 3 = 6  ✅                │    │
  │  │                                                      │    │
  │  │ → Mỗi lần gọi = tạo CLOSURE mới!                  │    │
  │  │ → Closure GHI NHỚ params trước đó!                 │    │
  │  │ → Khi đủ params → thực thi logic!                  │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

---

## §3. Tự Viết — Hàm curry() Tổng Quát!

```
  HÀM CURRY TỰ VIẾT:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  LOGIC:                                                       │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ 1. Lưu SỐ LƯỢNG params gốc ( fn.length )           │    │
  │  │ 2. Tạo hàm curried nhận ...args (rest params)      │    │
  │  │ 3. NẾU args.length >= fn.length → THỰC THI!        │    │
  │  │ 4. NẾU CHƯA ĐỦ → return hàm mới, nối thêm args!  │    │
  │  │ 5. Lặp lại cho đến khi đủ!                         │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

```javascript
// ═══════════════════════════════════════════════
// HÀM CURRY TỔNG QUÁT — TỰ VIẾT BẰNG TAY!
// ═══════════════════════════════════════════════

function curry(fn) {
  // Bước 1: Lưu số lượng params của hàm gốc
  var argsLength = fn.length;

  // Bước 2: Hàm đệ quy nhận params
  function curried() {
    // Thu thập args hiện tại
    var args = Array.prototype.slice.call(arguments);

    // Bước 3: ĐÃ ĐỦ params? → Thực thi hàm gốc!
    if (args.length >= argsLength) {
      return fn.apply(this, args);
    }

    // Bước 4: CHƯA ĐỦ? → Return hàm mới, nối thêm args!
    var self = this;
    return function () {
      var newArgs = Array.prototype.slice.call(arguments);
      // GỘP args cũ + args mới → gọi lại curried!
      return curried.apply(self, args.concat(newArgs));
    };
  }

  return curried;
}

// ═══════════════════════════════════════════════
// TEST — 4 CÁCH TRUYỀN PARAMS!
// ═══════════════════════════════════════════════

var add = function (a, b, c) {
  return a + b + c;
};
var curriedAdd = curry(add);

console.log(curriedAdd(1)(2)(3)); // 6 — từng cái một!
console.log(curriedAdd(1, 2)(3)); // 6 — 2 + 1!
console.log(curriedAdd(1)(2, 3)); // 6 — 1 + 2!
console.log(curriedAdd(1, 2, 3)); // 6 — tất cả 1 lần!
```

```
  TRACE — curriedAdd(1, 2)(3):
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  curry(add) được gọi                                         │
  │  → argsLength = 3 (add cần 3 params)                        │
  │  → return curried (hàm đệ quy)                              │
  │                                                              │
  │  curriedAdd(1, 2)                                             │
  │  → args = [1, 2]                                             │
  │  → args.length (2) < argsLength (3) → CHƯA ĐỦ!             │
  │  → return hàm mới (closure giữ [1, 2])                      │
  │                                                              │
  │  (3)  ← gọi hàm mới với 3                                   │
  │  → newArgs = [3]                                             │
  │  → gọi curried([1, 2, 3])  (concat!)                        │
  │  → args.length (3) >= argsLength (3) → ĐỦ!                 │
  │  → fn.apply(this, [1, 2, 3])                                │
  │  → 1 + 2 + 3 = 6 ✅                                         │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

---

## §4. Giá Trị Cốt Lõi — Tại Sao Dùng Currying?

```
  3 GIÁ TRỊ CỐT LÕI:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  ① PARAMETER REUSE (Tái sử dụng tham số!)                   │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ VẤN ĐỀ: Mỗi lần gọi đều phải truyền lại cùng      │    │
  │  │ tham số (ví dụ: tax rate luôn là 10%)               │    │
  │  │                                                      │    │
  │  │ GIẢI PHÁP: Curry hóa → FIX tham số cố định!       │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

```javascript
// ═══════════════════════════════════════════════
// GIÁ TRỊ 1: PARAMETER REUSE
// ═══════════════════════════════════════════════

// Hàm gốc: tính giá sau thuế (giá × (1 + thuế suất))
var calculateTax = function (taxRate, price) {
  return price * (1 + taxRate);
};

// Curry hóa → fix thuế suất 10%!
var calculateTax10 = curry(calculateTax)(0.1);

// Giờ chỉ cần truyền giá, KHÔNG cần lặp lại thuế suất!
console.log(calculateTax10(100)); // 110
console.log(calculateTax10(200)); // 220
console.log(calculateTax10(500)); // 550

// → taxRate = 0.1 được "FIX" trong closure!
// → Tái sử dụng! Không duplicate!
```

```
  PARAMETER REUSE — SƠ ĐỒ:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  TRƯỚC (không curry):                                         │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ calculateTax(0.1, 100)  // lặp 0.1                 │    │
  │  │ calculateTax(0.1, 200)  // lặp 0.1                 │    │
  │  │ calculateTax(0.1, 500)  // lặp 0.1                 │    │
  │  │            ^^^^ DRY violation! Lặp lại mãi!        │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  SAU (curry):                                                 │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ var tax10 = curry(calculateTax)(0.1) // fix 1 lần! │    │
  │  │ tax10(100)  // 110 — chỉ truyền giá!              │    │
  │  │ tax10(200)  // 220 — clean!                        │    │
  │  │ tax10(500)  // 550 — no duplicate!                 │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

```
  ② DELAYED EXECUTION (Trì hoãn thực thi!):
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  VẤN ĐỀ: Thu thập params TỪNG BƯỚC trước khi thực thi!     │
  │  → Form nhiều bước: step1 → step2 → step3 → submit!      │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

```javascript
// ═══════════════════════════════════════════════
// GIÁ TRỊ 2: DELAYED EXECUTION
// ═══════════════════════════════════════════════

var submitForm = function (name, phone, address) {
  console.log("Nộp form: " + name + " " + phone + " " + address);
};
var curriedSubmit = curry(submitForm);

// Bước 1: Thu thập tên → CHƯA thực thi!
var step1 = curriedSubmit("Nguyễn Văn A");

// Bước 2: Thu thập SĐT → CHƯA thực thi!
var step2 = step1("0909123456");

// Bước 3: Thu thập địa chỉ → ĐỦ 3 params → THỰC THI!
step2("Hà Nội");
// → Output: "Nộp form: Nguyễn Văn A 0909123456 Hà Nội"
```

```
  DELAYED EXECUTION — SƠ ĐỒ:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  STEP 1                STEP 2                STEP 3          │
  │  ┌──────────┐         ┌──────────┐         ┌──────────┐    │
  │  │ Nhập tên │ ──────→ │ Nhập SĐT │ ──────→ │ Nhập     │    │
  │  │ "Nguyễn  │ return  │ "0909..  │ return  │ địa chỉ │    │
  │  │  Văn A"  │ hàm mới │          │ hàm mới │ "Hà Nội" │    │
  │  └──────────┘         └──────────┘         └──────────┘    │
  │                                                   │          │
  │  CHƯA                  CHƯA                       ▼          │
  │  thực thi!             thực thi!            THỰC THI!       │
  │                                             ĐỦ 3 params!   │
  │                                             → Submit form! │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

```
  ③ ADAPTING FUNCTION PARAMETERS (Thích ứng tham số!):
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  VẤN ĐỀ: Một số API chỉ chấp nhận hàm 1 tham số!          │
  │  → Array.map(callback) → callback chỉ nhận 1 element!     │
  │  → React HOC → thường nhận 1 component!                    │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

```javascript
// ═══════════════════════════════════════════════
// GIÁ TRỊ 3: ADAPTING FUNCTION PARAMETERS
// ═══════════════════════════════════════════════

// Hàm gốc: nhân với bội số chỉ định
var multiply = function (multiplier, num) {
  return num * multiplier;
};
var curriedMultiply = curry(multiply);

// Fix bội số = 2 → tạo hàm "double" 1 tham số!
var double = curriedMultiply(2);

// Bây giờ "double" chỉ nhận 1 param → PERFECT cho .map()!
console.log([1, 2, 3].map(double)); // [2, 4, 6]

// Fix bội số = 3 → tạo hàm "triple"!
var triple = curriedMultiply(3);
console.log([1, 2, 3].map(triple)); // [3, 6, 9]

// → multiply(2, num) cần 2 params → KHÔNG fit map()!
// → double(num) chỉ 1 param → FIT map()!
```

```
  ADAPTING — SƠ ĐỒ:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  multiply(multiplier, num) → CẦN 2 params!                 │
  │        │                                                     │
  │        ▼ curry + fix multiplier = 2                          │
  │  double(num) → CHỈ CẦN 1 param!                            │
  │        │                                                     │
  │        ▼ fit .map() callback!                                │
  │  [1, 2, 3].map(double) → [2, 4, 6] ✅                      │
  │                                                              │
  │  ┌──────────┐     ┌──────────┐     ┌──────────┐            │
  │  │ map(1)   │     │ map(2)   │     │ map(3)   │            │
  │  │ double(1)│     │ double(2)│     │ double(3)│            │
  │  │ = 2×1    │     │ = 2×2    │     │ = 2×3    │            │
  │  │ = 2      │     │ = 4      │     │ = 6      │            │
  │  └──────────┘     └──────────┘     └──────────┘            │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

---

## §5. Hiểu Lầm Phổ Biến — Misconceptions!

```
  HIỂU LẦM PHỔ BIẾN:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  ❌ HIỂU LẦM 1:                                              │
  │  "Currying = chia hàm thành các hàm chỉ nhận 1 param,      │
  │   BẮT BUỘC phải chain (a)(b)(c)"                            │
  │                                                              │
  │  ✅ SỰ THẬT:                                                  │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ Core = "batch param passing + delayed execution"!   │    │
  │  │ → Hỗ trợ BẤT KỲ cách chia đợt nào!               │    │
  │  │   (a)(b)(c) ← OK!                                  │    │
  │  │   (a, b)(c) ← OK!                                  │    │
  │  │   (a)(b, c) ← OK!                                  │    │
  │  │   (a, b, c) ← OK! (cả 1 lần luôn!)               │    │
  │  │ → KHÔNG bắt buộc truyền từng cái một!              │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  │  ═══════════════════════════════════════════════════════════  │
  │                                                              │
  │  ❌ HIỂU LẦM 2:                                              │
  │  "Currying cải thiện PERFORMANCE"                            │
  │                                                              │
  │  ✅ SỰ THẬT:                                                  │
  │  ┌──────────────────────────────────────────────────────┐    │
  │  │ Currying KHÔNG cải thiện performance!                │    │
  │  │ → Thêm 1 lớp function nesting!                     │    │
  │  │ → Thêm closures!                                    │    │
  │  │ → Slight performance PENALTY!                       │    │
  │  │                                                      │    │
  │  │ GIÁ TRỊ THẬT SỰ:                                    │    │
  │  │ → Code REUSABILITY (tái sử dụng!)                  │    │
  │  │ → Code READABILITY (dễ đọc!)                       │    │
  │  │ → KHÔNG phải tốc độ!                               │    │
  │  └──────────────────────────────────────────────────────┘    │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

---

## §6. Sơ Đồ Tự Vẽ — Tổng Hợp!

### Sơ Đồ 1: Currying Flow — Từ Đầu Tới Cuối

```
  CURRYING COMPLETE FLOW:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  HÀM GỐC:   add(a, b, c) → cần 3 params                   │
  │       │                                                      │
  │       ▼                                                      │
  │  curry(add) → lưu argsLength = 3                            │
  │       │                                                      │
  │       ▼                                                      │
  │  return curried (hàm đệ quy)                                │
  │       │                                                      │
  │  ═════╧═══════════════════════════════════════════════════   │
  │                                                              │
  │  GỌI: curried(1)                                             │
  │       │                                                      │
  │       ▼                                                      │
  │  args = [1], length 1 < 3 → CHƯA ĐỦ!                      │
  │       │                                                      │
  │       ▼                                                      │
  │  return function(...newArgs) {                               │
  │    return curried([1, ...newArgs])  ← CLOSURE giữ [1]!     │
  │  }                                                           │
  │       │                                                      │
  │  ═════╧═══════════════════════════════════════════════════   │
  │                                                              │
  │  GỌI: (2)                                                    │
  │       │                                                      │
  │       ▼                                                      │
  │  newArgs = [2], concat → curried([1, 2])                    │
  │  args = [1, 2], length 2 < 3 → CHƯA ĐỦ!                   │
  │       │                                                      │
  │       ▼                                                      │
  │  return function(...newArgs) {                               │
  │    return curried([1, 2, ...newArgs]) ← CLOSURE giữ [1,2]! │
  │  }                                                           │
  │       │                                                      │
  │  ═════╧═══════════════════════════════════════════════════   │
  │                                                              │
  │  GỌI: (3)                                                    │
  │       │                                                      │
  │       ▼                                                      │
  │  newArgs = [3], concat → curried([1, 2, 3])                 │
  │  args = [1, 2, 3], length 3 >= 3 → ĐỦ RỒI!                │
  │       │                                                      │
  │       ▼                                                      │
  │  fn.apply(this, [1, 2, 3]) → 1 + 2 + 3 = 6 ✅              │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

### Sơ Đồ 2: Closure Chain

```
  CLOSURE CHAIN:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  curry(add)                                                   │
  │  ┌─────────────────────────────────────────────────────────┐ │
  │  │ Scope: argsLength = 3, fn = add                        │ │
  │  │                                                         │ │
  │  │  curried(1)                                             │ │
  │  │  ┌───────────────────────────────────────────────────┐  │ │
  │  │  │ Scope: args = [1]                                 │  │ │
  │  │  │ Closure: argsLength, fn                           │  │ │
  │  │  │                                                   │  │ │
  │  │  │  (2)                                              │  │ │
  │  │  │  ┌─────────────────────────────────────────────┐  │  │ │
  │  │  │  │ Scope: newArgs = [2]                        │  │  │ │
  │  │  │  │ Closure: args=[1], argsLength, fn           │  │  │ │
  │  │  │  │ → concat → [1, 2] → curried([1, 2])       │  │  │ │
  │  │  │  │                                             │  │  │ │
  │  │  │  │  (3)                                        │  │  │ │
  │  │  │  │  ┌───────────────────────────────────────┐  │  │  │ │
  │  │  │  │  │ Scope: newArgs = [3]                  │  │  │  │ │
  │  │  │  │  │ Closure: args=[1,2], argsLength, fn   │  │  │  │ │
  │  │  │  │  │ → concat → [1,2,3] → ĐỦ!           │  │  │  │ │
  │  │  │  │  │ → fn(1, 2, 3) = 6 ✅                 │  │  │  │ │
  │  │  │  │  └───────────────────────────────────────┘  │  │  │ │
  │  │  │  └─────────────────────────────────────────────┘  │  │ │
  │  │  └───────────────────────────────────────────────────┘  │ │
  │  └─────────────────────────────────────────────────────────┘ │
  │                                                              │
  │  → Mỗi lần gọi = tạo closure MỚI!                         │
  │  → Closure GHI NHỚ tất cả args đã truyền!                 │
  │  → Khi đủ → unwrap tất cả → gọi fn gốc!                  │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

### Sơ Đồ 3: 3 Giá Trị Cốt Lõi

```
  3 GIÁ TRỊ — TÓM TẮT:
  ┌──────────────────────────────────────────────────────────────┐
  │                                                              │
  │  ┌────────────────┐  ┌────────────────┐  ┌───────────────┐ │
  │  │ ① PARAM REUSE │  │ ② DELAY EXEC  │  │ ③ ADAPT ARGS │ │
  │  │                │  │                │  │               │ │
  │  │ Fix tax = 10%  │  │ Form steps:   │  │ multiply →   │ │
  │  │ → tax10(price)│  │ step1("name") │  │ double(num)  │ │
  │  │ Không lặp lại!│  │ step2("phone")│  │ → fit .map()│ │
  │  │                │  │ step3("addr") │  │               │ │
  │  │ DRY principle! │  │ → SUBMIT!    │  │ 2→1 params!  │ │
  │  └────────────────┘  └────────────────┘  └───────────────┘ │
  │                                                              │
  │  CÔNG CỤ CHUNG: Closure + Recursive check!                  │
  │                                                              │
  └──────────────────────────────────────────────────────────────┘
```

---

## §7. Tự Viết — CurryEngine Nâng Cao!

```javascript
/**
 * CurryEngine — Mô phỏng Currying System hoàn chỉnh!
 * Tự viết bằng tay, KHÔNG dùng thư viện nào!
 * Bao gồm: curry, partial, compose, pipe
 */
var CurryEngine = (function () {
  // ═══════════════════════════════════
  // 1. CURRY — Hàm curry tổng quát
  // ═══════════════════════════════════
  function curry(fn) {
    var argsLength = fn.length;
    function curried() {
      var args = Array.prototype.slice.call(arguments);
      if (args.length >= argsLength) {
        return fn.apply(this, args);
      }
      var self = this;
      return function () {
        var newArgs = Array.prototype.slice.call(arguments);
        return curried.apply(self, args.concat(newArgs));
      };
    }
    return curried;
  }

  // ═══════════════════════════════════
  // 2. PARTIAL APPLICATION (liên quan curry!)
  // Partial = fix MỘT SỐ params, return hàm mới
  // ═══════════════════════════════════
  function partial(fn) {
    var fixedArgs = Array.prototype.slice.call(arguments, 1);
    return function () {
      var remainingArgs = Array.prototype.slice.call(arguments);
      return fn.apply(this, fixedArgs.concat(remainingArgs));
    };
  }

  // ═══════════════════════════════════
  // 3. COMPOSE — Kết hợp nhiều hàm (phải → trái)
  // compose(f, g, h)(x) = f(g(h(x)))
  // ═══════════════════════════════════
  function compose() {
    var fns = Array.prototype.slice.call(arguments);
    return function (x) {
      return fns.reduceRight(function (acc, fn) {
        return fn(acc);
      }, x);
    };
  }

  // ═══════════════════════════════════
  // 4. PIPE — Kết hợp nhiều hàm (trái → phải)
  // pipe(f, g, h)(x) = h(g(f(x)))
  // ═══════════════════════════════════
  function pipe() {
    var fns = Array.prototype.slice.call(arguments);
    return function (x) {
      return fns.reduce(function (acc, fn) {
        return fn(acc);
      }, x);
    };
  }

  // ═══════════════════════════════════
  // 5. TRACE — Debug curried function calls
  // ═══════════════════════════════════
  function traceCurry(fn, name) {
    var argsLength = fn.length;
    var callCount = 0;

    function curried() {
      var args = Array.prototype.slice.call(arguments);
      callCount++;
      var step = callCount;

      console.log(
        "Step " +
          step +
          ": " +
          name +
          "(" +
          args.join(", ") +
          ")" +
          " — args so far: " +
          args.length +
          "/" +
          argsLength,
      );

      if (args.length >= argsLength) {
        var result = fn.apply(this, args);
        console.log("→ ĐỦ PARAMS! Result: " + result + " ✅");
        return result;
      }

      console.log("→ CHƯA ĐỦ! Return hàm mới...");
      var self = this;
      return function () {
        var newArgs = Array.prototype.slice.call(arguments);
        return curried.apply(self, args.concat(newArgs));
      };
    }
    return curried;
  }

  // ═══════════════════════════════════
  // DEMO
  // ═══════════════════════════════════
  function demo() {
    console.log("╔═══════════════════════════════════════╗");
    console.log("║  CURRY ENGINE — DEMO                    ║");
    console.log("╚═══════════════════════════════════════╝");

    // 1. Basic curry
    console.log("\n--- 1. CURRY CƠ BẢN ---");
    var add = function (a, b, c) {
      return a + b + c;
    };
    var c = curry(add);
    console.log("c(1)(2)(3):", c(1)(2)(3));
    console.log("c(1,2)(3):", c(1, 2)(3));
    console.log("c(1)(2,3):", c(1)(2, 3));

    // 2. Param reuse
    console.log("\n--- 2. PARAM REUSE ---");
    var tax = function (rate, price) {
      return price * (1 + rate);
    };
    var tax10 = curry(tax)(0.1);
    console.log("tax10(100):", tax10(100));
    console.log("tax10(200):", tax10(200));

    // 3. Adapting for .map()
    console.log("\n--- 3. ADAPT CHO MAP ---");
    var mul = function (m, n) {
      return n * m;
    };
    var double = curry(mul)(2);
    console.log("[1,2,3].map(double):", [1, 2, 3].map(double));

    // 4. Partial application
    console.log("\n--- 4. PARTIAL ---");
    var greet = function (greeting, name) {
      return greeting + ", " + name + "!";
    };
    var hello = partial(greet, "Xin chào");
    console.log(hello("Nguyễn Văn A"));
    console.log(hello("Trần Thị B"));

    // 5. Compose + Pipe
    console.log("\n--- 5. COMPOSE + PIPE ---");
    var addOne = function (x) {
      return x + 1;
    };
    var mulTwo = function (x) {
      return x * 2;
    };
    var subThree = function (x) {
      return x - 3;
    };

    var composed = compose(subThree, mulTwo, addOne);
    console.log("compose(sub3, mul2, add1)(5):", composed(5));
    // → add1(5)=6 → mul2(6)=12 → sub3(12)=9

    var piped = pipe(addOne, mulTwo, subThree);
    console.log("pipe(add1, mul2, sub3)(5):", piped(5));
    // → add1(5)=6 → mul2(6)=12 → sub3(12)=9

    // 6. Trace (debug)
    console.log("\n--- 6. TRACE ---");
    var tracedAdd = traceCurry(add, "add");
    tracedAdd(1)(2)(3);

    console.log("\n╔═══════════════════════════════════════╗");
    console.log("║  ✅ Demo Complete!                       ║");
    console.log("╚═══════════════════════════════════════╝");
  }

  return {
    curry: curry,
    partial: partial,
    compose: compose,
    pipe: pipe,
    traceCurry: traceCurry,
    demo: demo,
  };
})();

// Chạy: CurryEngine.demo();
```

---

## §8. Câu Hỏi Luyện Tập!

### ❓ Câu 1: Currying là gì? Giải thích bằng 1 câu!

**Trả lời:**

Currying = biến hàm nhận **nhiều params** → chuỗi hàm, mỗi hàm nhận **1 param**, liên tục return hàm mới cho đến khi **đủ params** → thực thi!

`f(a, b, c)` → `f(a)(b)(c)`

### ❓ Câu 2: Cơ chế hoạt động bên trong?

**Trả lời:**

1. **Lưu** `fn.length` (số params cần thiết)
2. Thu thập args qua **closure** (ghi nhớ params đã truyền)
3. **So sánh**: `args.length >= fn.length`?
   - ĐỦ → `fn.apply(this, args)` — thực thi!
   - CHƯA → return hàm mới (concat args cũ + mới)
4. **Lặp lại** cho đến khi đủ!

→ Keyword: **Closure + Đệ quy + Concat args!**

### ❓ Câu 3: 3 giá trị cốt lõi? Cho ví dụ mỗi cái!

**Trả lời:**

| Giá trị                 | Ví dụ                                             |
| ----------------------- | ------------------------------------------------- |
| **① Param Reuse**       | Fix tax rate → `tax10(price)`, không lặp 0.1      |
| **② Delayed Execution** | Form multi-step → collect → submit khi đủ         |
| **③ Adapt Parameters**  | `multiply(2, num)` → `double(num)` → fit `.map()` |

### ❓ Câu 4: Currying có cải thiện performance không?

**Trả lời:**

**KHÔNG!** Currying thêm function nesting + closures → **slight performance penalty!**

Giá trị thật sự = **code reusability + readability**, KHÔNG phải tốc độ!

### ❓ Câu 5: Currying vs Partial Application?

**Trả lời:**

| Tiêu chí    | Currying             | Partial Application   |
| ----------- | -------------------- | --------------------- |
| Output      | Chuỗi hàm 1-param    | 1 hàm với less params |
| Chain       | `f(a)(b)(c)`         | `partial(f, a)(b, c)` |
| Flexibility | Tự động từng bước    | Fix params cụ thể     |
| Batch       | Hỗ trợ mọi cách chia | Fix 1 lần, dùng rest  |

→ Currying = dạng **tổng quát** của partial application!

### ❓ Câu 6: Viết hàm curry() và giải thích từng dòng!

**Trả lời:**

```javascript
function curry(fn) {
  var argsLength = fn.length; // Lưu số params cần
  function curried() {
    var args = [].slice.call(arguments); // Thu thập args
    if (args.length >= argsLength) {
      // ĐỦ?
      return fn.apply(this, args); // → THỰC THI!
    }
    var self = this;
    return function () {
      // CHƯA?
      var newArgs = [].slice.call(arguments);
      return curried.apply(
        self, // → Concat + lặp!
        args.concat(newArgs),
      );
    };
  }
  return curried;
}
```

Key points:

- `fn.length` = biết cần bao nhiêu params
- `args.concat(newArgs)` = gộp params cũ + mới
- Closure giữ `args` qua mỗi lần gọi
- `curried.apply(self, ...)` = đệ quy kiểm tra lại!

---

> 🎯 **Tổng kết Currying:**
>
> - **Định nghĩa**: f(a,b,c) → f(a)(b)(c) — batch params + delayed execution!
> - **Cơ chế**: Closure + đệ quy + concat args!
> - **3 giá trị**: Param reuse, delayed execution, adapt parameters!
> - **Misconceptions**: KHÔNG cải thiện performance! KHÔNG bắt buộc 1-by-1!
> - **CurryEngine** tự viết: curry, partial, compose, pipe, traceCurry!
> - **6 câu hỏi** luyện tập với đáp án chi tiết!
