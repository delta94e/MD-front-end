# HTTPS Protocol — SSL/TLS & Security Deep Dive

> 📅 2026-02-11 · ⏱ 20 phút đọc
>
> Tài liệu chuyên sâu về HTTPS: TLS/SSL, 3 thuật toán mã hóa,
> chứng chỉ số, quá trình handshake, và cơ chế bảo mật.
> Độ khó: ⭐️⭐️⭐️⭐️ | Chủ đề: Network — Security

---

## Mục Lục

0. [HTTPS là gì?](#0-https-là-gì)
1. [TLS/SSL hoạt động thế nào?](#1-tlsssl-hoạt-động-thế-nào)
2. [Digital Certificate — Chứng chỉ số](#2-digital-certificate--chứng-chỉ-số)
3. [HTTPS Handshake Process](#3-https-handshake-process)
4. [Ưu nhược điểm của HTTPS](#4-ưu-nhược-điểm-của-https)
5. [HTTPS đảm bảo an toàn thế nào?](#5-https-đảm-bảo-an-toàn-thế-nào)
6. [Tóm Tắt & Câu Hỏi Phỏng Vấn](#6-tóm-tắt--câu-hỏi-phỏng-vấn)

---

## 0. HTTPS là gì?

> **🎯 Học xong phần này, bạn sẽ biết:**
>
> - HTTPS khác HTTP thế nào
> - Tại sao cần HTTPS
> - Vị trí SSL/TLS trong mô hình mạng

### Khái niệm

```
HTTPS — HYPERTEXT TRANSFER PROTOCOL SECURE:
═══════════════════════════════════════════════════════════════

  HTTPS = HTTP + SSL/TLS (Security Layer)

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  ĐỊNH NGHĨA:                                             │
  │  → Giao thức truyền thông AN TOÀN trên mạng máy tính   │
  │  → Giao tiếp qua HTTP + MÃ HÓA bằng SSL/TLS           │
  │  → Mục đích: XÁC THỰC server, BẢO VỆ tính riêng tư   │
  │    và TOÀN VẸN dữ liệu trao đổi                        │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### Tại sao cần HTTPS?

```
VẤN ĐỀ CỦA HTTP — TRUYỀN BẢN RÕ (PLAINTEXT):
═══════════════════════════════════════════════════════════════

  HTTP truyền thông tin dưới dạng BẢN RÕ (plaintext):

  ┌──────────┐    plaintext    ┌──────────┐
  │  Client  │ ═══════════════►│  Server  │
  │          │◄═══════════════ │          │
  └──────────┘                 └──────────┘
                    ▲
                    │  ⚠️ Hacker có thể:
                    │
       ┌────────────┴─────────────┐
       │  ① Eavesdropping (nghe lén)  │
       │  ② Tampering (sửa đổi data) │
       │  ③ Hijacking (chiếm quyền)  │
       └──────────────────────────┘

  TLS/SSL GIẢI QUYẾT VỚI 3 KHẢ NĂNG:
  ┌──────────────────────┬───────────────────────────────┐
  │ Authentication       │ XÁC THỰC danh tính           │
  │ (Xác thực)           │ → chống giả mạo              │
  ├──────────────────────┼───────────────────────────────┤
  │ Encryption           │ MÃ HÓA dữ liệu              │
  │ (Mã hóa)             │ → chống nghe lén             │
  ├──────────────────────┼───────────────────────────────┤
  │ Integrity            │ XÁC MINH toàn vẹn            │
  │ (Toàn vẹn)           │ → chống sửa đổi              │
  └──────────────────────┴───────────────────────────────┘
```

### Vị trí SSL/TLS trong mô hình mạng

```
HTTP vs HTTPS — VỊ TRÍ SECURITY LAYER:
═══════════════════════════════════════════════════════════════

  HTTP:                      HTTPS:
  ┌──────────────┐           ┌──────────────┐
  │     HTTP     │           │     HTTP     │
  ├──────────────┤           ├──────────────┤  ← Mã hóa
  │              │           │  SSL / TLS   │  ← (Security
  │              │           │ (安全层)      │     Layer)
  ├──────────────┤           ├──────────────┤  ← Giải mã
  │     TCP      │           │     TCP      │
  ├──────────────┤           ├──────────────┤
  │      IP      │           │      IP      │
  ├──────────────┤           ├──────────────┤
  │  Data Link   │           │  Data Link   │
  └──────────────┘           └──────────────┘

  SECURITY LAYER nằm GIỮA TCP và HTTP:
  → Mã hóa data của HTTP request trước khi gửi
  → Giải mã data của HTTP response khi nhận
  → KHÔNG ảnh hưởng đến TCP và HTTP gốc
  → Chuyển sang HTTPS thường KHÔNG cần sửa nhiều code
```

---

## 1. TLS/SSL hoạt động thế nào?

> **🎯 Học xong phần này, bạn sẽ biết:**
>
> - 3 thuật toán nền tảng: Hash, Symmetric, Asymmetric
> - Ưu nhược điểm của từng loại
> - Tại sao cần KẾT HỢP cả 3

### Tổng quan 3 thuật toán

```
TLS/SSL = 3 THUẬT TOÁN KẾT HỢP:
═══════════════════════════════════════════════════════════════

  ┌─────────────────┐  ┌─────────────────┐  ┌──────────────┐
  │ Asymmetric Enc. │  │ Symmetric Enc.  │  │ Hash Function│
  │ (Mã hóa bất    │  │ (Mã hóa đối    │  │ (Hàm băm)    │
  │  đối xứng)      │  │  xứng)           │  │              │
  │                 │  │                 │  │              │
  │ RSA, ECC, DH    │  │ AES, DES, 3DES │  │ MD5, SHA1,   │
  │                 │  │ AES-GCM         │  │ SHA256       │
  ├─────────────────┤  ├─────────────────┤  ├──────────────┤
  │ ① Xác thực      │  │ ② Mã hóa       │  │ ③ Kiểm tra   │
  │   danh tính      │  │   thông tin      │  │   toàn vẹn    │
  │ ② Trao đổi      │  │   truyền tải     │  │   dữ liệu    │
  │   khóa đối xứng │  │                 │  │              │
  └─────────────────┘  └─────────────────┘  └──────────────┘
         │                     │                    │
         │   Khóa đối xứng    │                    │
         ├────────────────────►│                    │
         │                     │      Mã hóa       │
         │                     ├───────────────────►│
         │                     │                    │
         ▼                     ▼                    ▼
  ┌──────────────────────────────────────────────────────────┐
  │ Xác thực danh tính → Trao đổi khóa → Mã hóa nội dung │
  │ + Kiểm tra toàn vẹn = TRUYỀN THÔNG AN TOÀN             │
  └──────────────────────────────────────────────────────────┘
```

### (1) Hash Function — Hàm băm

```
HASH FUNCTION — KIỂM TRA TOÀN VẸN:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  THUẬT TOÁN: MD5, SHA1, SHA256                           │
  │                                                          │
  │  ĐẶC ĐIỂM:                                               │
  │  ① Một chiều, KHÔNG THỂ ĐẢO NGƯỢC                      │
  │    Input → Hash → Digest (KHÔNG thể Digest → Input)    │
  │                                                          │
  │  ② Nhạy cảm cao với dữ liệu đầu vào                   │
  │    Thay đổi 1 bit → Digest thay đổi HOÀN TOÀN          │
  │                                                          │
  │  ③ Đầu ra có CHIỀU DÀI CỐ ĐỊNH                          │
  │    MD5: 128-bit | SHA1: 160-bit | SHA256: 256-bit       │
  │                                                          │
  │  MỤC ĐÍCH:                                                │
  │  → Ngăn chặn thông tin bị GIẢ MẠO                      │
  │  → Xác minh TÍNH TOÀN VẸN dữ liệu                     │
  │                                                          │
  │  ⚠️ HẠN CHẾ:                                              │
  │  → Không thể HOÀN TOÀN ngăn chặn man-in-the-middle     │
  │  → Vì truyền plaintext, attacker có thể SỬA DATA       │
  │    và TÍNH LẠI hash                                      │
  │  → Cần KẾT HỢP với mã hóa để bảo vệ cả data + hash   │
  │                                                          │
  │  VD:                                                      │
  │  "Hello"       → MD5 → 8b1a9953c4611296a827abf8c47804d7│
  │  "Hello!"      → MD5 → 952d2c56d0485958336747bcdd98590d│
  │  (thêm 1 kí tự → digest KHÁC HOÀN TOÀN)                │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### (2) Symmetric Encryption — Mã hóa đối xứng

```
SYMMETRIC ENCRYPTION — CÙNG 1 KHÓA:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  THUẬT TOÁN: AES-CBC, DES, 3DES, AES-GCM                │
  │                                                          │
  │  NGUYÊN LÝ:                                               │
  │  → Dùng CÙNG 1 KHÓA để mã hóa VÀ giải mã              │
  │                                                          │
  │  Sender ── [Key K] ── Encrypt ── ciphertext ──►         │
  │  Receiver ◄── [Key K] ── Decrypt ── plaintext           │
  │                                                          │
  │  ĐẶC ĐIỂM:                                               │
  │  ✅ Nhanh, hiệu suất tốt                                │
  │  ✅ Giao tiếp 1-đối-1                                    │
  │  ✅ Chỉ có key mới đọc được → chống nghe lén           │
  │                                                          │
  │  ❌ HẠN CHẾ:                                              │
  │  → Key vẫn phải TRUYỀN QUA MẠNG                        │
  │  → Nếu key bị lộ → toàn bộ mã hóa VÔ ÍCH             │
  │  → Server phải duy trì N key cho N client               │
  │  → Không thể thay đổi key dễ dàng                       │
  │                                                          │
  │  VẤN ĐỀ CHÍNH:                                           │
  │  Làm sao truyền key AN TOÀN?                             │
  │  → Cần MÃ HÓA BẤT ĐỐI XỨNG!                           │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### (3) Asymmetric Encryption — Mã hóa bất đối xứng

```
ASYMMETRIC ENCRYPTION — PUBLIC KEY + PRIVATE KEY:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  THUẬT TOÁN: RSA, ECC, DH                                │
  │                                                          │
  │  NGUYÊN LÝ:                                               │
  │  → Sử dụng CẶP KHÓA: Public Key + Private Key          │
  │  → Public Key: CÔNG KHAI (ai cũng có)                   │
  │  → Private Key: BÍ MẬT (chỉ server giữ)                │
  │                                                          │
  │  QUY TẮC:                                                 │
  │  → Mã hóa bằng Public Key → chỉ Private Key giải được │
  │  → Mã hóa bằng Private Key → chỉ Public Key giải được │
  │                                                          │
  │  Client A ── [Public Key] ── Encrypt ──► Server          │
  │           ◄── [Private Key] ── Decrypt ──                │
  │                                                          │
  │  Client B ── [Public Key] ── Encrypt ──► Server          │
  │           ◄── [Private Key] ── Decrypt ──                │
  │                                                          │
  │  → Client A KHÔNG THỂ giải mã data của Client B        │
  │  → Chỉ server (có private key) mới giải được           │
  │                                                          │
  │  ĐẶC ĐIỂM:                                               │
  │  ✅ Giao tiếp 1-đối-NHIỀU (1 private key, N clients)    │
  │  ✅ Client có thể xác minh danh tính server             │
  │  ✅ An toàn hơn symmetric encryption                     │
  │                                                          │
  │  ❌ HẠN CHẾ:                                              │
  │  → Thuật toán PHỨC TẠP, tốc độ mã hóa CHẬM            │
  │  → Không phù hợp mã hóa MỌI dữ liệu truyền tải       │
  │  → Data server gửi CÓ THỂ bị tất cả client giải mã    │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### Kết hợp: Giải pháp hoàn chỉnh

```
KẾT HỢP 3 THUẬT TOÁN — GIẢI PHÁP TLS/SSL:
═══════════════════════════════════════════════════════════════

  BƯỚC 1: Asymmetric Encryption
  → Client + Server TRAO ĐỔI key an toàn
  → XÁC THỰC danh tính

  BƯỚC 2: Thương lượng Symmetric Key
  → Dùng asymmetric để trao đổi symmetric key AN TOÀN
  → Mỗi session có symmetric key KHÁC NHAU

  BƯỚC 3: Symmetric Encryption + Hash
  → Dùng symmetric key để mã hóa DATA (nhanh!)
  → Dùng hash để kiểm tra TOÀN VẸN dữ liệu

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  Asymmetric (RSA)     Symmetric (AES)     Hash (SHA256) │
  │  ─────────────────    ────────────────    ────────────── │
  │  ① Xác thực server    ② Mã hóa data       ③ Kiểm tra   │
  │  ① Trao đổi key          truyền tải          toàn vẹn   │
  │       │                      │                    │      │
  │       │    Symmetric Key     │                    │      │
  │       ├─────────────────────►│    Encrypt data    │      │
  │       │                      ├───────────────────►│      │
  │       │                      │                    │      │
  │  CHẬM nhưng AN TOÀN    NHANH + AN TOÀN     TOÀN VẸN    │
  │  (dùng ít)              (dùng nhiều)        (luôn dùng) │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

---

## 2. Digital Certificate — Chứng chỉ số

> **🎯 Học xong phần này, bạn sẽ biết:**
>
> - Tại sao cần chứng chỉ số
> - Chứng chỉ số được tạo và xác minh thế nào
> - CA (Certificate Authority) là gì

### Vấn đề Man-in-the-Middle

```
VẤN ĐỀ: PUBLIC KEY CÓ THẬT SỰ AN TOÀN?
═══════════════════════════════════════════════════════════════

  KHÔNG ĐẢM BẢO! Attacker có thể THAY THẾ public key:

  Client                 Hacker               Server
  ┌──────┐              ┌──────┐             ┌──────┐
  │      │◄── PK_H ────│      │◄── PK_S ───│      │
  │      │              │      │             │      │
  │      │── [PK_H]    │      │             │      │
  │      │   encrypt ──►│      │── [PK_S]   │      │
  │      │              │ giải  │   encrypt──►│      │
  │      │              │ bằng  │             │      │
  │      │              │ SK_H  │             │      │
  └──────┘              └──────┘             └──────┘

  → Hacker THAY PK_S (public key server) bằng PK_H (của hacker)
  → Client mã hóa data bằng PK_H (tưởng là PK_S)
  → Hacker giải mã bằng SK_H (private key hacker)
  → Hacker ĐỌC ĐƯỢC data, rồi mã hóa lại bằng PK_S gửi server
  → Client và Server KHÔNG BIẾT bị lộ!

  GIẢI PHÁP: CHỨNG CHỈ SỐ (Digital Certificate)
```

### Cách hoạt động của Digital Certificate

```
DIGITAL CERTIFICATE — QUY TRÌNH TẠO & XÁC MINH:
═══════════════════════════════════════════════════════════════

  BƯỚC 1: TẠO CHỨNG CHỈ (Certificate Authority — CA)

  Server Public Key + Info
         │
         ▼
  ┌────────────────┐
  │ Hash Algorithm │ → Message Digest (bản tóm tắt)
  └────────────────┘
         │
         ▼
  ┌────────────────┐
  │ CA Private Key │ → Digital Signature (chữ ký số)
  │   (mã hóa)     │
  └────────────────┘
         │
         ▼
  ┌──────────────────────────────────────────┐
  │         DIGITAL CERTIFICATE              │
  │  ┌────────────────────────────────────┐  │
  │  │ Server Public Key + Server Info   │  │
  │  │ (original message)                │  │
  │  ├────────────────────────────────────┤  │
  │  │ Digital Signature                 │  │
  │  │ (CA private key encrypted digest) │  │
  │  └────────────────────────────────────┘  │
  └──────────────────────────────────────────┘

  ═══════════════════════════════════════════════════════════

  BƯỚC 2: XÁC MINH CHỨNG CHỈ (Client)

  Client nhận Digital Certificate:

  ① Dùng CÙNG hash algorithm → tạo Digest A
     (từ server public key + info)

  ② Dùng CA PUBLIC KEY → giải mã Digital Signature
     → được Digest B (do CA tạo ban đầu)

  ③ SO SÁNH Digest A vs Digest B:
     → Khớp nhau ✅ → Certificate chưa bị sửa → AN TOÀN
     → Không khớp ❌ → Certificate bị GIẢ MẠO → TỪ CHỐI

  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  💡 TẠI SAO AN TOÀN?                                     │
  │  → CA public key được TÍCH HỢP SẴN trong browser       │
  │  → Browser TỰ ĐỘNG tin tưởng các CA hàng đầu           │
  │  → Hacker KHÔNG CÓ CA private key                       │
  │  → Nên KHÔNG THỂ tạo chữ ký số hợp lệ                  │
  │                                                          │
  └──────────────────────────────────────────────────────────┘
```

### Nội dung trong Certificate

```
THÔNG TIN TRONG CHỨNG CHỈ SỐ:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │  ┌────────────────────┬─────────────────────────────┐    │
  │  │ Issuer             │ Tổ chức phát hành (CA)      │    │
  │  ├────────────────────┼─────────────────────────────┤    │
  │  │ Purpose            │ Mục đích chứng chỉ         │    │
  │  ├────────────────────┼─────────────────────────────┤    │
  │  │ User's Public Key  │ Public key của server       │    │
  │  ├────────────────────┼─────────────────────────────┤    │
  │  │ Hash Algorithm     │ Thuật toán hash được dùng   │    │
  │  ├────────────────────┼─────────────────────────────┤    │
  │  │ Expiry Date        │ Ngày hết hạn               │    │
  │  ├────────────────────┼─────────────────────────────┤    │
  │  │ Digital Signature  │ Chữ ký số (CA encrypted)   │    │
  │  └────────────────────┴─────────────────────────────┘    │
  └──────────────────────────────────────────────────────────┘
```

---

## 3. HTTPS Handshake Process

> **🎯 Học xong phần này, bạn sẽ biết:**
>
> - 5 bước handshake HTTPS
> - Vai trò của 3 random numbers
> - Cách tạo session key

```
HTTPS HANDSHAKE — 5 BƯỚC:
═══════════════════════════════════════════════════════════════

  Client                                        Server
  ┌──────┐                                     ┌──────┐
  │      │                                     │      │
  │  ①   │── ClientHello ─────────────────────►│      │
  │      │   • Protocol version                │      │
  │      │   • Random Number 1 (Client Random) │      │
  │      │   • Supported cipher suites         │      │
  │      │                                     │      │
  │      │◄── ServerHello ─────────────────────│  ②   │
  │      │   • Confirmed cipher suite          │      │
  │      │   • Server Certificate (public key) │      │
  │      │   • Random Number 2 (Server Random) │      │
  │      │                                     │      │
  │  ③   │── Client Key Exchange ──────────────►│      │
  │      │   • Verify certificate ✅            │      │
  │      │   • Generate Random Number 3         │      │
  │      │     (Pre-Master Secret)              │      │
  │      │   • Encrypt RN3 with public key     │      │
  │      │   • Send hash of all above content  │      │
  │      │                                     │      │
  │      │◄── Server Verify ───────────────────│  ④   │
  │      │   • Decrypt RN3 with private key    │      │
  │      │   • Send hash of all above content  │      │
  │      │                                     │      │
  │  ⑤   │══ Encrypted Communication ═════════│  ⑤   │
  │      │   Session Key = f(RN1, RN2, RN3)   │      │
  │      │   (symmetric encryption từ đây)     │      │
  └──────┘                                     └──────┘
```

### Chi tiết từng bước

```
5 BƯỚC CHI TIẾT:
═══════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────┐
  │  BƯỚC ①: CLIENT HELLO                                    │
  │  → Client gửi: protocol version, Random Number 1,      │
  │    danh sách cipher suites client hỗ trợ               │
  ├──────────────────────────────────────────────────────────┤
  │  BƯỚC ②: SERVER HELLO                                    │
  │  → Server xác nhận cipher suite dùng chung              │
  │  → Gửi: Server Certificate + Random Number 2           │
  ├──────────────────────────────────────────────────────────┤
  │  BƯỚC ③: CLIENT KEY EXCHANGE                             │
  │  → Client XÁC MINH certificate server (hợp lệ?)       │
  │  → Tạo Random Number 3 (Pre-Master Secret)             │
  │  → MÃ HÓA RN3 bằng public key từ certificate          │
  │  → Gửi RN3 (encrypted) + hash toàn bộ nội dung trước │
  ├──────────────────────────────────────────────────────────┤
  │  BƯỚC ④: SERVER VERIFY                                   │
  │  → Server GIẢI MÃ RN3 bằng private key                 │
  │  → Gửi hash toàn bộ nội dung trước để client verify    │
  ├──────────────────────────────────────────────────────────┤
  │  BƯỚC ⑤: SESSION KEY GENERATION                          │
  │  → Cả 2 bên dùng 3 random numbers (RN1 + RN2 + RN3)   │
  │    + cipher suite đã thỏa thuận                          │
  │  → Tạo SESSION KEY (symmetric key)                      │
  │  → Từ đây mọi data dùng SYMMETRIC ENCRYPTION           │
  └──────────────────────────────────────────────────────────┘

  💡 TẠI SAO DÙNG 3 RANDOM NUMBERS?
  → Tăng tính NGẪU NHIÊN của session key
  → Ngay cả khi RN1/RN2 bị lộ, attacker vẫn không có RN3
  → RN3 được mã hóa bằng public key → chỉ server giải được
```

---

## 4. Ưu nhược điểm của HTTPS

> **🎯 Đánh giá khách quan HTTPS**

```
HTTPS — ƯU ĐIỂM & NHƯỢC ĐIỂM:
═══════════════════════════════════════════════════════════════

  ✅ ƯU ĐIỂM:
  ┌──────────────────────────────────────────────────────────┐
  │ ① Xác thực user + server                                │
  │   → Đảm bảo data gửi đến ĐÚNG client/server            │
  │                                                          │
  │ ② Truyền dữ liệu mã hóa + xác thực danh tính          │
  │   → Bảo mật, chống nghe lén, chống sửa đổi             │
  │                                                          │
  │ ③ Giải pháp AN TOÀN NHẤT hiện tại                       │
  │   → Không tuyệt đối 100%, nhưng tăng chi phí           │
  │     tấn công man-in-the-middle RẤT LỚN                  │
  └──────────────────────────────────────────────────────────┘

  ❌ NHƯỢC ĐIỂM:
  ┌──────────────────────────────────────────────────────────┐
  │ ① Tốn tài nguyên server                                 │
  │   → Mã hóa/giải mã cả 2 phía → phức tạp hơn HTTP     │
  │                                                          │
  │ ② Handshake tốn thời gian                                │
  │   → Tăng thời gian load page                             │
  │                                                          │
  │ ③ SSL certificate có PHÍ                                 │
  │   → Certificate mạnh → chi phí CAO                      │
  │                                                          │
  │ ④ Tốn tài nguyên server hơn đáng kể                     │
  │   → Website traffic lớn → đầu tư server nhiều hơn      │
  │                                                          │
  │ ⑤ SSL certificate gắn với IP                             │
  │   → Không thể gắn nhiều domain vào 1 IP                │
  └──────────────────────────────────────────────────────────┘
```

---

## 5. HTTPS đảm bảo an toàn thế nào?

> **🎯 Tổng hợp toàn bộ cơ chế bảo mật**

```
HTTPS SECURITY — TOÀN BỘ CƠ CHẾ:
═══════════════════════════════════════════════════════════════

  VẤN ĐỀ 1: Symmetric encryption?
  → Key truyền qua mạng → dễ bị hacker chặn

  VẤN ĐỀ 2: Asymmetric encryption?
  → An toàn hơn nhưng CHẬM, ảnh hưởng performance

  GIẢI PHÁP: KẾT HỢP CẢ HAI!
  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  ① Dùng asymmetric encryption để trao đổi               │
  │    SYMMETRIC KEY an toàn                                 │
  │  ② Sau đó dùng symmetric key để mã hóa data            │
  │    → NHANH + AN TOÀN                                     │
  │                                                          │
  └──────────────────────────────────────────────────────────┘

  VẤN ĐỀ 3: Man-in-the-middle thay public key?
  → Giải pháp: DIGITAL CERTIFICATE (CA xác thực)

  VẤN ĐỀ 4: Hacker sửa certificate?
  → Giải pháp: DIGITAL SIGNATURE
  ┌──────────────────────────────────────────────────────────┐
  │                                                          │
  │  ① CA dùng hash algorithm → tạo digest từ certificate  │
  │  ② CA dùng private key → mã hóa digest = signature     │
  │  ③ Client dùng CA public key → giải mã → digest_CA     │
  │  ④ Client tự hash certificate → digest_local            │
  │  ⑤ So sánh digest_CA vs digest_local                    │
  │    → Khớp ✅ = certificate hợp lệ                       │
  │    → Không khớp ❌ = bị giả mạo                          │
  │                                                          │
  └──────────────────────────────────────────────────────────┘

  CHUỖI BẢO MẬT HOÀN CHỈNH:
  ┌────────────────────────────────────────────────────────┐
  │ Digital Signature → xác minh Certificate               │
  │ Certificate → xác minh Public Key                      │
  │ Public Key (asymmetric) → trao đổi Symmetric Key      │
  │ Symmetric Key → mã hóa toàn bộ Data                   │
  │ Hash → kiểm tra toàn vẹn Data                          │
  │                                                        │
  │ = TRUYỀN THÔNG AN TOÀN TỐI ĐA 🔒                      │
  └────────────────────────────────────────────────────────┘
```

---

## 6. Tóm Tắt & Câu Hỏi Phỏng Vấn

### Quick Reference

```
HTTPS QUICK REFERENCE:
═══════════════════════════════════════════════════════════════

  HTTPS = HTTP + SSL/TLS (nằm giữa TCP và HTTP)

  3 THUẬT TOÁN:
    Hash       (MD5, SHA)    → Kiểm tra toàn vẹn
    Symmetric  (AES, DES)    → Mã hóa data (nhanh)
    Asymmetric (RSA, ECC)    → Trao đổi key + xác thực

  HANDSHAKE 5 BƯỚC:
    ① Client Hello: version, RN1, cipher suites
    ② Server Hello: confirmed cipher, cert, RN2
    ③ Client: verify cert, encrypt RN3 with public key
    ④ Server: decrypt RN3 with private key
    ⑤ Both: session key = f(RN1, RN2, RN3) → symmetric

  DIGITAL CERTIFICATE:
    CA hash(info) → digest → CA encrypt(digest) → signature
    Client: hash(info) → digest_A vs CA decrypt(sig) → digest_B
    Match ✅ = hợp lệ | Not match ❌ = giả mạo
```

### Câu Hỏi Phỏng Vấn Thường Gặp

**1. HTTPS khác HTTP thế nào?**

> HTTPS = HTTP + SSL/TLS. HTTP truyền plaintext (nghe lén, sửa đổi, chiếm quyền). HTTPS mã hóa data, xác thực danh tính, kiểm tra toàn vẹn. SSL/TLS nằm giữa TCP và HTTP, không ảnh hưởng protocol gốc.

**2. TLS/SSL dựa trên những thuật toán nào?**

> 3 thuật toán: Hash (MD5/SHA — kiểm tra toàn vẹn), Symmetric encryption (AES/DES — mã hóa data nhanh), Asymmetric encryption (RSA/ECC — xác thực + trao đổi key an toàn).

**3. Tại sao không dùng asymmetric encryption cho toàn bộ?**

> Asymmetric rất CHẬM, không phù hợp mã hóa lượng data lớn. Giải pháp: dùng asymmetric chỉ để trao đổi symmetric key an toàn, sau đó dùng symmetric (nhanh) cho toàn bộ data truyền tải.

**4. Digital certificate giải quyết vấn đề gì?**

> Giải quyết man-in-the-middle attack. Không có certificate, hacker có thể thay public key của server bằng public key của mình. Certificate do CA (Certificate Authority) ký xác nhận public key thuộc đúng server. Browser tích hợp sẵn CA public key → xác minh certificate tự động.

**5. Mô tả quá trình HTTPS handshake?**

> 5 bước: ① Client gửi version + RN1 + cipher suites. ② Server xác nhận cipher + gửi certificate + RN2. ③ Client verify certificate, tạo RN3, mã hóa bằng public key gửi server. ④ Server giải mã RN3 bằng private key. ⑤ Cả 2 tạo session key = f(RN1+RN2+RN3), dùng symmetric encryption từ đây.

**6. Digital signature hoạt động thế nào?**

> CA hash certificate → digest → mã hóa bằng CA private key = digital signature. Client nhận certificate: tự hash → digest_A, dùng CA public key giải mã signature → digest_B. So sánh A vs B: khớp = hợp lệ, không khớp = bị giả mạo.

---

## Checklist Học Tập

- [ ] Giải thích được HTTPS là gì, khác HTTP thế nào
- [ ] Biết vị trí SSL/TLS trong mô hình mạng
- [ ] Hiểu 3 thuật toán: hash, symmetric, asymmetric
- [ ] Giải thích được tại sao cần KẾT HỢP cả 3 thuật toán
- [ ] Biết digital certificate giải quyết vấn đề gì
- [ ] Mô tả được 5 bước HTTPS handshake
- [ ] Hiểu digital signature hoạt động thế nào
- [ ] Nêu được ưu/nhược điểm của HTTPS

---

_Cập nhật lần cuối: Tháng 2, 2026_
